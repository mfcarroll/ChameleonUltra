# Log — what was learned, when, in order

⛔ **APPEND-ONLY.** The only permitted edit to an existing entry is appending a
`⛔ retracted by L##` pointer. Never reword an entry to match what you now believe — the
whole point of this file is that it records what was believed at the time.

**The detailed log is the git history.** Every entry below names its commit; `git show <hash>`
gives the full reasoning, the numbers and the controls. This file is the index: one line per
finding so the arc is readable without `git log`, and so retractions are visible at a glance.

**Current knowledge is `FINDINGS.md`** — that file is rewritten freely and holds no history.
Nothing here should be read as current unless `FINDINGS.md` also says so.

---

## Day 1 — 2026-09-10

| # | time | commit | finding |
|---|---|---|---|
| L01 | 18:28 | `e56692d` | Indala unreadable on Chameleon Ultra. Firmware has no PSK demodulator — `reader/lf/*_data.c` are all ASK or FSK, `psk1.c` is transmit-only. |
| L02 | 18:36 | `446622b` | Root-caused to the ADC sample clock: fc/2 sits at exactly Nyquist. ⛔ retracted by L03 |
| L03 | 18:47 | `c17fda7` | Nyquist diagnosis retracted — `lo_read.v:19` shows the Proxmark also samples once per carrier cycle and reads Indala fine. Front-end filtering is the leading cause. |
| L04 | 18:48 | `6a86533` | `fieldhold.sh` + a TP7 scope protocol. Unused — no scope available. |
| L05 | 18:57 | `13ead89` | A no-instrument T5577 PSKCF sweep designed as the decisive test. |
| L06 | 18:59 | `e1a07d0` | Reused the existing `t5577_campaign.py` harness; added a Proxmark reference leg. |
| L07 | 19:01 | `82b48c1` | One venv runs both halves; the two-interpreter claim (L07-pre) banded. |
| L08 | 19:25 | `c8080a7` | Harness leg landed, and four bugs it exposed along the way. |
| L09 | 19:50 | `4f6d8c4` | Sweep measured: the loss is analog, ~27 dB, and before the ADC. ⛔ superseded by L44 |
| L10 | 19:59 | `69a3d54` | ⭐ **FIRMWARE BUG:** the 8-bit debug path did `14-bit >> 5`, discarding 5 bits and distorting the rolloff shape. `--bits 16` added. |
| L11 | 20:16 | `ad47e4b` | macOS build+flash script. Homebrew's nrfutil cask is disabled for a Gatekeeper failure. |
| L12 | 20:23 | `69c50b2` | macOS flashing works via Nordic's direct download, as the wiki says. |
| L13 | 20:33 | `56143c9` | At 14 bits the front end is flat and the SAMPLER is the blocker. ⛔ superseded by L44 |
| L14 | 20:40 | `ee55356` | `lf sniff --phase`: programmable SAADC sample phase via TIMER3. |
| L15 | 20:47 | `a0d4c35` | Phase-sweep verdict retracted — it was tracking buffer-overrun bursts, and "659x" was a small denominator. |
| L16 | 20:51 | `91736c7` | Phase sweep lands properly: the sampler is real but worth ~7.5 dB of ~44. |
| L17 | 20:54 | `0f3c56b` | `sweep.py` aggregates repeats; deglitching alone revises RF/4 by 9 dB. |
| L18 | 20:58 | `d7aa812` | Repeated sweep: ~27 dB analog, ~7.5 dB firmware. ⛔ superseded by L44 |
| L19 | 21:04 | `9ce378e` | `lf sniff --rate`: free-running oversampled trigger. |
| L20 | 21:09 | `77d0d49` | Oversampling recovers nothing — but the first verdict was read off a `nan`. ⚠ measured on the wrong tag payload; see L33 |
| L21 | 21:16 | `4cef14b` | `lf sniff --gain`: SAADC gain control. |
| L22 | 22:29 | `6165462` | ⭐ **Gain closed:** the noise floor is analog-referred, so the ADC was never the limit. n=7, deglitched. Still holds. |
| L23 | 22:34 | `6edbf30` | `lf sniff --settle`; sweep says no effect on fc/2. ⚠ invalid conditions, see L36 |
| L24 | 22:36 | `29453ae` | Air-gap sweep, measured as response rather than raw amplitude. |
| L25 | 22:47 | `ddf0325` | Air gap closed — flat is already the optimum. ⚠ invalid conditions, see L36 |
| L26 | 23:01 | `e1a336d` | ⭐ **FIRMWARE BUG:** the "overruns" were BLE advertising collapsing the LF field. 41% of captures corrupted → 0%. |
| L27 | 23:06 | `370479c` | `sweep.py` takes baseline repeats and reports SNR against the floor. |
| L28 | 23:25 | `e3a15e3` | The clean PSKCF measurement: 31.2 dB excess at fc/2. ⛔ the measurement stands, the conclusion drawn from it is retracted by L44 |
| L29 | 23:35 | `62c3e4a` | Investigation summary and an adversarial review prompt. |
| L30 | 23:43 | `64cfefb` | Demod attempted — "~1.5 dB short of a working read". ⛔ retracted by L31 |
| L31 | 23:55 | `d159b15` | ⛔ **Demod result retracted — the tag was never transmitting Indala.** The campaign's `PSK1` cell writes `DEADBEEF/12345678`: correct PSK1 air shaping, not an Indala frame. Invalidated everything since the last campaign run. |

## Day 2 — 2026-09-11

| # | time | commit | finding |
|---|---|---|---|
| L32 | 00:05 | `d6edd2e` | Chunked retrieval — `NETDATA_MAX_DATA_LENGTH` is a hard 4096 and raising it faults the device. Captures now hold the two frames a demod needs. |
| L33 | 00:11 | `2d4a225` | The decisive test re-run on a correctly programmed tag: negative by 7.6 dB. ⛔ retracted by L44 |
| L34 | 00:16 | `7567f2d` | ⚠ The lever closures are not safe — settle, air gap and oversampling were all measured before the BLE fix and on the wrong payload. |
| L35 | 00:24 | `fe6c7ed` | Matched-filter demodulator: "+8.2 dB on white noise, no decode on real data". ⛔ retracted by L44 |
| L36 | 00:25 | `6a3f7a8` | `NEXT.md` — ranked next steps. |
| L37 | 00:28 | `8618110` | `RESUME.md` — session resume prompt. Later folded into `README.md`. |
| L38 | 00:41 | `fceeca4` | `lf sniff --input`: select AIN0/`LF_RSSI`, the only tap upstream of the filter poles. |
| L39 | 01:07 | `7f03d7e` | ⭐ **AIN0 closed:** carries no fc/2 (tag/empty 1.04x inside a 1.28x scatter; flat to 0.5 dB across 1–62kHz) but is demonstrably alive (+16-count DC shift, ±0 over 7 repeats). Still holds. |
| L40 | 01:07 | `7f03d7e` | ⭐ Zero-offset cross-capture stacking works: +7.3 dB at N=7, empty floor falling exactly √7. Captures are frame-locked to field-on. Still holds — but superseded in usefulness, the read needs no averaging. |
| L41 | 01:07 | `7f03d7e` | ⛔ Folding at 2048 samples **cancels the data** — the word has 19 ones, odd parity, so the subcarrier inverts every frame and the true period is 4096. |
| L42 | 01:07 | `7f03d7e` | ⛔ "52/64 bits vs a 45/64 null" retracted — half the frame is a constant preamble run and scoring it against 64 rotations gives up to 32 bits free. |
| L43 | 01:15 | `bd9a351` | `phasebits.py` — sweep phase scoring bit recovery rather than skirt amplitude. |
| L44 | 01:36 | `d4b2b4b` | ⭐⭐⭐ **SOLVED. The Chameleon Ultra reads Indala.** 43/160 single captures decode `a0000000e6bd0e92` exactly; empty field 0/160. Root cause: `mfdemod.py` demodulated **PSK2 against a PSK1 tag**, and `synth()` encoded with the same wrong convention so the self-test was self-consistent and passed forever. Retracts L09, L13, L18, L28-conclusion, L33, L35 and the whole "analog deficit" framing. |
| L45 | — | this commit | Notes restructured: `README` / `FINDINGS` / `LOG` / `METHOD` / `NEXT`, with the old working notes frozen under `archive/`. |
| L46 | 02:05 | `84b99a7` | ⭐ **The baseband filter's job is a NULL at fs/2, not a low cutoff.** A 3-tap [1,2,1] beats the 12 kHz FFT brick wall **51/160 vs 43/160**, and strictly — McNemar b=0, c=8, p=0.008. [1,1,1], which smooths as hard but nulls at fs/3, is the worst of the set at 31/160, which is the mechanism showing itself. Revises C03's numbers; the claim that the filter is load-bearing is unchanged (0/160 with none). |
| L47 | 02:40 | `84b99a7` | ⭐⭐⭐ **`lf indala read` runs on the device.** 20/20 consecutive reads of `a0000000e6bd0e92`, 0.41–0.55 s each, integer-only. The C decoder agrees with `mfdemod.py` word for word on all 320 committed captures. ⚠ And 40 fresh captures taken today exposed what the offline work had not: **one recovered frame in five is WRONG**, 24 of them across 200 captures and all 24 distinct — so the firmware returns a credential only when two captures agree. ⚠ The on-device empty-field null has NOT been run. |
| L48 | — | `3ea8d3e` | ⭐ **On-device empty-field null passed: 0 reads in 20**, closing C20. ⚠ And the timing had to be measured rather than eyeballed — a failed read and a successful one both land near 0.5 s of wall clock. Subtracting the 0.33 s host floor separates them: a failure spends 0.47–0.53 s on the device, the whole 500 ms budget, where a success spends 0.08–0.22 s. The rotation walks the full window and exhausts rather than aborting early. |
| L49 | — | `df463d6` | ⭐ **The decoder is word-agnostic** — 36 synthetic words, every one decodes, no wrong answer at any amplitude. Closes the structural half of generality: EVEN parity (never tested — C14's frame inversion only happens for odd), payloads of 31 zeros and 31 ones, and a preamble reproduced inside the payload. `generality.py`, written from the physics and validated by the C decoder rather than by a round trip. ⛔ Also fixed `stack.py`'s `psk_frame()`, which was dead — it called a name deleted in the PSK1 rename, and its body still ran the retracted PSK2 running XOR. An injection control that injects the wrong modulation reports "undetectable" and cannot fail visibly. |
| L50 | — | `d8fd7bd` | ⭐ **No false positive on a loud wrong signal.** An HID Prox tag on the antenna gives `LF tag not found` 10/10, with coupling confirmed by a 5/5 HID read immediately before, plus 10/10 from a separate run. Closes NEXT §1 for HID. |
| L51 | — | `d8fd7bd` | ⛔ **A claimed regression that was not one, and the rule it broke.** `lf hid prox read` went 5/5 before a burst of `lf indala read` and 1/5 after, and that was reported as a reproduced state-corruption bug. It was not: a **no-stressor control** swung 8/15, 11/15 and 6/15, and over 25 minutes the HID rate declined monotonically to 0/15 **through the control arm**, then did not recover after 150 s of rest while the field amplitude stayed put (mean 5447 vs 5438). ⇒ The physical tag state was never confirmed and the whole sequence is uninterpretable. `README`'s first warning says to confirm the tag state before trusting any measurement; it was written after this exact mistake cost a day, and it was not followed. |
| L52 | — | `ac3c84b` | ⭐ **The RF path does not degrade under load** — an HID tag's fc/8+fc/10 amplitude is flat to 1.00x across idle, sustained `lf indala read` load and recovery, carrier DC flat to 0.3%. That excludes detuning, thermal, coupling and field collapse for the L51 episode in about thirty seconds, and it is the measurement that should have been made first. `lfprobe.py`. ⚠ The 0/15 episode itself stays unexplained: not RF, not attributable to this work, self-clearing, not reproducible. `lf hid prox read` also fails ~15-20% on a signal that is demonstrably present and constant. |
| L53 | — | `ce1f9a2` | ⛔⛔ **A second Indala tag is inaudible to the Chameleon.** A spare T5577 written with `a0000000e6bd0e92` by a Proxmark — "Data written and verified", then read back as Fmt 26 FC 52 Card 63612 — decoded **0 of 12** on the Chameleon, and its fc/2 skirt sits at 0.89–1.11x the empty-field floor across twelve sample phases where the bench tag sits at 1.25–1.73x. The subcarrier is not there at all. ⚠ Cause unknown (position / tag / reader) and the control has not been run. ⇒ The working margin is only 1.25–1.73x over the floor, so anything worth more than ~1.3x makes a tag unreadable. |
| L54 | — | `ce1f9a2` | ⚠ **The Chameleon's T5577 writer is not known to work.** Three raw `lf_t55xx_write_block` calls landed exactly one block; routing through the proven `write_t55xx` (field held on, every block written twice) changed the spectrum not at all. Not diagnosable from this device — it has no T5577 read, so every attempt costs a Proxmark round trip. `NEXT.md` §7. |
| L55 | — | `0c43769` | ⭐⭐ **Stacking: 31.9% -> 71.9%, and it revives the stock phase.** Adding frame-locked captures before demodulating raises the correct-decode rate over every combination of the committed captures, with **0 empty-field frames at every depth**; phase 0 (0/5 singly) and phase 64 (outside the window) both decode stacked. Implemented in firmware as two accumulators over disjoint capture sets, so the two-capture agreement rule stays independent evidence. ⛔ **But it does nothing for the tag it was built for** — bench 1.56x -> 2.71x, white coin 1.08x -> 1.05x. ⭐ Revises L53: that tag is NOT inaudible. Stacked, it yields `a0000000e6ad0e92`, `a0000000e4bd0a92`, `a0000000e33d0e92` — 1, 2 and 3 bits from the truth at three separate phases — where stacked noise yields nothing at all. ⛔ And C11/C12 are now in doubt: the EMPTY field's lag-0 baseband correlation (0.75–0.83) is HIGHER than either tag's (0.15–0.49), so that measure is reading the field turn-on transient, not frame lock. |
| L56 | — | `1104f16` | ⭐ **Paired same-session capture set, and it is a COIL problem: 3.3x.** 32 copper + 32 white captures, same geometry, same payload, same phases, committed under `caps/coils/`. Copper sits at 3.55–3.67x the empty floor and decodes at every phase; white at 1.04–1.12x and decodes nowhere. ⚠ And the copper coil reads 3.55x now against 1.53x in the original sweep — **position alone is worth more than 2x**, which is larger than the entire working margin. Stacking gain: 1.5–2.1x copper, 1.0x white. ⛔ The lag structure is near-identical for the two tags (4 of 7 at lag 0, 3 at ≈−47), so the cross-capture correlation is a property of the capture path, not of any tag's frame — and the common background survives mixing, a 6 kHz band limit, DC removal and dropping 1024 samples. |
| L57 | — | `92f6fd2` | ⛔⛔⛔ **THE TAG WAS ON THE WRONG SIDE OF THE DEVICE, FOR THE ENTIRE PROJECT. The reading side is the FRONT. It is worth 21x.** Same coil: 1.0–1.1x the empty floor on the back, **20–34x** on the front. The coil that read 0/12 reads first try. Single-capture decode 32% -> **71%**. ⭐ **Sample phase 0 — the stock trigger, the headline "stock firmware would fail even with a correct decoder" — decodes 3/3 on the front**, as do phases 96–120; the only dead band is 56–88 and it carries the HIGHEST skirt, so it is a polarity null rather than weak signal. ⇒ ~26 dB of the "31.2 dB below the Proxmark" was placement. Every measurement in this project was taken at ~1/20 of the available signal; the ledger now marks those ⚠B and `NEXT.md` §1 is the re-measurement. ⚠ The Flipper reads from its back, which is where the assumption came from. |
| L58 | — | `3ded983` | ⭐⭐ **The front sweep confirms 71% and uncovers a wrong-credential path the agreement rule does not cover.** 32 phases x 5, tag and empty, on the front: **114/160 decode the truth and 0/160 empty captures produce a frame at all**. Phase is TWO working bands (0–56, 96–124) split by a dead band at 60–92 — and in that dead band the decoder returns the SAME wrong word every time (phase 64 -> `a0000000b5af0b92` 5/5; phase 88 -> `a0000000c6b90c92` 5/5), because the winning bit alignment is off 16, exactly half a bit period, so every integrator straddles a boundary. Two independent captures therefore agree on a wrong credential. ⛔ The premise of C17/C18 — 'every wrong word appeared exactly once' — is a property of the 26 dB-down back-side signal: on the back all 17 wrong frames were distinct, on the front 3 repeat. Latent today (no rotation phase lies in 60–92) but NEXT §2b, which proposed deriving the rotation from the union of both placements, would have made it live — the back-side stacking note specifically credits phase 64. Also settled offline from the same captures: **C03 reversed** (notch ON 114, OFF 121 — the ripple it suppressed is gone at 20x subcarrier), **C04 unchanged and still fatal** (107 -> 0, structural, not SNR), and **the fc/2 skirt is uncorrelated with decode** (peak skirt and minimum skirt both decode 5/5, dead band in between) — which retires the §7 sweep scripts rather than porting them | `caps/front/` (320) |
| L59 | — | `6b85a2d` | ⭐⭐ **The loud-signal null passes, and the HID read's intermittency is pinned to the decoder.** An HID Prox tag at **88-100x the empty floor in its own band** produced `LF tag not found` on 30 of 30 `lf indala read` attempts — the null §1d actually wanted, against a loud wrong signal rather than a quiet one. ⛔ But the run was nearly discarded: all 6 bracketing `lf hid prox read` calls failed, which by NEXT §3's rule means 'the tag was not there'. It was there — 6.3M against a 63k floor. A failed read is not evidence of absence, and bracketing must use `lfprobe.py`, not the binary metric whose unreliability is the reason the rule exists (F05). ⇒ `lf hid prox read` failing 0/6 on a 90x signal is a DECODER problem, not RF (C45). ⛔ My own error on the way: I reported the tag at '900x the floor' by dividing 10-18kHz band energy by `--floor`'s default of 6900, which is the fc/2 empty level — `lfprobe.py`'s `--band` and `--floor` defaults contradicted each other. `--floor` now has no default and the 10-18kHz floor is measured: 63322 (M24) | `caps/front/`, floorwatch n=90 |
| L60 | — | `29864f4` | ⭐⭐ **Three tags with byte-identical memory read 0/6, 3/6 and 7/9 on the Chameleon and 3/3 on a Proxmark — the HID problem is the package and the decoder, not the data.** Page 0 AND the page-1 factory trace are identical across blue dual, white coin and copper coin, so it is one lot of clone silicon carrying one payload; only the antenna differs (C46). A stress run on the white coin puts the subcarrier at 1.00x through idle / Indala-load / recover while reads fail 2 of 24 at the same amplitude as the successes — decoder, not RF — and confirms with a continuous metric that an Indala burst does NOT degrade HID reads, which is the M17/M18 false regression finally laid to rest (C45). ⭐ A mechanism fell out of it: `advertising_stop()` appears in 1 of 15 LF reader files. Only the generic capture path suspends BLE, and its own comment says an advertising burst collapses the field for ~1.6ms and hit 4 captures in 10. `hidprox_read()` has no guard — which predicts the long-unexplained *"after connecting it to my phone it does read"*, since connecting is what stops advertising (C47, UNMEASURED). ⚠ It cannot explain the blue dual never reading, so expect two causes. Also: `lfprobe.py` now scores the LOAD arm, which was silently discarding 32 Indala reads against a loud HID tag — a loud-signal null we were already paying for | stress n=24, 3 tags |
| L61 | — | `f1b566a` | ⭐⭐⭐ **The wrong-credential path is closed: on the front the decoder now returns 110 frames and all 110 are correct.** C39's straddles are rejected by their SHAPE — where two adjacent bits differ, an integrator sitting across the boundary averages +1 and -1 and lands near zero, so the frame's weakest bit collapses relative to its average. Gate: reject when `mean|integ| >= 2048` AND `min|integ| * 8 < mean|integ|`. 21 of 21 straddles rejected, 110 of 114 true frames kept, 40 of 40 at rotation phases, and the back-side set completely unchanged at 68/51 (C48). ⛔ The obvious gate was wrong twice over: the winner-vs-straddle amplitude ratio separates BACKWARDS (1.30 good vs 1.64 wrong), and shape alone fails on the back where true frames are equally ragged — it would cost 61% of back-side reads. Both terms are needed because the two populations differ by an order of magnitude in level, not in shape (C49). ⚠ The amplitude term is absolute and so coupling-dependent, exactly what C43 warns about, so this does NOT retire the 60-92 keep-out; it is the second layer. ⚠ compare.py caught a 4x unit error in my own mirror of the gate — the firmware folds [1,2,1] without dividing — which is precisely the job that cross-check exists to do | 640 captures, both placements |
| L62 | — | `add61a4` | ⭐ **Flashed the straddle gate and checked the never-executed even-parity path.** Device now runs `v2.2.0-105-gf1958ed`; empty pad gives 10/10 `LF tag not found`. ⚠ The first flash failed — the script sleeps a fixed 3s after `enter_dfu.py` and the bootloader window had closed before nrfutil looked; polling for the `nordicDfu` trait found it in 2s on the retry. Every measurement in this project uses one credential, `a0000000e6bd0e92`, which has 19 ones — ODD — so the subcarrier inverts every frame and the non-inverting branch had never run. Synthesised `a0000000d2e6b438` (18 ones, EVEN) through the shipping decoder: indistinguishable from the reference at every amplitude, and the new gate rejects neither (C50). The white coin now carries that word on hardware. ⛔ Two self-confirming-test bugs caught on the way: the repeat-a-command idiom in NEXT §3 never ran anything (literal quotes, cu.py printed its help and it looked like ten reads), and my first parity harness put the expected word in the capture FILENAME, so `word in line` matched every line and the tally read '-2 wrong' | synthetic, 168 captures |
| L63 | — | `11b3c1e` | ⭐ **Two Indala tags on hardware: 20/20 each, and the second one falsified the mechanism I had written for C39.** Copper (`a0000000e6bd0e92`, odd parity) 20/20 at phase 20, bit offset 9, one capture each — no regression from the straddle gate. White coin (`a0000000d2e6b438`, EVEN parity, a different package) 20/20 at phase 20, **bit offset 22**, one capture each: the non-inverting branch works on real hardware, and the gate rejected nothing, so the 2048 threshold is not too high for a second package. ⛔ But offset 22 is one of the offsets the dead-band WRONG frames won at, while true frames on the bench tag won at 9/10/12. I had written that up as "offset 16, exactly half the 32-sample bit period" and called it the straddle signature. It is not: half a bit from 9 is 25, not 16, so the arithmetic never worked, and a correct read at 22 settles it. The offset belongs to the TAG's frame timing. ⇒ Corrected in four files. The gate itself is untouched because it tests shape and never looks at the offset (C51) | 40 hardware reads, 2 tags |
| L64 | — | `8475875` | ⭐⭐ **The ASK/OOK null passes, with both brackets agreeing for the first time.** An EM410x tag at **18.1–18.9x** the empty floor in its own band (1–5kHz, floor 440000 measured pad-clear in the same session) produced `LF tag not found` on 20 of 20 `lf indala read` attempts, and `lf em 410x read` independently returned the right ID — where C44's HID null had only the amplitude probe because that decoder was broken (C52). ⭐ The tag carrying it is the BLUE DUAL, which has never once produced a successful `lf hid prox read`: it decodes ASK/OOK first try, so "it does not couple" is excluded and §3b narrows to the FSK path (C53). ⚠ **Control arm for C47, and it does not support the predicted magnitude:** `lf em 410x read` — a reader with NO BLE suspend — is **76 of 80 (95%)** on this tag. C47's mechanism, quoted from `lf_reader_generic.c`, is an advertising burst collapsing the field in 4 captures out of 10. A 5% failure rate is eightfold lower. It is not a clean refutation (a read command retries internally, so one collapsed capture need not fail it) but the effect is nothing like 40%, and the contrast with the blue dual's 0/6 on HID is now much starker | 80 EM410x reads, 20 nulls |
| L65 | — | `9875748` | ⭐ **Found the null that actually tests the preamble search, and built a tool so the bracket cannot be skipped.** IDTECK is 64-bit PSK1 at RF/32 with a T5577 config word identical to Indala's — same modulation, same bit rate, same subcarrier, same block count. Only the 64 bits differ (preamble `0x4944544B`, "IDTK"). Every other null so far (HID FSK, EM410x ASK) differs in modulation, so the mixer and bit integrator never saw valid PSK1 carrying the wrong frame. Synthetic arm: **0 frames from 90 captures**, control 15/15 (C54). ⭐ `nulltest.py` now runs the whole procedure — amplitude bracket in the interferer's own band against a floor measured in that band, then the null, then the interferer's own reader as a BONUS that cannot fail the test — and refuses to score below 3x presence. Validated against the known EM410x case: 19.3x, 8/8, 5/5. ⛔ Also fixed `flash-dfu-app-macos.sh`, which slept a fixed 3s after `enter_dfu.py`; it polls for the `nordicDfu` trait now, because the fixed delay is what made the first flash of the gate fail | 90 synthetic + 8 hardware |
| L66 | — | `b001fc1` | ⭐⭐⭐ **§3 and §4 are done: every loud-signal null passes on two units — 160 reads, 0 frames.** IDTECK, Viking, PAC and Jablotron, 20 `lf indala read` each on two Chameleons, every interferer bracketed as present first. ⭐ IDTECK is the null that mattered: 64-bit PSK1 at RF/32 with a T5577 config word IDENTICAL to Indala's, so the mixer and integrator see valid PSK1 carrying the wrong frame — and it returned nothing, 40/40 across both units (C55). The second Chameleon reads the copper coin 20/20 at the SAME sample phase 20, with only the bit offset moving 9 -> 10 (C56). ⛔ **My null band was wrong for the one null that mattered.** `nulltest.py` defaults to 500–20000 Hz, which covers envelope modulation but NOT a PSK1 subcarrier at fc/2 = 62500 Hz: IDTECK scored 5.5–6.3x where the ASK tags scored 16–19x, and that 6x is leakage. The amplitude bracket — the instrument F05 exists to insist on — was the WEAK evidence, and the interferer's own reader, which I had demoted to "a bonus that cannot fail the test", was the strong one: `lf idteck read` decodes PSK1 at fc/2, so it succeeding proves the tag was audible in the Indala decoder's own band and modulation (C57). Band table added to nulltest.py. ⚠ `lf pac read` scored 0/5 and 2/5 while its tag sat at 16x — another Chameleon LF decoder failing on a loud tag, alongside HID | 160 nulls, 2 units |
| L67 | — | `e4d71fa` | ⭐⭐ **§2's two offline re-opens, and they resolve opposite ways.** STACKING: on the front it gains **nothing** — 68.75% at every depth N=1..5, exactly the 22 good phases out of 32 — and in the dead band it actively *reinforces* the straddle, wrong frames converging from 9 distinct at N=1 to 4 at N=4 where every one of 20 is a repeat. More stacking defeats the agreement rule more thoroughly. The gate holds at 0 wrong throughout. On the back stacking is still worth 32% -> 72% ⇒ it earns its place only for the WRONG placement (C58). FRAME LOCK: the lag-0 correlation cited for C11–C13 is worthless — the EMPTY field scores 0.994 against the tag's 0.999 on the front and actually *higher* on the back (0.941 vs 0.912). But the conclusion survives, proven operationally: stack 4 back-side captures as captured and get 67%, roll each by a random offset first and get **0%**. ⇒ C11–C13 stand, their evidence is retracted, and C31 was right to doubt the number while wrong to doubt the claim (C59, M25) | 640 pairs + ~1000 stacks |
| L68 | — | `f270109` | ⭐⭐⭐ **§8 closes: the T5577 writer works, 9 of 9, and it was the placement all along.** `lf indala write` now reads the tag before and after, so it reports VERIFIED / WRITE DID NOT LAND / WRITE FAILED / WRONG DATA / CANNOT TELL instead of "Sent, NOT verified". A successful read-back verifies blocks 0, 1 and 2 together, because a tag whose config block did not land would not be transmitting PSK1 at RF/32 and could not be read at all. Measured by ALTERNATING between two words — writing the same word twice would verify trivially — 9 consecutive writes all VERIFIED (C60). ⇒ The question §8 was never able to separate resolves: the writer was never broken. Its original failures (one of three blocks landing) predate L57 and were taken with the tag on the BACK of the device, and writing needs more field than reading. Another item on the placement error's bill. ⛔ §8 also said the blocker was instrumentation and the fix was a T5577 block read; that was wrong by the time it was written — this project had already built the verifier, and the reader IS it | 9 writes |
| L69 | — | `e714517` | ⭐ **Asked whether the emulation refactor has a read-side analogue; it has two, and the first one corrects me.** The LF readers are two families: GPIO/comparator (em410x, jablotron, viking) and SAADC (hidprox, ioprox, pac, lf_reader_generic). Each SAADC reader duplicates the capture prologue, and only `lf_reader_generic.c`'s copy suspends BLE advertising. The three without it are HID, ioProx and PAC — and HID and PAC are exactly the two measured failing on loud tags, while the SAADC reader WITH the guard (Indala) is 60/60. ⛔ I weakened C47 in L64 because `lf em 410x read` scored 95%; EM410x is on the GPIO path and never touches the SAADC, so that was never a test of the hypothesis and the weakening was wrong (C47 rewritten). Second analogue: the Indala reader holds 48KB static, of which 32KB is the two stacking accumulators — and C58 measured stacking as worth exactly nothing on the front (68.75% at every depth). Two thirds of the reader's RAM serves only the wrong placement (C62) | static read + prior hardware rates |
| L70 | — | `8086a4c` | ⛔ **Firmware bug found while debugging silent Indala emulation: changing a slot to a PSK1 type while already emulating leaves the PWM base clock wrong.** `pwm_init()` picks `IS_PSK1_TYPE(m_tag_type) ? 1MHz : 125kHz`, but it runs only from `lf_sense_enable()`, which fires only on a sense DISABLE->ENABLE transition. `hw slot type` changes `m_tag_type` long after that, and nothing re-inits. With counter_top 16 an entry is 16us at 1MHz (62.5kHz subcarrier, correct) and **128us at 125kHz — 8x too slow and unrecognisable**; the reverse breaks ASK/FSK types 8x fast. **Affects IDTECK identically — pre-existing, not introduced by the Indala work.** Workaround: `hw mode -r` then `-e`. ⚠ REAL BY INSPECTION, NOT CONFIRMED AS THE CAUSE of the silent emulation — the mode cycle that would prove it had not been run when this was recorded, and it may not be the only thing wrong. ⛔ The control that should have come first: emulate IDTECK and see whether a Proxmark reads it. IDTECK shares the whole transmit path and nothing in this tree records it ever being verified end to end, so if it is silent too then PSK1 emulation never worked and Indala inherited a broken base. Backlogged as NEXT §3c | code read |
| L71 | — | `a830110` | ⭐⭐ **Indala emulation: transmitting, right frame rate, phase destroyed — and it is not the modulator.** Sniffed the emulating Chameleon with the second one in reader mode, against an empty control. It IS modulating (fc/2 band 62524 vs empty 6399) and the frame autocorrelates at lag 2070 against a true 2048, but the longest constant-phase run is 102 samples where the 28-zero preamble demands 896 and a real tag shows 912 (C63). ⭐ The modulator is exonerated by a host test in the ctest style: `lf_psk1_rf32_modulator` on `a0000000e6bd0e92` produces polarity-per-bit IDENTICAL to the frame, all 16 entries per bit agreeing, counter_top 16, duty 8. So the fault is between a correct PWM sequence and the air. ⭐ Emulation is FRONT-ONLY as well: skirt 49236 front, 5049 other side, 4591 empty (C64). ⛔ And one of my own measurements had to be thrown out: the apparent -671 Hz subcarrier offset looked like a 1% frequency error until the EMPTY field measured -793 Hz, proving the estimator rather than the signal — frequency resolution at the Nyquist edge is not to be trusted (C65). ⚠ Leading hypothesis, UNTESTED: a real T5577 DIVIDES the reader's own field to make its subcarrier and is therefore phase-locked by construction, which is the entire premise of this decoder — mix by (-1)^n, no oscillator, no phase estimate. A PWM free-running from the emulator's own clock has no such coherence. ⇒ The test is IDTECK emulation: it shares the whole transmit path, so if PSK1 emulation is broken this way it is broken there too | 3 sniffs + host modulator test |
| L72 | — | `c621fae` | ⛔⛔⛔ **PSK1 tag emulation has never worked on this device.** The IDTECK control settles it. Same frame `4944544b00000000`, same reader, same session: a REAL IDTECK tag gives a **1042-sample** constant-phase run (the frame demands 1024) at lag 2048; the EMULATED one gives **100** at lag 2071 — indistinguishable from the Indala emulator's 102 at 2070. Both modulate and neither carries phase (C66). ⇒ Not `indala.c`. The Indala emulation inherited a transmit path that was already broken, in a protocol that predates this project. ⭐ The real tag also validated the INSTRUMENT — 1042 against 1024 expected — which was the branch worth ruling out before building on the measurement. ⭐ And the test frame was chosen by computing its run length first: the IDTECK tag already on the bench would have demanded only 128 samples, which the broken emulator already reaches, so it could not have discriminated at all. ⚠ Mechanism still open: the mixed baseband peaks at 641/671 Hz for the two emulators and nothing comparable for an empty field, and a ~640 Hz beat flips phase every ~98 samples, matching the observed 100–102. But 640 Hz is 1.03% of 62.5 kHz — two crystals at 20 ppm give 2.5 Hz — so 'the PWM is not locked to the reader' does not by itself explain the size (C67) | paired real-vs-emulated |
| L73 | L71, L72 | `079b999` | ⭐⭐⭐ **INDALA EMULATION WORKS, AND L71/L72 WERE WRONG.** On a Proxmark, with the emulating Chameleon placed exactly where a real tag had just been proven to couple, the constant-phase run is **931 samples against the 928 the frame demands** and `cdemod` recovers `a0000000e6bd0e92` from **10 of 12** independent windows — real-tag control 12/12 (C68). ⛔ So C63 ("phase destroyed"), C66 ("PSK1 emulation has never worked on this device") and C67 ("~640 Hz offset") are RETRACTED, and C64 is provisional. ⛔⛔ THE MISTAKE WAS A CONTROL THAT MATCHED THE INSTRUMENT BUT NOT THE GEOMETRY. Both arms were Chameleon-to-Chameleon, and I compared an emulating Chameleon — a second device at an unproven distance and orientation — against committed captures of a real tag lying flat on the antenna. The emulator was coupling badly; every number downstream described the coupling, not the emulator. The IDTECK 'control' had the same flaw, which is why it agreed so convincingly (C69). ⚠ What survives: emulation is ~5x WEAKER than a real tag (skirt 78782 vs 415835, amp ~11000 vs ~55900) and the Proxmark's own `lf indala reader` will not lock onto it — a margin problem, not a correctness one. And a real clock drift exists, far smaller than claimed: the winning offset slides 12 -> 8 over 180 ms, ~180 ppm, harmless within a window | 12 windows + control |
| L74 | — | `23df402` | ⭐⭐ **The emulation is good enough for a conventional demodulator, and what limits it is CLOCK DRIFT.** Proxmark's own `lf indala demod`, offline on a saved trace of the emulating Chameleon: 131 ms ✓, 163 ms ✓, 196 ms ✗, 290 ms ✗, with the real-tag control decoding at every length (C70). `lf indala reader` fails only because it demods the whole 290 ms buffer. ⚠ The firmware plays **10 frames = 164 ms** per burst, which sits exactly on that threshold. ⭐ A real tag's frame period is EXACTLY 2048.000 samples with +0.000 slip — that is what dividing the reader's carrier means — and the emulator's free-running PWM cannot do that (C71). ⛔ For the first time in this project, PLACEMENT IS NOT THE ANSWER: 39 captures while moving, flipping and removing the device span 3.5–21.4 against a real tag's 33.9, and the best position beats the starting one by 14% (C72). Depth, transitions, duty cycle and the low-frequency burst envelope are each eliminated by measurement (C73). ⛔ My own instrument failed twice here: `pm3monitor.py` read a stale file and reported an identical 0.56x forty times running — including with the device out of the field, which is what the user noticed — because the Proxmark's `data save` appends -001, -002 rather than overwriting; and two separate attempts to put a ppm figure on the drift returned +3850 and −76553 ppm, both caught by the real-tag control reading non-zero when it must read zero. No ppm figure is claimed | 39 + 6 + 4 captures |
| L75 | — | `122c385` | ⭐⭐⭐ **Looked at how the Flipper Zero does it, and it is the fix.** Its emulation timer is clocked from the EXTERNAL TRIGGER — the reader's own carrier through the comparator — `LL_TIM_CLOCKSOURCE_EXT_MODE2` plus `LL_TIM_ConfigETR`. It counts carrier cycles, so its subcarrier divides the reader's carrier exactly as a T5577 does (C74). That is C71 confirmed by a working third-party implementation on different silicon, and it was arrived at independently — the real tag was measured at exactly 2048.000 samples per frame before the Flipper was looked up. ⚠ IT IS NOT A PORT. The STM32's TIM2 takes an external clock on ETR; the nRF52's PWM peripheral is clocked only from PCLK16M and has no such input, so the sequence-playback approach cannot be locked at all. The nRF equivalent has to be built from pieces this device already uses in READER mode: TIMER2 in counter mode (already there, `m_pwm_timer_counter`), PPI into `NRF_TIMER_TASK_COUNT` (already there, keyed off PWMPERIODEND because the reader generates its own carrier), and for emulation an LPCOMP or GPIOTE event per RECEIVED carrier cycle instead. Then TIMER COMPARE -> PPI -> GPIOTE toggle on LF_MOD gives a carrier-locked fc/2, with the CPU only needed once per bit (256us) to express a phase reversal as a skipped toggle | Flipper source + nRF PPI/timer audit |
| L76 | — | `ce55fc6` | ⭐⭐⭐ **The burst boundary was the cause and lengthening the burst fixes it.** `lf_tag_em.c` played 10 frames = 163.84 ms then paused to check the field, and the Proxmark decoded any window inside one burst while failing across a boundary. At 32 frames (524 ms) the cliff moved with it: 197 ms and 262 ms both went ✗ -> ✓ (C75). ⭐ **The USB cable costs ~40%** — unplugged, the emulator reaches 33.87 against a real tag's 33.85, exactly 1.00x, where every cabled run sits at 0.58–0.72x. C72's 'tops out at 0.63x' is retracted; that ceiling was the cable, and the user spotted it from the peak-to-peak column. ⚠ An emulating Chameleon also SLEEPS until a field appears, so the first read of any test sees an empty field — two runs refused at 3.08 before a third, run immediately after, scored 22.69 (C76). `emutest.py` now wakes it with a throwaway read, and does the bracket and the test from ONE capture so the device cannot move in between; three separate 'fails at every length' results this session were empty fields caught only by that bracket | 6 lengths x 2 firmwares |
| L77 | — | `0670242` | ⭐ **The harness now reproduces the 262 ms result reliably, and debugging it corroborated C75 from the opposite direction.** Chaining `lf read; lf read` in one Proxmark session brackets perfectly — coupling 23.06, phase run 929 — and decodes at NO length, because consecutive reads re-trigger field detection and restart the emulator's burst mid-capture. One read per invocation, same position and amplitude, decodes to 262 ms. The burst-boundary model predicted exactly that (C77). ⚠ Waking an emulating Chameleon takes several field cycles and no single-session trick substitutes: a separate throwaway invocation fails because the field drops in between, and three chained reads fail AND break the decode. `emutest.py` retries instead, typically succeeding on the 2nd or 3rd attempt; the mechanism is not established and the tool does not pretend otherwise (C78) | ~8 cold starts, 2 capture styles |
| L78 | — | `8d69d4a` | ⛔⛔ **The Chameleon cannot read its own emulation, and it is the only reader that cannot.** Front to front, emulator coupled at 15x the empty floor: `lf indala read` scores 0/14 and a bracketed sniff shows a 151-sample constant-phase run against the 928 the frame needs. The same emulator on a Proxmark gives 929 and decodes to 262 ms; the Chameleon reads a real tag at 912. Three of four reader/source combinations work and only ours-plus-emulator fails (C79). ⚠ Hypothesis: this decoder's premise is that fc/2 IS fs/2, so demodulation is multiplication by (-1)^n with no oscillator and no phase estimate — which is only valid if the subcarrier is LOCKED to the reader's carrier. A T5577 guarantees that by dividing the carrier; our free-running PWM does not, and at exactly Nyquist there is no margin. The Proxmark demodulates in analogue before sampling and is evidently more forgiving. ⇒ If that holds, carrier locking (C74) is not a refinement but a REQUIREMENT for self-consistency (C80, untested) | 14 reads + bracketed sniff |
| L79 | — | `6ca69c5` | ⭐⭐⭐ **The emulation works against both independent readers; ours is the outlier.** Flipper Zero, driven over its serial CLI with the Chameleon on its pad front-to-back: `rfid read` returns Indala26 FC 52 Card 63612 Parity + on **6 of 6** — our exact credential. Proxmark decodes to 262 ms. Our own reader: 0 of 14, with the emulator coupled at 15x the empty floor and confirmed awake (C81). ⇒ Two readers that share no code with us, or with each other, accept it. ⭐ And that reframes the reader limitation: a real T5577 divides the carrier so its subcarrier is locked by construction, which is precisely what our exact-Nyquist demodulation assumes — 60/60 real-tag reads across two tags and two units. The intolerance only appears against a source that is not carrier-locked, and no real tag is. `lf indala read` is specialised, not defective, and carrier locking buys self-consistency rather than correctness (C82) | 6 + 6 + 14 |
| L80 | — | `65791a8` | ⭐⭐ **IDTECK emulation works: 4/4 on a Flipper, closing the question C66 got wrong.** Chameleon on current firmware (burst 32) emulating `4944544b00000000`, read over the Flipper's serial CLI: `rfid read indala` returns Idteck 4944544B00000000 every time, and `rfid read normal` — ASK mode — returns nothing on the same setup, which is the control (C83). ⇒ The shared PSK1 transmit path in `utils/psk1.c` carries BOTH protocols correctly. C66 claimed 'PSK1 tag emulation has never worked on this device' on the strength of a Chameleon-to-Chameleon instrument that was measuring coupling; that retraction is now confirmed from the other side, with IDTECK working rather than merely unproven | 4 + 4 reads |
| L81 | — | `f3579ef` | ⛔⛔ **`lf idteck read` does not exist, and I scored it as a successful read in two ledger claims.** The IDTECK decoder in `idteck.c` is a documented stub and the CLI exposes only `write` and `econfig`; asking for `read` prints the subgroup listing, which contains the word IDTECK twice. `nulltest.py` counts `--hit` substrings across 5 invocations, so it reported **10/5 ✓** — a count larger than the number of attempts, printed plainly, which I read as success (C84). That false positive became the evidence in C55 that the IDTECK tag was 'audible in the Indala decoder's own band and modulation', and in C57 that the interferer's own reader was 'the strong bracket' where the amplitude probe was weak. ⇒ Both legs of the IDTECK null were broken: the band was wrong AND the reader was a phantom. C55's IDTECK arm is now marked unbracketed; Viking, PAC and Jablotron keep valid amplitude brackets and stand. ⭐ Found because the user asked whether the IDTECK decoder had ever been fixed — a question about scope, not about evidence. ⚠ C83 (IDTECK EMULATION works, 4/4) is unaffected: that was measured on a Flipper, which does implement an IDTECK reader | 1 command, 2 claims |
| L82 | — | `cb83b7b` | ⭐⭐ **The IDTECK null re-run, bracketed in the right band, and it passes.** After C84 showed both its legs were broken — wrong band, and a `lf idteck read` that does not exist — the tag was moved onto the Chameleon and measured at **22.65x the empty floor at 60–65 kHz**, the band a PSK1 subcarrier actually occupies. `lf indala read` then returned **0 frames in 20 reads** (C55 repaired, C85). ⇒ The adversarial null holds on real evidence: IDTECK is the only interferer whose physical layer is identical to Indala's, so it is the only one that puts valid PSK1 carrying the wrong frame in front of the decoder. ⭐ The phantom-reader detection added in L81 fired correctly on the exact case that caused it, reporting that the leg contributes nothing instead of a bogus 10/5. ⚠ I predicted a LOWER ratio than the ASK tags' 16–19x on sample-phase grounds; it came out HIGHER at 22.6x, so that expectation was wrong and did not matter | 20 reads, 1 unit ⛔ retracted by L86 |
| L83 | 20:01 | `c704a5c` | ⭐ **Rig A is back, and it is bracketed: PSK 4/4, ASK 0/4 on the committed driver.** `flipper.py` replaces the scratch file C81 and C83 were taken from. The Flipper reads the Chameleon's emulation as **Indala26 FC 52 Card 63612 Parity +** on 4 of 4 PSK attempts and nothing on 4 of 4 ASK attempts — the same built-in control as C83, reproduced on current firmware with an instrument that cannot repeat M28's mistake: success is an anchored `^name HEX$` line, not a substring, and the tool asserts hits ≤ attempts. ⚠ The scratch version could not have taken a trustworthy null: `rfid read` never times out, it loops until a tag decodes or the next character is ETX, so a failed read leaves the worker running and swallows whatever is sent next. That is invisible whenever the read succeeds and wrong exactly on the arm that matters. This sends ETX and waits for `Reading stopped`. ⭐ It also settles the device map by evidence rather than assertion: only `C3A1656543DE1` has an active slot emulating Indala (`F429364E46961`'s active slot 2 does not), and the Flipper reads Indala from its pad ⇒ C3A1 is the unit on rig A | 8 reads + 2 slot dumps |
| L84 | 20:50 | `ac058f7` | ⭐⭐⭐ **Our reader reads the FLIPPER's emulation, 8 of 8 — the carrier-lock theory made a prediction and it held.** C74 found that the Flipper clocks its emulation timer from the reader's own carrier, so a Flipper emulator is carrier-locked by construction, exactly like a T5577. Driven from `flipper.py emulate Indala26` into Chameleon #1 on rig A, `lf_indala_psk.c` decodes `a0000000e6bd0e92` FC 52 Card 63612 on every capture at energy ~8500; with the Flipper stopped, 0 of 4 at 294–1772 (C87). ⇒ The limitation is narrower than we have been writing it — not 'cannot read an emulator' but 'cannot read a FREE-RUNNING source', and the only one known is our own PWM (C79). README and NEXT corrected. ⛔ The first null was INVALID and briefly said the opposite: `timeout` SIGTERMs, SIGTERM skips `finally`, so the Flipper never stopped and the 'idle' captures decoded too (M29). ⭐ Also lands the energy measure the undecodable-signal status needs: 324 empty captures span 190–1772 and front-side tags 5075–11720, but a BACK-side tag is quieter than a front-side empty, so the threshold is set from the empty distribution alone and detects a loud source only (C88) | 12 captures + 488 for calibration |
| L85 | 21:05 | `79fd509` | ⭐⭐ **`STATUS_LF_SIGNAL_NOT_DECODED` shipped, flashed to both units and verified on four arms.** Bare antenna 5/5 'not found'; Flipper emulating IDTECK 5/5 the new status; Flipper emulating Indala26 5/5 a full read; real Indala tag on the second unit 3/5 read and 2/5 the new status (C89). ⇒ §1 is done. ⚠ The last arm says the status cannot separate 'wrong protocol' from 'marginal real tag', so the host message now leads with retry-or-reposition. ⛔ And chasing that arm turned up a bench that does not match its own description: Chameleon #2 reads Indala **a0000000801119c0** while the Proxmark reads an **IDTECK** T5577 **4944544B55667788** and no Indala at all — two PSK1 tags, not one, and 'the Proxmark writes the tag Chameleon #2 reads' is now UNVERIFIED. Queued under Needs hands, because §1c and §2 both lean on it | 20 reads + 2 Proxmark searches |
| L86 | 21:35 | `50f9eb8` | ⛔⛔ **`lf indala read` returns a WRONG CREDENTIAL from an IDTECK tag — L82's null does not hold at this coupling.** The user's question about the bench geometry (Proxmark — T5577 — Chameleon, one tag) forced the check: the tag dumps as `00081040 / 4944544B / 55667788`, an IDTECK frame, and `lf indala read` calls it `a0000000801119c0` "FC 144 Card 10504" on 4 of 8 reads (C90). ⭐ Isolated to **sample phase 28**, which `PHASE_ROTATION` uses: 0 of 8 at phase 0, 0 of 4 host-stacked depths, and the false frame at phase 28 with amp 5788 — 2.8x the straddle gate's amplitude bar with the shape test PASSING, so the gate correctly sees a loud well-formed frame and the agreement rule cannot see a deterministic error. ⇒ Not a straddle, not a stacking artefact: Indala's preamble is 33 bits of which 28 are a constant run, and that is too weak a pattern to survive a loud non-Indala PSK1 source at the wrong phase. ⚠ C85 is retracted; the earlier null passed at a different coupling and that is exactly the property C43 warns about | 8 reads + 20 captures + 1 memory dump |
| L87 | 22:05 | `2cd431d` | ⭐⭐⭐ **The wrong-credential bug is fixed, by decoding the protocol that causes it.** `lf_psk1_decode_ex()` takes a REJECT preamble: a capture carrying a valid IDTECK frame yields no Indala credential. On the bench the tag that returned `a0000000801119c0` on 4 of 8 reads now returns 0 credentials in 10 and reports 0x43 every time, while the Flipper's Indala emulation reads 6 of 6 on the same firmware (C91). ⭐ The veto is safe because the confusion is ASYMMETRIC — Indala's preamble is 33 bits of which 28 are a constant run and a loud IDTECK tag can produce it, whereas "IDTK" has no run longer than two and matched 0 of 480 committed Indala and empty captures. ⇒ An IDTECK reader must NOT veto on an Indala match; that direction would discard genuine reads. ⚠ Enforcing Wiegand-26 parity was the alternative and is NOT sufficient on its own: `make check` reports 13 of 17 wrong back-side frames failing parity, so four would still get through | 16 device reads + 488 captures |
| L88 | 22:30 | `0192f2e` | ⭐⭐ **§1c done: `lf idteck read` ships, and it cost one function pointer.** `lf_psk1_read()` is the Indala capture engine with the protocol passed in as a decode function, so IDTECK inherits the sample-phase rotation, both accumulators, the agreement rule and the 32KB of stacking without a byte of new RAM or a second copy of the 320-capture validation. 6 of 6 on the bench tag at card 8943462, matching the Proxmark and the memory dump; 4 of 4 not found on an empty antenna; 5 of 5 reporting 0x43 rather than a credential when pointed at an Indala source whose `lf indala read` succeeds 3 of 3 in the same minute (C92). ⭐ That simultaneous pair is the control §1c predicted — the project no longer needs an amplitude bracket to show a PSK1 reader rejects the other protocol | 18 reads |
| L89 | 23:15 | `1b5055b` | ⚠ **§3 not reproduced, and I broke rig A's emulation chasing it.** The PWM base clock is chosen only in `pwm_init()`, which runs only from `lf_sense_enable()`, so a type change should leave it stale — but switching slot 1 ASK↔PSK1, with the field held continuously, kept reading 4/4. Then the emulation stopped working altogether and has not come back: not after a forced sense re-enable, not after `hw slot store`, not after a full DFU reboot, with the slot verified as Indala carrying the right payload in a fresh session (C93). ⇒ A reboot re-runs `pwm_init` by construction, so whatever this is, it is not the stale base clock — §3 is neither confirmed nor refuted. ⛔ Two of my own errors are in this: `hw slot type -t INDALA` printed 'success' while doing nothing (the enum member is `Indala`), so three experiments ran against a slot that was still EM410X; and the EM410x arm I used as a control had never been shown to read at all, making it an all-negative test that could not have informed anything (F05 again) | 16 reads across 4 conditions |
| L90 | 23:45 | `cb85a2d` | ⭐⭐ **§8 written up as a decision, and the arithmetic removes the trade-off.** Reading the link map rather than guessing: 49.6 KB free between `__HeapLimit` and `__StackLimit`, the reader's 48 KB reclaimable, ~98 KB reachable. A 224-bit frame needs 168 KB in the current shape and 28 KB decoding in place without stacking (C94). ⇒ §1d is not gated on a preference, it is blocked outright while stacking exists. Stacking's measured value is 0% at the front — 68.75% either way — and 32%→72% on the back, which is the placement the README tells users to avoid. ⭐ §1 also lowers the cost of dropping it: a back-side failure now says 'subcarrier present, not decoded' instead of 'not found', which is the nudge to reposition. Recommendation recorded; the call is the user's | link map + 320 captures |
| L91 | 00:15 | `cf745fb` | ⚠ **§2 part one: the advertising guard is shared and applied, and its effect is untested.** Four LF readers use the SAADC and only `lf_reader_generic` suspended BLE advertising for the capture; hidprox, PAC and ioProx each started the field with no guard, and two of those three are the readers measured failing on loud tags. `lf_adv_suspend`/`lf_adv_resume` now live in `lf_reader_data.c` and all four call them (C95). ⛔ The read-rate comparison that would test C47 needs a HID or PAC tag and there is none on this bench — the T5577 carries IDTECK. Regression on the shared path is clean: IDTECK 6/6, Indala 0 credentials. ⚠ I nearly reported that regression against STALE FIRMWARE: the DFU trigger silently failed, `nrfutil` said 'No devices ... found', and the device kept running the previous build while answering every command normally. `hw version` carries the build's git describe and is what caught it; the retry loop then flashed it properly and the numbers above are from the confirmed build | 11 reads |
| L92 | 00:40 | `9d34ea9` | ⭐ **Three unblocks, and one of them is a finding.** The user power-cycled both Chameleons, approved DROPPING STACKING, and cleared the T5577 to be rewritten freely. ⭐ Rig A came back at 3/3 on the power cycle after surviving a full DFU reflash broken — so a reflash restarts the application without clearing peripheral state, and 'I rebooted it' is not evidence the hardware was reset (C96). ⇒ §8 becomes the first task rather than a question: remove the two accumulators and the scratch buffer, decode in place, 48 KB → 8 KB, keeping the two-capture agreement rule which is not stacking and is what holds wrong words at 0. §1d, §2 and §3 all unblock behind it. ⚠ The accepted cost is the back-side rate, 72% → 32%, at the placement the README tells users to avoid | 3 reads |
| L93 | 01:10 | `aed2c22` | ⭐⭐⭐ **§8 done — stacking is out, 41,176 bytes come back, and §1d is unblocked.** The two 16 KB accumulators and the 8 KB scratch are gone; the decoder works in place on the capture buffer and the reader is 8 KB. Free RAM 49.6 → 89.8 KB, so Indala224's 28 KB fits with ~62 KB spare (C97). ⛔ The agreement rule stayed — it is not stacking, and consecutive captures are a better independent pair than the two hand-separated accumulators ever were. Verified on four bench arms and 480 committed captures, with both decoders still agreeing word for word on 320. ⚠ The accepted cost, 72% → 32% on the back, is PREDICTED from committed captures and was not re-measured on hardware — that needs a placement this bench cannot make (C98) | 21 reads + 480 captures |
| L94 | 01:30 | `89ba1d6` | ✅ **The back-side rate is a non-goal, not a gap.** The user's call: that placement is not something the project is trying to support, so the 72% → 32% from removing stacking is accepted outright and no hardware arm is queued to confirm it. C98 is rewritten from 'the one number hardware has not confirmed' to what it actually is — a number about a use case we do not have. ⭐ The back-side CAPTURES stay committed and keep earning their place: 26 dB down, they are the only low-SNR corpus here, and `make check` regresses every gate against them. Not supporting a placement is not the same as discarding data taken there. README now says the back is unsupported rather than merely worse | — |
| L95 | 02:05 | `dcbf7cf` | ⛔⛔ **§1d hits a wall worth hitting: Indala224 is PSK2.** The Indala224 reader is built — format descriptor, repeat gate, 14336-sample capture, `lf indala read --224`, all compiling — and then the test tag turned out to be the wrong modulation. `lf indala clone -r <224 bits>` writes T55xx config **`000820E0`** and detect reports **PSK2**, against `00081040`/PSK1 for the 64-bit bench tag (C99). ⭐ The fix is already described in our own decoder header: the Proxmark matches on the PSK1 stream first and falls back to `psk1TOpsk2()`, which is XOR-ing consecutive bits — the same demodulator serves both, and the repeat gate survives because a repeating frame differentiates to a repeating stream. ⚠ Separately, the Proxmark's own read-back of the tag it had just written did not match it, at 'len 235' (C100), so §1d's independent reference has to be the memory dump. ✅ The bench tag was restored to `00081040 / 4944544B / 55667788` and re-verified: our IDTECK reader returns card 8943462, 3 of 3 | 1 write, 2 dumps, 3 reads |
| L96 | 02:55 | `28b5f4d` | ⚠ **The 224-bit capture path works; the 224-bit DECODE does not, and the host tooling cannot see it.** The PSK2 fallback is built (differential stream searched only when a format asks, direct stream first as the Proxmark does, no polarity search needed on the differential because XOR is inversion-invariant). ⛔ Two bugs found on the way: a leftover clamp to `INDALA_PSK_CAPTURE_SAMPLES` threw away 70% of every long capture, and energy was measured only after the frame had to fit — so a too-short capture reported "not found" instead of "something is there" (C101). Both fixed; the 224 read now reports 0x43 on a live tag rather than silence. ⚠ But it still does not decode (C102), and `lf sniff` caps at 4096 samples against the 7168 a 224-bit frame needs, so the host decoder and `ctest` — which have settled every previous decode question in this project — cannot be pointed at it. ⇒ Next is raising that cap. ⚠ I also shipped a scaled per-capture timeout on a hypothesis that turned out to be wrong; it is defensible on its own terms (a flat timeout for a 3.5x longer capture is incorrect) but it was not the bug, and reading the code path would have been faster than the build-and-see | 13 reads |
| L97 | 03:50 | `2b021be` | ⭐ **The 224-bit demodulation works; the acceptance rule does not, and the reader is gated off rather than shipped wrong.** Raising `lf sniff` from 8192 bytes to 28672 — it counted BYTES under a name saying samples, and 4096 samples is less than one 224-bit frame — finally let the host tooling see this format. It immediately paid: all four captures yield the tag's exact 224 bits in the differential view (C103). ⛔ But three acceptance rules in a row returned WRONG credentials — amplitude 1 of 4, repeat score 2 of 4, Proxmark-order direct-first 3 of 4 — because the direct view of a PSK2 tag carries an impostor that repeats just as well, identically across captures, which the agreement rule would confirm rather than catch (C104). ⇒ `INDALA224_READER_TRUSTED 0`: the command reports 0x43 and cannot return a credential. ⚠ And the Proxmark's own memory dump misread a block, which I had used as ground truth for two claims — caught only because a match failed at exactly bit 96, three blocks in (C105) | 4 captures, 3 rules, 2 dumps |
| L98 | 04:35 | `6a85905` | ⭐⭐⭐ **§1d done — Indala 224-bit reads, 6 of 6, and Indala is finally complete.** The three failed acceptance rules were all attacking the wrong problem. The direct view of a PSK2 tag is the running XOR of its data: a deterministic transform that repeats just as well, is identical across captures, and looks preamble-like wherever the data does. Nothing inside one capture can tell a frame from its own integral, and Momentum's exact two-preamble test cannot help either — at our ~2% bit error it rejected the true frame in all four captures (C106). ⇒ The reader must be TOLD the modulation. Every specimen is PSK2, so the format decodes the differential view and only that one: 6/6 on device matching the tag's memory, 3/3 not found on an empty antenna, 3/3 at 0x43 against a loud IDTECK tag, and the IDTECK reader still 3/3 (C107). ✅ Bench tag restored to IDTECK | 15 reads + 4 captures |
| L99 | 05:20 | `fd1f87a` | ⭐⭐ **§2 measured at last: the guard does nothing, and PAC is reproducibly broken.** With the T5577 written as H10301 and the guard made switchable so one line separates the two builds, the paired test C47 never had: **12/12 with the guard off, 12/12 with it on**, both matching the Proxmark's FC 118 CN 1603 (C108). ⇒ The guard cannot be shown to help a well-coupled specimen, because there is nothing to fix at 12/12; C47 predicts an effect only on marginal reads. ⚠ I suspected C45/C46 were stale back-side data and checked: they were taken at 10:47 and 11:44, after the 09:53 front-side discovery, so their failures are real. ⭐ Then the same tag written PAC/Stanley `CD4F5552` — which the Proxmark reads perfectly — gave `lf pac read` **0 of 10** (C109). That is the first reproducible failure in §2 and it is worth more than the ones merely believed | 34 reads, 2 builds |
| L100 | 06:05 | `4970242` | ⭐⭐ **PAC: the signal is clean, so the bug is interpretation — and that narrows §2 enormously.** Captured the PAC tag at 14336 samples and demodulated it on the host independently of the firmware: per-bit levels are sharply bimodal (2803 vs 7037) and the bit stream repeats **99.1% at lag 128**, the exact frame length, against 53-65% at every other lag. So a real, correctly-clocked 128-bit frame reaches the ADC and neither coupling nor the front end is at fault. ⛔ But the recovered frame is NOT the tag's blocks: best alignment 102 of 128 across every start, rotation and polarity, on a plateau rather than a peak (C110). ⇒ Either the on-air bits are not the raw blocks, or level-thresholding is the wrong recovery for T5577 NRZ. ⭐ The cheap next test is to make the Proxmark dump its own samples of the same tag and compare bitstreams — it reads this tag correctly, so whichever demodulation differs from its is the wrong one | 3 captures |
| L101 | 06:45 | `c86f1eb` | ⭐⭐⭐ **PAC's air bits are not its block data — proved by running our demodulation on the PROXMARK's samples.** 99.0% periodicity at lag 128, no exact match to the blocks, best alignment 119/128; our own capture gave 99.1% and 102/128. Two receivers sharing no hardware or code agree with each other and disagree with the memory dump, so the Chameleon front end is exonerated and the Proxmark's printed `Raw:` is a decoded form rather than the literal air bits (C111). ⇒ §2's PAC half is now a decoding problem with a named target: `pac.c` expects 10-bit UART frames carrying STX, '2', '0', eight ASCII card-ID bytes and an XOR checksum. ⚠ And a parser bug nearly derailed it: `.pm3` files are TEXT, one decimal per line, and reading them as int8 gave a plausible 59.7%/89-of-128 that was pure ASCII. Caught by the sample COUNT disagreeing with what the Proxmark had just printed (C112) | 36295 PM3 samples + 3 captures ⛔ retracted by L102 |
| L102 | 07:10 | `e6dc21f` | ⛔⛔ **L101 retracted within the hour: the air bits ARE the block data and my demodulation is the bug.** Running `pac.c`'s own rules over the tag's blocks in pure software: first 19 bits **0x7F902**, exactly `PAC_PREAMBLE`; twelve 10-bit UART frames all with valid start, odd parity and stop; spelling STX '2' '0' CD4F5552; XOR checksum **0x72** matching the twelfth byte exactly (C113). ⇒ The tag is transmitting a textbook PAC frame and both my demodulations failed to recover it. ⛔ The error in L101 was structural, not arithmetic: the Chameleon and the Proxmark are genuinely independent RECEIVERS, but I ran both through one level-threshold script of mine, so their agreement measured my bug rather than the air. M31. ⭐ And the check that caught it needed no hardware — a format with a checksum will say whether you have read it right, if you ask before theorising | 1 frame |
| L103 | 07:45 | `34e0992` | ⭐ **Two narrowings: the HID read is a cliff, and PAC's timing is exonerated.** Air-gap sweep with paper spacers between tag and Chameleon: 16/16 at 6, 12 and 18 mm, then **0/16 at 24 mm** (C114). No slope to sit in, so C47's marginal band is 1-2 mm wide at most — which is itself why that claim has never been testable. ⚠ The user's Flipper work cut out by 12 mm, but that was T5577 DEEP READS needing a bidirectional downlink; this is a broadcast read that only has to hear the tag. ⭐ And PAC: autocorrelation of the smoothed envelope peaks at exactly **4096 samples** — 128 bits x 32 — confirming both the RF/32 bit rate and the frame length, so clock recovery and bit-boundary locking are ruled out and only the bit DECISION remains (C115). Thresholding raw samples toggles on carrier ripple, median run 4 samples; smoothing 4-32 and edge-interval demodulation have not yet recovered a frame | 64 reads + 3 captures |
| L104 | 08:15 | `12c5f74` | ⭐⭐ **PAC's frame is provably in the capture; only the bit decision is wrong.** A matched filter built from the tag's own 128 bits scores |r| = 0.70 at one definite offset per capture, and the offsets differ by exactly 4096 — one frame period. Signal, timing and alignment all confirmed; r = 0.70 means ~15% of bit decisions are wrong (C116). ⚠ The sample-phase hypothesis remains UNTESTED: eight phases all scored 0.09-0.12 with cluster separation ~230 against 4234 flat on the pad, because the 21 mm spacer costs ~18x of the signal and buries the effect (C117). ⇒ The HID gap hunt and the PAC debugging now want opposite geometries, and PAC is the concrete reproducible bug while C47's marginal band is at most 3 mm wide and may not exist usefully. Recommending the tag goes flat | 3 + 8 captures |
| L105 | 08:30 | `4d27d5f` | ⚠ **The bench moved mid-experiment and the upper gap points are not trustworthy — verified by measurement rather than by reading the messages.** The T5577 was lifted off the Proxmark onto the table, and the spacer was named as both 20 and 21 mm in messages that crossed. Rather than reconcile the narrative I measured the state: the **Proxmark now sees nothing at all** (`lf search` silent), so the independent reference and the ability to rewrite the tag are both gone, and the Chameleon reads 0 of 4 — which is uninformative because the tag currently holds PAC, which never read even flat on the pad. ⇒ C114's ≤ 18 mm points stand; its upper end is marked uncertain. ⛔ Neither open thread can progress in this geometry: HID/C47 needs the tag rewritten, which needs the Proxmark coupled, and PAC needs the tag flat | 4 reads + 1 search |
| L106 | 09:00 | `d693ba0` | ⭐⭐⭐ **PAC explained: the LF path responds to TRANSITIONS, not levels.** At the known alignment, the level of a 1-bit depends on how long the run of 1s is — isolated 10679, pairs 6791, triples 5959, four-or-more 5422 — while 0-bits sit flat at 2114. An AC-coupled front end: sharp excursion on a change, decay toward baseline on a run. Every error lives in the long runs and isolated 1s are 0 wrong out of 20, so even a perfect threshold leaves 22 of 128 bits wrong (C118). ⇒ Level decoding cannot work for NRZ on this hardware, which vindicates `pac.c`'s edge-interval architecture and condemns my host decoder, not the firmware's design. ⚠ And the obvious software fix does not work: a first-order integrator takes 22 errors to 13-19, not to 0, so the response is not a simple high-pass (C119). ⇒ Next is instrumenting the firmware decoder rather than modelling the analogue path further. ✅ Bench restored to base config, no spacers | 3 captures |
| L107 | 09:40 | `88f8136` | ⭐⭐⭐ **§2's PAC half is fully diagnosed, with an offline reproduction to fix it against.** `pactest/` compiles the SHIPPING `pac.c` on the host behind a few Nordic stubs and reproduces the failure exactly: 0 of 3 captures, against 0 of 10 on the device (C120). ⇒ Then the calibration, transcribed and run on the same data, says why: `spike_cap` pins at its 8000 floor, `clip_min` comes out **0** rather than the 500-2000 the model assumes so both thresholds are misplaced to 2000/6000, **24-25% of samples get clipped** and **34% fall in the dead zone** (C121). ⛔ Worst of all, by C118 the clipped "spikes" are not LC ringing — they are the isolated bits, which are the only ones read with zero errors. The decoder discards its best data because its three-zone amplitude model describes hardware this is not | 3 captures |
| L108 | 10:15 | `9ab6f9b` | ⛔ **PAC needs a decoder REDESIGN, and the search space is now bounded from below.** Two more families tried and failed: transition detection against a running baseline, 0 of 3 (C123). ⭐ More useful is the ceiling: with bit boundaries known and the threshold at the ideal midpoint, a memoryless level decoder still gets **22 of 128 bits wrong**, and no variant beats 13 — so ~85% per bit is the cap for that entire family, far below what twelve parity-checked bytes plus a checksum need (C122). ⇒ Any working decoder must exploit the run-length dependence as INFORMATION rather than fight it as noise. That is a design task, not a tuning task, and worth doing deliberately rather than by trial | 3 captures |
| L109 | 10:50 | `fe07e0b` | ⭐⭐⭐ **Why only PAC breaks, and what the reference implementations do instead.** AC coupling is harmless to every other modulation here: Manchester guarantees a transition every bit, PSK is constant-amplitude. NRZ alone permits long runs, and NRZ alone fails (C124). ⭐ Momentum decodes PAC from EDGE TIMINGS — `decoder_feed(protocol, bool level, uint32_t duration)`, durations converted to bit counts, no threshold or amplitude anywhere — fed by a comparator (C125). ⇒ The Chameleon already has a comparator path that em410x, Viking and Jablotron all use successfully; PAC alone chose the SAADC, justified by an untested assertion that the comparator cannot give clean NRZ edges. That is the cheapest remaining experiment. ⚠ Also: §3 is blocked again and C96's trigger is now identified — I reflashed C3A1 during §1d, and reflashing kills LF emulation until a power cycle. Receive still reads 3 of 3 | 3 reads ⛔ partially retracted by L111 |
| L110 | 11:20 | `71483a5` | ⛔⛔ **§3 answered: the hypothesis is WRONG and the real bug is worse.** Clean paired run after a power cycle, using PSK as the readable side because EM410x emulation would not read on this pad at all (C127, unexplained): Indala 4/4 → switch to EM410X and force `pwm_init` → switch back to Indala, no mode cycle → **0/4** → force `pwm_init` again → **still 0/4**. ⇒ A stale PWM base clock would be fixed by the last step, since `lf_sense_enable()` re-runs `pwm_init()`. It is not. ⭐ What replaces it is concrete and reproducible: **changing a slot's LF tag type kills emulation until the device is power-cycled** — receive still reads 3/3, and neither a mode cycle nor a DFU reflash brings transmit back. Whatever is stuck survives a software reset, which rules out everything `pwm_init` touches. ⇒ A real user-facing bug with a one-line repro, and a candidate for §9 upstreaming | 19 reads ⛔ partially retracted by L111 |
| L111 | 12:10 | `4f616a1` | ⭐⭐⭐ **Instrumentation settles §3 and retracts two claims I made from the wrong explanation.** `hw emudebug` reports the LF emulation state in one call, which is what a bug costing a power cycle per test needs. Across `hw slot type` Indala → EM410X → Indala it shows `tag type` tracking 300 → 100 → 300 while the PWM base clock stays at 1MHz and `pwm_init` never re-runs — §3's mechanism, caught exactly as described. ⚠ Yet the round trip reads 3/3, because coming back to Indala restores the match, so the CONSEQUENCE remains untested (C128). ⛔ And the retractions: a reflash RESTORES emulation (4/4 right after one, no power cycle), and a slot-type change does NOT break it. C96 and C126 were the same error — a device left emulating EM410X, which this pad cannot read at all (C127) — dressed up as two different mysteries. ⭐ Both survived because I never had a positive control for the ASK arm; the instrumentation gave one that does not depend on reading anything | 3 dumps + 7 reads |
| L112 | 12:50 | `c521979` | ⭐⭐⭐ **C127 resolved, §3 demonstrated, and a second real bug found — all from one instrumented session.** EM410x emulation is fine: **5 of 5** once the clock is right and a sequence is loaded. The 0/4 that had confused three experiments was two firmware bugs interlocking. ⛔ **C129:** `lf_sense_disable()` nulls `m_pwm_seq` and `lf_sense_enable()` never reloads it, so any mode cycle leaves the device in Tag Emulator mode, sense enabled, clock correct — and emitting nothing. That is what killed emulation every time, because I used mode cycles to force `pwm_init`. ⭐ **C130:** with the sequence loaded, EM410X at the stale 1MHz clock reads 0/4 and at the correct 125kHz reads 5/5 — §3's consequence, finally shown, same data and pad. ⚠ **C131:** `hw slot type` does not persist without `hw slot store`, which had silently reverted two experiments | 9 reads + 6 dumps |
| L113 | 13:30 | `dda973d` | ⭐⭐⭐ **All three emulation defects fixed and verified — the bench can now change a slot's LF type and emulate it without a human.** C129: stop nulling `m_pwm_seq` on sense disable; the sequences are static and outlive the uninit, which `psk1.h` already said. C130: `lf_tag_data_loadcb()` becomes a wrapper that re-inits the PWM when the type's required base clock differs, so every early return in the loader is covered. C131: the CLI now says a type change is RAM-only until `hw slot store`. ⇒ Verified as a round trip with no reboot and no power cycle: Indala → EM410X reads ASK 4/4 → back to Indala reads PSK 4/4, with `pwm_init runs` incrementing and the clock moving 125kHz ↔ 1MHz on demand (C132). ⭐ This is the defect that most obstructed unattended work: a driver alternating read and emulate tripped C129 every cycle | 19 reads |
| L114 | 14:10 | `76f89b4` | ⭐⭐ **§4 part one: the burst is a time budget now, not a frame count.** A frame is not a fixed duration — 16.4 ms for Indala 64-bit, 32.8 ms for PAC and EM410x, 57.3 ms for Indala224 — so the old constant handed the longest-frame protocol the worst field-loss latency, 1.83 s, and nobody had noticed because that emulation did not exist yet. The count is now derived from the sequence's own `counter_top` sums: Indala 31, EM410x 16, both verified on device and still emulating (C133). ⭐ Checked both reference implementations while deciding: the Flipper runs `while(true)` on a DMA'd timer and the Proxmark `for(;;)` until host or button — NEITHER does periodic field detection, because both are tools under direct user control. The constraint is ours alone, so there was no design to copy. ⇒ But both stay clock-locked to the reader's field while emulating, which points at counting carrier edges for field detection instead of pausing — the same plumbing §5 needs | 6 reads |
| L115 | 14:50 | `7de9b30` | ⭐⭐ **§4 answered — and the design that was going to make it obsolete is dead on the schematic.** L114 closed by proposing carrier-edge counting for field detection, the way the Proxmark and Flipper stay clock-locked while emulating. It cannot be built here: **every path from the LF antenna to the MCU passes through VD1, a detector diode at the coil**, and the one pin that looked like a way in is the SELECT input of an analog switch, not a sense node (C134). Two sources that predate the question agree — the manufacturer's schematic, and an upstream maintainer's "envelope-only" comment in `lf_tag_em.c`. ⛔ My own notes already opened with `VD1 detector` and I proposed the experiment anyway; that is M32. ⭐ So the burst stays, and the sweep it would have retired is the measurement that counts: 24 runs across **120×** of burst length — 2 frames / 32.8 ms, 31 / 508 ms, 32 / 524 ms, 245 / 4014 ms. **Flat above ~500 ms, falling below it** (C135): 8× more burst changed nothing and left 262 ms and 290 ms undecodable in all 5 runs, while the 32.8 ms arm collapsed to a 66 ms ceiling 7 times in 8 **despite carrying 27% more signal than every other arm**. ⇒ 500 ms stays, now for a measured reason rather than because 32 frames happened to land near it. ⚠ Two by-products are worth more than the number: the 131–197 ms ceiling at long bursts is **not** the boundary and is unexplained, and `emutest.py` measures a decodable PREFIX rather than a decodable WINDOW — a 131 ms window decodes at offset 65.5 ms in the very capture whose prefix ceiling was 66 ms (C136), so every ceiling here is a lower bound | 24 emutest runs + 5 window offsets |
| L116 | 15:30 | `9d4e6ad` | ⭐⭐⭐ **The ceiling §4 could not explain is clock slip, measured at 131 ppm.** `clockoffset.py` asks the question without reference to decoding at all: at fc/2 = fs/2 the mixed baseband is real, so a frequency error shows up as `A·d[n]·cos(2πΔnT+φ)`, and **squaring** removes the ±1 data and leaves a tone at 2Δ. Five runs give **131.1–132.0 ppm** with peak/median 59–72 — one whole subcarrier cycle of slip every **122 ms**, against a decode ceiling of **131–197 ms** measured by the Proxmark's demodulator on pass/fail alone (C135, C137). Two routes sharing no step, one scale. ⛔ The first attempt at this failed and is documented in the file rather than deleted: correlation-lag fitting reported +137 to +323 ppm with 27-sample residuals against a 0.3-sample effect, because Indala is RF/32 and the autocorrelation lattice puts a competing peak every 32 samples. ⭐ The replacement was validated on synthetic PSK1 at known offsets BEFORE it was pointed at the bench — ±1 ppm from 20 ppm up, and at a true zero it returns nonsense with peak/median ≈3, which is what now gates it. ⇒ §5 changes character: the cost of not locking is a ~122 ms coherent window, and C134 already says the standard fix is unbuildable here. ⚠ What is missing is the null, and it needs hands: a carrier-locked T5577 in front of the Proxmark should read below the 20 ppm floor. `lf search` finds nothing there now — the re-aim took the tag out of the field | 5 runs + 15 synthetic validations |
| L117 | 16:10 | `5178310` | ⭐⭐⭐ **The control landed and C137 is now closed from both ends.** One placement — the T5577 back in the sandwich between the Proxmark and Chameleon #2 — and the whole thing resolves. The tag was wearing HID Prox, not the PAC the notes recorded; blocks dumped first (`00107060 / 1D555955 / 5569A9A5 / 55A59569 / D5B2649F` + stale Indala224 in 5–7), then written with **the same frame the emulator plays**, so the pair differs in exactly one thing: whether the source divides the reader's carrier. **Locked tag:** NO TONE, 5 of 5, peak/median 2.4–3.1 — the band the synthetic true-zero produced — and the Proxmark decodes the **full 290 ms, 5 of 5**. **Free-running emulator:** 131 ppm, peak/median 59–72, 0–197 ms and erratic. ⚠ The tag managed it on **less** signal, fc/2 16.0 against 17.9. ⇒ Amplitude excluded, the Proxmark's demodulator excluded, the burst boundary already excluded by C135 — the clock is what is left, and two instruments sharing no step agree on which source is which (C138). ⭐ Practical shape of the result: **any reader demodulating coherently for more than ~122 ms will read a real tag and fail our emulation.** ⭐ The sandwich also turns out to be strictly better than the old rig B — Chameleon #2 reads the tag 4/4 through it, so the rig now runs Proxmark↔tag, Chameleon↔tag AND Proxmark↔Chameleon, the last by lifting the tag out | 10 bench runs + 4 reads |
| L118 | 16:40 | `87f2963` | ⭐ **Attacked C137 before calling it closed, and it held.** Writing the review prompt surfaced a specific way the 131 ppm could have been an artefact: the burst cycle is ~510 ms, its 4th harmonic ~7.9 Hz, the measured tone 8.20 Hz, and the FFT bin 3.44 Hz — too close to separate by arithmetic. `burstnull.py` settles it in simulation rather than a reflash: zero clock offset plus 2 ms gaps at 507.9 / 250 / 120 / 32.8 ms never reaches the confidence gate (peak/median 2.4–5.5), while 131 ppm is recovered to ±0.5 ppm at every burst length **with the tone fixed at 8.17–8.21 Hz** — a burst harmonic would have moved with the burst (C139). ⚠ Kept in the notes: at 32.8 ms the artefact climbs to 5.5, near the gate of 6, so the margin is a function of how many gaps land in the record. ⇒ The clock conclusion is marked **likely closed pending an independent adversarial review**, and `ADVERSARIAL.md` now carries a second brief written to be hostile to it | 8 synthetic cases |
| L119 | 17:20 | `4e4e6c6` | ⛔⛔ **§2 reframed: PAC's levels are destroyed before the ADC, so "fix the demodulation" was the wrong plan.** The failure reproduces on the host (0 of 3, shipping `pac.c` via `pactest/`), and the reason is visible in the capture rather than the decoder: **33–37% of every capture is pinned at the bottom rail**, 2.6–4.1% at the top, and `lf sniff` says so itself — *"gain 1/6 is too high for the 5328 count DC pedestal"*. 1/6 is already the lowest SAADC gain and the clipping is in the op-amp chain above it; the alternative tap AIN0 reads `0xff–0xff`, pinned the other way (C140). ⭐ That explains C46's three byte-identical tags reading 0/6, 3/6 and 7/9 — coupling-dependent saturation. ⭐⭐ **What survives is TIMING:** the frame period is exactly **4096.000 samples** = 32.000 per bit (C141), which turns L109's comparator recommendation from an analogy with Momentum into a mechanism — clipping keeps transition times and destroys amplitudes, and NRZ needs only the former. ⇒ Measured with a graded score for the first time, after writing a KNOWN frame to the tag: no level recovery beats **6 errors of 128** against a random baseline of 47, and PAC has no error correction, so 6 is as useless as 60 (C142). ⚠ Also found and NOT the cause: `lf sniff` never suspends BLE advertising while every LF reader does, so the diagnostic instrument and its subject use different acquisition paths (C143). ⛔ And a self-correction inside the session: the first scored comparison reported the mean aggregator flooring at 26 errors against the vote's 6, a 4x gap — it had swept threshold for both arms but bit phase for only one. Equal sweeps give 8/15/18 against 6/8/6 (M33) | 3 host runs + 4 captures + 672 scored configurations |
| L120 | 18:10 | `44e1597` | ⭐⭐⭐ **PAC READS. 0 of 10 becomes 10 of 10, and the bug was the reader's own transmitter.** The amplifier saturates on a well-coupled tag and the clipping is upstream of the ADC (C140), so the cure is to drive the field more weakly — reducing the signal into the amplifier without moving the tag. Scored as bit errors of 128 against the known frame: drive 2 → 6, stock 4 → 6, **drive 6 → 0**, drive 7 → 1, with the rail fraction tracking (34.0%, 36.4%, 26.0%, 0.0%). A real optimum, because drive 7 stops the clipping and is then too weak to modulate. ⇒ `pac_read()` sweeps `{4, 6, 2, 7}`, fresh calibration per step, stock first so a tag that already reads is unaffected; which step wins is a property of COUPLING, which is exactly the spread C46 saw across three identical tags (C144). ⚠ It needed the shared LF PWM at 1MHz/top 8 instead of 500kHz/top 4 for the finer steps — every LF reader runs on that, so both paths were regressed: **Indala 6/6** and **EM410x 6/6**. ⛔ Also caught in my own change before it shipped: flooring the step length would have turned a 200ms request into 480ms; it now drops steps rather than overrun the caller. ⛔⛔ And a retraction of something I wrote into the plan four hours ago — the comparator/edge route is **worse**, not better: 26 errors against the level path's 6, with a systematic duty bias visible directly in the run lengths (C145). Momentum's approach does not transfer to this front end | 20 device reads + 4 scored captures + 288 simulated configurations |
| L121 | 19:00 | `4843a15` | ⛔⛔ **Three wrong explanations in one hour, and the notes had already answered two of them.** `lf hid prox read` read 0 of 12 and I said the re-aim had handed us the failing specimen §2 wanted — the user pointed out nothing had moved. I then said it was my own PWM change and the pre-change build agreed at 12 of 12. ⭐ The return leg killed both: post-change build, flashed again, **12 of 12** (C146). A/B is not an experiment against a noisy subject; A/B/A is, and C45 already recorded HID failing 15-20% with an unexplained 0/15 (M35). ⛔ The mechanism claim was wrong too — HID shows **0.0% of samples at either rail** against PAC's 33-37%, and I had been reading the sniff tool's `gaps` line, which is a threshold computed relative to the capture rather than a rail measure. HID never had the saturation disease. ⚠ **And then PAC went 10/10 to 0/10 on the unchanged build.** First a false alarm of my own making — I had written HID onto the tag and forgotten, so there was no PAC tag to read (the bench-tag row exists for exactly this and I still did it). With PAC restored it was still 0/10, and that part is real: **at the identical drive 7 the signal is now 68% larger, envelope rms 3272 → 5486, and the bottom rail has gone 0.0% → 37.1%** (C147). Coupling rose, every step of the sweep is saturated, and drive 7 is the weakest the current top_value allows. ⇒ The model predicted this exactly; the fix's RANGE is what is short. Both the 10/10 and the 0/10 are true at their own coupling | 36 HID reads + 20 PAC reads + 12 scored captures |
| L122 | 19:40 | `6ee1369` | ⛔⛔ **A fourth wrong explanation, and the one that cost the user's time.** I reported that the bench's coupling had risen 68% and asked whether the tag had been reseated; the user reasonably suggested the Proxmark's coil. Both were chasing a fact that is not true. ⭐ The discriminating measurement was the one I never took — **drive 4**. It is unchanged across the whole session (rms 5592 → 5435 on PAC, 3100 → 3105 on HID) while **drive 7 stopped attenuating** (1105 → 3106 on identical tag contents). A coupling change moves both arms; only the weak arm moved. ⇒ C147 retracted: the drive CONTROL goes inert partway through a session (C148). Firmware version, tag contents, block 0, stock amplitude and the absence of any external field are all verified constant, and a mode cycle does not restore it. ⚠ A power cycle is the one reset unavailable from here and is the next test. ⛔ Also fixed, and real regardless: `cmd_processor_lf_sniff` applied the drive on EVERY chunk while resetting it only on chunk 0, so every `lf sniff --drive N` left the field weakened for whatever ran next. The capture itself was always correct, which is why it survived — the bug was invisible in the thing being measured. ⚠ It is not the inertness, and the A/B/A that would have blamed it came out 6/6, 6/6, 6/6 | 20 captures + 18 device reads |
| L123 | 20:20 | `48119a4` | ⭐⭐ **Stopped testing C148 behaviourally and instrumented it instead.** Every attempt on the drive inertness so far inferred a peripheral's condition from a decode rate, which is how one session produced four wrong explanations. `hw lfdebug` reads the state directly: the drive value, and PWM0's base clock, COUNTERTOP, DECODER load mode, ENABLE, SEQ[0].CNT and whether SEQ[0].PTR still points at our own array. ⭐ The fields that matter are snapshotted **at playback start** — the instrument's own first run showed why, reporting drive 4 after a `--drive 7` capture, because the capture path restores the stock value on the way out. ⇒ One call now decides between "the RAM was clobbered" and "the peripheral ignored a correct value", and names the third case — another module holding the sequence pointer (C149). ⚠ The leading suspect is recorded but NOT shown: `lf_tag_em.c` and `lf_125khz_radio.c` both drive `NRFX_PWM_INSTANCE(0)` with separate ownership beliefs, and the tag path calls `nrfx_pwm_uninit()` on it from two places while the reader's `m_reader_inited` stays true (C150). A mode cycle does not reproduce the fault, which weakens it | 2 captures + 4 state dumps |
| L124 | 21:10 | `fd2babc` | ⚠ **Soaked C148 and could not reproduce it — 112 operations, clean.** `drivesoak.py` ran the session's own mix against the fixed firmware, checking the drive control after every single operation: drive sweeps 1-7, AIN0 / phase / gain / settle / free-run sniffs, mode cycles, slot changes and type flips, all four reader paths, and Proxmark rewrites cycling PAC → HID → Indala → EM410x. Maximum deviation from baseline **0.020**, against a detection margin of 0.20 and a real fault that moves the ratio 0.41 (C151). ⭐ Run 1 of the soak caught something at op 13 and it was the DETECTOR, not the fault — a fixed threshold fired the moment the tag became Indala, whose constant-amplitude PSK1 gives a weaker envelope and a naturally higher ratio. The trap's register dump disproved it on the spot: `at last start: drive 7, seq ptr ours True`, every register correct. ⇒ That is `hw lfdebug` paying for itself on its first outing; under the old regime it would have been written up as a fifth wrong explanation. ⛔ **The fault has still never been observed with the trap armed**, so none of C149's three branches is chosen. ⚠ And a correction: I said the chunk leak was not the mechanism because an A/B/A came out 6/6, 6/6, 6/6 — that test measured HID read success, which is insensitive to drive at every setting, not drive effectiveness. It answered a question nobody asked, and the leak is now the leading UNEXCLUDED suspect rather than an excluded one | 112 operations × 2 captures |
| L125 | 05:50 | `237c73a` | ⭐⭐⭐ **Indala224 emulates — 6 of 6 exact — and the bug it took to get there is the best thing learned today.** `lf indala write --224` and `TAG_TYPE_INDALA224` are built, the PSK1 modulator is parameterised by modulation and frame length, and the PWM buffer went 8192 bytes → 3584 while its longest frame grew 64 → 224 bits, because all 16 entries in an RF/32 bit are identical and `SEQ[n].REFRESH` will hold one (C154). ⛔⛔ Then the emulation decoded **6 of 6 as a stable WRONG credential** — twice, differently, once for each encoding I tried. An odd-parity PSK2 frame repeats at TWICE the frame period: a real tag's modulator is never reset at the boundary so it inverts the next frame, and our single-buffer replay does not. The wrap error moves where the preamble search lands, so the whole frame re-frames around it (C152). ⚠ **My first fix was in the wrong direction and I shipped it** — the two wrong readings bracketed the true frame at rotations −1 and +1, which is arithmetically impossible for a whole-bit shift and should have stopped me; what worked was fitting a reader model to BOTH wrong readings, which named the period immediately. ⭐ Two nulls throughout: ASK control 0/6 on every arm, and an Indala26 A/B/A at 5/6 → 6/6 with `frames per burst` 31, the exact number C133 measured before the buffer changed. ⭐ **C100 closes too**: the Proxmark's 224 read-back is noisy, not wrong — three reads of a real tag gave one nibble out, badly wrong, and exact (C153). ⛔ **The T5577 stopped coupling to either Chameleon** — rms 415 against 5541, with `hw lfdebug` clean, drive 4 vs 7 still separating, and Chameleon #1 reading the Flipper 3/3 — so the write half of §1d is verified only as far as the wire and needs hands (C155) | 30 Flipper reads + 9 device reads + 3 pm3 reads |
| L126 | 06:40 | `8a5784a` | ⭐⭐ **Closed the hole C152 came through: the emulation encoder now has a host round trip, and it is shown to catch the bug.** `psk1.h` has said "verify a change here by DECODING it, never by reading it" since it was written, and the only decoder that could was a Flipper on a bench — so three encodings shipped to the radio in one session with nothing between. `ctest/roundtrip.c` compiles the shipping encoder and the shipping decoder into one binary and renders the PWM sequence the way the peripheral does, **replaying the buffer rather than extending it**, which is the exact property the bug lived in. Four arms exact; with the doubling disabled the odd-parity arm alone fails, on both the entry count and the decode. ⭐ It also found a macro collision the device could never have reported — reader and emulator both defined `LF_PSK1_MAX_FRAME_BITS` — which is an argument for the harness independent of the bug it was built for (C156). ⚠ `make check` still agrees word for word on all 320 captures | 4 arms x 2 builds + 320 captures |
| L127 | 07:40 | `23b0f09` | ⭐⭐ **§10 opens with Keri, and the PSK1 family claim is now measured rather than argued.** NEXT.md has said for days that Keri and NexWatch "should reuse the PSK1 path nearly whole"; four captures of a Momentum-emulated Keri credential decode through our Indala demodulator **unchanged, 4 of 4**, to the frame predicted from Momentum's source. So Keri is a `lf_psk1_format_t` entry plus a de-scramble — `lf keri read`, `lf keri write`, `lf keri econfig`, `TAG_TYPE_KERI` — and nothing in the demodulator moved (C157). ⛔ **The cross-protocol null caught a real defect within ten minutes of the format existing**: with no veto, 1 of 5 Indala captures returned a confident Keri credential, the Indala payload under a forged preamble. That is C90 in a third pairing, and it is structural — any 64-bit PSK1 frame at RF/32 is a preamble generator for every other protocol on that layer. With the Indala veto: Keri 4/4, Indala 0/160, empty 0/160, IDTECK 0/16, Indala224 0/4. ⭐ **Rig A running backwards is what made this possible with the T5577 dead** (C155): the Flipper emulates any of Momentum's 26 protocols into our reader, so every new protocol's READ arm is testable with no tag and no hands. ⚠ Keri's T5577 blocks turn out to be a ROTATION of its air frame — `(id << 3) \| 7` — so the writer rotates rather than transcribes (C158). Write is built and unverified, blocked behind the same placement as Indala224's | 4 captures + 340 nulls |
| L128 | 08:30 | `23b0f09` | ⛔⛔ **STOPPED: both rigs have lost coupling, and it is not two placements.** Keri's on-device read returned 0x43 three times, so I ran the Indala26 control that read 3/3 this morning — and it returns 0x43 too. The fc/2 subcarrier amplitude has collapsed **24.3 → 9.6**, below the presence threshold, while broadband rms ROSE 937 → 1550; and the idle-versus-emulating pair is FLAT at 9.8/9.6, so the Flipper is not reaching the antenna at all. Rig B went the same way hours earlier (C155). Every instrument check is clean on both units — sense DISABLE, emulating False, PWM0 correct, drive 4 and 7 still separating. ⇒ A raised floor with a collapsed signal is interference, not a moved tag, and two rigs failing alike within hours points at something shared (C159). ⭐ **Worth saying what this did NOT cost**: Keri's decoder is verified against four committed captures of a real Momentum emulation taken while the bench still worked, with four nulls — the evidence is real, it is simply not the on-device arm, and it is labelled that way in the grid | 6 captures + 4 state dumps |
| L129 | 10:20 | `90485db` | ⭐⭐⭐ **The bench came back and Keri closed to read + write + emulate, all verified on hardware — but only after two defects that both have the same shape.** ⛔ **The reader needed a longer capture**: at 4096 samples `lf keri read` returned 0x43 six times on a real Proxmark-written tag while the host decoded the same captures 3 of 4. A length sweep over those captures gave a clean threshold — 4096 → 0/4, 5120 → 3/4, flat to 14336 — and at 8192 the device reads **6 of 6**. The veto was disabled and re-tested to exclude it first (C161). ⛔⛔ **And the emulation must carry the BLOCK form, not the reader's frame view**: the two are the same 64-bit cycle three bits apart, and emitting the frame view gave Momentum a stable WRONG credential 6 of 6, where the block form reads 5 of 5 correct with an Indala26 control correct either side (C160). ⇒ **Emulate what the tag puts on the wire.** ⚠ Both mechanisms are inferred rather than isolated, and both notes say so. ⭐ **Write verified independently**: the Proxmark reads our Chameleon-written Keri as `Internal ID: 12345`, 3 of 3. ⚠ Separately, `lf t55xx dump` misreports bit 31 of every block on a PSK2 tag — one dump, flagged for re-test, with the PSK1 dump from the same writer exact as the control; it means the advice this project gave, to verify a PSK2 write against the dump, is backwards (C162). ⛔ C155/C159's coupling collapse cleared without a diagnosis — recorded as intermittent, not fixed | 15 device reads + 32 host decodes + 6 pm3 reads |
| L130 | 11:00 | `a6778c0` | ⛔ **The coupling failed again within the hour, with a DIFFERENT signature — and that weakens C159 rather than confirming it.** Thirty minutes after `lf keri read` returned 6 of 6, the same rig reports `LF tag not found` and a Chameleon write does not land, while the Proxmark reads the same tag perfectly. A drive sweep separates the halves cleanly: rms scales **298 / 417 / 596** across drives 1/4/7, so our transmit and receive are working, while **fc/2 sits at 0.1-0.4 at every drive** — nothing is answering. ⚠ C159's episode looked like interference (signal down, noise UP); this one is both at the floor, which is an empty antenna. ⇒ Either two faults or one whose signature depends on state, and C159's inference should stop being quoted as settled (C163). ⭐ What this does NOT touch: everything verified this session was verified while the bench worked, with its own controls, and none of it rests on the coupling being stable | 3 sniffs x 3 drives + 5 reads |
| L131 | 01:30 | `c5ffd94` | ⭐⭐ **NexWatch decodes on the first attempt — 4 of 4 exact, 508 nulls clean — and the capture-length measurement confirms C161's inferred mechanism from the other side.** A Proxmark-cloned NexWatch (`--cn 12345678 -m 1 --nc`) reports exactly what §10a predicted from both references: `lf t55xx detect` PSK1 / RF-32, block 0 `00081060`, and blocks `56000000 / 00436455 / 121E6000`. Four captures at four sample phases decode through the unchanged demodulator to `5600000000436455121e6000`, byte for byte the Proxmark's own raw — so NexWatch is a `lf_psk1_format_t` entry plus a de-scramble, the fourth protocol through that descriptor (C164). ⛔ **The capture length is 3456 samples, not the 12288 §10a guessed** — and a 96-bit frame needing a SHORTER capture than Keri's 64-bit one is backwards unless C161's inferred mechanism is right, which is exactly the prediction it makes: NexWatch's frame starts AT a block boundary as Indala26's does, where Keri's starts three bits before one. C161 could only infer that from a single protocol; this is a second and independent, and it was a prediction before it was a measurement (C165). ⚠ **A correction to §10a, which said Momentum gates on the parity AND the checksum**: it gates on the parity only, and the checksum cannot be a gate at all — its magic byte is not in the frame, both references infer it by trying three vendor constants, so refusing an unmatched checksum would refuse a vendor we have not met. ⛔ **And the null for the gate came back BLANK**: with the parity check removed the 40 fixed bits alone still reject all 508 nulls, so the parity is kept on Momentum's authority and a structural argument, NOT because this bench has seen it catch anything — `ctest/cdemod --nogate` exists so that stays measurable | 4 captures × 9 truncation lengths + 508 null captures + 320 regression captures |
| L132 | 02:10 | `419edb8` | ⭐⭐ **NexWatch reads AND writes on device — 6 of 6 and 3 of 3 — and the write test was built so a stale read could not pass it.** `lf nexwatch read` returns card 12345678 / mode 1 / Nexkey **6 of 6** against the Proxmark-cloned tag, matching its own reader field for field. Then the tag was wiped and our firmware wrote **a different card number, a different mode AND a different vendor magic** (87654321 / mode 2 / Quadrakey), which the Proxmark reads back **3 of 3** with `lf t55xx detect` reporting PSK1 / RF-32 / `00081060` — so no field of the result could have survived from the previous credential (M33). ⭐ The writer TRANSCRIBES where Keri's ROTATES, and that is settled by measurement rather than symmetry: a real clone's blocks are `56000000 / 00436455 / 121E6000`, so NexWatch's frame starts AT the block boundary while Keri's `(id << 3) | 7` starts three bits before one (C158, C160). ⭐ The host frame builder — scramble, parity, checksum — is asserted at import against the Proxmark's own clone bytes rather than against its own inverse, because a codec checked only against itself passes with both directions wrong, which is exactly how this project lost a fortnight (C166) | 6 device reads + 3 pm3 reads + 1 detect |
| L133 | 02:45 | `5cda84a` | ⭐⭐⭐ **NEXWATCH EMULATES, AND THE PSK1 FAMILY IS CLOSED.** The Flipper reads our emulation of card 11223344 / mode 3 / Honeywell **6 of 6**, then **0 of 4** with the emulation stopped, then **4 of 4** on the return leg — A/B/A rather than A/B, because a single pass against an intermittent bench is not an experiment (M35). All three fields match what was set, and the card number was chosen to differ from BOTH the factory default and the credential on the T5577, so neither could masquerade as a pass. ⭐ **No rotation was needed and that was known before the flash**: the emitted frame is the Proxmark clone's own block contents, and the host round trip carries two NexWatch arms — the reference credential and our own second one, which differ in the mode nibble, the parity nibble and the checksum byte, so an encoder that mangled any of those three could not pass both (C167). ⛔ That check ran BEFORE the radio, which is the whole point of `ctest/roundtrip.c`: three wrong encodings once shipped to the air in a single session because the only decoder that could verify them was a Flipper on a bench (C156). ⭐ **Five protocols now share one `lf_psk1_format_t` and one modulator** — Indala26, Indala224, IDTECK, Keri, NexWatch — all three arms each | 10 Flipper reads + 4 null reads + 7 round-trip arms + 320 regression captures |
| L134 | 03:20 | `0f4b832` | ⭐⭐ **C162 re-tested at n=70 and half of it is retracted — the PSK2 dump artefact is real, is exactly bit 31, and is STOCHASTIC rather than systematic.** Ten dumps of an Indala224 tag we wrote: **23 of 70 data-block reads have bit 31 wrong and none of the other 2170 bit positions read is wrong**, in both directions (19 down, 4 up), with **1 dump of 10 completely clean**. C162 described seven blocks all differing by an identical `80000000` — that was one draw from this process read as the process itself, and it is corrected in place (C168). ⭐ The PSK1 control is spotless, 40 of 40 block reads byte-exact, and both independent readers recover the 224-bit credential in full — so the tag's memory is right and the dump is the instrument. ⚠ **A harness error of my own, caught before it became a bench diagnosis**: three dumps came back completely empty and I nearly recorded a fourth coupling failure. `lf t55xx dump` reads the chip config that `detect` caches, and every `pm3 -c` is a fresh session — the empty dumps had no `detect` in front of them. ⚠ **And a genuine transient WAS seen**: one interleaved run had the Proxmark read the credential 0 of 3 while returning garbage blocks with duplicate numbers; minutes later both readers were 3 of 3 and the fc/2 skirt measured 393011 against a healthy-tag reference of ~181000. Recorded as an observation, not a diagnosis — the amplitude was taken after it cleared, so it does not characterise the failure | 10 PSK2 dumps + 5 PSK1 dumps + 6 reader checks + 1 fc/2 capture |
| L135 | 04:05 | `4306320` | ⭐⭐ **The ASK/biphase family opens, and its FIRST measurement is that our reader saturates on the tag — before a line of firmware was written.** A Proxmark-cloned Gallagher (region 1 / facility 4321 / card 6789 / issue 2) characterises cleanly: **ASK, RF/32, block 0 `00088060`, sequence terminator SET**, 96-bit frame, 16-bit preamble `0x7FEA`, blocks `7FEAA31E / 76D86C6D / 868CC249` — block-aligned, so its writer will transcribe as NexWatch's does. ⛔ **But the capture rails at BOTH ends at every drive but one**: 26-36% of samples pinned at the bottom at drives 1, 2, 4 and 6, and **0.0% at either rail at drive 7**. ⭐ That is C140/C144 reproduced in a second protocol family, and for ASK it is not cosmetic — PSK1 carries its data in subcarrier PHASE, so clipping costs it little, while **ASK carries the data IN the amplitude** (C124/C144). ⭐ **The damage is visible in the instrument, not just the histogram**: run counts across four sample phases are 717/745/755/833 when clipped and **646/646/646/648 at drive 7** — noise-driven against deterministic. ⇒ Every ASK reader added here must sweep drive the way `pac_read()` does; that is now known before the code exists rather than after it reads 0 of 10 (C169). ⚠ **The decode is NOT achieved and this entry does not claim it.** Run lengths cluster at ~16, ~26 and ~32 samples where Manchester permits only ~16 and ~32, and the ~26 cluster is unexplained — recorded with its histogram as an open question (C170). ⚠ One bug of mine found and fixed on the way: the host demodulator paired half-bits from index 0, which fails completely rather than degrading when a capture starts mid-bit; the pairing phase is now searched | 4 captures × 5 drives + 4 captures × 8 low-pass widths + 1 pm3 clone |
| L136 | 04:35 | `a1f2851` | ⭐⭐⭐ **Gallagher decodes 4 of 4 exact — and it cost a correction to a claim I had committed an hour earlier.** The bit-centre decoder recovers `7feaa31e76d86c6d868cc249`, byte for byte the Proxmark's raw, from four captures at four phases, with **17 cross-protocol nulls clean** and the edge decoder scoring **0 of 4** at every one of 32 settings tried. So the ASK/biphase family's air layer is measured, not assumed: Manchester at RF/32, 96-bit frame, preamble `0x7FEA`, block-aligned (C171). ⭐ **The level path beat the edge path a second time, in a second modulation family** — C145 scored it 6 errors to 26 on PAC; here it is 4 of 4 against nothing at all — and it needed NO low-pass where the edge route needed one to suppress glitches. ⛔⛔ **C169 is corrected and its design implication withdrawn.** I wrote that the saturation "is not cosmetic" for ASK and that every ASK reader must therefore sweep drive. The paired control refutes it: the same decoder recovers the credential **4 of 4 from the CLIPPED captures too**. Manchester is a transition code, so clipping destroys magnitude and preserves the sign of the mid-bit transition — the same structural reason C124 already gave for Manchester surviving AC-coupling decay, which I should have applied to my own measurement before drawing a design rule from it. What survives is that the saturation is real and destabilises the EDGE decoder; what does not is that it limits ASK decoding. ⚠ C170's three-cluster histogram remains unexplained — the bit-centre path sidesteps run lengths rather than accounting for them, and the note says so | 8 host decodes + 17 null captures + 32 edge-decoder settings |
| L137 | 05:05 | `182f12c` | ⭐⭐ **The shipping ASK/Manchester decoder reads Gallagher 4 of 4, and two of its constants were wrong until they were measured.** `lf_ask_manchester.c` samples levels at bit centres and never measures an edge — the route C171 measured winning 4-of-4 against the edge decoder's 0-of-4 — and reuses the existing capture engine whole, which is modulation-agnostic despite its `lf_psk1_read` name, so the family inherits the phase rotation, the BLE-advertising guard (C47) and the two-agreeing-stacks acceptance rule for free. ⛔ **The CRC parameters I wrote from memory were wrong — 0x1D/0xFF returns 0xEE where the tag carries 0x49.** The real ones are poly 0x07, init 0x2C, and the payload bytes are taken at STRIDE 9 rather than by filtering every 9th bit out. Checked against the bench credential on the host before the file compiled; had it shipped, every genuine Gallagher would have been rejected and it would have looked exactly like "the protocol does not work on this hardware" (C172). ⛔ **And the capture length I guessed was wrong by more than 2x**: 6144 ("two frames, as NexWatch uses") decodes **0 of 4**, and the measured threshold is **10240** — 3.3 frames, not 2. That is the third time the measure-do-not-guess rule has paid (C161, C165). ⚠ It costs 114ms a read against Indala26's 33ms, and the note says so rather than hiding it. ⭐ **17 cross-protocol nulls clean through the shipping decoder**, and the CLIPPED captures also read 4 of 4 — confirming the C169 correction in firmware and not only in the host script | 4 captures × 8 truncation lengths + 17 null captures + 1 CRC verification |
| L138 | 05:45 | `507e909` | ⭐⭐⭐ **GALLAGHER READS AND WRITES ON DEVICE — 6 of 6 and 3 of 3 — so the ASK/biphase family is open on real hardware.** `lf gallagher read` returns region 1 / facility 4321 / card 6789 / issue 2 **6 of 6**, every field matching the Proxmark's own reader. Then the tag was wiped and our firmware wrote **a different credential in all four fields** (region 3 / facility 1111 / card 2222 / issue 5), which the Proxmark reads back **3 of 3** with `lf t55xx detect` reporting ASK / `00088060` — so nothing in the verification could have survived from the previous credential (M33). ⭐ **The ASK reader cost no new capture path**: `lf_psk1_read` is modulation-agnostic despite its name, so the family inherits the phase rotation, the BLE guard (C47) and the two-agreeing-stacks rule by handing it a different decoder. ⚠ One place where sharing had to STOP, and it is marked in the code: `scan_gallagher` does NOT call `lf_psk1_failure_status`, because the ASK decoder's `energy` is a Manchester-violation percentage while that mapping expects a bit-integrator amplitude — the same number on two different scales, which would have produced a confident meaningless hint. ⭐ **The descramble is asserted at import against the real clone's four fields**, not against its own inverse (C173). ⚠ **Emulation is NOT done and is not claimed**: the shared PSK1 modulator emits a phase-modulated subcarrier and ASK needs the amplitude keyed, so the emitter is separate work | 6 device reads + 3 pm3 reads + 1 detect + 1 wipe |
| L139 | 06:20 | `87db578` | ⭐⭐⭐ **GALLAGHER EMULATES 10 OF 10 — U5 IS COMPLETE AND THE ASK/BIPHASE FAMILY IS OPEN ON ALL THREE ARMS.** The Flipper's own Gallagher decoder reads our emulation as region 1 / facility 4321 / card 6789 / issue 2, **6 of 6**; with the Chameleon in reader mode, **0 of 4**; then — and this is the part that matters — the return leg was run with a **DIFFERENT credential** (region 3 / facility 1111 / card 2222 / issue 5) and read **4 of 4, tracking the change**. ⭐ **A changed return leg is strictly stronger than a repeated one**: repeating the first credential would have been consistent with a cached or ambient read, while following a change cannot be. ⭐ **The emitter cost almost nothing new, because em410x has been doing this since before any PSK work**: at the 125kHz base clock one PWM tick is one carrier cycle, so an entry with `counter_top = 32` spans one RF/32 bit and a half duty makes it Manchester, with the data bit selecting polarity. em410x is the same code with 64 and a 64-bit frame — the two protocols differ by two numbers at this layer (C174). ⛔ That is also exactly why the PSK1 modulator could NOT be bent into carrying it: it emits a phase-modulated fc/2 subcarrier at a 1MHz base clock and keeps amplitude constant, and `IS_PSK1_TYPE` is what selects between the two clocks — so Gallagher must stay OUT of that macro or be clocked 8x too fast. ⚠ **No sequence terminator is emitted** though a real Gallagher T5577 sets the ST bit; both reference decoders gate on the preamble recurring 96 bits later, which a plain looping frame satisfies. Deliberate, recorded, and the first thing to suspect if a reader is ever found that needs it | 10 Flipper reads + 4 null reads + 2 credentials |
| L140 | 06:55 | `e70337a` | ⭐⭐ **Securakey decodes 4 of 4, and the trap I wrote into the contract last night caught itself within the hour: the bit rate is NOT a family constant.** Securakey is **ASK RF/40, config `000C8060`**, 96-bit frame, 19-bit preamble — where Gallagher is RF/32 and em410x RF/64. `LF_ASK_BIT_SAMPLES` had been a hard-coded 32 for exactly one protocol's lifetime; it is a `lf_ask_format_t` field now, and with that one change the decoder carried a second protocol at a different bit rate on the first attempt, with Gallagher unregressed (C175). ⛔ **A decoder that samples a 40-sample bit at 32 finds nothing and gives no hint why** — which is the failure mode the note at that constant now describes. ⭐ **21 cross-protocol nulls clean, and the one that mattered is the same-family pair**: Gallagher and Securakey are both 96-bit ASK/Manchester frames whose preambles SHARE THEIR FIRST TEN BITS (`0111111111`), which is precisely the C90/C157 forging shape — neither decoder fires on the other's tag, in either direction, and that is measured rather than argued from the differing bit rates. ⚠ **Securakey's gate is 19 preamble bits and nothing else.** Both references agree there is no checksum anyone knows how to compute — the Proxmark's reader says so in as many words — so `accept` is NULL and the code says that is a gap rather than a design. ⚠ Only one of the three known format selectors is implemented, the one on this bench; the other two are deliberately absent, because an untestable format looks supported | 4 captures × 7 truncation lengths + 21 null captures |
| L141 | 07:35 | `c657042` | ⭐⭐ **Securakey reads and writes on device — 6 of 6 and 3 of 3 — but its EMULATION does not decode, and the failure is isolated rather than guessed at.** `lf securakey read` returns `7fcb400001adea5344300000` **6 of 6**; our write of a **different** credential onto a wiped tag reads back on the Proxmark **3 of 3** as FC 0x2A / card 60057, config `000C8060`, RF/40 (C176). ⛔ **The emulation reads 0 of 6**, and the control that matters was taken in the same minute on the same rig: **Gallagher 4 of 4**. So the bench, the Flipper, the pad and the PWM path are all working and the failure is specific to this protocol (C177). ⭐ **Two candidate causes eliminated, both by measurement rather than reasoning**: the inverted frame also reads 0 of 4, so it is not that Momentum's Securakey decoder fails to search both polarities; and both protocols are declared `LFRFIDFeatureASK` in Momentum's registry, so `rfid read normal` genuinely was sweeping Securakey and the test was valid. ⚠ **The leading hypothesis is the sequence terminator, and it is UNTESTED**: a real Securakey T5577 sets the ST bit, and Momentum's `protocol_securakey_can_be_decoded` checks its 19-bit preamble ONCE where Gallagher's checks the preamble at bit 0 AND again at bit 96 — so Gallagher can synchronise on a plain looping frame while Securakey may need the terminator as its frame boundary. That is exactly the caveat written into the Gallagher emitter last night, coming due | 6 device reads + 3 pm3 reads + 10 emulation reads + 4 control reads |
| L142 | 08:05 | `96f46ab` | ⛔⛔ **I RETRACT C177 — SECURAKEY'S EMULATION WAS WORKING AND MY INSTRUMENT COULD NOT SAY SO.** Momentum names this protocol **"Radio Key"**, with a space, and `flipper.py`'s success matcher was `^([A-Za-z][A-Za-z0-9]*) <hex>$` — one word — so a good read could never match. Widened, the unchanged emulation reads **6 of 6** as facility 53 / card 64169, matching the Proxmark on the real tag, with a **0 of 4** null and a **4 of 4** return leg on a DIFFERENT credential it tracked (C178). ⭐⭐ **M28 biting from the side it was written to guard**: the rule says match the success PATH rather than the protocol name, and this pattern did match the path while still assuming names are single words. The sharper form: **an instrument that can only express some outcomes reports the others as absence.** ⚠ **The control was sound and my inference was not** — Gallagher 4/4 on the same rig excluded the bench, which leaves "the emulator OR the instrument", and I read it as the emulator. The polarity null is void for the same reason: it would have read 0 of 4 whatever was emitted. ⚠ **And a standing limitation the operator raised, now recorded (C179)**: every emulate arm in the grid rests on a Flipper reading rig A, while a T5577 written with the same credential and read by the Proxmark is actual hardware behaviour — including frame-boundary behaviour like the sequence terminator that we do not reproduce. The write arms already carry that stronger evidence; the emulate arms cannot without lifting the tag out of the sandwich | 10 emulation reads + 4 null reads + 1 regex fix verified 6 ways |
| L143 | 08:30 | `aea1ff4` | ⭐⭐ **Answered the operator's point about emulation evidence, for Gallagher, with a no-hands measurement — and stopped short of answering it for Securakey because my instrument disagreed with a known-good result.** The concern (C179) is that every emulate arm here rests on a Flipper reading rig A, where a real T5577 read by the Proxmark is actual hardware behaviour and can differ at frame boundaries we do not reproduce — the sequence terminator being the known candidate, set in both these tags' configs and absent from our emitter. ⭐ **Measured on four committed captures of the real Gallagher tag: 12 frame-to-frame periods, all 3072 samples against a nominal 3072, to within ±1 sample — 1/32 of a bit.** So the terminator produces no displacement on the wire and a plain 96-bit loop is indistinguishable from the tag at the boundary (C180). ⚠ **Scope kept narrow**: this is the frame BOUNDARY, not waveform identity, and it does not replace the Proxmark reading our emulation. ⛔ **Securakey deliberately not reported**: `framedrift.py` finds no frame in `sk_20.bin` while `ctest/cdemod --securakey` reads that same file exactly. An instrument that disagrees with a known-good result on a known-good input is evidence about itself, not about the subject — which is the lesson from one hour earlier, applied before publishing rather than after (C178) | 4 captures × 40 bit phases × 2 polarities, 12 measured periods |
| L144 | 08:55 | `d78303a` | ⭐⭐ **The frame-period tool now agrees with the shipping decoder, and the fix was not the one I predicted.** Last entry withheld any Securakey number because `framedrift.py` found 0-1 frames where `ctest/cdemod --securakey` reads all four captures exactly. ⛔ **My stated hypothesis — that the tool swept one low-pass width where the decoder sweeps two — was WRONG: widening it changed nothing.** The real difference was the SLICING REFERENCE. The tool sliced against a 512-sample trailing moving average, which lags the signal by ~256 samples; the firmware uses 256-sample BLOCK MEANS, which do not lag. Gallagher tolerated the difference and Securakey did not. ⭐ With the tool mirroring the firmware's front end, **Securakey gives 8 intervals across 4 captures, all exactly 3840 samples = 96 bits × RF/40, zero excess**, and its frame start of 4124 reproduces the decoder's own `ph 4 pos 103` exactly. ⇒ **C180 now covers both ASK protocols: 20 frame-to-frame periods, none showing a terminator gap**, so a plain looping emission matches real tag behaviour at the boundary for both. ⭐ **The general rule, which is the part worth keeping**: an analysis tool that reasons ABOUT a decoder must share the decoder's front end rather than resembling it — a different DC estimator is a different instrument, and the symptom it produces is a confident zero | 8 captures × 40 bit phases × 2 polarities × 2 low-pass widths, 20 measured periods |
| L145 | 09:45 | `840083b` | ⭐⭐ **Noralsy reads 6 of 6 on device — and getting there refuted a rule I had withdrawn and exposed a defect my own assertion was blind to.** The protocol itself is the cheapest in the ASK family: RF/32, config `00088068`, 12-bit preamble plus TWO nibble checksums (the strongest gate here), capture threshold 3584 against Gallagher's 10240 at the same bit rate, 23 cross-protocol nulls clean including Gallagher both ways (C181). ⛔⛔ **But it read 0 of 6 on device while the host read it 4 of 4** — because every ASK capture in this campaign was taken at `--drive 7` and the reader uses stock. Measured: **stock, 1, 2, 4 and 6 decode nothing; only drive 7 decodes.** With a drive sweep the device reads 6 of 6 (C182). ⛔ **That reinstates the rule C169 raised and C171 withdrew — and both of those generalised from a single protocol**, PAC then Gallagher. Gallagher tolerates clipping and Noralsy does not; neither licenses a claim about the family. The defensible form is procedural: sweep, and let the tag decide. ⚠ **And a defect of my own that the import-time assertion did not catch**: the host printed card 1315392 / year 255 where the Proxmark reads 112233 / 2024, because the fields are BCD and the extraction was not — while the assertion beside it passed, checking only the checksum flag and not the two numbers a person reads. ⇒ Assert what you report, not only what you gate on (C183) | 6 drive settings + 12 device reads + 4 captures × 8 lengths + 23 nulls |
| L146 | 10:20 | `4020580` | ⭐⭐⭐ **Noralsy closes — read 6/6, write 3/3, emulate 10/10 — and the shared-path change that made it work is regression-clean.** The write put card 445566 / year 2019 onto a wiped tag against the 112233 / 2024 before it, read back by the Proxmark 3 of 3; the emulation reads 6 of 6, nulls at 0 of 4, and the return leg carried a changed credential the Flipper tracked (C184). ⭐ **The regression is the substance of this entry.** C182's drive sweep altered the read path of ALL THREE ASK protocols to fix one of them, so Gallagher and Securakey were each re-written to the tag and re-read: **4 of 4 and 4 of 4**. Putting the stock drive first was supposed to cost the already-working protocols nothing, and that is now measured rather than asserted. ⇒ **Three of the four ASK/biphase protocols are done**; InstaFob is the last. ⚠ Every emulate arm here still rests on the Flipper alone (C179) | 6 + 3 + 10 + 4 + 8 = 31 hardware reads |
| L147 | 10:55 | `23b5813` | ⚠ **InstaFob scoped before it was started, and one of its three arms turns out to be unverifiable on this bench.** It is the odd one in family 2: **no Proxmark support at all** (its own `lf help` lists none), a **225-bit frame** against the others' 96 — over `LF_ASK_MAX_FRAME_BITS` and over the PWM entry budget — and a decoder carrying `INSTAFOB_READ_SEQTERM_TIME_BASE`, making it **the first protocol here whose reader explicitly times a sequence terminator**. That is the exact case flagged when the Gallagher emitter was written and confirmed harmless for three protocols since (C180). ⭐ **An arithmetic correction to my own first reading**: 7200 samples a frame in a 14336 buffer is 1.99 frames, which looked like a wall and is not — a whole frame lands inside for 99.1% of start offsets, so it is a cost, not a blocker. ⛔ **The write arm genuinely cannot be verified here**: the Proxmark cannot read InstaFob and the Flipper is on the other rig, so the only reader available for our own written tag is our own reader — self-certification, which this project forbids. Added to Needs hands. ⭐ **The read arm IS available with no hands**: the Flipper's InstaFob emulation measures fs/2 **39314 against a 15287 paired null**, and both captures are committed (C185) | 2 captures + 1 paired null |
| L148 | 11:35 | `80498c3` | ⭐⭐ **InstaFob reads — and its preamble turns out to be the T5577's own configuration word.** The frame is 1 + 7x32 = 225 bits because the tag clocks out its ENTIRE page 0, block 0 included, and both references recognise it by finding `0x00107060` in that stream. So the 32 bits this format gates on are not a vendor marker at all; they are the chip's configuration (C186). ⭐ **Verified against a controlled plaintext twice**: the Flipper emulating `0011223344556677` decodes to `00107060 44556677 ...` and `AABBCCDDEEFF0102` to `00107060 eeff0102 ...` — the low 32 bits, tracked across a change — with a paired null yielding nothing and **24 cross-protocol nulls clean in both directions**. ⛔ **Two shared buffers had to grow first, and the overflow was caught by arithmetic rather than a crash**: 225 bits exceeds `id[]` (28 bytes) and `word_bits[]` (224) by one bit's worth, and both live in the result struct the PSK and ASK families SHARE — so the fault would have corrupted a neighbouring member and surfaced as a wrong credential somewhere unrelated. `LF_PSK1_MAX_FRAME_BITS` is 240 now, with a static assert on each side. ⚠ The returned frame is a 7-bit rotation of Momentum's numbering (the Keri situation, C158), and nothing in the frame checks the payload | 3 captures + 24 nulls + 320-capture regression |
| L149 | 12:15 | `8929efb` | ⭐⭐ **InstaFob reads on device — 5 of 5, null 0 of 4, 4 of 4 on a changed payload — and the first null I took was invalid.** The device reports config word `00107060` and card data `44556677` for the Flipper emulating `0011223344556677`, then `eeff0102` when the payload changes (C187). ⛔ **The invalid null is the part worth keeping**: it read the old payload four times with the emulation supposedly stopped, because a background `flipper.py` started in an EARLIER shell was still running — each command here runs in a fresh shell, so `wait` had no job to wait for. That is M29, and it is **rule 6 of this project's own autopilot contract**; knowing the rule did not prevent it, and only re-reading the failing output did. ⇒ Stop the emulation with an explicit ETX to the Flipper's port and confirm the null is EMPTY before believing any return leg. ⭐ **No writer ships for this protocol, deliberately** — the config word is known and the code would be four lines, but nothing here could verify it, so it would ship on self-certification (C185). Read-only, with the reason written at the declaration. ⚠ Also fixed: a silent no-op `.replace()` left `INSTAFOB_ASK_CAPTURE_SAMPLES` undefined — the build caught it, and the define now carries a static assert against the shared sample buffer | 13 device reads + 1 build failure |
| L150 | 12:45 | `d109801` | ⭐⭐ **InstaFob's sequence terminator is measured — 98-99 samples of frame-boundary excess, where the other three ASK protocols show zero.** `framedrift.py` on two captures of the Flipper's InstaFob emission gives frame periods of **7299 and 7298 samples against a 225-bit nominal of 7200**; Gallagher and Securakey gave zero excess across 20 intervals with the same tool. ⭐ **That confirms from the signal what C185 predicted from the source** — InstaFob is the only protocol here whose decoder carries a terminator timing constant — **and it validates C180's negative result**, because a tool reporting 0 on three protocols and 98 on a fourth is measuring the tags rather than itself (C188). ⛔ **It does not yield an emitter, and I am not guessing one.** The gap's runs are 24, 24, 26, 40, 28, 20 samples where a half-bit is 16 and a whole bit 32 — an encoding violation, as a terminator should be — and Momentum decodes it with a SIX-state machine rather than one interval. One capture at this slicing quality cannot reconstruct that. ⇒ The other three emitters were two numbers each; this one is not, and it stays unbuilt rather than shipped on a fitted guess | 2 captures + 20 control intervals |
| L151 | 13:15 | `edce314` | ⭐⭐ **U7: §9 reassessed against what the branch actually became — 49 files, ~7,500 lines, NINE protocols in two families — and the useful output is an evidence GRADE per arm, not a removal list.** The old §9 was written when this was Indala-only; its instrumentation table is still correct and is kept verbatim, but it said nothing about the eight protocols added since. ⭐ **Every arm is now graded by WHO verified it**: A = hardware, independent tool, with a null and a changed-plaintext control; B = one independent reader; C = host only. Read and write are **A** for every protocol but InstaFob; **no emulate arm is better than B**, and that is the operator's C179 point made structural rather than mentioned — all five rest on a Flipper reading rig A, and InstaFob's measured 98-sample frame-boundary excess (C188) is a live example of a difference one reader cannot see. ⛔ **Three blockers a reviewer will hit before the protocols**: `lf_psk1_read` is the shared capture engine for BOTH families and reads as a mistake where `lf_ask_manchester.c` calls it; `LF_PSK1_MAX_FRAME_BITS` was raised 224 → 240 for InstaFob and is load-bearing; and 32 new command ids need coordinating rather than asserting. ⭐ **Recommended as THREE PRs in dependency order** — the renamed capture engine, then the PSK1 family, then the ASK family — because 7,500 lines in one PR will not be reviewed, it will be declined. InstaFob stays out until its write arm can be verified | static assessment of 49 files + the existing hardware record |
| L152 | 14:05 | `d74a507` | ⭐⭐⭐ **The operator lifted the T5577 out of the sandwich, and the first measurement it enabled found something the Flipper could never have shown: our PSK emulation is 1 of 13 to a Proxmark where our ASK emulation is 16 of 16.** Gallagher 10/10, Securakey 3/3, Noralsy 3/3, NexWatch 1/10, Keri 0/3 — a clean split by modulation, with the three ASK protocols as the same-device same-session control. ⛔ The Flipper reads those same PSK emulations 6/6 and 10/10 all night, so the emulation is not broken; it is reader-dependent, and ONE READER WAS HIDING IT — which is precisely the limitation the operator raised as C179 and the reason the lift was worth asking for (C189). ⭐ **The mechanism is C74's and the split is its signature**: our PWM is free-running where a real T5577 and the Flipper clock from the reader's own carrier. ASK carries data in amplitude and shrugs off clock drift; PSK1 carries it in subcarrier phase, which a reader correlating against its own carrier loses as the clocks walk apart. ⇒ **§5's carrier-locking decision now has a measured cost** rather than being "likely closed, document and move on". ⚠ Also learned, and folded into the sweep: a slot's LF TYPE CHANGE does not take effect until the emulator is re-entered — `hw mode -r` then `-e` — which is why the first NexWatch attempt read nothing | 29 Proxmark reads across 5 protocols |
| L153 | 14:40 | `ee5bb46` | ⛔⛔ **I corrected C189 within the hour — the dividing line is not ASK versus PSK, it is ABSOLUTE PHASE versus everything else.** Extending the sweep from two PSK protocols to four found **Indala224 reading 14 of 16**, as well as the ASK protocols, while the three PSK1 protocols total **3 of 21** and ASK totals **16 of 16**. Indala224 is PSK; what separates it is that it is DIFFERENTIAL (C190). ⭐⭐ **The mechanism was already written in our own `psk1.h`**: the differential view is invariant under a global phase inversion, which is the whole point of differential encoding — and a free-running emitter's drift against the reader's carrier is exactly a slowly-varying global rotation. Differencing removes it, amplitude never saw it, absolute phase loses to it. ✓ The Proxmark reads REAL PSK1 tags perfectly all night, so its demodulator is sound and the fault is our clock. ⚠ **One confound stated rather than buried**: Indala224 is also the longest frame, and this bench has no short PSK2 protocol to separate frame length from differential encoding. ⇒ C189's measurements stand; the generalisation I drew from n=2 did not, and that is the second time today a family-wide rule was published from a single protocol (C169/C171/C182 was the first) | 61 Proxmark reads across 7 protocols |
| L154 | 15:15 | `02e1076` | ⭐⭐ **Closed the ASK family's round-trip gap — and found a false-pass bug in the harness while proving the new arms can fail.** Every PSK protocol has had a `roundtrip.c` arm since C152; the four ASK protocols had none and reached hardware with nothing between them and the radio. Three arms now compile the SHIPPING emitters against the SHIPPING decoder, and all ten are exact (C191). ⭐ The ASK renderer had to be written separately — a PSK entry is a subcarrier polarity held for `repeats`+1 periods, an ASK entry is one bit of `counter_top` carrier cycles split into two Manchester halves — and rendering either with the other's model gives a plausible waveform that decodes to nothing. ⭐⭐ **Sensitivity was proven rather than assumed**: breaking Securakey's bit rate 40 → 32, the exact bug C175 found, makes its arm report NO DECODE. ⛔ **That experiment then exposed the harness bug**: restoring the source and re-running `make` produced a STALE binary, because the edit and the build landed in the same second and make called it up to date. It printed the old failure — harmless — but the same mechanism prints a stale PASS for code that no longer exists, which in a correctness harness is worse than a false failure. `make check` rebuilds from scratch now | 10 arms + 1 deliberate break + 320 captures |
| L155 | 15:50 | `3f3b569` | ⭐⭐ **§9's blocker #1 cleared — the shared capture engine no longer claims to be PSK — and the rename failed twice before it worked.** `lf_psk1_read` is the engine BOTH families use, and `lf_ask_manchester.c` calling it reads as a bug. The rule applied is: shared things lose `psk1`, genuinely-PSK things keep it, so `lf_sampled_read` / `lf_decode_result_t` / `LF_SAMPLED_MAX_CAPTURE_SAMPLES` replace the shared names while `lf_psk1_format_t` and friends are untouched (C192). ⛔ **Two silent failures on the way, and both left the tree half-changed**: BSD `sed` ignores `\\b`, so the first pass did NOTHING while reporting success; the Python retry then died on a non-UTF-8 file after rewriting some files and not others. The version that worked substitutes on BYTES. ⚠ Neither failure announced itself — counting residual references is what caught both, and a half-applied rename is worse than an unstarted one. ✓ Verified four ways: residual shared names 0 and PSK names unchanged; firmware builds; 10 round-trip arms exact and 320 captures word for word; and the renamed READER path reads 4 of 4 on hardware with a clean null | ≈120 references + 10 arms + 320 captures + 5 device reads |
| L156 | 16:25 | `a0ccf54` | ⭐⭐⭐ **FSK resolves at 99% on the shared sampled path, and §10's reason for deferring the whole family turns out to be wrong.** A Flipper AWID emulation, captured by our reader: **1650 of 1655 adjacent run-pairs are exactly 8 or 10 samples** — RF/8 and RF/10, the FSK2a tones AWID, Paradox and Pyramid all share — against a paired null with no structure at all (C193). ⭐ **The runs are asymmetric (8 splits 3+5, 10 splits 4+6) and it does not matter**: that is C145's duty bias, and it CANCELS in the period sum. FSK is therefore easier on this front end than ASK was, not harder. ⛔ **§10 deferred FSK because it "reuses the HID Prox/ioProx SAADC machinery", where HID's unexplained 15-20% intermittency lives.** The ASK family already showed a new decoder on the shared engine needs none of the legacy per-protocol readers — and `lf_hidprox_data.c` rolls its own `saadc_cb`, the family C47 identified as MISSING the BLE-advertising guard that is the leading explanation for that very intermittency. ⇒ An FSK decoder on `lf_sampled_read` inherits the guard rather than the defect, and might be the fix for C45 rather than another casualty of it. ⚠ **No frame decoded yet** — physical layer only, and the claim says so. ⚠ Also: the first attempt captured nothing because the Flipper REJECTED a 5-byte AWID payload (it needs 9), and `flipper.py` reported the rejection rather than letting it pass as a silent null | 1655 sub-period pairs + 1 paired null + 1 rejected attempt |
| L157 | 17:00 | `1e4243a` | ⭐⭐ **An FSK2a demodulator recovers AWID frames on the shared sampled path — and the field mapping is honestly NOT solved.** `fskdemod.py` implements Momentum's own pulse-count rule (a '0' is six RF/8 periods, a '1' is five RF/10, both 50 carrier cycles) and returns frames passing all three of the reference's checks: preamble `00000001`, the same preamble 96 bits later, and odd parity over every nibble of bits 8..95. Two payloads give two distinct frames and the **paired null returns ZERO BITS** — it fails at the tone level, not merely the frame level (C194). ⛔ **The credential cannot be reported yet**: stripping the parity does not recover the 9 bytes passed to `rfid emulate`, and a search over 16 starts x 4 spans x 8 shifts found no alignment, so AWID's encoder does more than insert parity. That is InstaFob's position exactly — a frame, not a credential — and the grid says so rather than implying a working reader. ⭐ The wider point stands: the family §10 ranked LAST because of HID's unexplained intermittency turns out to be the cheapest one remaining, because none of the legacy per-protocol SAADC readers is involved (C193) | 2 payloads + 1 paired null + 512 alignment combinations |
| L158 | 17:30 | `a73d1dd` | ⭐⭐ **AWID's payload decodes exactly — and the mapping I could not find an hour ago was a bug in my search, not a property of the format.** `protocol_awid_encode` takes THREE payload bits at a time, shifts left one and fills the vacated bit with odd parity, so each nibble at `8 + 4i` is 3 data bits plus a parity LSB and the frame carries **66 payload bits**. Inverting that gives `010203040506070800` and `aabbccddeeff001100` against the emulated `010203040506070809` and `aabbccddeeff001122` — exact, with the final 6 bits simply not carried on the wire. ⛔ **My 512-combination strip search found nothing because I had prepended the 8 preamble bits to the payload**, shifting every bit after it. The answer was never an alignment, and searching harder over the wrong model would never have found it — reading `protocol_awid_encode` took two minutes and settled it. ⇒ **AWID's read arm is host-complete**: frame, three-part gate and credential, exact on two independent plaintexts with a zero-bit null (C194 amended) | 2 payloads decoded exact + 1 null |
| L159 | 18:05 | `234cdca` | ⭐⭐ **AWID's read arm is in firmware — `lf_fsk2a.c` on the shared capture engine — and the slicer both families need is now shared rather than duplicated.** Host-compiled, the shipping decoder returns `010203040506070800` and `aabbccddeeff001100` for the two emulated payloads, byte-identical to `fskdemod.py` which shares none of its code, with all three of AWID's own checks enforced and **22 cross-protocol nulls clean in both directions** (C195). ⭐ **`lf_slicer.c` exists because of a bug, not a tidying impulse**: `framedrift.py` sliced against a trailing average where the firmware used block means, and the two disagreed about which captures contained a frame at all. Two decoders claiming to see the same signal must share a front end for exactly that reason. ⚠ The extraction rewired every ASK decode path, so it was regression-tested rather than assumed: 10 of 10 round-trip arms exact, 320 captures word for word, firmware builds clean. ⚠ **Not yet on device** — this is the firmware decoder verified on the host; the command, CLI and hardware read are next | 2 payloads + 22 nulls + 10 arms + 320 captures |
| L160 | 18:40 | `ca1af6e` | ⭐⭐⭐ **AWID reads on device — 5 of 5, null 0 of 3, 4 of 4 on a changed payload — and the FSK family turns out to be the cheapest of the three attempted, not the most expensive.** `lf awid read` returns `010203040506070800` for the Flipper emulating `010203040506070809`, nothing with the emulation stopped, and `aabbccddeeff001100` when the payload changes (C196). The trailing zeros are the format: AWID carries 66 of 72 bits, and the CLI says so. ⭐⭐ **§10 put FSK LAST of four families because it would reuse the HID/ioProx per-protocol SAADC reader and inherit HID's unexplained intermittency.** It reuses none of it — the decoder goes to the shared capture engine, which HAS the BLE guard those readers lack (C47). Measured end to end it cost one decoder and one evening, against ASK's two false starts: the comparator route that scored 0 of 4 (C171) and the drive sweep that had to be withdrawn and reinstated (C169/C182). ⚠ **Read only, and the distinction matters**: AWID's writer is DEFERRED because the Proxmark can verify it once the tag is back, where InstaFob's is REFUSED because nothing here ever could | 12 device reads across 2 payloads + 1 null |
| L161 | 19:15 | `7c80209` | ⭐⭐ **Paradox and Pyramid decode on the first attempt — one format entry each, no new demodulator — which is the prediction the FSK design made and the ASK family never managed.** Both share AWID's tones and pulse counts, so `lf_fsk2a_format_t` carried them whole: Paradox returns `0f55565559555a5565556655` and Pyramid `000101010101010101010161201910bc` through the shipping decoder, with 26 cross-protocol nulls clean and the 320-capture regression unchanged (C197). ⭐ **Both gates are structural rather than preamble-only**, which is why 8 bits suffices for Paradox: it carries 44 independent alternating-pair checks over bits 8..95, and Pyramid carries a 24-bit preamble, a repeat at 128 and a CRC-8. ⭐⭐ **Pyramid's CRC parameters (poly 0x31, both ends reflected) were transcribed and then CHECKED against a real capture** — the gate passing on a genuine signal is the proof, because a wrong CRC rejects everything and reads exactly like an unsupported protocol. That is Gallagher's lesson (C172) applied before the fact rather than after. ⚠ Host-verified only; the device arms are next | 2 captures + 26 nulls + 320 captures |
| L162 | 19:50 | `4869a4f` | ⭐⭐⭐ **Paradox and Pyramid read on device — three FSK protocols now, and the second and third genuinely cost one descriptor each.** Paradox `0f55565559555a5565556655` 4 of 4 with a 0 of 2 null; Pyramid `000101010101010101010161201910bc` 4 of 4, then `...57bc67755e` 3 of 3 when the payload changed — fixed prefix identical, tail and CRC moving (C198). Both gates held on device: Paradox's 44 alternating-pair checks and Pyramid's CRC-8. ⭐⭐ **The FSK family is the only one that has fully honoured §10's claim for grouping by modulation.** After AWID neither protocol needed a line of demodulator; the ASK family needed a new renderer, a drive sweep and a bit-rate parameter before its SECOND protocol worked. ⚠ **A flash-verification trap worth recording**: three "failed" flashes in a row were my capability check racing the device's re-enumeration at 6 seconds, not failed flashes — the script reported success each time and I discarded its output. Reading the output I had been throwing away is what settled it. ⚠ All three FSK writers are deferred until the T5577 returns | 13 device reads across 3 payloads + 1 null |
| L163 | 20:25 | `e31c560` | ⭐⭐ **FDX-A decodes — the FSK family is complete on read — and its first "failure" was my test payload rather than the format.** FDX-A is the only protocol here with two encoding layers: FSK2a carrying Manchester, 40 bit-pairs in bits 16..95, gated by a 16-bit preamble, the frame repeat, 40 pair checks and odd parity on each decoded byte (C199). ⛔ **The first attempt decoded PERFECTLY and was rejected correctly**: clean Manchester, bytes exactly the emulated `01 02 03 04 05` — but bit 7 of each FDX-A byte IS its odd-parity bit, so that string is not a valid credential. `0102830485` satisfies the rule and passes. ⭐ **The diagnosis came from printing intermediate state rather than tuning**: the preamble sat at bits 37/133/229, spacing exactly 96, which localised the rejection to `accept` before a line was changed — the opposite of the AWID field-mapping episode, where I searched 512 alignments over a wrong model. ⚠ The device arm is wired and built but **NOT yet verified on hardware**: the flash landed on Chameleon #2 rather than #1, which is the documented trap (the script runs its own DFU trigger and it picks whichever port the OS lists first) | 2 payloads + 26 nulls + 320 captures |
| L164 | 09:25 | `551a0f2` | ⭐⭐ **FDX-A reads on device 4 of 4 — all four FSK protocols now read on hardware — and the four-hour stall turns out to have been a missing command on `PATH`.** ⛔⛔ Every `nrfutil device list | grep -c nordicDfu` check returned 0 because **nrfutil is not on this shell's PATH**: the command failed and grep counted zero lines. With the real path — `.tools/bin/nrfutil`, which the flash script resolves explicitly and I did not — the same trigger shows exactly one DFU device and flashes the intended unit (C200). ⭐ The targeted procedure is now proven: trigger on the chosen `/dev/cu.` port, confirm ONE nordicDfu device with the other unit's normal port still present as the cross-check, then call nrfutil directly. ⚠ **What was real: #1 genuinely was wedged** — `hw mode` timed out, independently of nrfutil, and the power cycle was needed. The false part was everything I inferred on top. ⇒ **Three instrument failures this campaign, all reporting absence**: a one-word name matcher (C178), a mismatched slicer (C180), and now an uninstalled command. The defence is identical every time — check the instrument against a known-good case before believing a negative | 4 device reads + 1 null + 1 targeted flash |
| L305 | 06:45 | pending | ⭐⭐⭐⭐ **U17 CLOSED — the FDX-A writer ships and its blocks are IDENTICAL to the Proxmark's own clone.** Config `00105060` (FSK2, not the family's FSK2a), three blocks, frame complemented, `fsk2a_t55xx_blocks()` shared unchanged. Wiped tag confirmed blank, then 4 of 4 with the credential alternating every round; pm3 reads back exactly what we wrote and the dump equals `lf destron clone`'s own. ⚠ Our writer's read-back said VERIFIED before pm3 agreed — the self-certification C185 refused, reported and not counted. ⭐ Host vectors and the complement relationship pinned in `roundtrip.c` (C340) | 4 alternating write+verify rounds from a blank tag |
| L304 | 06:10 | pending | ⭐⭐⭐ **U17 opens and half lands: FDX-A reads a REAL TAG 10 of 10 across two credentials, so its read arm is grade A.** C333 unlocked it — the Proxmark has FDX-A under `lf destron`, not `lf fdx`. ⚠ Our payload prints with FDX-A's odd-parity bit still in bit 7 of each byte, so `1aabbccd5e` masks to pm3's `1A2B3C4D5E` (C338). ⭐ **Write constants measured off the reference clone**: block 0 `00105060`, three blocks, and it is **FSK2 not FSK2a** — one bit from AWID's `00107060`, exactly C171's trap. The stored frame is the bitwise complement of our reader's raw, confirmed on both tags (C339) | 10 device reads, 2 clones, 2 dumps |
| L303 | 05:40 | `4d23ab01` | ⛔⛔⛔ **FDX-B reads 15 of 15 — the `0 of 4` that drove three ticks of investigation does not reproduce.** 6/6 via CLI, 3/3 via raw scan, 9/9 in an A/B/A across preceding GProxII and Indala reads. The grid downgrade is withdrawn and C215's *6 of 6 on device* is restored. ⭐ The device counters earned their keep anyway: they caught the failure mid-flight at 17 captures kept, 8 phases, ZERO decodes — so when it does fail it is the DECODER that produces nothing, not the agreement rule. ⭐ Drive inheritance tested and refuted: a GProxII read leaves drive 7 and the next FDX-B read is fine. ⚠ Third measurement error of the night, same shape as the other two and just as cheap to have avoided — re-measure the thing you are explaining, before you explain it (C337) | 15 device reads, two paths, one A/B/A |
| L302 | 05:05 | `baa0acde` | ⛔⛔ **Retracting C335's mechanism: the *hundredfold* edge difference was `cut -c55-120` slicing *edge 8578* into *edge 85*.** Re-decoding the identical files gives 8444-8728 where I recorded 84-86; corrected ranges are failures 9191-9494 and successes 8367-9265, which **overlap**. The saturated-front-end story and its appeal to C204 have no support. ⭐ The 5-of-16 vs 16-of-16 field-hold result stands — it was scored on frame hex, not on that column. ⭐ **Also a clean negative**: with the field held FDX-B decodes at **all eight** `PHASE_ROTATION` phases, 15 of 16, so the device is not failing for want of a workable phase (C336) | 16 files re-decoded in full; 16 fresh captures across the 8 real phases |
| L301 | 04:45 | `6bac4387` | ⭐⭐⭐ **U16: FDX-B decodes 16 of 16 from a held field and 5 of 16 from a cycled one (p = 6.8e-5) — and that still does not explain 0 of 4 on device.** The reader calls `start_lf_125khz_radio()` per capture with a 2 ms settle, so every capture is a field-on transient; failures read edge ~9000 against ~85 for successes, a saturated front end, matching C204's AC-coupling constant. ⛔ that edge claim is retracted by L302 — it was a truncated column, and the real ranges overlap. ⛔ But 31% per capture over 8 tries gives 0.77 of two agreeing at one phase, so the device should usually read. Recorded as a large real effect that is NOT sufficient (C335). ⚠ Also names the two invalid measurements from the previous tick — rdrcap defaults to drive 7, and bidemod needs the real preamble | 32 captures, 16 per arm, shipping decoder as judge |
| L300 | 04:05 | `8643775c` | ⛔⛔ **Retracting my own mechanism from L299: the corroboration fix did NOT expose FDX-B.** Rebuilt and flashed with only the comparison reverted to 8 bytes — **FDX-B still reads 0 of 4** — and six host-decoded captures show a STABLE word, the one outlier differing in byte 3, inside the range the old rule compared. ⇒ FDX-B was already broken before F7, by something between C214 and now (the password fix, F5's buffer merge and F6 all landed in between). ⭐ F7 itself is unaffected and stays: correct on its own terms, verified on six protocols. ⚠ The error was reaching for the explanation that made my own change the protagonist, when the control that refutes it costs one build and one flash — and M35 already demanded it (C334) | 1 A/B build pair, 8 device reads, 6 host captures |
| L299 | 03:40 | `8ec9f5be` | ⭐⭐⭐⭐ **F7: the repeat-read corroboration rule compared only the first 8 bytes of any frame — 160 of 224 bits unchecked for Indala224 — and widening it exposed that FDX-B cannot corroborate its own tail.** Six of seven long-frame arms unaffected on hardware (Indala224 4/4 with all 28 bytes corroborated, NexWatch 4/4, GProxII/Gallagher/Securakey 3/3, Pyramid 3/3); FDX-B 0 of 4, which is a second defect revealed rather than caused — its C214 grade of A 6/6 was earned on half the frame. Cause unknown, queued; NOT the free-running-clock limit, which is the emitter direction (C332). ⛔ the FDX-B half of this entry is retracted by L300 — the A/B shows the fix neither caused nor exposed it. ⭐ **Also: the Proxmark DOES have FDX-A** — `lf destron`, with demod/reader/clone/sim — so C185's *refused, not deferred* rests on a false premise and reopens (C333). Same shape as the InstaFob miscount minutes earlier: absence concluded from looking in one place | 21 hardware reads across 7 protocols; 1 pm3 command listing |
| L298 | 03:05 | `c7d4eab1` | ⭐⭐⭐⭐ **The write column is finished: 18 protocols re-graded 4 of 4, 72 writes, 0 failures.** `regrade.sh` extended past U15's eight to Indala224, IDTECK, EM410x, ioProx, PAC, Viking, Jablotron, FDX-B, Paradox and Pyramid. ⭐ **C155's standing Indala224-write gap is closed** — it said the 224-bit write could not be verified while the T5577 sits where it does; it verified 4 of 4. ⚠ NexWatch and FDX-B were passing on the protocol NAME under `lf search` and are now judged on their own reader commands against the credential — a protocol-deep pass is not a credential-deep one (C331). ⚠ IDTECK's test vector fails its own checksum; that is the vector, not the writer — the raw round-trips byte-exact | 72 writes, 72 independent pm3 reads, bench restored to `H10301 FC 123 / CN 4567` |
| L297 | 02:45 | `a8423056` | ⭐⭐⭐⭐ **U15 done — all eight write arms re-graded against an unlocked tag, 4 of 4 each, 32 writes with 0 failures.** `regrade.sh` writes with #2 and lets the Proxmark judge; each round is a fresh write, so the score counts writes that landed rather than reads of one write. AWID, Keri, Indala26, NexWatch, Gallagher, Securakey, Noralsy, GProxII — every raw frame pm3 printed is byte-identical to the one sent. ⇒ C305's *0 of 9* was measuring the password lock, not the writers (C330). ⚠ NexWatch is judged on `lf nexwatch read` because `lf search` shows only a raw id for it. ⚠ GProxII first scored 0 of 4 on a grep bug (`G Prox` vs pm3's `G-Prox`) — caught only because the FAIL branch prints what pm3 actually said | 32 writes, 32 independent pm3 reads, bench restored to `H10301 FC 123 / CN 4567` |
| L296 | 08:15 | `90006c92` | ⭐⭐⭐⭐ **Sandwich rebuilt and the fix CONFIRMED end to end: a GProxII write LANDS** (`VERIFIED — GProxII frame read back off the tag`), where C305 scored the non-HID protocols 0 of 9. ⭐ The criterion was pre-registered in `benchab.sh` before the cause was known. ⭐ C306's signature is gone too — `detect` succeeds and block reads differ (`00107060` vs `1D555955`) instead of returning one identical word. ⇒ C299/C305/C306 were all the password. U15 is now 1 of 8 (C329) |
| L295 | 08:00 | `cc9bc1b7` | ⭐⭐⭐ **Emulate-arm re-grade done with the Flipper: IDTECK PSK 4/4 (ASK 0/4 as control), EM410X ASK 4/4.** Control first — Flipper read the real tag `H10301 FC 42 / Card 999`, matching pm3, so the instrument survived its crash. Null clean at 0/3. ⭐ Indala slot 1 read 0/4 and the firmware said why: emu id `7FEAA31E…` carries *"not the Indala preamble"* — slot DATA, not emitter code. ⚠ EM410X hit on BOTH arms, unexplained, recorded as such. ⇒ Also proves the earlier pm3 null was pm3's inability to hear a PWM emulation (C328) |
| L294 | 07:45 | `7d2974f6` | ⭐⭐ **F6 closed: the HID writer now reads back.** It used to print `write done.` with no tag on the pad — which fooled me twice today. Five verdicts now, pinned to the format written (an unpinned check would fail correct writes for the 12 check-less formats), with a read BEFORE the write to separate *failed* from *uncoupled*. ✓ Verified both ways: empty pad → `CANNOT TELL`, tag present → `VERIFIED` (C327) |
| L293 | 07:30 | `aff5e377` | ⭐⭐⭐⭐ **Fixed the password defect and verified it on hardware.** `T5577_PWD` out of all 8 configs; `write_t55xx()` sets it only when a non-zero key is supplied; host `new_key` now zero and `old_keys` includes `20206666` so previously-locked tags stay writable. ✓ Blank tag written with the fix: `Block0 00107060`, **`Password set: No`**, block 7 `00000000`, credential correct. ⚠ It landed with a SINGLE open write — the repetition I added afterwards was speculative and is labelled as such. ⭐ Started `FIXES.md`, six incidental defects each scoped as its own PR (C326) |
| L292 | 07:15 | `f2aa960f` | ⭐⭐⭐⭐⭐ **THE CAUSE: two different T5577 passwords depending on which write command you call.** `chameleon_cmd.py:13` globals `new_key = 20206666` / `old_keys = [51243648, 19920427]` are used by `hidprox` and `em410x`; `indala`/`indala224`/`gproxii`/`awid` take a function default `51243648`. ⇒ HID locks the tag with `20206666` and can always get back in; the others authenticate with `51243648`, are refused, and land nothing. ⛔⛔ **C305 is dissolved** — its "iff the config matches" rule fitted 14 results and was never about config words. ⛔ C324 WITHDRAWN in full: there is no corruption, `20206666` is a literal on line 13 and I had the old and new keys backwards (C325) |
| L291 | 06:55 | `97418854` | ⭐⭐⭐⭐⭐ **SOLVED: our writer sets the WRONG password — intends `51243648`, lands `20206666`, deterministically on both tags.** Recovered both with an AUTH=1 / PWD_BIT=0 build; `detect` now succeeds (`Block0 00107060`, `Password set: No`) and `dump` finally shows **block 7 = `20206666`**. ⇒ That is why nothing unlocked them — every key we swept was the wrong target. ⚠ Not a shift/rotation/inversion; the `20 20 66 66` byte-pairing and period-4 low half point at BIT TIMING. ⭐ Recovery key for anyone hit: `20206666` (C324) |
| L290 | 06:25 | `21042585` | ⭐⭐⭐⭐ **REPRODUCED ON DEMAND — one HID write from the shipping firmware locks a healthy T5577 out of the Proxmark.** Clean start measured (`detect` OK, `Password set: No`, block 7 = `00000000`), one write, then `lf search` still reads a perfect `H10301` while `detect` fails — and fails with `-p 00000000` and `-p 51243648` too. ⇒ One write, not a session. ⭐ Confirms mechanism (A): block 0 is a VALID config, not misaligned garbage. ⛔ But no key unlocks it, so the password account is incomplete and the end state is still unobserved (C323) |
| L289 | 05:45 | `1b3eb1bf` | ⭐⭐⭐⭐ **CONFIRMED: our writer password-locks every T5577 it touches, and it is an UPSTREAM defect.** `LF_T55XX_SET_PASSWORD=0` removes the PWD bit, the block-7 write and the authenticated frame; default build byte-identical (334,932). One HID write to a fresh spare → pm3 `detect` SUCCEEDS and is correct (FSK2a RF/50, `Block0 00107060`, **`Password set: No`**) and `dump` returns every block distinct, block 7 = `00000000`. Against the locked tag's ~20 failed operations. ⛔ `main` carries the identical 8 configs and the same `51243648`, so this hits every user (C322) |
| L288 | 21:30 | `cd5ad7e0` | ⛔⛔ **Withdrew my own C320 headline: the detect difference is not significant.** Operator refused to read 1/8 → 0/8 as real; the same arithmetic kills my claim too. Fisher: #2 removed 2/19 vs present 0/≥8, **p = 0.487**; no-weight 2/10 vs post-it 0/9, **p = 0.263**. ✓ What survives needs no test: the block-read constant changed `0EAAACAA` → `80000000` when #2 was lifted, each invariant within its configuration. ⚠ Still not data — both blocks return the same word. ⇒ Live question: is `80000000` from the tag at all? (C321) |
| L287 | 21:20 | `95910fbf` | ⭐⭐⭐ **#2 lifted off: `lf t55xx detect` SUCCEEDED for the first time ever (1 of 2), and the `0EAAACAA` constant vanished.** Tag still reads `H10301` throughout, so it stayed coupled — the change is in the downlink. ⛔ But reads still fail: both blocks now return `80000000`, the same identical-across-blocks signature. ⚠⚠ The successful detect reported PSK2/PSK3 RF/8 against a tag that transmits FSK2a RF/50 — a false positive, not a win. ⚠ Confound: #2 rested ON the tag, so this is not a pure coil removal (C320) |
| L286 | 21:15 | `16f07a78` | ⭐⭐ **Bench A/B run by the operator: #2's power state and a small physical nudge both change NOTHING.** USB pulled at the host end and at the device end — pm3's downlink gave the identical C306 signature both times (`detect` fails, blocks 0 and 1 both `0EAAACAA`), and both closing A-runs reproduced the full baseline. ⇒ The failure is ROBUST, not marginal, which weakens the geometry reading of C299 and retires the BLE-burst mechanism. ⚠ `0EAAACAA` across every block, power state and run is the signature of a constant, not a measurement (C319) |
| L285 | 04:30 | `dd43f83a` | ⭐⭐⭐ **Implemented C317: 28,672 B of RAM returned, BSS 172,760 → 144,088, `nm` confirming one 0x7000 buffer where there were two.** Placed in `lf_reader_generic.c` with a `_Static_assert` guarding the size. ✓✓ Three paths verified on the flashed device: reader 4/4, `lf sniff` full 28,672 samples, capture probe at period-96 agreement 1.000. `make check` green. ⇒ Removes the objection C316 predicted would sink PR 1 (C318) |
| L284 | 03:55 | `16e19de3` | ⭐⭐⭐ **Two 28 KB capture buffers are resident at once — 57,344 B, 22% of RAM — and they are never live together.** `nm` on the shipping build: `m_samples` and `sniff_buf` both 0x7000, both static. Sharing one would return 28,672 B, more than PR 1's entire RAM cost (C316). ⛔ Placement: NOT by reaching into `m_samples` (a protocol file) from the generic sniff handler — that inverts the dependency. It belongs in `lf_reader_generic.c`, which owns `capture_begin()`. ⚠ Consequence stated up front: sniff and reader-capture would clobber each other, already the documented contract for 3060. ⛔ Not implemented this tick — it needs `lf sniff`, every read arm and `make check` re-verified (C317) |
| L283 | 03:20 | `7784f1f6` | ⭐⭐⭐ **PR 1 BUILDS: 10 files + `ble_main.c/.h` + a THREE-LINE `app_cmd.c` hunk.** C315 called the hunk-split the blocking task; the PR-1 share of that +827 −8 is 3 lines, because PR 1 should adapt callers to the new API rather than import HEAD's chunked sniff. ⛔⛔ The real blocker is RAM: flash +1,024 B but **BSS +24,692 B**, `nm`-attributed almost entirely to `sniff_buf` going 4,000 → 28,672 via PR 1's own header — ~10% of the chip's RAM for one buffer, unmentioned by §9d. ⛔ I nearly filed the 3-line hunk as "minimal" without noticing it carried a 7× buffer growth (C316) |
| L282 | 02:45 | `28e00ff4` | ⛔⛔ **PR 1 as §9d specifies it does NOT build — the five-PR split is a reading, not a plan.** Applied the exact file list (10 files, +1,726 −35) to clean `main`: `lf_adc_set_acq_fast` and `g_is_ble_connected` undeclared (both from `ble_main.c/.h`, unlisted), and after adding those the ONLY remaining errors are `app_cmd.c` calling the changed `raw_read_to_buffer` / renamed `LF_SNIFF_MAX_SAMPLES`. ⇒ PR 1's minimum is the 10 files + `ble_main` + an `app_cmd.c` hunk — and that hunk-split (+827 −8 across all five PRs) is the blocking task the plan never did. ✓ PR 0 unaffected, still builds alone (C315) |
| L281 | 02:05 | `be6d7bb6` | ⛔⛔ **Corrected my own C313: PR 0b is not 4 lines, it is 5 files and +140 −3 — and only compiling it found that out.** `main` + PR0 + the 4-line call site fails on `lf_adv_guard_t`; adding the guard fails on `g_is_ble_connected`; that needs `ble_main.c/.h`. Each link invisible until the previous compiled. ⭐ Corrected ladder, each step a real build: 297,892 → 298,140 (+248) → 298,260 (+120). ✓ PR 0's own numbers re-verified and unchanged. New M43: a diff's size is not a PR's size (C314) |
| L280 | 01:30 | `c10dabdd` | ⭐⭐⭐ **Proved PR 0 separable by CONSTRUCTION: applied the 3-file patch to a clean `main` worktree and built it.** Control (unpatched main) built first at 297,892 text; patched builds at 298,140 — **+248 B flash, no new BSS**, clean apply, no warnings. ✓ `formats[]` is byte-identical to main, so the fix rests on upstream data only. ⚠ One dependency named and bounded: `lf_hidprox_data.c`'s 4-line BLE advertising guard (C47/C250) affects read RELIABILITY, not format selection — send it as PR 0b (C313) |
| L279 | 00:55 | `824ed3ae` | ⭐⭐ **U7 refresh (§9i): the `unpack()` fix is separable TODAY — 3 files, +102 −16, carrying none of this branch's LF work.** `wiegand.h` gains no new includes or externs and all three files exist upstream, so PR 0 needs none of the five-PR sequence. Recounted: reviewable code is 64 files / +11,327, not §9c's stale "59 files / 10,242"; 1,633 files are notes and captures. ⛔ Recorded that it IS a shipping behaviour change and the PR must open with it. ⭐ Three smaller landable items logged with their blast radius (C312) |
| L278 | 00:20 | `059411db` | ⭐⭐ **Built `benchab.sh` and captured the `before` baseline while the sandwich is still assembled.** One battery, six sub-tests, every tag state read by pm3: listening OK, detect fails, blocks 0 and 1 both `0EAAACAA`, reader 4/4, data-block write lands, block-0 write fails, restore verified. ⇒ Reproduces C305, C306 and the reader grade in a single pass. ⛔ The run ORDER is the point: `nochamp2` must run before the stack is opened, because powering down #2 is the entire C299 test and opening it destroys that experiment (C311) |
| L277 | 23:50 | `c602f46f` | ⭐⭐⭐ **Answered C309's open question from air already captured: the read command is WELL-FORMED and the tag ignores it.** Decoded our own downlink by relative-dip detection — start gap then spacings 53,25,25,25,25,26 = **ONE,ZERO,ZERO,ZERO,ZERO,ZERO**, exactly opcode `10` + lock `0` + address `000`, the block-0 read the source says it builds. ⛔ So the transmitter is not at fault. ⚠ Absolute ON times NOT claimed — the LC rings far longer than a 72 µs gap, so the dip over-reports. ⇒ Joins C305 into one shape: this tag acts on data-block writes and ignores block-0 writes and reads (C310) |
| L276 | 23:15 | `221bfe77` | ⭐⭐ **Closed C308 negatively: the tag never enters regular-read mode.** Autocorrelation on the raw samples — no modulation assumed — shows the 96-bit HID frame present (lag 4800) and the 32-bit block absent (lag 1600), both with and without the password. ⛔ Corrects C308: the period-96 collapse I reported as the tag changing behaviour was the downlink's field gaps wrecking a short demod window, not a mode change. ⚠ Whether the command is malformed or correctly sent and ignored is NOT established. ⭐ Joins the pattern — data-block writes are acted on, block-0 writes and reads are not (C309) |
| L275 | 22:40 | `e436bd75` | ⭐⭐ **Built the missing T5577 block read and proved the mechanism — the block itself is still not decoded.** `raw_read_samples_probe()` fires a callback with the field already up, so a downlink command can be transmitted INTO a live capture; two separate calls cannot work because `capture_begin()` raises the field and resets the tag. New command 3063 behind the research flag, chunked like 3060. ✓✓ Control: the plain capture demodulates to a perfect 96-bit frame (agreement **1.000**); after the read command it is gone (0.512), and the downlink gaps are visible in the capture. ⛔ But no window shows 32-bit periodicity (p32 ≈ 0.5, chance) despite correct bit timing — so the tool works and the measurement does not, and why is not established (C308) |
| L274 | 21:55 | `7cca7fe1` | ⭐⭐ **Narrowed C305 from "the frame" to "block 0's value" by entailment, with no new measurement (M36).** The five successful HID writes carried five DIFFERENT credentials, so blocks 1-3 held five different payloads and all landed — the data words are not the gate. With L273 showing HID and GProxII identical down to block addresses, block 0's value is the only variable left. ⛔ And that makes it self-contradictory under any simple mechanism: if block-0 writes were merely ignored, GProxII's blocks 1-3 would have landed and pm3 would read garbage. It reads a perfect credential, so nothing is written at all (C305) |
| L273 | 21:40 | `280bfe5e` | ⛔ **Corrected a factual error in C305 that I had asserted twice, and the correction strengthens the claim.** I wrote that HID writes 3 blocks "like Keri"; `HIDPROX_T55XX_BLOCK_COUNT` is **4**. Measured: HID 4 → succeeds, GProxII 4 → fails, AWID 4 → fails, Keri 3 → fails, Indala 3 → fails. ⇒ Block count is refuted more cleanly than before — same count, opposite outcomes — and **HID and GProxII write the SAME number of blocks to the SAME addresses through the SAME function with the SAME password, differing only in the 32-bit values.** That is the sharpest statement of C305's mystery yet |
| L272 | 21:10 | `c4457236` | ⭐ **Closed a silent-success hole in five writers and gave the raw block writer a CLI command.** All 16 `write_*_to_t55xx()` audited: 11 guarded `blk_count == 0`, 5 did not — exactly the family sharing `fsk2a_t55xx_blocks()`. Without it a packer returning 0 writes nothing and reports `LF_TAG_OK`. ⛔ NOT the cause of C305 — those writers returned `LF_TAG_OK`, not `PAR_ERR`, so their counts were already non-zero. ⭐ `lf t55xx write` now exists for the bench diagnosis, with its ~1-in-3 unreliability stated in its own help (C307) |
| L271 | 20:30 | `f527fc43` | ⭐⭐ **pm3's downlink is broken while its listening is intact — a second, independent symptom class for C297/C299.** `lf search` works on every attempt; `lf t55xx detect` fails and `lf t55xx read` returns the SAME word for every block (`7FFFFFFF`, then `0EAAACAA` with the modulation set correctly). Identical words across blocks cannot be real data — block 0 is config and block 1 is credential — so the read command never arrived (C306). ⛔ **Also WITHDRAWS an over-strong refutation in L270**: I called *block 0 locked* dead because blocks 1-3 would have been written, but the firmware transmitting them does not mean the TAG accepts them after rejecting a locked block. It is the leading candidate again |
| L270 | 19:55 | `aeecc1b9` | ⚠⚠⚠ **Sharpened C305 to a rule and killed both of my surviving explanations.** A write lands **iff the config word already matches the tag's**: `lf hid prox write` 5 of 5 (it writes the config already there), GProxII/AWID/Keri/Indala 0 of 9 (all write a different one), pm3 confirming the credential bit-identical after every failure. ⛔ *Block 0 locked* is dead — blocks 1-3 would have been written and pm3 would read garbage. ⛔ *`blk_count == 0`* is dead — Keri and Indala guard it with `PAR_ERR` and all three writers returned `LF_TAG_OK`. ⚠ The raw path (0 of 11) is excluded as evidence: the tree already documents it as ~1-in-3. ⭐ Would unify with C297/C299 if a mechanism were found (C305) |
| L269 | 19:15 | `0e258f07` | ⚠⚠ **Narrowed C305 hard and refuted my own two best explanations.** Only `lf hid prox write` lands (5 of 5); GProxII, AWID, Keri **and a raw single-block write** all fail (5 of 5), pm3 confirming the prior credential intact each time. ⛔ Refuted by direct test: block count (Keri is 3 blocks like HID), time-ordering (HID still writes afterwards), the password path, a preceding read, and a silent no-op. ⚠ Two candidates survive and **neither explains all ten results** — block 0 being unwritable (invisible in HID's writes, which write the config already there) and single-vs-doubled downlink. ⭐ (a) would unify with C297/C299. Mechanism open; probes listed (C305) |
| L268 | 18:35 | `570229ca` | ⚠⚠ **The raw-frame T5577 writers do not land — AWID and GProxII both — while `lf hid prox write` works on the same tag minutes either side.** Nothing written at all, three times, verified by pm3 reading the prior credential intact. ⛔ Excluded: tag, field, coupling, the password path (both pack the same key and every block is sent twice, with and without), a silent no-op (no block cap, handler validates), and my own changes. ⭐ AWID is what makes it general: both failing writers take a raw frame through `fsk2a_t55xx_blocks` and write 4 blocks; HID builds its own and writes 3. ⛔ Cause open — the pattern is not a mechanism (C305). ⛔ **Superseded by L269: the block-count reading was WRONG — Keri writes 3 blocks like HID and fails too** |
| L267 | 18:05 | `778eefec` | ⭐⭐⭐ **Flashed #2 to HEAD and verified C302 and C303 on hardware — and the verification caught a message C302 had made false.** A tag written KASTLE fc 1 / cn 1 now reads back as Kastle 4 of 4 (it was C284's headline failure, reporting Check Point card 8389632), and 3037/3038/3060 survive the compile gate. ⛔ But the read printed *"the numbers below are fiction"* over correct numbers: the corruption branch assumed an HID format always wins at 32 bits, which was true only under TABLE order. ⚠ The harness shared the defect — it asked who ACCEPTS, not who WINS. Both fixed; 26 and 37 bits keep the message (C304). ⭐ Bench restored to H10301 FC 123 / CN 4567, confirmed by pm3 |
| L266 | 17:20 | `4fcc75d2` | ⭐⭐⭐ **Compile-gated the instrumentation instead of stripping it, at the operator's direction.** One macro `LF_RESEARCH_CMDS_ENABLED`, default 0 in `data_cmd.h`, set to 1 by a single `application/Makefile` line that an upstream PR deletes. Follows the tree's own idiom (`#if defined(PROJECT_CHAMELEON_ULTRA)` already gates handlers and dispatch rows). ⭐ Cost measured both ways: **1,088 B flash + 4,008 B RAM**, the RAM attributed to the byte by `nm`, which also CONFIRMS the source comment that the probe reuses `m_samples`. ✓ Host needs no gating — #2 declares 195 command ids via `GET_DEVICE_CAPABILITIES` (C303) |
| L265 | 16:45 | `7168f73e` | ⭐⭐⭐ **Fixed `unpack()` in place rather than beside it, at the operator's direction.** The walk now makes two passes and prefers a format that can VALIDATE; the card it returns carries `matches`, `verified` and `others`, and `wiegand_other_matches()` is deleted so the count cannot drift from the winner. ⭐ The key was already in the table — `fields.has_parity`, which C300 independently confirms is accurate on all 31 rows. ⇒ Never-reads-back-as-itself 14 → 12; **KASTLE, C284's headline failure, is repaired**. ⚠ `ambig.c` was grading table order, not the real walk; fixing that also caught the CLI's warning lists going stale (C302) |
| L264 | 16:10 | `a01b6629` | ⭐⭐⭐ **Measured every reader acceptance gate in bits, and found 4 of 11 formats carry no gate at all.** `ctest/gates.c`, 200,000 random frames per format through the exported shipping descriptor. ⛔ My GProxII prediction (2 bits) was WRONG and the code was right — it is ~25, because I counted only C255's parity and ignored 18 spacers and the length gate. ⭐ The two gates that admit nothing are DECOMPOSED, because a 25-bit gate and an always-false hook look identical: satisfying the structural bits makes them climb to 7.05 and 17.61, proving both reachable. ⚠ INSTAFOB and IDTECK have neither a gate nor `require_repeat` (C301) |
| L263 | 15:40 | `26242ba3` | ⭐⭐⭐ **Measured what C285 had only read: the 12 check-less formats carry no check bits at all, so `unpack()` cannot be repaired by implementing them.** 4096 random frames per format at its own discovered bit length, through the shipping `unpack()`: 12 accept **100%**, 19 reject some, 11 are unimplemented. ⭐ The null is printed and pinned — H10301's two parity bits admit **26%**, so the sweep reaches the checks. ✓✓ The 12 reproduce C285's list exactly, by behaviour rather than by reading, and pm3's unpackers for all 12 reject only on LENGTH. ⛔ Corrects C285's arithmetic to 12 of 31 (C300) |
| L262 | 14:55 | `7cef987c` | ⭐⭐ **Eliminated five causes for pm3's write failure and reduced the hands-needed test to one action.** Not the password (C298), not the downlink mode (all four of pm3's `--r0`..`--r3` swept, none landed), not pm3's antenna (`hw tune` says 21.05 V at 125 kHz and *LF antenna ok*), not the tag, not the field (C299). ⇒ What fits is the second antenna smearing pm3's transmit gaps while leaving the tag's uplink untouched — which matches the exact asymmetry and explains why our own writer, the near antenna, still works. ⛔ Labelled a hypothesis, not a measurement. ⭐ **The test needs no disassembly: power down Chameleon #2 and retry `lf hid clone`** — §5 now says that instead of "take the sandwich apart" |
| L261 | 14:25 | `d00afedd` | ⭐⭐ **Chased the obvious explanation for C297 and it is wrong — not a password.** The timeline was compelling: our `write_t55xx()` calls `try_reset_t55xx_passwd()` and writes using password `51243648`, and pm3 wrote fine all day until C295's tests, the first use of our own writer today (C298). ⛔ Refuted directly: a pm3 write of block 2 WITH that password left the air unchanged — and block 2 would have destroyed the credential, so it cannot be a false negative. pm3 cannot write with or without it. ⇒ C297's downlink account stands. ⭐ The side finding is the keeper: **every write this project performs sets the card's password**, which was undocumented and matters to anyone bringing a third tool. ⚠ Whether the PWD bit is actually ENFORCED is unknown — reading block 0 needs the broken downlink |
| L260 | 13:55 | `f74c5bcc` | ⛔⛔ **The Proxmark's T5577 write path has stopped working and says `Done!` anyway.** Restoring the bench credential failed four times — pm3 printing success each time — while both readers still saw the previous credential, and `lf t55xx detect` now fails outright (C297). ⭐ The tag and field are healthy: peak-to-peak 13908, 3778 field drops, four devices, both readers decoding the air. So it is pm3's DOWNLINK, which retro-explains the all-ones `lf t55xx dump` and the empty `lf t55xx read -b 2` I logged earlier today without connecting them. ⭐⭐ Recovered with OUR writer, first time, verified 4/4 by us and `parity ( ok )` by pm3 — so the bench is NOT blocked. ⛔ I trusted `Done!` three times before checking; pm3's clone does not read back, which is exactly M33's warning |
| L259 | 13:25 | `ba652f75` | ⭐⭐ **Lifted the session's transferable lessons out of the claims and into `METHOD.md`, which had stopped at M35.** M36 entailment beats captures — three claims to reach what one function would have told me. M37 a host capture loop is not the device's loop unless the timing matches. M38 an append-only document puts its oldest layer where the reader finishes, and a superseded warning should be dated rather than deleted. M39 re-running arms whose code cannot have changed is ritual. M40 verify the FAILING branch of a display fix — three shipped this session with only their happy path seen, and one of them dropped two fields once reached (C296). ⚠ Method is the one thing here with no experimental test; each is argued from a single incident and should be revised when one fails |
| L258 | 12:55 | `2fab52fa` | ⭐⭐⭐ **§1 said only the sandwich was left; testing that turned up a symmetric gap.** The reader warns an unpinned read is a guess; the WRITER said nothing, so `-f IND26` made a tag that reads back as H10301 with a different credential and nobody was told — C278 had seen exactly that and I had not connected it (C295). ⭐ Measured over a credential grid rather than one bench tag: **14 formats never read back as themselves, 3 sometimes**. ACTPHID is among the 14 and fills a gap C284 flagged, because the Proxmark refused to write it and the grid did not care. ⭐ Verified end to end: the warning named H10301, and the unpinned read returned H10301 FC 123 / CN 4567 while `-f IND26` returned 1969 / 471. Both lists pinned in `make check` |
| L257 | 12:30 | `78181b79` | ⭐ **Brought §1 up to date and reflashed #2 from a clean HEAD, so the bench is verifiable against git.** The device reported `789a28a-dirty` — built mid-edit, matching no commit — where the code was in fact identical to HEAD bar two comment-only changes. Rebuilt clean and flashed: `hw version` now reads `96c9d1c`, HEAD exactly, and the tag reads 4 of 4 after. ⭐ §1 itself was 45 claims stale, describing the reader work as of 09:50 yesterday. A fresh context inherits §1 and nothing else, so it now states the bench, the four gates and their regression pass, the HID format-walk finding, U14's result, and the standing caution measured three ways. Same discipline C286 applied to §9f — history stays in this file |
| L256 | 12:10 | `d37de9a4` | ⭐⭐ **C293 left C266's survivors "unexplained"; they were never unexplained — the answer is entailed.** `winner_res` is assigned in exactly one place, inside the agreement match, and the function returns false otherwise. So every frame this reader has ever returned matched its predecessor at the same sample phase, and C266's 3 wrong credentials recurred within a phase by construction (C294). ⇒ C207's deterministic-distortion account is not the likely explanation, it is the only one the code permits. ⛔ And the trap that misled me: my 48 broken-tag captures were separate `rdrcap.py` invocations with a 50ms gap, where the device takes its tries back to back inside one read. Different timing regimes, compared as if they were one measurement |
| L255 | 11:35 | `2946f085` | ⛔ **Placing our rule against the references' meant reading it instead of describing it, and that found C269 wrong.** `lf_sampled_read_phases` needs two CONSECUTIVE decodes at the SAME sample phase and resets when the phase changes — so the cross-phase recurrence C269 observed cannot pass the rule, and its explanation of C266's survivors is void (C293). ⭐ The right mechanism was already in `gproxii_read`'s comment from C207: at a fixed drive the distortion is identical in both captures, so they agree on the same wrong answer. That is why that reader pins drive 7. ⇒ Our rule is as strict as the Flipper's and stricter in one respect — same phase required. ⛔ Second stale warning in the same file, C292's shape again: the arm still said NOT VERIFIED when C213 closed its open question and it has read 12/12 and 4/4 since. ⚠ C266's 3 survivors are unexplained again, and the claim says so |
| L254 | 10:55 | `aa914689` | ⭐⭐ **U14 asked why the references pick 3 and 6; nobody documents it, so I asked whether OUR 2 is right instead — and the answer dates a warning in our own source.** Front corpus with today's gates: 160 captures, 110 frames, **0 wrong**. Phasebits: 13 wrong and **every one distinct** (C292). ⇒ Agreement at 2 removes all of them; 6 would remove nothing. ⛔ The note above `INDALA_AGREE_COUNT` warns that 3 of 21 front-side wrong frames repeat within a phase — measured before the straddle gate and the zero-bit gate, and those 21 are now 0 on the same captures. Labelled as history rather than deleted, because widening PHASE_ROTATION would make it live again. ⚠ One tag, one unit: our 2 is sufficient HERE, which is not the same as 6 being wrong for a Flipper on tags we have never seen |
| L253 | 10:20 | `c3e23f17` | ⭐⭐⭐ **U14 opened and largely answered: the Flipper family doubles its repeat count for PSK1 and only for PSK1.** Three models across six trees — the Proxmark validates structurally and prints every candidate, never requiring two reads to agree; the Flipper family validates only by repetition, N consecutive byte-identical reads; we do both (C291). ⭐⭐ `validate_count` is 3 almost everywhere and **6** for indala26, indala224, keri, nexwatch, idteck — the whole PSK1 family — plus `hid_generic`. ⇒ We found PSK1 fragile from the emulation end (C190: 3 of 21 against a Proxmark); they found it from the read end and never emulated anything. ⭐ `hid_generic` is the format-agnostic reader, which is C284/C285 in someone else's words. ⚠ Why 3 and 6 is nowhere explained or tested in any of the five |
| L252 | 09:40 | `90703fcd` | ⭐⭐ **Pinned C287's new API against a second independent walk over the same table.** `wiegand_other_matches()` decides what the reader tells an operator about the alternatives, and four hardware reads were its only check. The new arm requires its count to equal `ambig`'s own per-format enumeration minus the excluded one — two different walks, so a bug in the API cannot hide in the expectation (C290). 1,519 frames, 0 disagreements. ⭐ Break test: removing the `except` skip took it to 1,519 of 1,519 disagreeing and failed `make check`; restored clean, rebuilt from scratch first because this harness has been bitten by a stale binary twice |
| L251 | 09:05 | `fc47a0ab` | ⭐ **Nearly flashed Chameleon #1 to re-run the emulate arms, then checked whether today's changes could reach them. They cannot.** Seven firmware files changed since C241's build: four are readers and three are HID-only. Every file the five working arms run through has **0 commits** in that range (C289). ⇒ No flash, and the bench stays as the operator will find it — which matters more than the reassurance would have. ⚠ The HID emulator does share `hidprox.c`, but it has been 0 of 6 since C246, so there is nothing to regress. ⭐ The general point: re-running arms whose code cannot have changed proves the bench works, not the build |
| L250 | 08:35 | `05fcaed1` | ⭐⭐ **Consolidated regression pass before the operator returns: 12 read arms, 48 reads, all exact on the current build.** Four gates changed today and one payload grew; each was verified as it landed and none had been checked against the others (C288). ⭐ The rate check is the part worth keeping: C287 added 31 `unpack()` calls per successful HID read, so HID was re-measured at 24/24 exact — matching C248's 48/48 from before any of today's changes. ⇒ The four stricter gates cost nothing on real tags, which is the claim none of them could make alone. ⚠ A regression pass re-runs known answers; it would not catch a gate that is too loose |
| L249 | 07:55 | `4554ad23` | ⭐⭐⭐ **Built the fix C285 said was one of only two correct ones: the reader now NAMES the other layouts that fit.** The firmware enumerates, because it owns `formats[]` — a host-side copy of 31 unpackers would be the mistake this project refuses — and the answer rides in payload bytes 13-15, which were already there and already zero, so old hosts and old firmware both degrade honestly (C287). ⭐ Verified against the Proxmark on three tags: our counts equal its parity-PASSING candidates exactly, 3 for 3, because our unpackers refuse the ones it annotates `parity ( fail )`. A Kastle tag now says Check Point **and names Kastle among the alternatives** — the information C284 showed was being silently discarded. ⚠ Only two are named for want of payload space; the count is exact and says how many more |
| L248 | 07:10 | `11a2acba` | ⚠ **Rewrote §9f, the section a reviewer actually reads, because it had become a record of how I got there rather than what is true.** Ten claims had been appended to it in order, and its TAIL still said the defect is "reachable only when something else is already perturbing the capture" — which C284 disproved on perfectly good tags (C286). Two other tail statements were contradicted by C276. A reviewer reading to the end would have taken the weakest and oldest reading. ⇒ Now states the strongest measurement first and keeps the history in LOG.md, which is what this file's own convention says. ⭐ The lesson generalises: a plan that is only ever appended to puts its oldest layer where the reader finishes |
| L247 | 06:35 | `81e14deb` | ⭐⭐⭐ **Asked whether the Proxmark has C284's problem too. It does not — it prints EVERY candidate with a parity verdict — and chasing why told me the walk cannot be fixed.** 13 of 32 `unpack_*` functions have no rejection path at all, and that list predicts C284's relabel map exactly: HCP32 swallows Kastle, Kantech, WIE32 and HPP32; Optus34 swallows SMP34; ind27 swallows INDASC27 and TECOM27 (C285). ⭐ The guarded ones behaved: we returned Optus34 rather than H10306 for the SMP34 tag because `unpack_h10306` checked parity and refused — and pm3 independently marks H10306 `parity ( fail )` on that same frame. ⛔ The 13 cannot be hardened; a format with no parity has nothing to check, and pm3 prints no parity beside them either. ⇒ C277 asked whether narrowing the walk was cheap. The answer is that narrowing cannot make it correct — only enumerating every candidate, as the reference does, or pinning. ⛔ **Count corrected by L263: 12 of 31, not 13 of 32** — `unpack_nonlinear` is a shared helper, not a table row. The list and the conclusion stand |
| L246 | 05:55 | `3b349dfb` | ⭐⭐⭐ **C283 handed me an instrument and this is what it found: half the HID format table cannot survive an unpinned read.** Every format the CLI accepts, written to a real tag by the Proxmark at fc 1 / cn 1 and read back — **14 exact, 15 relabelled with a wrong credential** (C284). A Kastle tag holding fc 1 / cn 1 reads as Check Point card 8389632. ⭐ The control settles where the fault is: `-f KASTLE`, `-f IND26`, `-f SMP34` each return the written credential exactly, so the tags are fine and the WALK loses the information. ⇒ C276's "no format pinned" warning turns out to cover half the table rather than a corner case, which is the number to put in front of upstream. ⚠ W2804 and ACTPHID absent — the Proxmark refused every credential shape tried, a writer limitation rather than a reader finding |
| L245 | 05:15 | `48f1da4a` | ⭐⭐ **C282 said three of the four message branches could never be exercised here. That was wrong, and the fix was obvious in hindsight: the Proxmark WRITES those formats.** `lf hid clone -w ind27` and `-w Optus34` put foreign layouts on an HID Prox tag, so both the neutral note and the "cannot tell" hedge were seen on real tags with the cloned credentials reading back exactly (C283). ⭐ The 27-bit case is C280's crying-wolf scenario demonstrated NOT crying. ⭐ The Optus34 case is the hedge being right: the tag genuinely carries a foreign layout and the reader says it cannot tell that from corruption. ⚠ Three N10002 attempts read as H10306 first — at 34 bits an HID format takes 307 of 484 — so `ambig` had to NAME a frame in the other 177. Also fixed: `ambig` printed its example above the summary line it belonged to |
| L244 | 04:35 | `b434fc06` | ⛔ **Re-read what I shipped two claims ago and found it dropped fields.** The `no HID format at this length` branch returned early, re-printing FC and CN itself and losing IL and OEM, plus a blank line when FC was 0 (C282). No test would have caught it — there is no 27-30 bit tag on this bench. Restructured into a three-way branch that falls through to the shared field printing. ⭐ The classification is now checked structurally instead: the three sets are disjoint, and the four formats that fall through to "cannot tell" are exactly the four non-HID 34-bit ones — the single length C280 measured as partial. I did not build the sets from that, so the agreement is a real check. ⚠ Three of four branches stay unexercised on hardware and the claim says so |
| L243 | 04:00 | `7ea785ac` | ⭐ **Chased "lengths above 37 are unmeasured" and found there ARE none — the sweep was complete all along.** `formats[]` holds 31 entries and stops at 37 bits, while the firmware enum declares 42; eleven names have no row and can never be packed or unpacked (C281). ⇒ C280's per-length table covers everything the code can produce. ⛔ I predicted `lf hid prox write -f C1K48S` would silently write zeros — the CLI refuses it, because the HOST enum has 31 members and mirrors the table where the firmware's has 42. Tag verified untouched by both readers afterwards. ⚠ What is left is dead surface in an upstream header, worth a reviewer's line and nothing more |
| L242 | 03:20 | `953eea2a` | ⛔ **The message I shipped two claims ago would have cried wolf on every 27-, 28-, 29- and 30-bit read.** There is no HID format at those lengths at all — `ind27`, `w2804`, `ind29`, `atsw30` and friends are the only layouts there — so `⛔ no HID layout fits this frame` would have fired on every single one, correctly and uselessly (C280). ⭐ Found by extending the per-length sweep rather than by anyone hitting it; there is no such tag on this bench. ⇒ The message now branches three ways on measured ground: corruption at 26/32/37 where an HID format takes every valid foreign frame, "cannot tell" at 34 where it takes only 307 of 484, and a neutral note where no HID format exists. ⚠ Above 37 bits is unmeasured and falls through to the cautious wording. ⭐ Verdicts pinned in `make check`, because the CLI branches on them now |
| L241 | 02:40 | `393f0e7d` | ⭐⭐ **Walked the branch instead of trusting it.** C278 shipped text on the relabelling path with nobody having seen it print — C251 proved the path fires, which is not the same as proving the message is right. Guard-off probe, 40 reads, and read 29 rendered it correctly end to end (C279). ⭐ The corrupted credential came back FC 1973 / card 471 against the true ind26 reading of FC 1969 / card 471 — right card, wrong facility, C266's signature showing up in a second protocol. ⚠ 1 in 40 here against C251's 7-in-48; different session, not reconciled, reported as measured. ⭐ The restore flash also puts #2 on HEAD, so the operator returns to a bench with no firmware drift |
| L240 | 01:55 | `f56d2a65` | ⭐⭐⭐ **The operator asked what Proxmark and Flipper do, and answering it turned up that two different `lf hid clone` commands write the SAME TAG.** `-w ind26 --fc 1969 --cn 471` and `-w H10301 --fc 123 --cn 4567` are the same 26 bits; our reader says H10301, pm3 said ind26, both are right (C278). ⭐ Then the sweep: H10301 accepts **all 8,658** validly packed ind26 frames, so at 26 bits a non-HID format can never win — the relabelling path is reachable only through corruption. ⛔ **Two wrong messages caught before shipping**: first I asserted corruption, the real ind26 tag refuted it, so I hedged — then the sweep showed the hedge is wrong at 26 bits. Now length-aware. ⭐ References: pm3 prints EVERY match with a false-positive banner; Momentum takes the first but demands N consecutive identical reads (3 for Securakey). We did neither — our two-agreeing-stacks is the Momentum-shaped defence one layer down |
| L239 | 01:10 | `ff342ea1` | ⭐⭐⭐ **Asked the shipping `wiegand.c` how ambiguous a Wiegand frame actually is, and the answer reframes the whole relabelling question.** A VALID H10301 frame is accepted by two formats — H10301 and IND26 — so the table's ORDER decides the answer on every read, not just corrupted ones (C277). And of 26 single-bit corruptions, **none** still match H10301: 13 die, 13 are relabelled. Relabelling is what corruption does here, not a rare accident. ⇒ C276 wondered whether narrowing the walk was cheap now that it has one caller; this says it is not — reorder the table and working readers change their answers. ⭐ New `ctest/ambig.c` compiles the real `wiegand.c` with Nordic's macros shimmed verbatim, and the counts are pinned in `make check` |
| L238 | 00:30 | `ca664eb4` | ⭐⭐ **The operator asked for the `unpack()` relabelling to be OURS to fix, so it is.** `lf hid prox read` now says outright that an unpinned read is a guess — the FIRST layout that fits rather than the only one — names the `-f` flag that removes the ambiguity, and quotes C251's measured 7-in-48 against 1-in-48 (C276). Verified on a real H10301 clone in both branches. ⛔ Deliberately NOT in `unpack()`. ⭐⭐ **And checking that decision turned up that §9f overstated it**: the walk has exactly ONE caller, `hidprox.c:134` — not "every reader that guesses a format". ioProx has a fixed layout and Indala never touches the table. The upstream conversation is about one reader, not a shared foundation |
| L237 | 23:55 | `6cf1ab98` | ⚠ **Went looking for an Indala-style dead band in GProxII's phase list and found something narrower and more awkward.** Phase 120 returns the true frame at drive 7 like every other phase, but across 14 marginal captures it produced **0 true frames and 3 odd ones**, and on the broken tag it produced 6 of the 13 odd frames from one of six phases (C275). ⛔ So "phase 120 is a dead band, drop it" is FALSE — at the operating point it contributes, and the drive-7 control is what refuted my own hypothesis before I acted on it. ⇒ What stands is worse for the method than for the reader: `PHASE_ROTATION`'s note warns the map is one tag and one unit, and it should also warn that the ranking depends on SIGNAL LEVEL. Every phase map here was measured at one level. ⚠ Not enough drive-7 data to re-rank phases — one capture each |
| L236 | 23:20 | `4f2a3757` | ⛔ **C273 sent me to the drive sweep's own comment, and it asserts something false.** It said step order matters for latency and not correctness; Noralsy reads at drive 7 only (C182) and FDX-B at drive 4 only (C273), so for those two the sweep IS the correctness (C274). ⛔ The table also truncates — `steps = timeout_ms / 250` takes a prefix — so a short budget silently drops the tail and Noralsy with it. ⭐ Followed through to the numbers instead of leaving it as a worry: every caller passes 3000 ms, which clamps to all four steps, so **nothing is broken today**. The margin is the protection, and shortening the timeout for snappier reads is exactly the change that would break Noralsy. Comment-only, firmware rebuilt clean |
| L235 | 22:50 | `2f8864e5` | ⭐⭐⭐ **FDX-B settles C270 the hard way and turns up a fragility nobody had noticed.** GProxII and FDX-B share the biphase decoder WHOLE, so sweeping FDX-B separates decoder from bit rate in a way Gallagher and Securakey could not: 56 captures, 24 frames, **zero wrong credentials** (C272). ⇒ It is RF/64's half-bit against the front end's ~27-sample coupling constant, not the decoder. ⭐ One raw did differ — by a single bit at position 113, past the 64 the CRC covers — and the credential is identical, which is exactly the over-count C267 warned about. ⛔ **And FDX-B reads at drive 4 and at NO other field strength**: 16/16 at 4, 0/16 at 7, 0/16 at 6, nothing at 2 or 1, reproduced across two passes (C273). It works only because the shared sweep happens to try 4 first, and nothing at the call site says so — while C182 has Noralsy needing 7 and only 7. Two protocols, two different single settings, one undocumented ordering |
| L234 | 22:00 | `3729582` | ⭐⭐⭐ **Swept field strength on all three ASK protocols and the answer inverts how C252's table reads.** Gallagher (RF/32) and Securakey (RF/40): perfect at every drive down to 2, then a cliff to nothing at drive 1, **zero false frames in 40 captures each**. GProxII (RF/64): degrades gradually into WRONG ANSWERS (C270). ⭐ The front end's AC coupling has a ~27-sample time constant (C204); RF/32 and RF/40 keep their half-bit inside it, RF/64 does not — so only RF/64 has a degraded regime, and that regime emits frames rather than silence. ⛔⛔ GProxII has the STRONGEST gate of the three and Securakey the weakest, and it is GProxII that produces wrong credentials. **Gate strength and field robustness are independent; C252 measures only the first.** ⚠ Also cost twenty minutes and is now written down: `rdrcap.py --drive 0` silently means drive 4, which looked exactly like C148's inert-drive defect (C271) |
| L233 | 21:05 | `9dc4553` | ⭐⭐⭐ **Closed the gap between C266 and C268 — the two failure modes differ in the SHAPE of their error population, not in luck.** Captured the parity-broken tag the same way C268 captured the valid one, so they are finally comparable: 48 captures, 13 frames, 12 distinct, **12 of 12 wrong credentials**, and the on-tag frame returned 0 times because the Wiegand gate refuses it every capture (C269). ⛔ 11 of the 12 keep card 6789 and differ only in the facility code — the tag's frame is one bit from valid parity, so the nudges that restore it land in a few places. ⭐⭐ **One frame recurred at two different capture phases**, which is exactly what passes two-agreeing-stacks, and that explains C266's 3 survivors in 52 device reads with nothing new. ⇒ Marginal field scatters and agreement catches everything; a broken frame concentrates and it sometimes cannot |
| L232 | 20:20 | `750ace0` | ⭐⭐⭐ **Turned C267's "would have been caught" into a measurement, because four distinct frames is not enough to rest a defence on.** Eight more captures at each of the four phases that had produced an odd frame: 19 frames, and every non-true one appeared exactly once while the true frame came back 13 times across the set (C268). ⇒ 92 captures, 33 frames, 12 odd raws, **all 12 unique** — a marginal capture's error does not reproduce, so two-agreeing-stacks rejects it. ⭐ The true frame repeating is the positive control: the pipeline CAN produce a repeat, so "none repeated" is not an artefact. ⛔ 9 of the 12 are wrong CREDENTIALS, scattered across both fields — unlike C266's broken-tag frames, which all kept the right card number. Two failure modes, and only one of them repeats |
| L231 | 19:40 | `4d2ab32` | ⭐⭐⭐ **Asked whether ordinary marginality does what C266's broken tag did. It does, and the answer changes which defence gets the credit.** A tag holding the CORRECT credential, captured through the reader's own path at its own six phases: drive 7 clean 6/6, drive 6 gives 12 frames of which **4 are not the true raw**, drive 4 gives nothing at all, drive 2 gives two frames and neither is true (C267). ⛔ Scored on the CREDENTIAL — what a user acts on — 4 of 14 frames are wrong. ⚠ Two of the six odd raws decode to the RIGHT credential, differing only outside the credential field, so scoring by raw hex over-counts; recorded because I nearly published the larger number. ⭐⭐ **All four wrong credentials are distinct, so two-agreeing-stacks would have rejected every one** — the agreement rule is doing work the frame gates cannot, and C252's sweep cannot see any of this. ⚠ Contrast with C266: on the BROKEN tag the false frames survived that same rule, so they repeated |
| L230 | 18:50 | `e5aacf7` | ⭐⭐⭐ **Chased C255's false frame to a rate and a mechanism, and the rate is not what n=4 suggested.** 52 reads of a deliberately parity-broken GProxII tag: 49 silent, **3 false frames — about 6%, not the 25% "one in four" implied** (C266). ⭐ The mechanism is exact: the tag's frame fails Wiegand parity by construction, and a single mis-sliced bit inside the same parity group RESTORES it, so the frame passes every check both references make. Two of the three are one bit from what is on the tag. ⛔ **All three carry the right card number and a wrong facility code** — 6789 with FC 39, 46, 51 — which is the near-miss shape that matters in access control rather than a harmless garble. ⚠ It took a deliberately broken tag to produce on demand; whether a naturally marginal tag does the same at the same rate is untested |
| L229 | 18:00 | `f047b42` | ⭐⭐ **The display audit was not as complete as I said it was — three protocols were missed and one of them was wrong.** C260 checked ten and called the layer done; FDX-B, IDTECK and Jablotron also derive fields and had never been compared (C265). FDX-B and Jablotron agree with the Proxmark. ⛔ IDTECK printed the checksum BYTE and no verdict, where the reference prints both — so our output was a number and theirs a judgement. Fixed, and verified on the tag in both directions: a constructed failing frame and a constructed passing one, each matching pm3's word. ⚠ The reference's own caveat is carried across rather than dropped: pm3 marks this check "(TBD)" and its worked example disagrees with its code, and no real IDTECK tag exists here — so an "ok" has only ever been seen on a frame we built |
| L228 | 17:05 | `af88ee7` | ⛔ **Built the instrument C254 designed, and it fails the same question a second time — caught by toggling the flag rather than by trusting the design.** Two passes, corrupted first, buffer capped so no third clean frame exists, on formats whose emitted length cannot change with the data, with a sanity check that two clean passes decode. Then `require_repeat` was turned ON for Indala26 — the format under test, edit confirmed on disk, binary deleted and rebuilt — and not one number moved (C264). ⇒ Backed out. ⚠ The flag is not inert: C258 measured it changing real captures. It is the synthetic buffer that misses it, and why is open. ⭐ One thing survives and is labelled as host-synthetic: with a corrupted copy and a clean copy in the same buffer the decoder returns the CORRUPTED one 23 times in 64. ⇒ Two instruments, two failures, both recorded — the question now costs more than it is worth and should wait for a reason to reopen it |
| L227 | 16:20 | `35936f4` | ⭐⭐ **"Strip the instrumentation" became a checklist with line numbers, and the split turns out to be subtraction.** §9b had carried the decision without a plan since it was written, so whoever eventually did it would have re-derived the whole thing (C263). Four items, every call site grepped: nothing shippable depends on any of them — one caller for the capture probe, none for the two debug handlers beyond their own dispatch rows. ⭐ The GProxII row is two lines, because `scan_gproxii()` was already in the tree as the plain wrapper that discards the energy. ⛔ Deliberately not executed here: it would break the tooling this branch runs on, and `research/` ships in no PR anyway. ⚠ What the cut costs is named — the probe cracked C211 after six refuted hypotheses |
| L226 | 15:45 | `b0cac61` | ⚠ **Refreshed §9, the document a reviewer actually gets, and it had decayed in four places.** The command-id count was 46 and is 48, with ranges wrong in both directions — recounted by diffing `data_cmd.h` against `main` rather than quoting the row forward (C262). ⭐ The useful number is 41, once the three instrumentation ids and the four emu-id pairs for emitters that must not ship come out. ⛔ Worse than the count: the grid still called HID Prox "intermittent 15-20%" three claims after C250 closed it, §9a's evidence grid omitted six whole protocols, and a blocker row argued from "both references are the same" without either having been read. ⭐ New §9g carries the two audits a reviewer cannot run themselves — the frame gates against each reference, and what the CLI prints against the Proxmark. |
| L225 | 14:40 | `98af493` | ⭐⭐ **Securakey's missing display built, and it takes the option C253 never listed.** C253 weighed ENFORCING the Wiegand parity bits against IGNORING them and chose ignore, because being stricter than both references is how a reader starts refusing real tags. The reference does a third thing: it PRINTS the parity and gates on nothing. That costs nothing and hands the operator the signal (C261). ⭐ Fields transcribed from `protocol_securakey_decode` and checked against the Proxmark's own output — including the 32-bit wiegand word, byte-identical — before shipping. ⭐ The break arm is the one worth keeping: frame bit 38 flipped leaves every spacer and the length intact, so the GATE still accepts it, and the reader duly read it 4/4 and flagged `PARITY FAILS`. On that same tag the Proxmark prints no wiegand line at all |
| L224 | 13:55 | `b1871e8` | ⭐⭐⭐ **Audited what the CLI PRINTS, in ten protocols, against the Proxmark on the same tag — a layer no read arm in this campaign has ever touched.** Every read arm scores the raw frame, so a wrong number printed under a right frame was invisible by construction. Result: eight clean, one defect (Keri, fixed in C259), one gap — **Securakey prints no derived fields at all** where the Proxmark prints len/FC/card and a Wiegand parity line (C260). ⭐ That parity line is worth having: the reference REPORTS it without gating, which is the middle path C253 passed over when it chose not to enforce those bits. ⭐ Each credential was one we chose and handed to the clone, so both readers are checked against a third thing rather than against each other |
| L223 | 13:10 | `67d1114` | ⭐ **Keri's `Internal ID` now matches the reference, and the other half of C259 was my own error.** The id fix is real — `cmdlfkeri.c` masks bit 31 because it is the 33rd preamble bit, we printed the whole field, and a `--cn 12345` tag read back as 80003039. Both print sites now show the reference's number and the raw field the writer wants, verified on the tag. ⛔ **The MS card-number half is RETRACTED**: I compared our settled read against a Proxmark read taken seconds after its own write, which was a marginal frame — 0x8000377E descrambles to 3223872 and 0x80003039 to 12544, on either tool, from byte-identical tables. ⚠ I had already seen that read report id 14206 against the 12345 cloned, and used its card number anyway. ⇒ A settled reading and a fresh-after-write reading are not the same measurement |
| L222 | 12:35 | `ff56e0d` | ⭐⭐ **Keri gets the repeat check, Indala26 is refused it, and the measurement rather than the protocol decided which.** Probed with the firmware's own rule: Keri 3/4 either way, Indala26 51 true frames down to 43 — C161's window, 8192 against 4096 (C258). Hardware 8/8 on a fresh `--cn 12345` clone with the Proxmark agreeing byte for byte. ⚠ The benefit is argued, not measured: 705 null captures give zero false frames both ways, because C157's veto got there first. ⭐ And the sweep does not move at all, which is C254 proving itself in use. ⚠ **Found on the way and NOT fixed: our Keri reader prints `Internal ID: 80003039` where the Proxmark prints 12345** — ours is the raw field under the reference's label — and our MS card number disagrees too, unexplained (C259). Every read arm scores the raw frame, so a display defect could live here indefinitely |
| L221 | 11:40 | `ff197e3` | ⭐⭐⭐ **Indala26's missing 2-bit check added, and the arm that proves it also prices it.** Measured on the 320-capture corpus before writing a line: 68 frames to 64, all four lost are wrong, all 51 true ones survive (C257). ⛔ **The hardware arm is the interesting one**: a frame with bit 61 set reads 0/4 here and the PROXMARK reads the same tag perfectly as Fmt 26 FC 52 Card 63612. The two references disagree — Momentum rejects, the Proxmark does not — and we have followed Momentum on C205's precedent, that a multi-protocol reader can afford strictness a single-protocol demod cannot. ⭐ `compare.py` had to be mirrored and the harness is what said so: the gate on one side only produced four per-capture mismatches naming exactly the four frames. ⚠ Still open and deliberately untouched: the REPEAT requirements in Indala26's and Keri's references. That is a capture-window question and C161 says measure it |
| L220 | 10:45 | `5317277` | ⭐⭐⭐ **Audited every gate against its reference, closed GProxII's, and watched a corrupted tag beat the closed gate once in four.** Three formats match their reference exactly — IDTECK, InstaFob and Indala224 — and saying so is the useful half, because InstaFob's `.accept = NULL` looks exactly like the gap Securakey's was and is not one (C256). ⭐ GProxII was a real gap: the reference checks Wiegand parity over the descrambled credential and we stopped at the format length. Sweep 36→64 rejected; hardware A/B/A 8/8, then 0 exact with one named bit broken, then 8/8 restored (C255). ⛔ **The fourth read of the broken arm returned a DIFFERENT frame that passes every check both references make** — FC 51 where the tag holds FC 45, same card number, parity valid. C205's shape again: stable enough that two-agreeing-stacks would confirm it. ⇒ Still open: Indala26 wants bits 60/61 zero, and both it and Keri want the frame repeated — the repeat is a capture-window question and must be measured, not assumed (C161) |
| L219 | 09:50 | `950722d` | ⛔ **Tried to make C252's sweep reach `require_repeat`, and the control killed the arm — written down rather than quietly deleted.** The sweep loops the sequence, so a corrupted frame repeats identically and a repeat check passes it; the obvious fix is one corrupted pass followed by clean ones. ⛔ The control moved as far as the test did: Indala26, which has no repeat gate, went 33/0/31 to 0/39/25, because once a VALID frame is in the buffer the decoder finds it and the outcome turns on which copy comes first (C254). ⭐ And Indala224 did not move at all, for a reason worth keeping: one flipped bit turns an even-parity frame odd, the differential emitter doubles its buffer, so "one pass" was already two corrupted copies. ⇒ The arm is backed out and the design the instrument would need is written down. ⚠ C252's low numbers are a FLOOR for repeat-gated formats, not an estimate, and the table now says so |
| L218 | 09:15 | `02e1585` | ⭐⭐⭐ **The one row of C252's table that was ours to fix is fixed, and the fix was verified by breaking a single named bit on a real tag.** Securakey had no `accept` hook at all; the reference has one and it is structural rather than a checksum — ten zero spacers, one per 9-bit group (C253). ⭐ The hardware arm is the part that makes it evidence: valid frame 8/8, spacer bit 46 flipped 0/4 with the field fully modulated and the Proxmark's own readback proving the tag held the broken frame, valid frame restored 4/4. ⚠ Stopped at what the references check. The frame carries Wiegand parity at bits 35 and 63 that neither reference validates, and enforcing it on one bench credential is how a reader starts refusing real tags — so it is written down rather than added. ⭐ Sweep 19→28 caught; four committed captures unchanged; 53 null captures still silent |
| L217 | 08:20 | `6b51ccb` | ⭐⭐⭐ **Asked every decoder we ship what it does with a WRONG frame, and the answers run from 100% to 12%.** C251 was about a reader accepting a frame it should have refused; this generalises it to our own readers with no device at all — flip one frame bit, re-emit, re-decode, 1,024 corrupted frames across ten protocols (C252). ⭐ AWID catches all 96 because its format is 22 independent nibble parities; Indala26 and IDTECK catch half because a preamble is all they have. ⛔ **`LF_ASK_FORMAT_SECURAKEY.accept` is NULL and the reference checks spacers we do not** — the one row in the table that is ours to fix. ⭐ The break test is the part worth keeping: removing Gallagher's accept hook left its round trip ✓ EXACT and moved the sweep 88/8 → 16/80. The harness could not see a dropped gate before today |
| L216 | 07:30 | `ac3bb3d` | ⭐⭐⭐ **The wrong credentials are mostly not parity failures — they are Indala.** Re-ran the guard-OFF arm keeping the FORMAT line the first run threw away, which is the whole reason it could not be settled: 6 of 7 wrong reads came back as `Indala 26-bit` at FC 1953-1977 / CN 471, hugging the ind26 reading of this tag's own uncorrupted frame (C251). H10301's parity was doing its job and rejecting them; `unpack()` then handed the mangled frame to the next 26-bit format, which took it. ⭐ `-f H10301` cuts it 7/48 → 1/48, and the four H10301-labelled wrongs are all two flips inside one parity group. ⛔ Both halves — the walking `unpack()` and the CLI's `format = 0` — are on `main`, so this is a defect to report upstream rather than one to fix here quietly. ⚠ The lesson is about the harness, not the firmware: a rate script that scores only the fields you expect cannot see the field that explains them |
| L215 | 06:10 | `f7b3d30` | ⭐⭐⭐ **C45 is explained — it was the BLE advertising burst — and the defect it names is worse than intermittency.** `lf_reader_data.c` already carried the switch and the note that C47's paired test "has never had" its other arm; it cost one probe build to run it. Guard ON 96/96 exact, guard OFF 71/80 on the same tag, p = 6.4e-4 (C250). ⛔ **Five of the nine were WRONG CREDENTIALS returned as successes**, one of them two adjacent bits inside a single parity group where H10301's parity is blind. ⛔ **I published "our reader checks no parity at all" and it is false — retracted within the hour.** `unpack_h10301()` checks both bits; I grepped `hidprox.c` and `lf_hidprox_data.c` and not `wiegand.c`, where the check actually lives. The corruptions passed a WORKING check, and whether by parity-invariant flips or by `unpack()`'s format guessing — it is handed `format_hint = 0` and returns the first 26-bit format that fits — is the next measurement. ⭐ Each arm was flashed from its own commit and confirmed by `hw version`'s git hash before a single read was taken, because a flash that reports success and does not land has cost this branch a day before (L162). ⚠ The honest limit: this measures the mechanism, not C46's package factor, which may still be a second and independent cause of the 0/6-vs-7/9 spread |
| L214 | 04:40 | `e80fd22` | ⭐⭐ **C45 measured on the current build: 48 of 48, and the shared FSK2a engine matches the legacy decoder arm for arm on the same tag.** The unit was meant to answer whether the shared engine fixes C45 or inherits it. It answers half: it does not inherit it (C248). It cannot answer the other half, because the 8% defect does not reproduce here at all — and the honest reading is that I had no baseline to compare against, not that the engine cured anything. ⭐ The A/B/A was worth its cost: A 32/32 HID, B 32/32 AWID, A' 16/16 HID after rewriting the credential, so the 100% is not the bench being kind for twenty minutes. ⚠ **Two explanations survive and they are testable in different places** — the C47 advertising guard that `cf745fb` put into this exact decoder, and C46's package factor. The first is a deliberate break away; the second needs the coin on a reachable pad. ⛔ **And one piece of tooling nearly published a false null**: pm3 with two chained `-c` arguments reported the pre-wipe credential, parity and all, from a tag that had just been zeroed (C249). The wipe-and-confirm-null step is precisely where that would have been believed |
| L213 | 04:00 | pending | ⭐ **§9e: the FSK2a emulation defect is PRE-EXISTING and the upstreaming notes now say so.** Two of the three broken emitters are upstream's own, `hidprox.c` and `ioprox.c`, and upstream's feature table claims both emulate — on this hardware they do not. A reviewer needs that stated: the AWID emitter this branch adds sits on the same broken path, which is why §9d marks it DO NOT SHIP and why shipping it would read as our bug. ⚠ Also recorded: ten hypotheses were eliminated before anyone thought to try a shipped emitter, and that should have been the FIRST test | compute only |
| L212 | 03:35 | pending | ⛔ **Varying counter_top per entry is not the cause — em410x with an alternating 63/65 still reads 4/4.** It was the one property separating the FSK2a emitters from every working one: ASK and PSK1 hold counter_top constant, FSK2a alternates 8 and 10. The peripheral handles the change fine (C247). ⭐ Tenth hypothesis down. ⚠ What remains cannot be separated here: every working emitter uses a top of 31+, every failing one 8 or 10 — but for a Manchester emitter a small top and a fast square wave are the same change | A/B/A, 12 reads |
| L211 | 03:10 | pending | ⭐⭐⭐ **All THREE FSK2a emitters on this device are silent — two of them shipped long before this branch.** ioProx joins HID Prox and AWID at 0 of 6, with Gallagher 4/4 as the control after each (C246). ⭐ The pattern is by MODULATION, not authorship: every PSK1 and ASK/Manchester emitter works, no FSK2a one does. ⛔ So the grid's ✓ for HID Prox and ioProx EMULATE was inherited from upstream's feature list and has never been true here — corrected | 16 reads, 3 controls |
| L210 | 02:45 | pending | ⭐⭐⭐ **AWID's silence is not our bug — the firmware's OWN shipped FSK2a emitter is silent too.** `hidprox.c` has been here all along with a modulator structurally identical to ours: counter_top 8 and 10, duty top/2, several entries per bit, length computed per call. Emulating H10301 the Flipper reads 0 of 6, Gallagher on the same slot 4 of 4 (C245). ⇒ FSK2a emulation does not work on this device for the shipped protocol either, which is why nine hypotheses about our emitter all died — there was nothing wrong with it. ⚠ Nobody had tested it: every emulate arm ever graded here is PSK1 or ASK | 10 reads, control passed |
| L209 | 02:20 | pending | ✅ **Challenged C242's method and it survived — held levels are silent whichever idiom expresses them.** `jablotron.c` and `pac.c` both hold levels with compare = counter_top + 1, citing the nRF52840 PS and warning that exactly counter_top leaves a 1-tick glitch. Our GProxII emitter and C242's probe both used `counter_top`, so the probe had really shown the GLITCHY form is silent. Re-run correctly: em410x still 0/4 held, 4/4 restored (C244). ✅ GProxII's emitter is fixed regardless — a documented glitch on every held-high bit is a defect either way — and it still reads 0/6 | A/B/A, 14 reads |
| L208 | 01:55 | pending | ⭐ **§9d: the five-PR split at FILE level, so it can be executed rather than re-derived.** 61 files change; the table says which PR owns each, and names the ten that EVERY PR touches and must be split by hunk — `app_cmd.c`, `data_cmd.h`, `lf_reader_main`, `lf_indala_data`, `tag_base_type.h`, `tag_emulation.c`, the Makefile and all three Python files. ⛔ **Two emitters are marked DO NOT SHIP**: AWID's is silent on hardware with nine explanations eliminated (C243), and GProxII's cannot work at all because a biphase 0 is a held level (C242). A reviewer handed a silent emitter has no way to know it is silent | compute only |
| L207 | 01:30 | pending | ⛔ **The tone value is not AWID's problem either — 8 and 12 is as silent as 8 and 10.** Both straddle the Flipper demodulator's own threshold of 72 inside its [44,100) window, with the pulse counts unchanged, so 8/12 is a valid AWID emission by its own rules and differs from ours only in the long tone's counter_top. 0 of 4 (C243). ⭐ Ninth hypothesis down. ✅ Restored and checked two ways: make check green from a clean rebuild, and Gallagher 4/4 on the same slot afterwards | 4 reads + control |
| L206 | 01:05 | pending | ⭐⭐⭐ **A held-level PWM entry emits NOTHING — proved on a working shipped emitter by changing two entries, and restored.** em410x reads 4/4 at counter_top 64 with all squares (the isolating control: size is fine). Make two of its 64 entries a HELD level and it reads 0/4; restore them and it reads 4/4 (C242). ⭐⭐ That explains GProxII completely — its 0-bits are held levels, so half the frame is silence — and it means biphase cannot be emulated this way AT ALL, since a biphase 0 is by definition a held level. ⛔ It does NOT explain AWID, whose entries are all squares | A/B/A, 12 Flipper reads |
| L205 | 00:40 | pending | ✅ **All five working emulate arms are regression-clean on the current build.** Since they were graded, the shared sweep function was renamed, `lf_sampled_read_phases` changed signature four times, two tag types landed, a biphase decoder landed and rig A was reflashed a dozen times — so this was worth running rather than assuming. Keri 4/4, NexWatch 4/4, Securakey 4/4, Noralsy 4/4, Gallagher 6/6, each with its opposite-modulation arm at 0 as the control (C241) | 22 Flipper reads |
| L204 | 00:15 | pending | ✅ **The emulation engine is eliminated — it reports the two silent tag types as transmitting, identically to the one that works.** `hw emudebug` for AWID, GProxII and Gallagher: sense ENABLE, 125kHz base clock, pwm sequence present, 210 playbacks started, and burst lengths of 14, 11 and 21 frames that all come to ≈520ms — so the variable-length FSK sequence and the biphase one are both handled correctly (C240). ⇒ sequence loaded, clock right, burst right, peripheral told to play — and the Flipper hears one of the three. Seventh hypothesis down | 3 tag types, one slot |
| L203 | 23:50 | pending | ⭐⭐ **Our AWID emitter now matches the FLIPPER'S OWN emission duty exactly — and the Flipper still reads it 0 of 6.** Measured off the committed real-tag captures: a real AWID tag runs HIGH 3 or 4 and LOW 5 or 6; the Flipper's emission runs HIGH fixed at 4. They differ, and we match the Flipper (C239). ⇒ duty is dead as a cause, harder than before. ⚠ C141's slicer duty bias means the absolute numbers are suspect, but both were measured through the same slicer so the DIFFERENCE stands. ⭐ Noralsy 16/16 and Securakey 19/21 are symmetric, matching their emitters, and both read 6/6 — the positive controls | 4 capture sets |
| L202 | 23:25 | pending | ⚠⚠ **Tried to build GProxII the same shape pin AWID got, and the measurement cannot support one.** A Flipper GProxII emission (correct frame this time, 3/3 exact) and a real GProxII tag both slice to a short fixed mark with a variable gap — which looks nothing like our emitter's symmetric held level, and is NOT evidence against it: an AC-coupled front end with a ~27-sample time constant turns a held level into exactly that (C238). The two predict the same slicer output. ⇒ a pin here would encode a receiver artefact as a transmitter requirement. ⭐ Three reference captures committed anyway — they establish that a real tag and the Flipper's emulation are indistinguishable at this level | 3 captures, 2 emissions compared |
| L201 | 23:00 | pending | ⭐⭐ **Pinned the measured emission duty, and the break shows the round trip could never have caught it.** Reverting AWID's mark from a fixed 4 back to `counter_top / 2` passes the round-trip arm with "decode exact" and fails the new one — our decoder sums high and low into a period and cannot see the split (C237). ✅ The failure line reports what it found, "mark reaches 5, want 4", rather than restating the intention | 1 arm, 1 break, make check green |
| L200 | 22:35 | pending | ✅ **The committed corpus now has REAL-TAG captures for the FSK family, not just emulations.** Rule 18 was satisfied on paper — every FSK reader had a capture — but all of them came from a Flipper emulation, the very thing C179 distinguishes from tag behaviour and that the ARMS were upgraded away from two ticks ago. The corpus had not followed. Nine captures added, three each of AWID, Paradox and Pyramid, every credential new to this bench and every frame byte-exact (C236). ⛔ FDX-A gets none and cannot — no pm3 clone exists, so an emulation is its only possible capture | 9 captures, nulls blank |
| L199 | 22:10 | pending | ⭐⭐⭐ **The last five read arms upgraded from an emulation to a real tag — 25 reads, all byte-exact, four of five on a credential new to this bench.** NexWatch, Keri, Gallagher, Noralsy and Securakey were still graded against a Flipper emulation, which is one decoder's rendering of a protocol rather than tag behaviour (C179). Now against T5577s the Proxmark wrote (C235). ✅ With C201 that completes it: every read arm that CAN be tested against a real tag has been. FDX-A and InstaFob cannot, and the grid says so | 25 reads, 5 protocols, 5 fresh tags |
| L198 | 21:45 | pending | ⭐⭐⭐ **Keri's rotation verified end to end on hardware — the one that cost a confident wrong credential 6 of 6.** Our writer, given a CHANGED internal id on a wiped tag with the null confirmed, produces blocks byte-identical to the Proxmark's own clone, and the Proxmark reads them back 4/4 (C234). Pinned twice now: on the host with a deliberate break, and on hardware against a reference. NexWatch the same. ✅ All ten write arms are now at one standard — wiped tag, confirmed null, independent reader, and a changed credential for nine of the ten | 8 pm3 reads, 2 wipes |
| L197 | 21:20 | pending | ⭐⭐ **The three ASK write arms are re-verified to the standard the newer ones set.** Gallagher, Securakey and Noralsy were graded 3/3 before wiping the tag and changing the credential became routine. Re-run: 4/4 each, every one on a tag wiped immediately before with the protocol's own reader confirmed blank (C233). ⚠ Securakey's credential could not be changed — its clone command takes only a raw frame and nothing here encodes one — so its evidence rests on the wipe alone, which the note says | 12 pm3 reads, 3 wipes |
| L196 | 20:55 | pending | ✅ **Every T5577 writer in the tree now has host coverage — ten arms.** The last two needed a measurement, not a search: nobody had ever recorded Securakey's or Noralsy's block forms, so two Proxmark clones produced them and they are now in `t55xx.h` beside config words that used to cite a dump nobody could see (C232). ⭐ Both are straight transcriptions — which is cheap, and is exactly what had to be checked rather than assumed, because Keri's is not | 2 clones, 10 arms |
| L195 | 20:30 | pending | ⭐⭐ **T5577 writer coverage goes from three arms to eight, and Keri's three-bit rotation is finally pinned on the host.** C202 flagged six writers with no test; five had their reference dumps already in the tree, unused. Added Keri, Gallagher, NexWatch, GProxII and FDX-B (C231). ⭐ Keri is the point: removing its rotation makes the arm fail with a printed diff, which is the C160 bug — a confident wrong credential 6 of 6 — caught in a second on the host. ⚠ Securakey and Noralsy still have none, because their reference dumps were never recorded and inventing the expected blocks from our own writer would test nothing | 8 arms, 1 break |
| L194 | 20:05 | pending | ✅ **Closed an evidence gap I had left open: the biphase family's null in the OTHER direction.** Eleven decoders against the GProxII and FDX-B captures — not one produces a frame (C230). Rule 11 asks for both directions and only one had been run. ⭐ Also renamed `lf_ask_read` → `lf_drive_swept_read`: it is the field-strength sweep, the four FSK2a readers go through it, and a reviewer seeing `lf_fsk2a.c` call an `ask` function would take it for a bug. §9b's naming blocker is closed | 11 decoders × 12 captures, build clean |
| L193 | 19:45 | pending | ⭐⭐⭐ **Both silent emitters are now shown CORRECT by reference to Momentum's own code.** AWID by its demodulator (C220), GProxII by its encoder — which emits two short pulses of 32 for a 1 and one long pulse of 64 for a 0, exactly the waveform both our versions describe (C229). ⇒ the design is not the fault, and five hypotheses are dead. ⛔ What remains is unobservable here: the only reader that can hear rig A's emission is the Flipper, and it is the thing under test. The tag lift is no longer a nice-to-have. ⭐ §1 now opens with the whole state in one paragraph so the next session does not have to reconstruct it | source reading + everything from C216 |
| L192 | 19:35 | pending | ⛔⛔ **Retracting L191/C227 — the input was mine and it was wrong.** Momentum's GProxII encoder reads the full 96-bit frame; I passed the 90-bit decoded form. Both are 12 bytes so nothing complained, and the resulting null looked like a finding for one whole tick. ✅ With the right bytes our decoder reads the Flipper's GProxII emulation **3 of 4 exact** (C228). ⭐ The corrected table is better than the wrong one: every failure is in the SAME direction, ours into the Flipper, and every Flipper emission decodes for us | 4 captures, wrong-input null has 0 preamble hits |
| L191 | 19:10 | pending | ⭐⭐⭐ **Biphase fails in BOTH directions between these two devices.** Our decoder reads real GProxII tags 12/12 and reads the Flipper's GProxII emulation 0 of 4, across four drives — captured the same way the byte-exact AWID reference was, minutes earlier (C227). ⭐ The table is the finding: ASK both ways, FSK2a one way, biphase neither — while a real T5577 satisfies both ends of all three. So it is not simply that our emitter is wrong; whatever differs, it differs symmetrically | 4 captures, 4 drives, AWID control decodes |
| L190 | 18:45 | pending | ⭐⭐ **The instrument was there all along: rig A runs BOTH ways, so the Flipper emulating AWID and Chameleon #1 sniffing gives a reference-grade capture of a WORKING FSK2a emission.** Decoded byte-exact by our own reader, and its structure is not what we emit: HIGH run fixed at 4 samples on 1,207 of 1,668 periods, LOW run 4 or 6 — a fixed mark with the gap carrying the data, not 50% duty (C226). ⛔ Matching it did not fix the read: still 0/6, control 4/4. Fourth hypothesis down. ⭐ Kept anyway — a real tag shorts its coil for a fixed time, and differing from the only working emission here needs a reason. ✅ Capture committed as the reference for future emitter work | 1,668 periods, 6 reads, control after |
| L189 | 18:20 | pending | ⛔ **Tried to get the Proxmark to stand in for the Flipper without hands, and it cannot while the tag is in the sandwich.** With Chameleon #2 emulating Gallagher — chosen because the Flipper reads it 6/6 — the Proxmark reads 0 of 4, and `lf fdxb reader` also returns nothing on a tag it reads perfectly the moment #2 goes back to reader mode (C225). Two sources, one field. ⭐ Recorded so nobody spends another flash cycle on it. ✅ It did rule out the boring causes: the frame reads BACK through `econfig` for AWID as well as Gallagher, so buffer, registration and load are all fine and the data does reach the emulator | 4 + 2 reads, control after |
| L188 | 18:00 | pending | ⛔ **AC coupling ruled out for AWID** — its emission decodes through Momentum's own demodulator at every high-pass time constant from 2000 down to 20 samples (C224). So the mechanism that covers GProxII's held levels does not cover AWID, and the two failures still need two explanations. ⚠ **The model cannot test the GProxII half and the tool now says so in its own source**: it thresholds at zero and its high-pass decays to zero, so a held level survives artificially. Printing that row and reading it as support would have been the easiest mistake available today | 7 time constants, 3 emitters, compute |
| L187 | 17:40 | pending | ⛔⛔ **Entries-per-bit is refuted too.** GProxII rebuilt as ONE entry per bit — 96 instead of 192, nothing else changed — reads the same nothing, control 6/6 (C223). ⭐ Four builds measured now, and one structural property separates the working emitter from all three failures: **every entry Gallagher emits is a 50% square**. GProxII's zero-bits HOLD a level, which cannot modulate a load-switching emulator seen through an AC-coupled front end — the same front end C204 measured at a ~27-sample time constant. ⛔ AWID stays unexplained: all its entries are squares at 64 and 80us, well inside that passband | 24 reads, 4 builds, control on every run |
| L186 | 17:15 | pending | ⛔⛔ **The counter_top hypothesis is dead. Gallagher 6/6, AWID 0/6, GProxII 0/6 — one slot, one session.** GProxII uses counter_top 32, exactly what the working Gallagher uses, and is just as silent (C222). ⭐ What survives is a correlation across nine protocols: every emitter that works stores ONE PWM entry per bit, and both that fail store several. ⚠ The obvious mechanisms do not fit — GProxII's entry rate matches Gallagher's exactly, and Indala224 plays 448 entries and reads 11/11. ⚠ Also flashed and wired GProxII's full emulation plumbing to run this, so the arm is ready the moment the emission question is answered | 18 Flipper reads, 3 protocols |
| L185 | 16:50 | pending | ⭐⭐ **The GProxII biphase emitter round-trips exactly — 192 entries, two per bit — and two deliberate breaks failed to fail it.** Inverting the mid-bit convention is genuinely indistinguishable (the decoder sweeps inversion, as real tags differ in polarity); removing the bit-boundary transition entirely is ALSO exact, which says our decoder uses boundaries only as a threshold reference and would accept a signal that is not biphase at all (C221). ⚠ So this arm's sensitivity is weaker than AWID's and the note says so. ⭐ It uses counter_top 32 against AWID's 8 and 10, which makes it the experiment that localises the AWID defect once rig A is back | host only, 2 breaks |
| L184 | 16:30 | pending | ⭐⭐⭐ **Ran Momentum's OWN demodulator over our emitter's ideal output and it decodes exactly** — 383 bits containing the frame. So what the emitter asks for is not the problem, and by elimination the fault is what the peripheral emits (C220). ⭐ Decided entirely on the host, with the Flipper's CLI wedged and its raw instrument retired: `momdemod.py` transcribes `fsk_demod_feed` and AWID's own constants. ⭐ It corroborates C219 from the other side — a right design whose emission is rejected means the 909 anomalous periods are hardware artefacts | 4 frames, 383 bits, compute only |
| L183 | 16:15 | pending | ⭐⭐⭐ **Read the Flipper's own thresholds out of Momentum's source and they explain the rejection.** AWID's demod accepts [44, 100) and calls a period LONG at or above 72. Our emission's genuine long tone is the 77-79 cluster (613 periods) — but **73-75 carries 909 periods that should not exist**, all of them counted as long, and with 5 pulses per long bit that is enough to corrupt the decode (C219). ⚠ The mechanism — a hybrid period where `counter_top` changes — is a hypothesis; the count fits but nothing has confirmed the peripheral does that. ✅ Also confirmed from source that `rfid read ask` DOES try AWID, closing off the easy explanation | source reading + the C218 histogram |
| L182 | 16:00 | pending | ⚠⚠ **The AWID emitter DOES produce both tones — retracting my own lead from last tick — but the measurement has no control and stays a lead.** 10,784 periods: 7,716 at 62-65 and 1,678 at 73-79, ratio 1.21 against the protocol's 1.25. C217's "single tone" came from 24 pairs of 72,518 and was simply wrong. ⭐ The real asymmetry is that the short tone is TIGHT and the long one is BROAD and 2-8% short, which is a plausible reason for a decoder to reject it (C218). ⛔ The Gallagher control returned ZERO periods twice on a protocol the same rig reads 6/6, so the instrument is uncalibrated. ⛔ `rfid raw_analyze` has now wedged the Flipper twice and is retired | 10,784 periods, no control |
| L181 | 15:45 | pending | ⭐ **U13, a compute unit taken because the Flipper is off USB: §9 recounted, and every number in it was wrong.** Command ids 32 → **46**. "7,500 lines" → **10,242 of code**, and the branch total of 204,959 across 1,654 files is mostly the 36,012 lines of committed CAPTURES plus 4,560 of notes — saying either figure alone would mislead, in opposite directions. Three PRs → **five**, because two families landed since it was written. ⛔ Added the instrumentation that grew while nobody was updating the list: `DATA_CMD_LF_READER_CAPTURE`, the GProxII failure-energy payload and `rdrcap.py` — they earned their place by cracking C211, but a reviewer must not be handed them as features. ⚠ Also flagged the shape question a reviewer will actually ask: `lf_sampled_read_phases()` now takes four override parameters for ONE protocol, and the second protocol in that family needs none of them | compute only |
| L180 | 15:15 | pending | ⛔ **The AWID emulation is silent to the Flipper, 0 of 6 — and the positive control passes 6 of 6 on the same slot minutes later.** Switching Chameleon #1 from AWID to Gallagher makes the Flipper read it perfectly, so the rig, the coupling and the emulation path all work and the FSK emission specifically does not (C217). ⭐ The control is what makes this a finding rather than a shrug — `flipper.py` flagged the bare null as uninterpretable before it was run. ⚠ It does not refute the round trip: what the PWM is ASKED to emit decodes exactly. Whether the peripheral emits it is untested, and this is the first emitter to use counter_top 8 and 10 where every other uses 32, 40 or 64. ⛔ Nothing here can capture rig A's emission — both Chameleons are on separate rigs | 6 reads + 6 control reads |
| L179 | 14:45 | pending | ⭐⭐ **U11 opens: the first FSK2a emitter, and it round-trips exactly on the host.** One PWM entry per TONE PERIOD rather than per bit — six entries of counter_top 8 for a 0, five of 10 for a 1 — which also makes it the first emitter here whose entry count depends on the data (543 for this frame). ⚠ 48 against 50 carrier cycles: the two symbols are not the same length, and anything assuming a constant bit period on this family is wrong by 4% (C216). ✅ Sensitivity proven by swapping the pulse counts: 513 entries, NO DECODE. ⚠ NOT yet on hardware — the tag-type plumbing and the Flipper check come next | host round trip + a deliberate break |
| L178 | 14:15 | pending | ⭐⭐⭐ **FDX-B complete on hardware both ways — read 6 of 6 on device, write 4 of 4 by the Proxmark with CRC ok and the block dump identical to its own clone (C215).** ⭐ And it needed none of GProxII's overrides: stock drive where GProxII needs 7, and four back-to-back captures with no gap where GProxII's second is already wrong. That is the second protocol of a family doing its job — telling you which of the first one's workarounds were the protocol and which were the family | 6 device reads, 4 pm3 reads, 2 credentials |
| L177 | 13:50 | pending | ⭐⭐ **FDX-B decodes 4 of 4 on the host and its nulls are completely blank — 0 of 386.** The biphase decoder carried it as a format entry, which is the first evidence the family is a family. ⛔ Its structural gate is worthless alone: ten zeros, a one, then 117 ONES passes every control bit, and a drive-7 capture returned exactly that. ⭐ The CRC-16 parameters were SOLVED against the bench frame rather than copied — the Proxmark's own call reads as unreflected input and does not reproduce the tag's value; KERMIT does (C214). ⚠ Drive 4, not GProxII's 7. ⚠ Capture length is the first here where longer is WORSE: 14336 gives 3 of 4, 8704-12288 gives 4 of 4 | 4 captures, 4 drives, 13 truncation lengths, 386 nulls |
| L176 | 13:25 | pending | ⭐⭐⭐ **FIXED: a 50ms field-off gap between captures, and `lf gproxii read` goes from 3 of 10 to 12 of 12 exact with 0 wrong.** Measured threshold ~25ms and soft near it (35ms came back one bit out); 50 is 2x. That is the one thing that worked after settle, threshold shape, drive, phase order, budget and try count were each tried and refuted — six hypotheses, each killed by its own measurement (C213). ✅ Cross-protocol nulls re-run on the shipping decoder and clean: 0 of 320 front captures, 0 of 5 empty, 0 across nine protocols, NexWatch 1/4 unchanged and still reported. ⚠ Only GProxII pays the latency; every other reader passes 0 | 12 device reads, 10 probe runs, 9 gap values, 386 null captures |
| L175 | 13:00 | pending | ⭐⭐ **The degraded capture is a CLIPPED one: min 0 and max 16380 on a 14-bit ADC, against a clean capture's 472 and 14176, with every percentile in between identical.** Not a level shift, not a gain change — the peaks are cut off, and this decoder thresholds on step MAGNITUDE, which is the one thing clipping destroys (C212). ⛔ Three fixes tried and refuted by measurement: settle time (6 values, 0-100ms, all still degraded), a median threshold (broke the clean captures), a local threshold from the bracketing boundaries (2 of 7 against 7 of 7). ⚠ `capture_end` does stop the field, so whatever holds charge holds it across that | 6 settle values, 2 threshold variants, 11 captures |
| L174 | 12:35 | pending | ⭐⭐⭐ **Reproduced it deterministically in one command: the SECOND back-to-back capture is degraded and every one after it.** Same drive, same phase, same tag — repeats=1 decodes exactly at edge 2266, repeats=2 returns a WRONG frame at edge 5033, and 4, 8 and 16 hold at ~5030. That 5030 is the ~5047 every failing device read reports (C211). ⛔ Retires both earlier explanations at once: the drive is re-asserted per capture here and the phase is pinned, and it degrades anyway. ⚠ The two-agreeing-captures rule turns it from lossy into fatal — capture 1 is clean and 2..N agree with each other on something wrong | 5 probe runs, 1/2/4/8/16 repeats |
| L173 | 12:10 | pending | ⭐⭐⭐ **Built the instrument C209 said was needed, and it answered in three captures: the reader path is FINE at drive 7 and produces exactly the observed failure at drive 4.** `DATA_CMD_LF_READER_CAPTURE` + `rdrcap.py` run the reader's own capture and hand back the samples undecoded — the measurement `lf sniff` cannot make, because sniff is the other path. Drive 7 decodes exactly (edge 2178 and 2944); drive 4 gives no frame at edge 7514, and the failing reads report ~5070. ⇒ There is no 1.8x mystery: it is the same path at the wrong field strength, and the drive we set once per read is not reaching every capture (C210) | 3 probe captures, 2 drives, 2 phases |
| L172 | 11:45 | pending | ⛔ **C208's explanation is refuted by its own null: doubling the read budget to reach five sample phases recovered 0 of 12, where the shipped 3s configuration gets 3 of 10.** Time is out. What remains is a measured 1.8x: the reader path reports a mean boundary step of ~5070 on every failing read where a `lf sniff` capture of the same tag, taken by the same device in the same minute, reports 2819 and decodes exactly on the host (C209). ⭐ Everything else is ruled out by measurement — phase, settle, gain, sample width, splicing, stale binaries, and the bench. Both paths share `capture_begin` and the same setters. ⚠ Three tuning configurations tried and TABULATED in the source so the next person does not repeat them. The read arm stays ungraded; the write arm is untouched and verified | 34 device reads, 27 host captures |
| L171 | 11:15 | pending | ⭐⭐⭐ **Found it: GProxII decodes at SAADC phase 112, SEVENTH of eight in the rotation, so the 3s budget never gets there.** At 8s the device returns the frame EXACTLY (C208). Every C206 symptom follows — the timeouts, the marginal early-phase decodes, and why the host was always exact (one phase, unlimited time). ⭐ Found by instrumenting the failure path to report the decoder's energy, which separates "bad capture" from "phase never reached" in a single read. ⚠ The rotation's own comment called this: it was measured on ONE Indala tag and 112 is in it as spread insurance, not rank | 5 device reads at an 8s budget, 2 reporting energy |
| L170 | 10:50 | pending | ⭐⭐ **GProxII WRITE verified — our write, the Proxmark's read, 4 of 4, blocks identical to its own clone — while the READ arm stays ungraded.** A clean separation: the write arm never depended on our reader. ⛔ Our reader returns 0 of 6 on that same tag where the host decodes sniff captures of it 3 of 3 exact, so the read failure is credential-dependent and total, not marginal (C207). ✅ The spliced-capture discard did not regress the working arms: AWID still 5 of 5 | 4 pm3 reads, 6 device reads, 3 host captures, 5 AWID regression reads |
| L169 | 10:30 | pending | ⛔ **GProxII reads on device and is WRONG BY ONE BIT EVERY TIME — not graded, and two real bugs found on the way.** (1) The ASK drive sweep starts at stock and stops at the first success, so it took drive 4's erroneous decode over drive 7's exact one; GProxII is pinned to 7. (2) `raw_read_samples` reported SUCCESS on a capture that dropped ring samples — a spliced buffer fills the count perfectly and a decoder resynchronises after the gap and returns a wrong tail. The shared engine now discards those, which is a fix for every reader, not just this one. ⚠ Wrong-card reads 1 of 8 → 0 of 8, but the bit-93 error survives and n=8 proves nothing about a 12% rate (C206). ⭐ Also fixed: `ctest`'s binaries did not depend on the decoder HEADERS, the same false-pass shape the clean-first rule was added for | 8 device reads, 19 host captures, 3 parameters swept |
| L168 | 10:45 | pending | ⭐⭐⭐ **`lf_ask_biphase.c` — the firmware decoder, 4/4 exact on the real tag, and the cross-protocol null earned its keep again.** Gated on what both references check (6-bit preamble + 18 spacers) it claimed GProxII on Securakey 4/4, Noralsy 4/4, Gallagher 4/8, InstaFob 2/3 — and Securakey's false frame was IDENTICAL across captures, so two-agreeing-stacks would have confirmed it. Adding the format's own length field (26 or 36, descrambled through the tag's own XOR key) takes 11 false positives to 1 (C205). ⛔ Capture length MEASURED by truncation and the guess was wrong a fourth time: 12288 gives 1 of 4, the threshold is 12800, and 12800 = 200 bit slots is exactly the 196 the frame needs at bit position 100 plus the slope windows | 4 tag captures + 386 null captures; truncation sweep at 9 lengths |
| L167 | 10:15 | pending | ⭐⭐ **U9 opens: GProxII decodes on the host 96/96, and it took a FOURTH decode path.** The ASK level path reads nothing at RF/64 and the reason is measured rather than guessed — the front end's AC coupling has a ~27-sample time constant, shorter than an RF/64 half-bit, so the level has decayed away before you can sample it (C204). ⭐ Biphase supplies its own escape: a transition at every bit boundary is a built-in amplitude reference, so the decoder asks one question per bit — did the middle step too? ⭐ Scored by bit errors on a known credential: drive 7 96/96, drive 6 95/96, drive 4 84/96, drive 2 (stock, clipping) 83/96. `bidemod.py` + four captures committed | 4 captures, 1 credential, bit-error scored; level path blank across 384 settings |
| L166 | 09:40 | `363ce59` | ⭐⭐ **The FSK write arms, built on measured constants and pinned by the first host test any T5577 writer in this tree has had.** Three config words and three block forms measured off real Proxmark clones (C202), then `fsk2a_t55xx_blocks()` — one transcription shared by all three, which the measurement is what licenses. ⭐ It lives in `protocols/fsk2a_t55xx.c` rather than beside its callers **so that it host-compiles**: `ctest/roundtrip.c` now pins all three block vectors, and a deliberately injected 3-bit rotation — Keri's exact failure — fails all three arms with a printed diff. ⛔ **No FDX-A writer, and that is a refusal rather than a deferral**: `lf fdx` is FDX-B, so nothing here could read an FDX-A tag we wrote and the arm would certify itself (C185) | host: `make check` green, sensitivity proven by a break; then **13 Proxmark reads of our own writes, 3 wiped tags, all three block dumps identical to the Proxmark's own clones (C203)** |
| L165 | 09:45 | `45d564d` | ⭐⭐ **Three FSK read arms upgraded from a Flipper emulation to a REAL TAG — 13 reads, all byte-exact.** With the tag back in rig B the Proxmark wrote each credential and our reader read the tag: AWID 5/5, Paradox 4/4, Pyramid 4/4, every frame identical to the Proxmark's own raw (C201). ⭐ **That is the operator's C179 distinction applied to the READ side** — those arms were verified against one decoder's rendering of the protocol, and a T5577 is actual tag behaviour. The two agree exactly, which also retires any worry that the Flipper's emulation differed from a real tag for these formats. ⛔ **FDX-A cannot be upgraded** — the Proxmark has no FDX-A clone (`lf fdx` is FDX-B, a different protocol) — so it stays emulation-verified alongside InstaFob, and the grid says so. ⭐ Cost: zero new code. The whole upgrade was pm3 clone + our read, three times | 13 device reads + 3 written tags |
