# Findings — current knowledge

⭐ **This file holds no history.** It is rewritten freely whenever understanding changes, and
it states only what is believed **now**. What was believed before, and when, is `LOG.md`.
If a claim is not in the ledger below, it is not established.

---

## ⛔⛔ PLACEMENT: READ THIS BEFORE TRUSTING ANY NUMBER BELOW

**Every measurement in this project up to 2026-09-11 was taken with the tag on the BACK of
the Chameleon Ultra. The reading side is the FRONT, where the buttons are. It is worth
21x.**

That is not a caveat, it is the dominant term. The whole investigation ran at roughly 1/20
of the available signal — about 26 dB — which is most of the "31.2 dB below the Proxmark"
the project spent days trying to explain. The PSK1/PSK2 decoder bug was one cause of that
figure; the tag being on the wrong side of the device was the other, and larger, one.

⚠ The Flipper Zero reads from its back, which is where the assumption came from. The
Chameleon is the other way round.

| | tag on the BACK | tag on the FRONT |
|---|---|---|
| fc/2 skirt over the empty floor | 1.0–3.7x | **20–34x** |
| single-capture decode rate | 51/160 = 32% | **34/48 = 71%** |
| sample phase 0 (the stock trigger) | 0/5 | **3/3** |
| sample phases 96–120 | 0/5 | **3/3** |
| the weak "white" coil | 0/12, never read | **reads first try** |

⇒ Claims measured on the back are marked **⚠B**. They are not automatically wrong — many
are relative comparisons that survive a common scale factor — but none of them has been
confirmed at the signal level the device actually delivers, and several are now known to be
artefacts. `NEXT.md` §1 is the re-measurement.

---

## The result

**The Chameleon Ultra reads Indala, on the device, as a command** — with the tag on the
**front**, 71% of single captures decode and every sample phase outside 56–88 works,
including the stock phase 0.

```
lf indala read   ->   Indala PSK1
                      Raw: a0000000e6bd0e92
                      Fmt 26 FC: 52 Card: 63612 Parity: 11
```

20 of 20 consecutive reads returned the credential, in 0.41–0.55 s each. The whole
demodulation is integer arithmetic on the nRF52840 — no float, no FFT, 8 KB of buffer.

Offline, the same decoder recovers the credential from 51 of 160 single 300 ms captures,
and the empty field produced a frame **0 times in 160**. No stacking, no folding, no
averaging.

## The decode, end to end

Everything below is integer arithmetic over 4096 samples. No float, no FFT.

```
sample at 125 kHz, carrier-locked, phase in the working window
  -> mix by (-1)^n                    fc/2 is exactly fs/2, so this is the whole mixer:
                                      no oscillator, no phase estimate. DC and slow drift
                                      move UP to fs/2 and fall out.
  -> [1,2,1] notch at fs/2            NOT optional, see C03. And it is a NOTCH, not a
                                      cutoff: its job is to remove what the mix just
                                      moved up there, see C16.
  -> 32-sample boxcar per bit         the optimal filter for a rectangular bit at RF/32
  -> threshold: bit = (integrator > 0)  PSK1 — the phase IS the data, see C02
  -> search preamble64 = 1010 + 28 zeros + 1, normal and inverted
  -> read 64 bits from the preamble position
```

⚠ **Three details are each individually fatal.** Any one of them alone takes the decode
from 51/160 to 0/160 or near it: the PSK1 mapping (C02), the baseband filter (C03), and
**not** discarding the settle window (C04).

⚠ **And a fourth detail is fatal to trusting the answer rather than getting one.** One in
five recovered frames is WRONG (C17). A reader that returns the first frame it decodes
returns a wrong credential about 20% of the time, so the firmware requires two captures to
agree (C18).

## Claims ledger

Every load-bearing claim, with the evidence that supports it. **`indep` is the column this
project learned the hard way**: whether the claim was checked against something that does
not share its own code path. The retracted `+8.2 dB` had n=20 and a null — and no
independent check, which is exactly why it survived for a day.

`null` — was a control run, and what kind. `emptyfield` is the real control here; `white`
noise is a much weaker one against a chain whose floor spans 17x across bands.

### Holds

| id | claim | n | null | indep | ref |
|---|---|---|---|---|---|
| C01 ⚠B | The Chameleon Ultra decodes Indala from one 300 ms capture: 51/160 exact | 160 | emptyfield 0/160 | ✓ Proxmark reads the same tag | L44, L46 |
| C02 | **PSK1: the phase IS the data.** Not differential — differential is `psk1TOpsk2`, the Proxmark's *fallback* | — | — | ✓ `cmdlfindala.c:1259` vs `:1293` | L44 |
| C03 ⚠B | The baseband filter is load-bearing: **51/160** real captures decode with it, **0/160** without | 160 | — | ✓ real captures, not synthetic | L44, L46 |
| C04 ⚠B | Discarding the 400-sample settle window is fatal: **43/160** with the full capture, **0/160** without | 160 | — | ✓ real captures | L44 |
| C05 | The preamble needs an exact search, not a correlator — the template is dominated by a 28-bit constant run and the peak lands a nibble out | — | — | ✓ Proxmark `preambleSearch()` | L44 |
| C06 ⚠B | Working phase window is ticks 4–60; best 12–36. **Stock phase 0 decodes 0/5** | 5/phase | emptyfield | — | L44 |
| C07 ⚠B | Works at the stock 8-bit sample width; the 16-bit path is not required for the read | 1 | — | — | L44 |
| C08 ⚠B | `LF_RSSI`/AIN0 carries no fc/2: tag/empty 1.04x inside a 1.28x scatter, flat to 0.5 dB across 1–62 kHz vs AIN5's 31.6 dB rolloff. Not clipped | 7 | emptyfield | — | L39 |
| C09 ⚠B | …but AIN0 is alive: the tag shifts its DC by +16 counts with **±0 spread over 7 repeats** | 7 | emptyfield | — | L39 |
| C10 ⚠B | The fc/2 noise floor is analog-referred, not ADC-referred — it tracks gain at or above the gain ratio | 7 | emptyfield | — | L22 |
| C11 ⚠B | Captures are frame-locked to field-on: they cross-correlate at lag 0 with one sign; empty captures scatter with mixed signs | 7 | emptyfield | — | L40 |
| C12 ⚠B | Zero-offset cross-capture stacking gives √N: +7.3 dB at N=7, empty floor falling exactly 2.65x | 7 | emptyfield + arithmetic null | — | L40 |
| C13 ⚠B | The frame is visible in the sideband envelope — the 28-zero run as a reproducible null at the 2048-sample period | 7 | emptyfield | — | L40 |
| C14 | The word has **19 ones — odd parity** — so the subcarrier inverts every frame and the true repetition period is 4096 samples, not 2048 | — | — | ✓ arithmetic on the known word | L41 |
| C15 | Stock firmware *had* no PSK demodulator: `reader/lf/*_data.c` were all ASK or FSK, `psk1.c` transmit-only. `lf_indala_psk.c` is the first | — | — | ✓ source | L01, L47 |
| C16 ⚠B | **The filter's job is a NULL AT fs/2, not a low cutoff.** [1,2,1] beats the 12 kHz FFT brick wall 51 vs 43, strictly (McNemar b=0 c=8, p=0.008) — and [1,1,1], which smooths as hard but nulls at fs/3, is the worst of the set at 31/160 | 160 paired | emptyfield 0/160 at every variant | ✓ mechanism control: same smoothing, wrong null | L46 |
| C17 ⚠B | **One recovered frame in five is WRONG.** 24 bad frames from 200 captures across two sessions — and all 24 were DISTINCT, while the truth recurred 77 times | 200 | emptyfield: no frame at all, 0/160 | ✓ two sessions, two geometries | L46, L47 |
| C18 ⚠B | Requiring two captures to agree removes them: **0 wrong in 40000** resampling trials on either pool, at a median of 2–3 captures | 2x20000 | ✓ vs need=1 at 20/22% wrong | ✓ replicated on live captures taken after the design was fixed | L47 |
| C19 ⚠B | The Wiegand-26 parity is **not** a sufficient gate: it rejects 13 of 17 bad frames but passed `a0000000b9be47a4`, which is wrong in 20 bits | 200 | — | ✓ a counter-example, not a rate | L47 |
| C20 | The firmware read works: **20/20** consecutive `lf indala read`, 0.41–0.55 s each, integer-only on the nRF52840 | 20 | emptyfield **0/20**, on the device | ✓ agrees with the Proxmark's read of the tag | L47, L48 |
| C26 ⚠B | A second, Proxmark-verified Indala tag reads **0/12** on the Chameleon. Its fc/2 skirt sits at 1.04–1.12x the empty-field floor where the bench tag sits at 1.53–1.61x. ⚠ **Revised: "inaudible" was too strong** — see C28 | 12 phases | ✓ empty-field captures at the *same* phases | ✓ the Proxmark wrote, verified and read the tag back; the Flipper reads both tags | L53, L55 |
| C36 | ⭐⭐ **The reading side is the FRONT and it is worth 21x.** Same coil, same session: 1.0–1.1x the empty floor on the back, 20–34x on the front. The coil that read 0/12 reads first try | 48 front + 32 back | ✓ empty floor unchanged (no tag either way) | ✓ found by an instrument built for something else, then confirmed by a read | L57 |
| C37 | ⭐ **On the front the phase structure is TWO WORKING BANDS, not a window.** Phases 0–56 and 96–124 decode 5/5; 60–92 is dead. 114 of 160 single captures return the truth (71%) and **0 of 160 empty captures produce a frame at all** | 32 phases x 5, tag and empty, shipping C decoder | ✓ empty arm at every phase, same session | ✓ numpy decoder agrees on the band structure (107/160, dead band 60–96) | L57, L58 |
| C32 ⚠B | ⭐ **The two coils differ by 3.3x in the fc/2 band** — copper 3.55–3.67x the empty floor, white 1.04–1.12x — same session, same geometry, same payload, same phases | 8 x 4 phases each | ✓ empty-field reference per phase | ✓ both read on a Flipper; a Proxmark wrote and verified both | L56 |
| C33 ⚠B | ⚠ **Position is worth more than 2x.** The copper coil reads 3.55–3.67x now against 1.53–1.61x in the original sweep — same tag, same reader, different placement. That is larger than the whole working margin | 8 x 4 | ✓ same empty reference | — | L56 |
| C34 ⚠B | Stacking gain is **1.5–2.1x on the copper coil and 1.0x on the white one**, so the white coil's captures are not coherent with each other in the way the copper coil's are | 8 x 4 | ✓ ratio is against a stacked empty, so a deterministic background cancels | — | L56 |
| C35 ⚠B | ⛔ **The lag structure is the same for both tags** — 4 of 7 captures at lag 0, 3 at ≈−47 samples, near-identical for copper and white. So the cross-capture correlation is a property of the CAPTURE PATH, not of either tag's frame | 7 pairs x 2 tags | ✓ the two tags are the control for each other | — | L56 |
| C28 ⚠B | ⭐ **That tag IS heard — the band ratio is simply not sensitive enough to see it.** Stacking 8 captures yields `a0000000e6ad0e92` (1 bit out), `a0000000e4bd0a92` (2), `a0000000e33d0e92` (3) at three separate phases, against a truth of `a0000000e6bd0e92`. Stacked EMPTY captures produce no frame at all, ever | 4 phases x 8 | ✓ emptyfield 0 frames at every stack depth | ✓ the exact 33-bit preamble is what noise never produces (0/160) | L55 |
| C29 ⚠B | ⭐ **Cross-capture stacking works: 31.9% -> 71.9% correct**, over every combination of the committed captures, with **0 empty-field frames at every depth**. It revives sample phase 0 — the stock trigger, 0/5 singly — and phase 64, outside the single-capture window | 160–320 per depth | ✓ emptyfield 0 at N=1..5 | ✓ decoded by the C firmware decoder, not the Python one | L55 |
| C30 ⚠B | ⛔ **…but it does nothing for the weak tag**: bench 1.56x -> 2.71x stacked, white coin 1.08x -> 1.05x. The cross-capture alignment stacking depends on is not holding for that tag | 4 phases x 8 | ✓ same treatment, same phases | — | L55 |
| C31 ⚠B | ⛔ **C11/C12 need re-examining: the EMPTY field correlates better than either tag.** Median lag-0 baseband \|r\| is 0.75–0.83 empty, 0.18–0.49 bench, 0.15–0.22 white — so that correlation is measuring the field turn-on transient, not frame lock | 10–28 pairs x 4 phases | ✓ empty is the control and it *wins*, which is the finding | — | L55 |
| C27 ⚠B | ⇒ **The working margin is tiny: ~1.5x over the floor reads, ~1.1x does not.** There is almost nothing between "works every time" and "never" | 12 | ✓ per-phase empty reference | — | L53 |
| C25 ⚠B | **The RF path does not degrade under heavy LF load.** An HID tag's fc/8+fc/10 amplitude is flat to **1.00x** across idle, sustained load and recovery, with the carrier DC flat to 0.3% | 22 | ✓ idle arms before and after the loaded one | ✓ a continuous measurement, not the binary read whose swings prompted it | L52 |
| C24 ⚠B | **No false positive on a non-Indala tag**: an HID Prox 36-bit tag on the antenna gives `LF tag not found` 10 times in 10, with coupling confirmed by a 5/5 HID read immediately before | 10 | ✓ the loud-signal null, which the empty field does not test | ✓ a second run of 10 by the user, same result | L50 |
| C23 | The decoder is **word-agnostic**: 36 synthetic words all decode, no wrong answers at any amplitude. Odd-parity mean threshold 23.5, even 24.5 — one ladder rung apart | 36 words x 20 seeds | ✓ control = the bench word, generated by the same code | ✓ the generator is written from the physics and validated by the C decoder, which was validated on 320 real captures | L49 |
| C22 | The empty-field failure is genuine **timeout exhaustion**, not an early abort: 0.47–0.53 s of device time against a 500 ms budget, where a success takes 0.08–0.22 s | 5 vs 20 | ✓ the 0.33 s host floor measured separately and subtracted | ✓ timing, independent of the decoder's own verdict | L48 |
| C21 | The C decoder and the numpy decoder agree **word for word on all 320 committed captures**, including the failures | 320 | — | ✓ *this is the independent check* — integer vs float, notch vs FFT, no shared code | L47 |

### Firmware bugs found and fixed

| id | bug | effect | ref |
|---|---|---|---|
| F01 | The 8-bit debug path did `14-bit >> 5` | discarded 5 bits **and distorted the rolloff shape** | L10 |
| F02 | BLE advertising collapsed the LF field during capture | 41% of captures corrupted → 0% | L26 |
| F03 | The DMA ring dropped 75% of every batch (`CIRCULAR_BUFFER_SIZE` 512 vs `ADC_BUF_SIZE` 2048) | captures were islands of 512 samples spanning 64 ms, not 16 ms | L01 |

### Retracted

| claim | why it was wrong | retracted by |
|---|---|---|
| **"the working phase window is ticks 4–60; the stock phase 0 decodes 0/5"** (C06) | ⛔ a placement artefact. With the tag on the FRONT, phase 0 decodes 3/3 and so do phases 96–120. The surviving dead zone is 56–88, and it carries the HIGHEST skirt of the sweep — so it is a genuine polarity null, not weak signal | L57 |
| "the working margin is tiny — ~1.5x over the floor reads, ~1.1x does not" (C27) | ⛔ measured entirely on the back. On the front the same coils sit at 20–34x | L57 |
| "a second Indala coil is inaudible / 3.3x weaker" (C26, C32) | ⛔ both coils were on the back. On the front the weak one reads first try | L57 |
| "tag position is worth ~5.7 dB" | ⛔ understated by a factor of ten. Side of the device alone is 21x, ~26 dB | L57 |
| "fc/2 sits at Nyquist, therefore it cancels" | the Proxmark samples once per carrier cycle too, and reads Indala | L03 |
| "the Indala tag produces no detectable modulation" | measured through the `>>5` truncation | L10 |
| "phase sweep shows 659x" | tracking overrun bursts; small denominator | L15 |
| "31.2 dB below the Proxmark ⇒ not viable" | the **measurement** stands; the conclusion assumed a decoder that could not work | L44 |
| "7.6 dB demodulation gap, confirmed twice" | both routes measured that broken decoder | L44 |
| "detectable but not decodable, 52/64 vs a 45/64 null" | a constant preamble run scoring itself — and the wrong decoder besides | L42, L44 |
| "matched filter worth +8.2 dB over PSKDemod" | measured on a synthetic that shared the decoder's own bug | L44 |
| "the phase is gone before the ADC" | the injected synthetic shared the decoder's convention; it was testing self-consistency | L44 |
| "coherent frame averaging is dead" | the circularity argument was wrong — captures are frame-locked, so there is no alignment step | L40 |
| "folding at 2048 samples improves things" | it averages a frame against its own inverse; only the polarity-blind skirt survives | L41 |
| "settle has no effect" / "air gap is flat" | both measured pre-BLE-fix on a tag carrying `DEADBEEF/12345678` | L34 |
| "the baseband low-pass needs a ~12 kHz cutoff" | the cutoff was incidental. What the filter must do is NULL fs/2, where the mixer just put the carrier ripple; a 3-tap notch does it better and cheaper (C16) | L46 |

### Open / untested

| claim | status |
|---|---|
| Tag position is worth ~5.7 dB | n=1, from an accidental probe. Large, concentrated at high frequency, and plausible — but one capture |
| Settle, air gap, oversampling | closures invalid (L34); never re-measured against a working decoder |
| Does a second **physical** tag read? | ⛔ **NO** — C26, and it is silicon not payload: both tags carry the same word, both read on a Flipper, the thin copper coin reads on the Chameleon and the white coin does not. C28 shows its signal is present but 1.4x weaker, which on a ~1.5x margin is the whole difference |
| Why does stacking not rescue the weak coil? | ⛔ open (C34). Best current story: a weakly-coupled T5577 charges more slowly and more variably, so its frame start jitters between captures and there is nothing to add coherently. ⇒ align on the PREAMBLE the decoder already finds, rather than assuming lag 0 |
| What is the common background? | ⛔ open and it defeats every filter tried (C31, C35). Empty captures correlate at 0.92–0.95 after mixing, band-limiting to 6 kHz, removing DC and dropping 1024 samples. Not the odd/even imbalance (1.7–2.1 counts, 0.1% of the ripple). Until it is identified, no cross-capture correlation on this bench means what it appears to mean |
| Can the Chameleon WRITE a T5577 reliably? | ⛔ not known to work — one of three raw blocks landed, and the proven multi-block path changed nothing. Undiagnosable without a T5577 block read, which the device does not have. `NEXT.md` §7 |
| C38 | ⛔⛔ **THE SKIRT DOES NOT PREDICT DECODE — it is anti-correlated with it over half the sweep.** The fc/2 skirt is a smooth single-peaked curve: **highest at tick 44 (212), which decodes 5/5**, lowest at 116–120 (134), which also decodes 5/5. The dead band 60–92 sits in the MIDDLE of the skirt range (145–200). ⇒ the sweep scripts in NEXT §7 score a quantity that cannot rank what they were used to rank | 32 phases x 5, skirt and decode measured on the same captures | ✓ decode and skirt come from the identical capture set | ✓ predicted by M8 (the skirt is polarity-blind); this is the direct test | L58 |
| C39 | ⛔⛔ **IN THE DEAD BAND THE DECODER RETURNS A DETERMINISTIC WRONG FRAME, WHICH DEFEATS THE TWO-CAPTURE AGREEMENT RULE.** Phase 64 returns `a0000000b5af0b92` on **5 of 5** captures, phase 88 `a0000000c6b90c92` on **5 of 5**, phase 92 `a0000000c6b90e92` on 4 of 5. Two independent captures therefore AGREE on a wrong credential and `indala_read()` returns it. The frames are not aligned to the data, so their weakest bit integrator cancels to near zero — which is what C48's gate rejects. ⚠ Wiegand-26 parity rejects two of the three but **passes `a0000000c6b90e92`**, which reports the CORRECT facility code 52 with a wrong card number | 5 captures at each of 32 phases | ✓ the 22 good phases produce 0 wrong frames in 114 decodes | ✓ mfdemod.py independently produces wrong words at the same phases | L58 |
| C40 | ⭐⭐ **MORE SIGNAL MADE THE FAILURE MODE WORSE, NOT BETTER.** On the back (26 dB down) all 17 wrong frames were DISTINCT — errors were noise-driven, so they scattered, which is exactly what C17/C18 measured and built the acceptance rule on. On the front, 3 wrong words repeat within a phase, two of them 5/5. ⇒ **"wrong words never repeat" is a property of low SNR, not of the decoder**, and the rule it justifies does not cover the strong-signal case | 160 back + 160 front tag captures, same decoder | ✓ same tag, same decoder, same capture length; only placement differs | ✓ the two capture sets were taken weeks apart | L58 |
| C41 | ⚠ **C03 HAS REVERSED SIGN: the fs/2 notch now costs decodes.** Shipping C decoder on the front: notch ON 114 truth / 21 wrong, notch OFF (plain boxcar) **121 truth** / 26 wrong; empty stays 0/160 both ways. On the back it was 43->51 and strictly dominant. The mechanism it was justified by — ~9 counts of carrier ripple leaking into a ~10-count subcarrier — is gone at 20x the subcarrier. ⇒ no longer load-bearing; it now trades ~7 decodes for ~5 fewer wrong frames | 160 front captures, both builds | ✓ same captures, same binary except the 3-tap fold | ✓ numpy decoder reproduces the reversal (107 with lpf, 121 without) | L58 |
| C42 | ⭐ **C04 SURVIVES INTACT AND IT WAS NEVER ABOUT SNR.** Discarding a 400-sample settle window from the capture still takes the decode from 107/160 to **0/160** on the front, unchanged from the back. It is structural: 400 samples removes the first frame's preamble and leaves too few bits after the second. ⇒ the only one of the three "individually fatal" details that is fatal for a reason 26 dB cannot touch | 160 front captures, cut 0 vs cut 400 | ✓ same captures, only the cut differs | ✓ same result as the back-side measurement | L58 |
| C43 | ⚠ **Integrator amplitude separates truth from wrong with no overlap here — but it is not a gate.** Truth 7240–12699, wrong 5120–6833. The gap is real and matches the half-bit mechanism (a straddling integrator recovers ~half the energy), but amplitude scales with coupling, so a weakly-coupled tag would fall under any fixed threshold. ⇒ a diagnostic, not an acceptance test | 135 frames from 160 front captures | ✓ both classes from the same sweep | — | L58 |
| C44 | ⭐⭐ **THE LOUD-SIGNAL NULL PASSES, against a genuinely loud interferer.** An HID Prox tag measured at **88-100x the empty-field floor in its own band** produced `LF tag not found` on **30 of 30** `lf indala read` attempts. The 10-18kHz empty floor was measured for the first time here: **63322, range 56395-69012** (n=63). Tag present: mean 6307327, range 6097398-6515642 (n=26), a clean two-regime split as the tag was lifted mid-run | 30 reads + 90 continuous band measurements | ✓ the empty floor came from the SAME run, same band, same session — the tag leaving is the control | ✓ carrier DC shifts 5875 -> 5446 at the same sample, independently marking the tag's departure | L59 |
| C45 | ⛔ **`lf hid prox read` fails on a 90x signal with the RF path provably flat.** ⭐ **SUPERSEDED IN ITS HEADLINE BY C250, which found the cause: a BLE advertising burst collapsing the field mid-capture.** The observation below stands exactly as measured — the amplitude really is flat across successes and failures — but "the intermittency is the DECODER" was the wrong conclusion to draw from it. The RF path is flat *as sampled between bursts*; the burst is a 1.6 ms hole that no amplitude median taken around it can show. Stress run on the white coin: subcarrier median 6034358 idle, 6023801 under an Indala load, 6020802 recovering — **1.00x throughout** — while reads went 8/8, 7/8, 7/8. The two failures sat at the same amplitude as the successes. ⚠ And the Indala load does NOT degrade HID reads, which is M17/M18 confirmed with a continuous metric rather than a binary one | 24 reads + 24 amplitude samples, 3 phases | ✓ amplitude measured on every single read, same session | ✓ a second tag at 6.31M (within 5% of this one's 6.03M) reads 0/6 — same level, opposite outcome | L59, L60 |
| F05 | ⛔ **A FAILED READ IS NOT EVIDENCE THE TAG IS ABSENT, and NEXT §3 told us to use it that way.** The bracketing rule said to confirm the probe tag's coupling with a read immediately before and after. Followed literally here it would have thrown away a VALID null: all 6 bracketing reads failed while the tag sat at 90x the floor. ⇒ Bracket with `lfprobe.py`, which measures presence directly, not with the very binary metric whose unreliability is why the rule exists | 6 failed bracket reads against 26 amplitude samples | ✓ the amplitude and the reads come from the same minutes | ✓ this is M17/M18's lesson recurring in a new place | L59 |
| C46 | ⭐⭐ **THREE TAGS, BYTE-IDENTICAL MEMORY, THREE DIFFERENT DECODE RATES — so it is the PACKAGE, not the payload, the config or the silicon.** Blue dual, white coin and copper coin all dump page 0 as `00107060 / 1D555A6A / 5595A555 / 95A95969` and page 1 as `00107060 / E0150A4C / 5B020499` — the page-1 factory trace data is supposed to be unique per chip, so these are the same clone silicon from one lot. A Proxmark reads all three perfectly (`raw 000000000000003708c08e26`). The Chameleon reads them **0/6, 3/6 and 7/9** | 3 tags x (PM3 dump + PM3 read + 6-9 Chameleon reads) | ✓ the tags are each other's control — everything except the package is held identical | ✓ the PM3 is a second reader and it does not discriminate between them | L60 |
| C47 | ⚠ **Only `lf_reader_generic.c` suspends BLE advertising, and the readers that duplicate its capture path instead of sharing it are the ones that fail.** The LF readers split into two families: **GPIO/comparator** (em410x, jablotron, viking — 128-entry rings, `register_rio_callback`, no SAADC at all) and **SAADC** (hidprox, ioprox, pac, plus lf_reader_generic). Each SAADC reader rolls its own `saadc_cb` + ring + field start/stop; only `lf_reader_generic.c` also suspends advertising, and its comment records why — a burst collapses the 125kHz field for ~1.6ms and hit **4 captures in 10**. ⇒ Of the three SAADC readers without the guard, **two are the ones measured failing on loud tags** (HID 0/6 to 22/24, PAC 0/5 and 2/5); ioProx is untested. The SAADC reader that HAS the guard is Indala, at 60/60. ⛔ I previously weakened this on `lf em 410x read` scoring 95% — **that was a bad comparison**: EM410x is on the GPIO path and never touches the SAADC, so it was never a test of this at all | static read of 15 files + HID/PAC/EM410x/Indala hardware rates | ⚠ CORRELATIONAL, n=2 failing protocols. The clean test is to move HID onto the shared path and re-measure the blue dual | ✓ the two families make a matched comparison the earlier one lacked | L60, L64, L69 |
| C48 | ⭐⭐⭐ **THE STRADDLE GATE CLOSES C39: on the front the decoder now produces 110 frames and 110 of them are correct.** Reject a frame when it is BOTH loud and ragged — `mean|integ| >= 2048` AND `min|integ| * 8 < mean|integ|`. Over 320 front captures it rejects **21 of 21** straddles and keeps 110 of 114 true frames (**40 of 40** at the phases `PHASE_ROTATION` actually uses), and over 320 back-side captures it changes **nothing** — 68 frames, 51 truth, exactly as before | 640 captures, both placements, shipping C decoder | ✓ the back-side set is the control: the gate must be inert there, and is | ✓ mirrored in compare.py; both decoders agree word-for-word on all 640 again | L61 |
| C49 | ⛔ **SHAPE ALONE IS NOT A GATE, AND THE OBVIOUS GATE IS WORSE THAN USELESS.** `min/mean` separates perfectly on the front (truth 0.36–0.62, straddles 0.001–0.092) and NOT AT ALL on the back, where true frames are also ragged (0.015–0.059): gating on shape alone keeps only 11 of 28 back-side reads at rotation phases. The two populations are an order of magnitude apart in LEVEL instead (front 7240–12700, back 468–936), which is why the gate needs both terms. ⚠ Also falsified: the winner-vs-straddle amplitude ratio, which was the obvious candidate and separates BACKWARDS — 1.30 for a good phase against 1.64 for a wrong one | 135 front + 68 back decoded frames, 6 candidate features | ✓ every feature scored on both placements before being believed | ✓ the back-side set was never used to tune, only to falsify | L61 |
| C50 | ⭐ **The even-parity path decodes identically to the odd one — in simulation.** Every capture in this project carries `a0000000e6bd0e92`, 19 ones, ODD, so the subcarrier inverts every frame (C14) and the non-inverting case had **never once executed**. Synthesised against `a0000000d2e6b438` (18 ones, EVEN) through the shipping C decoder: both are 12/12 down to amp 30 and degrade together below 20 (odd 8/12, even 6/12 at amp 15; identical at 12 and 10). The straddle gate rejects neither | 2 words x 7 amplitudes x 12 seeds, synthetic | ✓ the odd word is the control, same amplitudes, same seeds, same start offsets | ⚠ SYNTHETIC ONLY — `psk1_stream()` is written from the physics and tiles the bits so polarity continues between frames, which is the whole parity effect, but it is still a model | L62 |
| C51 | ⛔ **THE WINNING SAMPLE OFFSET IS THE TAG'S FRAME TIMING, NOT A CORRECTNESS SIGNAL — and the mechanism story attached to C39 was wrong.** On the bench tag the wrong frames won at offsets 16 and 22 and the true ones at 9, 10 and 12, which was written up as "offset 16, exactly half the 32-sample bit period". ⛔ The arithmetic never worked — half a bit from a true alignment of 9 is 25, not 16 — and a SECOND Indala tag decodes **correctly at offset 22** on hardware, 20 times out of 20. ⇒ Nothing may gate on the offset. C48's gate tests SHAPE and never looks at it, which is the only reason this error cost nothing | 2 tags on hardware, 20 reads each, + 320 committed captures | ✓ the two tags differ in payload AND package; both read at phase 20, so the phase is held constant | ✓ caught by a hardware test that was run for an unrelated reason (even parity) | L63 |
| C52 | ⭐⭐ **THE ASK/OOK NULL PASSES, AND IT IS THE BEST-BRACKETED MEASUREMENT IN THE PROJECT.** An EM410x tag (`1122334455`, ASK RF/64) measured at **18.1–18.9x** the empty floor in its own band produced `LF tag not found` on **20 of 20** `lf indala read` attempts. ⭐ Both brackets agree for the first time: the amplitude probe says the tag is loudly present AND `lf em 410x read` returns the right ID — where C44's HID null had only the probe, because that decoder was broken | 20 nulls + 5 amplitude samples + a confirming read | ✓ floor measured in the SAME band, same session, pad clear (440000); tag 8.0M | ✓ two independent instruments confirm presence, and the Proxmark wrote and verified the tag | L64 |
| C53 | ⛔ **THE BLUE DUAL IS NOT A BAD TAG — the Chameleon reads EM410x off it first try, 18 of 20.** The same package that has NEVER produced a single successful `lf hid prox read` (0/6, C46) decodes ASK/OOK immediately. ⇒ "the blue dual does not couple well enough" is excluded as an explanation, and §3b narrows from the package to the FSK path. ⚠ Not a clean isolation — the HID config was FSK2a RF/50 and this is ASK RF/64, so modulation and bit rate moved with the package's fortunes | 20 EM410x reads vs 6 HID reads on the same physical tag | ✓ same tag, same reader hardware, same session | ✓ the Proxmark reads both configurations of it perfectly | L64 |
| C54 | ⭐⭐ **IDTECK IS THE ADVERSARIAL NULL AND THE DECODER PASSES IT IN SIMULATION — 0 frames from 90 captures.** IDTECK is a 64-bit **PSK1 frame at RF/32** whose T5577 config word is *identical* to Indala's (`RF_32 | PSK1 | PSKCF_RF_2 | 2 blocks`); only the 64 bits differ, and its 32-bit preamble is `0x4944544B` ("IDTK"). ⇒ Every sample offset carries real PSK1 data at exactly the right bit rate, which is the hardest possible test of an exact 33-bit preamble search — unlike HID (FSK) and EM410x (ASK), where the mixer and integrator see a completely different signal | 6 card numbers x 15 seeds, synthetic | ✓ control: a real Indala frame at the same amplitude decodes 15/15 | ⚠ SYNTHETIC — the hardware arm is NEXT §3 |L65|
| C55 | ⭐⭐ **Every loud-signal null passes, and the IDTECK arm is now properly bracketed.** Viking, PAC and Jablotron: 40/40 each across two units, bracketed by amplitude in a band covering their envelope modulation (16–19x). **IDTECK re-tested** after C84 exposed both its legs as broken: bracketed at **22.65x the empty floor in the fc/2 band (60–65 kHz)** — the band a PSK1 subcarrier actually occupies — and **20/20 not found, 0 frames**. ⇒ The adversarial case holds: valid PSK1 at RF/32 carrying Indala's exact T5577 config word, rejected with the tag at 22.6x | 4 interferers; IDTECK re-run bracketed | ✓ the fc/2 empty floor (6350–7900) is independently established from committed captures | ✓ the phantom-reader leg now correctly reports that it contributes nothing | L66, L81, L82 |
| C56 | ⭐ **A second Chameleon reads 20/20 at the SAME sample phase.** Same firmware, same copper coin: phase 20, one capture each, no gate rejections — only the bit offset moves, 9 -> 10, which is C51's timing again and now shown to depend on the READER as well as the tag. ⚠ §4's own caveat stands: two units bought together are the same revision from the same batch, so this tests unit-to-unit tolerance and NOT whether the design generalises. A pass is weak evidence; the failure would have been strong | 20 reads on a second unit | ✓ identical firmware image, identical tag, same session | ⚠ same batch — this is the weak direction of the test | L66 |
| C57 | ⛔ **RETRACTED IN PART — and the half I called "the strong one" was a phantom.** The band criticism stands: `nulltest.py` defaults to 500–20000 Hz, which misses a PSK1 subcarrier at fc/2 = 62500 Hz, so IDTECK's 5.5–6.3x was leakage where the ASK tags scored 16–19x. ⛔ But the conclusion drawn from it — that the interferer's own reader was the stronger bracket — is FALSE: `lf idteck read` does not exist and never did. Both legs of that null were broken, not one | 4 interferers x 2 units | ✓ the ASK tags in the same band are the contrast | ⛔ the "strong" leg was CLI help text | L66, L81 |
| C58 | ⭐⭐ **STACKING GAINS NOTHING ON THE FRONT AND MAKES THE WRONG-CREDENTIAL MODE WORSE.** Front decode rate is **68.75% at every depth N=1..5** — exactly 22 good phases out of 32, because a good phase decodes from one capture and a dead one never decodes at all. ⛔ And in the dead band stacking *reinforces* the straddle: with the gate disabled the wrong frames CONVERGE as N rises — 9 distinct words at N=1, 4 at N=4 where **all 20 wrong frames are repeats**. More stacking defeats the two-capture agreement rule more thoroughly. With the gate: **0 wrong at every depth**. On the back stacking is genuinely worth it (32% -> 72%, unchanged, the gate being inert there) ⇒ it earns its place only for the WRONG placement | every combination of 5 captures x 32 phases x 2 placements, gated and ungated builds | ✓ empty field gives 0 frames at every depth, both placements | ✓ the ungated build is the control showing what the gate actually catches | L67 |
| C59 | ⭐⭐ **FRAME LOCK IS REAL — BUT THE CORRELATION CITED FOR IT PROVES NOTHING.** Lag-0 baseband \|r\| is **0.999 tag / 0.994 empty** on the front and **0.912 tag / 0.941 empty** on the back: the EMPTY FIELD correlates as well or better, so that number measures the capture path, exactly as C31 suspected. ⭐ The operational test settles it instead — stacking 4 back-side captures as captured gives **67%**, and rolling each by a random offset first gives **0%**. Stacking cannot work unless the captures are genuinely aligned, and it does work. ⇒ C11–C13's CONCLUSION stands and their EVIDENCE is retracted | 640 correlation pairs + 160 stacks each way | ✓ empty field as the control in part 1, the rolled stack in part 2 | ✓ two independent methods disagreeing is what exposed it | L67 |
| C60 | ⭐⭐⭐ **THE T5577 WRITER WORKS — 9 of 9 verified writes — AND §8's FAILURES WERE THE PLACEMENT ERROR.** Writes ALTERNATED between two words, which is what makes each result mean something: writing the same word twice would read back as VERIFIED without anything having happened, so every one of these proves a state change. ⇒ The question §8 could never separate — *is the writer broken, or was the tag never coupled well enough to be written* — resolves as **coupling**. The original failures (1 of 3 blocks landing) all predate L57, so they were taken with the tag on the BACK, and writing needs more field than reading: it was the worst case of the worst placement | 9 writes, alternating 2 words | ✓ alternation is the control — a repeated word would verify trivially | ✓ `lf indala read` independently reads the written credential back afterwards | L68 |
| C61 | ⚠ **The winning bit offset does not depend on the payload.** The same physical tag read at offset 22 carrying `a0000000d2e6b438` and again at offset 22 carrying `a0000000892f902b`. Supports C51 — the offset is frame TIMING (tag plus reader), not data | 2 payloads on one tag | ✓ only the payload changed between the two reads | ⚠ n=1 tag; the copper/white difference (9/10 vs 22) is the stronger evidence | L68 |
| C62 | ⭐ **Two thirds of the Indala reader's RAM buys nothing at the correct placement.** It holds **48KB static**: 8KB samples, 8KB scratch, and **32KB of stacking accumulators** (two int32[4096]). C58 measured stacking at exactly 68.75% for N=1..5 on the front — no gain at any depth — and 32%→72% on the back. ⇒ 32KB serves only the wrong placement. ⚠ Not a recommendation to delete it: that is a product decision about whether back-side reads matter, and the numbers are here so it can be made rather than drifted into | measured from the declarations + C58 | ✓ C58's stacking curve is the evidence | — | L69 |
| C63 | ⛔ **RETRACTED — see C68.** Claimed Indala emulation transmitted with its phase destroyed (102-sample constant-phase run against 928 needed). Measured Chameleon-to-Chameleon. On a Proxmark, in a geometry proven by a positive control, the same emulator gives **931 samples** and decodes | — | — | — | L71, L73 |
| C64 ⚠ | ⚠ **PROVISIONAL — measured on the instrument C68 discredits.** Claimed emulation couples only from the front (skirt 49236 vs 5049). The front/back ratio may well hold, but it was measured Chameleon-to-Chameleon and has not been redone on the Proxmark | 2 orientations + empty | — | — | L71, L73 |
| C65 | ⛔ **A frequency measurement at Nyquist is not trustworthy, including the one I just made.** Peak near fc/2: real tag -61 Hz from 62500, emulator -671 Hz — which looks like a 1% frequency offset until the EMPTY field is measured and comes out at **-793 Hz**. An empty field has no subcarrier, so that number is the estimator, not the signal: near the Nyquist edge, windowing and noise dominate. ⇒ The offset is NOT established. The phase-run measure (C63) is the one that holds, because it does not depend on resolving a frequency at the band edge | 3 captures | ✓ the empty field is the control and it falsifies the estimator | — | L71 |
| C66 | ⛔⛔ **RETRACTED, AND IT WAS THE MOST EMPHATIC CLAIM IN THE LEDGER — see C68.** Claimed "PSK1 tag emulation has never worked on this device" because a real IDTECK tag gave a 1042-sample run and the emulated one 100. Both arms were Chameleon-to-Chameleon, and the REAL tag sat on the antenna while the EMULATOR was a second device at an unproven distance — so the control matched the instrument but NOT the geometry | — | — | — | L72, L73 |
| C67 | ⛔ **RETRACTED — see C68.** The ~640 Hz mixed-baseband beat was a property of the Chameleon-to-Chameleon setup, not of the emulator. A real drift does exist and is far smaller: over 180 ms of Proxmark capture the winning bit offset slides 12 -> 8 and the polarity flag flips, which is ~180 ppm and decodes fine within a 4096-sample window | — | — | — | L72, L73 |
| C68 | ⭐⭐⭐ **INDALA TAG EMULATION WORKS — the decoder reads the emulated frame off the air, 10 windows of 12.** Proxmark capture of the emulating Chameleon, placed exactly where a real tag had just been proven to couple: constant-phase run **931 samples against the 928 the frame demands**, and `cdemod` recovers `a0000000e6bd0e92` in 10 of 12 independent 4096-sample windows (real-tag control on the same conversion: 12/12). ⚠ It is WEAKER than a real tag — skirt 78782 vs 415835, integrator amp ~11000 vs ~55900, about 5x — and the Proxmark's own `lf indala reader` does NOT lock onto it. So emulation is correct but thin, which is a margin problem and not a correctness one | 12 windows + 12 control windows, 1 capture each | ✓ the real tag is the positive control for BOTH the placement and the trace conversion | ✓ independent hardware — the Proxmark shares nothing with the Chameleon | L73 |
| C69 | ⛔⛔ **A POSITIVE CONTROL MUST MATCH THE GEOMETRY, NOT JUST THE INSTRUMENT — this is what made C63/C66/C67 wrong.** The retracted work compared an emulating Chameleon against committed captures of a real tag. Same reader, same analysis, same sample rate — and a real tag lies flat on the antenna while a second Chameleon sits at some unproven distance and orientation. The emulator was simply coupling badly, and every downstream number described that rather than the emulator. ⇒ The control has to be taken in the SAME session, the SAME position, with the only change being what is under test | 4 retracted claims | ✓ the Proxmark run did it correctly and immediately reversed the conclusion | — | L73 |
| C70 | ⭐⭐ **The emulation is decodable by an INDEPENDENT conventional demodulator, up to ~163 ms of capture.** Proxmark's own `lf indala demod`, run offline on a saved trace of the emulating Chameleon: **131 ms ✓, 163 ms ✓, 196 ms ✗, 290 ms ✗**. The real-tag control decodes at every length. ⇒ Emulation is not broken; it drifts. `lf indala reader` fails because it demods the whole 290 ms buffer at once | 6 lengths, emulator + control | ✓ real tag decodes at all lengths through the same code path | ✓ the Proxmark's demodulator shares nothing with ours, which reads the same trace 10 windows in 12 | L74 |
| C71 | ⭐ **A real tag's frame period is EXACTLY 2048.000 samples with zero slip; the emulator's is not.** Autocorrelating the first frame against later ones: real tag slip **+0.000 samples per frame** over 15 frames, which is what dividing the reader's own carrier means. The emulator slips, and the slip accumulates to a demod-breaking amount between 163 and 196 ms (C70). ⚠ NO ppm FIGURE IS CLAIMED — two attempts to measure one gave +3850 ppm and −76553 ppm, and BOTH were caught by the real-tag control reading non-zero when it must be zero. The operational threshold in C70 is the robust statement | 15 frames, both tags | ✓ the real tag is the control and it is what falsified both estimators | — | L74 |
| C72 | ⛔ **RETRACTED — the ceiling was the USB CABLE, not the emulator.** Claimed the emulator tops out near 0.63x a real tag wherever it is placed. With the cable UNPLUGGED it reaches **33.87 against a real tag's 33.85 — 1.00x**. Every cabled run sits at 0.58–0.72x, so the cable costs about 40% by detuning the antenna. ⚠ The original claim's *placement* conclusion still holds — face-down and centred was best in every run — but its ceiling was an artefact | 39 + 75 captures | ✓ out-of-field readings land on the independently measured empty floor | ✓ spotted by the user from the peak-to-peak column, not by me | L74, L76 |
| C73 | ⛔ **Three plausible causes of the 0.6x eliminated by measurement.** Modulation DEPTH is fine — mid-bands match a real tag at 0.82–0.94x. It is not a TRANSITION artefact — the emulator is 2.8x down even where the phase is constant (10.88 vs 30.73). It is not DUTY CYCLE — 8 of a 16 counter_top is 50%, which already maximises the fundamental. And it is not the low-frequency excess (4.1x below 500 Hz, from the burst envelope): high-passing the trace at 500 Hz and at 2 kHz did not make the Proxmark decode it | 4 measurements on committed traces | ✓ each against the real-tag trace in the same placement | — | L74 |
| C74 | ⭐⭐⭐ **THE FLIPPER ZERO SOLVES THIS BY CLOCKING ITS EMULATION TIMER FROM THE READER'S CARRIER, AND THAT CONFIRMS C71 INDEPENDENTLY.** `targets/f7/furi_hal/furi_hal_rfid.c` sets `LL_TIM_SetClockSource(FURI_HAL_RFID_EMULATE_TIMER, LL_TIM_CLOCKSOURCE_EXT_MODE2)` with `LL_TIM_ConfigETR(...)` — the emulation timer COUNTS CARRIER CYCLES recovered through the comparator, so its subcarrier is an exact division of the reader's carrier, exactly as a T5577's is. ⇒ A working third-party implementation of the same job, on different silicon, does the one thing our free-running PWM cannot | Flipper firmware source | ✓ it is an existence proof: Flipper Indala emulation works in the field | ✓ arrived at independently — C71 measured the real tag at exactly 2048.000 samples before this was looked up | L75 |
| C75 | ⭐⭐⭐ **THE BURST BOUNDARY WAS THE CAUSE, AND LENGTHENING THE BURST FIXES IT.** `lf_tag_em.c` played 10 frames per burst = 10 x 2048 samples = 163.84 ms, then paused to check the field. The Proxmark's own demodulator decoded any capture window lying inside one burst and failed as soon as the window crossed a boundary. Raising the burst to 32 frames (524 ms) moved the cliff with it: **197 ms ✗ -> ✓ and 262 ms ✗ -> ✓**, with 290 ms still failing because this capture happened to span a boundary (the device was already awake, so bursts are not aligned to the capture) | 6 lengths before and after, bracketed | ✓ the same bracket, threshold and demodulator on both sides; fc/2 amplitude 22.69 confirms coupling | ✓ the Proxmark's demodulator shares nothing with ours | L76 |
| C76 | ⚠ **An emulating Chameleon sleeps until a field appears, so the FIRST read of a test always sees an empty field.** Two `emutest.py` runs refused at 3.08 and then a third scored 22.69 — the difference being that the third followed immediately on the second, with the device already awake. ⇒ Any emulation harness must wake the tag with a throwaway read and discard it, or it will report 'not coupled' for a device that is working | 3 consecutive runs | ✓ the bracket is what made the failure legible rather than a silent wrong answer | ⚠ the fix is not independently verified — the device moved before it could be tested in isolation | L76 |
| C77 | ⭐ **Deliberately causing extra burst restarts breaks decoding at every length — which independently confirms C75.** Chaining `lf read; lf read` inside one Proxmark session re-triggers the emulator's field detection and restarts its burst mid-capture. Result: coupling 23.06 and a 929-sample phase run — both perfect — and **0 of 6 lengths decoded**. One read per invocation, same position, same amplitude (22.78): **decodes to 262 ms**. ⇒ The burst-boundary model predicted this and it holds from the opposite direction | 2 capture styles, 6 lengths each | ✓ bracket and phase run identical in both, so only the burst alignment differed | ✓ reproduces the user's independent 262 ms result exactly | L77 |
| C78 | ⚠ **An emulating Chameleon needs SEVERAL field cycles to wake, and no single-session trick substitutes.** A throwaway `pm3 -c "lf read"` fails (each invocation exits, the field drops for seconds, the device sleeps again); three reads inside one session also fails, and additionally breaks the decode (C77). What works is repeated separate attempts over several seconds — typically the 2nd or 3rd. ⚠ The mechanism is NOT established: wake timer, charge-up and field-detect hysteresis all fit. `emutest.py` retries rather than claiming to know | ~8 cold starts | ✓ the bracket makes a sleeping device legible instead of scoring it as a failure | — | L77 |
| C79 | ⛔⛔ **OUR OWN READER IS THE ONE THAT CANNOT READ OUR OWN EMULATOR — and it is the least tolerant by construction.** Front to front, emulator strongly coupled (fc/2 skirt **64704** against an empty 4300, 15x): `lf indala read` scores **0 of 14**, and a sniff shows a **151-sample** constant-phase run where 928 is needed. The SAME emulator on a Proxmark gives **929** and decodes to 262 ms. The Chameleon reads a REAL tag at 912. ⇒ The Chameleon is the odd one out, and only for the emulator | 14 reads + 1 bracketed sniff | ✓ coupling measured, not assumed — this is not another empty field | ✓ three of the four reader/source combinations work; only ours + emulator fails | L78 |
| C80 | ⚠ **HYPOTHESIS: exact-Nyquist mixing makes our decoder uniquely intolerant of an unlocked subcarrier.** This decoder's whole premise is that fc/2 IS fs/2, so demodulation is multiplication by (-1)^n — no oscillator, no phase estimate (C01). That is only valid when the tag's subcarrier is locked to the reader's carrier, which a real T5577 guarantees by dividing it and our free-running PWM does not. At exactly Nyquist there is no margin for a frequency offset. The Proxmark's LF front end demodulates in analogue before sampling and is evidently more forgiving. ⇒ If true, **carrier locking (C74) is not a refinement but a requirement for the Chameleon to read its own emulation** | — | ⚠ UNTESTED. Consistent with all four combinations in C79, but the alternative — something else in our capture path — is not excluded | ⚠ reasoning, not measurement | L78 |
| C81 | ⭐⭐⭐ **THE EMULATION WORKS AGAINST BOTH INDEPENDENT READERS — it is OUR reader that is the outlier.** Flipper Zero, driven over its serial CLI with the Chameleon on its pad: `rfid read` returns **Indala26, FC 52, Card 63612, Parity +** on **6 of 6** attempts — exactly the emulated credential. Proxmark decodes to 262 ms (C75). Our own `lf indala read`: **0 of 14** with the emulator coupled at 15x and confirmed awake (C79). ⇒ Two readers sharing no code with us accept it; the third is ours | 6 Flipper reads + 6 Proxmark lengths + 14 Chameleon reads | ✓ all three against the same emulator, same firmware, same credential | ✓ the Flipper and Proxmark are independent of each other as well as of us | L79 |
| C82 | ⭐⭐ **⇒ The reader limitation does NOT affect real tags, and the emulation could ship without it being fixed.** A real T5577 divides the reader's carrier, so its subcarrier is locked by construction and our exact-Nyquist demodulation is exactly right for it — 60/60 reads across two tags and two units (C56, C68). The intolerance in C80 only appears against a source that is NOT carrier-locked, which no real tag ever is. ⇒ `lf indala read` is not defective; it is specialised. Carrier locking (C74) buys SELF-CONSISTENCY — our reader reading our emulator — not correctness against real tags | — | ✓ the 60/60 real-tag record is the control | — | L79 |
| C83 | ⭐⭐ **IDTECK EMULATION WORKS TOO — 4 of 4 on a Flipper — so C66's retraction was fully deserved and the shared PSK1 transmit path is sound for BOTH protocols.** Chameleon on current firmware emulating `4944544b00000000`: `rfid read indala` (the Flipper's PSK mode) returns **Idteck 4944544B00000000** every time. ⭐ `rfid read normal` (ASK mode) returns nothing on the same setup, which is the built-in control — the Flipper was looking and found a PSK tag, not any tag | 4 PSK reads + 4 ASK reads | ✓ the ASK arm is a null on the identical physical setup | ✓ the frame was chosen for a 32-bit constant-phase run, the strongest probe available | L80 |
| C84 | ⛔⛔ **A SUBSTRING MATCH ON CLI OUTPUT SCORED A COMMAND THAT DOES NOT EXIST AS A SUCCESS.** `lf idteck read` is not implemented — `idteck.c`'s decoder is a documented stub and the CLI exposes only `write` and `econfig`. Asked for it, the CLI prints the subgroup listing, which contains "IDTECK" twice; `nulltest.py` counted substrings across 5 invocations and reported **10/5 ✓**. A count EXCEEDING the number of attempts was visible in the output and nobody read it as impossible. ⇒ Fixed: the tool now detects the help banner and says the leg contributes nothing | 1 tool, 2 claims corrupted | ✓ verified by running the command and reading what it prints | ⛔ the IDTECK *emulation* result (C83) is unaffected — that was measured on a Flipper, which does implement an IDTECK reader | L81 |
| C85 | ⛔ **RETRACTED by C90 — the IDTECK null FAILS at a different coupling.** ⭐⭐ (as written) **The re-bracketed IDTECK null is the strongest false-positive evidence in the project.** IDTECK is the only interferer whose physical layer is IDENTICAL to Indala's — same modulation, bit rate, subcarrier and T5577 config word — so it is the only one that puts *valid PSK1 carrying the wrong frame* in front of the decoder. At **22.65x** the fc/2 empty floor it produced **0 frames in 20 reads**. Every other null differs in modulation, where the mixer and bit integrator see something structurally unlike Indala | 20 reads + 5 amplitude samples | ✓ bracketed in the correct band, refusing to score below 3x | ⚠ one unit, where the unbracketed original claimed two — fewer reads, but they mean something | L82 |
| C86 | ⭐ **Rig A reproduces C81 on current firmware through a committed driver: PSK 4/4, ASK 0/4.** The Flipper reads the Chameleon's Indala emulation as **FC 52 Card 63612 Parity +** on every PSK attempt and nothing on any ASK attempt. ⚠ This adds no new physics — it re-takes C81's measurement with an instrument that cannot make M28's mistake (success is an anchored `name HEX` line, and the tool asserts hits ≤ attempts) and that takes a valid null, which the scratch tool could not: `rfid read` never times out, so a failed read left the worker running and ate the next command. The failure was invisible on any arm that passed | 4 PSK + 4 ASK reads | ✓ the ASK arm, same pad, same session, same minute | ✓ the Flipper shares no code with us | L83 |
| C87 | ⭐⭐⭐ **OUR READER *DOES* READ THE FLIPPER'S EMULATION — 8 of 8 — and that CONFIRMS the carrier-lock theory rather than breaking it.** Flipper emulating Indala26 into Chameleon #1 on rig A: `a0000000e6bd0e92`, FC 52 Card 63612, parity ok, on every capture, energy ~8500 against a real front-side tag's 5075–11720. With the Flipper stopped: 0 of 4, energy 294–1772. ⭐ C74 predicted exactly this — the Flipper clocks its emulation timer from the READER's carrier (`LL_TIM_CLOCKSOURCE_EXT_MODE2`), so a Flipper emulator IS carrier-locked and our exact-Nyquist demodulation applies to it unchanged. ⇒ The limitation is narrower than this project has been writing it: not 'cannot read an emulator' but **cannot read a FREE-RUNNING source**, which so far means only our own PWM (C79). A prediction of the theory, tested and confirmed, is much stronger evidence for it than the original observation was | 8 decoding + 4 null captures | ✓ same device, same session, same geometry, only the Flipper's state changed | ✓ the source is a Flipper and the credential is the one we asked it to emit | L84 |
| C88 | ⭐ **A whole-capture energy measure separates an empty antenna from a loud source cleanly, and CANNOT separate a weak one — which is the finding, not a limitation to hide.** Largest mean |bit integrator| across all 32 sample offsets, computed from integrators the decoder already builds: empty 190–1772 over 324 captures; front-side real tag 5075–11720; Flipper emulation 8451–8685. ⛔ But a BACK-side real tag reads 286–1793 — quieter than a front-side empty antenna — so no absolute level distinguishes 'weak tag' from 'nothing'. ⇒ `INDALA_PSK_ENERGY_PRESENT` is set at 3000 from the EMPTY distribution: 1.7x above the loudest empty and 1.7x below the quietest real tag. It detects a LOUD undecodable source and says nothing about a weak one, which keeps the false-positive-on-a-bare-antenna rate at zero | 324 empty + 164 tag + 8 emulation captures | ✓ the empty arm IS the calibration set | ✓ two committed capture sets from earlier sessions plus a fresh paired pair | L84 |
| C89 | ⭐⭐ **The undecodable-signal status works, on four arms and 20 reads, and the arm that matters is the empty one.** On the flashed device: bare antenna **5/5 'LF tag not found'** — the new status does not fire on nothing; Flipper emulating IDTECK, a loud PSK1 source with a preamble Indala can never match, **5/5 the new status**; Flipper emulating Indala26 **5/5 a0000000e6bd0e92 FC 52 Card 63612**, which is C87 end to end through the real `lf indala read` rather than through captures; a real Indala tag on the other unit **3/5 read, 2/5 the new status**. ⚠ That last arm is the honest caveat: the status CANNOT distinguish a wrong-protocol source from a marginal real tag, and an IDTECK T5577 is independently confirmed present on this bench, so it may be either. ⇒ The message therefore leads with 'retry or reposition' and offers the emulator diagnosis second | 20 reads across 4 arms | ✓ the bare antenna, same device and session, taken first | ✓ the loud arm's source is a Flipper, and the real-tag arm is a second Chameleon and a different credential | L85 |
| C90 | ⛔⛔ **`lf indala read` RETURNS A WRONG CREDENTIAL FROM AN IDTECK TAG, repeatably, and it defeats both safety mechanisms.** The T5577 between the Proxmark and Chameleon #2 dumps as config `00081040`, blk1 `4944544B` ("IDTK"), blk2 `55667788` — one tag, an IDTECK frame, confirmed by the Proxmark reading it as IDTECK. `lf indala read` on that tag returns **`a0000000801119c0`, "FC 144 Card 10504", on 4 of 8 reads**. ⭐ Isolated to **sample phase 28**, which is in `PHASE_ROTATION`: single captures at phase 0 give 0 of 8, host-stacking 8 of them gives 0 of 4 depths, and a capture at phase 28 gives the false frame with amp 5788 and Wiegand-26 parity FAILING. ⛔ So it is neither a stacking artefact nor a straddle — mean|integ| is 2.8x `INDALA_PSK_STRADDLE_AMP` and the shape test passes, meaning the gate sees a loud, well-formed frame. The two-capture agreement rule cannot help either: the error is deterministic at that phase. ⇒ The 28-bit constant run in Indala's preamble is only 33 bits of pattern, and a loud non-Indala PSK1 signal sampled at the wrong phase can produce it | 8 device reads + 20 captures (8 at phase 0, 4 stacked, 8 across the rotation) | ✓ the same tag read as IDTECK by the Proxmark, and its memory dumped | ✓ the T5577 memory dump is independent of both readers | L86 |
| C91 | ⭐⭐⭐ **C90 IS FIXED BY DECODING THE OTHER PROTOCOL: 0 wrong credentials in 10 reads, and the real tag is untouched.** A capture that yields a valid IDTECK frame now vetoes any Indala credential from the same capture. On the bench: the IDTECK T5577 that returned a confident `a0000000801119c0` on 4 of 8 reads now returns **0 credentials in 10** and reports 0x43 every time — informative, not silent. The Flipper emulating Indala into the same firmware reads **6 of 6**. ⭐ The veto is safe because the false match runs ONE WAY: IDTECK's preamble matched **0** of 480 committed Indala and empty captures, while Indala's matched the IDTECK tag. Host-side the fix leaves the front-side set at **110 of 160 decoding, all 110 correct** — unchanged — and `make check` still has both decoders agreeing word for word on all 320 | 16 device reads + 488 captures | ✓ the Indala arm is the null for the veto, and 480 captures are the null for the false match | ✓ the numpy decoder is independent of the C one and the IDTECK tag was dumped by the Proxmark | L87 |
| C92 | ⭐⭐ **`lf idteck read` works, and the two PSK1 readers are now each other's control.** On the IDTECK T5577: **6 of 6** `4944544b55667788`, card **8943462** — byte for byte what the Proxmark reads and what the tag's memory dump says. Empty antenna: **4 of 4 not found**. Pointed at a Flipper emulating Indala: **5 of 5 report 0x43 and 0 credentials**, while `lf indala read` on that same source in the same minute returns the right credential **3 of 3**. ⭐ That last pair is the bracket §1c promised: same signal, same placement, same moment, only the requested protocol changing — no amplitude proxy needed. The reader is the shared capture engine with a different preamble and no new RAM | 18 device reads across 4 arms | ✓ two nulls, one empty and one cross-protocol, the latter with its positive control taken simultaneously | ✓ the credential matches the Proxmark and the T5577 memory dump, both independent of our decoder | L88 |
| C93 | ⚠ **§3's PWM-clock hypothesis is NOT supported by the only test that could show it, and rig A's emulation is currently broken for an unknown reason.** Chameleon #1 emulating Indala read **3/3 and 4/4** on the Flipper early in the session. After cycling slot 1's LF type EM410X ↔ Indala several times it reads **0 of 4**, and stays 0 of 4 after: a forced sense re-enable (`hw mode -r; -e`), `hw slot store`, a fresh-session read-back confirming slot 1 is Indala with payload `a0000000e6bd0e92`, and a **full reboot via DFU**. ⇒ Persisted or physical, not the runtime PWM base clock, which a reboot re-initialises by construction. ⭐ The rig is otherwise sound: with the Flipper EMITTING Indala, Chameleon #1 reads it **3 of 3** in the same minute — so pad geometry, the Flipper's transmit and the Chameleon's receive all work, and only the Chameleon's TRANSMIT is affected | 4 emulation-read attempts x4 conditions + 3 reverse reads | ✓ the reverse direction is the positive control, same pad same minute | ✗ **no independent check** — one rig, one unit, and the cause is not identified | L89 |
| C94 | ⭐⭐ **Indala224 and stacking are mutually exclusive — arithmetic, not a trade-off.** The link map puts `.data`+`.bss` at `__HeapBase` 0x20025988, the heap at 16 KB and the stack at 8 KB below `__StackTop`, leaving **49.6 KB free** with the Indala reader's 48 KB the only large reclaimable block — about 98 KB reachable. A 224-bit frame at RF/32 is 14336 samples, so the current shape (samples + scratch + two int32 accumulators) would need **168 KB**, and 112 KB even with int16 accumulators. Decoding in place with no stacking needs **28 KB**. ⇒ Indala224 is impossible at ANY stacking depth and comfortable without it. ⭐ And stacking buys nothing where it matters: 68.75% front with or without it, 32% vs 72% on the back — the placement the README tells users to avoid | link map + 320 committed captures | ✓ the no-stacking rate is the shipped decoder over single captures, re-run this session | ✓ the RAM figures come from the linker, not from the source | L90 |
| C95 | ⚠ **The BLE advertising guard now covers all four SAADC readers, and its effect on the two broken ones is UNMEASURED — that is the finding, not an omission.** `lf_adv_suspend` /`lf_adv_resume` replace `lf_reader_generic`'s private copy and are now called by hidprox, PAC and ioProx, which each started the field with no guard. C47 says an advertising burst collapses the 125kHz field for ~1.6ms and hit 4 captures in 10. ⛔ **No HID or PAC tag is on this bench**, so the before/after read rate that would confirm or refute C47 could not be taken. What WAS verified is that the shared path still works: on the confirmed new build, IDTECK 6 of 6 and Indala 0 credentials with 5 of 5 reporting 0x43. ⚠ ioProx lacks the guard AND is not on the unreliable list, which already argues the guard is not the whole story | 11 regression reads | ✓ the Indala arm is the null and it behaves | ✗ **none for the claim that matters** — the HID/PAC rate is untested | L91 |
| C96 | ⛔ **RETRACTED by C128 — the reflash was never the trigger.** A reflash demonstrably RESTORES emulation (4/4 immediately after one, with no power cycle). The dead emulation was the device emulating EM410X, which does not read on this pad at all (C127). ⭐ **A DFU REFLASH IS NOT A POWER CYCLE, and the difference was a whole evening.** Rig A's Chameleon stopped emulating during §3 work and stayed broken through a forced sense re-enable, `hw slot store`, a fresh-session read-back confirming the slot config, and a **full DFU reflash** — 0 of 4 on every attempt, while the same unit read the Flipper's own Indala emission 3 of 3, proving pad, geometry and its receive path were all fine (C93). A USB unplug restored it immediately: **3 of 3**. ⇒ The stuck state was peripheral — LPCOMP, the PWM block or the HFXO/SoftDevice request — and the application restart a DFU performs does not clear it. ⚠ So 'I rebooted it' is not evidence the hardware was reset, and any conclusion resting on that is unsafe | 4 conditions x ~4 reads, then 3 after the power cycle | ✓ the reverse-direction read is the positive control throughout | ✓ the power cycle was performed by the user, independently of anything this session did | L92 ⭐ **TRIGGER IDENTIFIED 2026-09-12: it is the REFLASH.** Reproduced exactly — C3A1 was reflashed during §1d, and its LF emulation is dead again while its receive path reads 3 of 3 from the Flipper. ⇒ Not a mystery but a rule: **reflashing a Chameleon leaves LF emulation dead until the device is power-cycled**, and any emulation test after a flash is invalid without one |
| C97 | ⭐⭐⭐ **Stacking removed: the reader drops 48 KB → 8 KB, free RAM goes 49.6 → 89.8 KB, and every arm still passes.** `.bss` falls by **41,176 bytes** — the two 16 KB accumulators and the 8 KB scratch — measured from the link map before and after. The decoder now works in place on the capture buffer, and the two-capture agreement rule survives with consecutive captures as its independent pair. Bench, on the confirmed build: IDTECK **6/6**; Indala refusing the IDTECK tag **0 credentials, 5/5 at 0x43**; rig A empty **4/4 not found**; Flipper emitting Indala **6/6 `a0000000e6bd0e92`**. Host: front **110/160, all 110 correct** — unchanged — empty **0/160**, and `make check` still agrees word for word on 320. ⇒ Indala224's 28 KB now fits with ~62 KB spare | 21 device reads + 480 captures | ✓ two nulls, the empty antenna and the cross-protocol refusal | ✓ the numpy decoder and the link map are both independent of the change | L93 |
| C98 | ✅ **The back-side read rate is a NON-GOAL, not an unverified number.** Removing stacking takes it from 72% to 32%, computed by running the shipped decoder over the 160 committed back-side captures (51 correct), which is by construction the no-stacking case. ⛔ The user's decision, 2026-09-12: that placement is not supported and the degradation is accepted, so no hardware arm is queued and none should be — measuring it would confirm a regression in a use case the project does not have. ⭐ The captures themselves stay: 26 dB down, they are the only low-SNR corpus here and `make check` regression-tests every gate against them | 160 committed captures | n/a — nothing is being claimed that needs a null | ✓ the captures predate the change by two days | L93, L94 |
| C99 | ⛔⛔ **A 224-bit Indala tag written by the Proxmark is PSK2, NOT PSK1 — our decoder cannot read it as it stands.** `lf indala clone -r 80000001...c14e5` (the Proxmark's own documented 224-bit example) leaves the T5577 config at **`000820E0`**, and `lf t55xx detect` reports **Modulation PSK2**, bit rate RF/32. The 64-bit bench tag is `00081040` — PSK1. ⇒ §1d needs the PSK1→PSK2 conversion, not just a longer frame. ⭐ The mechanism is already documented in our own decoder header: the Proxmark matches its preamble on the PSK1 stream FIRST and only then calls `psk1TOpsk2()` and tries again (cmdlfindala.c:1293). XOR-ing consecutive bits of the absolute-phase stream gives the differential one, so the same demodulator serves both. ⚠ The repeat gate survives the change: a repeating frame differentiates to a repeating stream | 1 tag written, 1 config dump, 1 detect | ✓ the 64-bit tag's `00081040` in the same session is the contrast | ✓ the Proxmark's own detect names the modulation, independently of our reader | L95 |
| C100 | ⚠ **The Proxmark's own 224-bit read-back did not match what it had just written, and that is unexplained.** After `lf indala clone -r 80000001b23523a6c2e31eba3cbee4afb3c6ad1fcf649393928c14e5`, `lf indala reader` returned **`Indala (len 235) Raw: 80000001d97ebe3a6c2e31efdffcbee4afb3c6ad1fcf64e9c9c9460a`** — a different value, at a length that is not 224. The blocks it wrote were correct (04-07 match the tail of the requested raw exactly). ⇒ Either its 224-bit demodulation is mis-framing or the tag needs settings the clone did not apply. ⛔ **This removes the independent reference §1d's verification was going to use**, so the memory dump has to serve instead, as it did for IDTECK | 1 write + 1 read | ✗ **none** — the cause is not identified | ✓ the written blocks were verified against the request | L95 |
| C101 | ⛔ **A leftover clamp truncated every long capture to the 64-bit size, and the symptom named the wrong thing.** `lf_psk1_decode_fmt` clamped `n` to `INDALA_PSK_CAPTURE_SAMPLES` — 4096 — which was correct for as long as 4096 was the only capture size there was. Indala224 hands in 14336, so 70% of every capture was discarded, leaving less than one 224-bit frame; every sample offset was skipped before energy was even computed, and the read reported **"LF tag not found"** on a tag the 64-bit reader heard perfectly. Fixed by clamping to the BUFFER size and by measuring energy before the frame has to fit: the same read now reports "subcarrier is present" 3 of 3 | 3 reads before, 3 after | ✓ the 64-bit read on the same tag in the same minute is what isolated it — it reported energy while the 224 read reported nothing | ✓ the 64-bit regression set is unchanged, 110/160 all correct | L96 |
| C102 | ⚠ **The Indala224 decode does NOT work yet — energy present, no frame, 4 of 4.** With a confirmed PSK2 224-bit tag on the antenna (blocks `80000001 B23523A6 C2E31EBA BCBEE4AF B3C6AD1F CF649393 928C14E5`, config `000820E0`), `lf indala read --224` reports 0x43 every time. ⇒ The capture and the energy path are proven; the failure is in the decode, and the candidates are the differential convention, the repeat gate, or the preamble search. ⛔ **It cannot be diagnosed from the device**, because `lf sniff` caps at 4096 samples and a 224-bit frame needs 7168 — so the host tooling that has solved every previous decode question here cannot see this one | 4 reads | ✓ the same command returns 0x43 rather than a credential, so nothing false is being claimed | ✓ the tag's contents come from its own memory dump | L96 |
| C103 | ⭐⭐ **THE 224-BIT DEMODULATION WORKS — the exact bits come out, confirmed against the tag's own memory.** With `lf sniff` raised to 14336 samples, all four committed captures (`caps/indala224/`) yield a **full 224-bit match** for `80000001 B23523A6 C2E31EBA 0x3CBEE4AF B3C6AD1F CF649393 928C14E5` in the DIFFERENTIAL view, at many sample offsets. ⇒ The mixer, notch, integrator and the PSK2 conversion are all correct for this format | 4 captures x 32 offsets x 3 views x 2 polarities | ✓ only the differential view matches; direct and integral reach 31-39 of 224, which is what chance looks like | ✓ the reference is the tag's memory, written by the Proxmark and independent of our decoder | L97 |
| C104 | ⛔⛔ **NO ACCEPTANCE RULE TRIED SO FAR CAN PICK THE RIGHT CANDIDATE, AND ALL THREE RETURN WRONG CREDENTIALS.** Indala224's preamble is a 1 and 29 zeros, so a one-bit-shifted alignment matches nearly as well; and the DIRECT view of this PSK2 tag carries a preamble-matching candidate that repeats at the frame period as convincingly as the true one — ~98% either way — so the repeat test cannot arbitrate between the views. Measured: rank by amplitude **1 of 4 wrong**; rank by repeat score **2 of 4, both wrong**; direct-first-and-exclusive as the Proxmark orders it **3 of 4, all wrong**. ⛔ The impostor is IDENTICAL across captures, so the two-capture agreement rule would confirm it — C90's failure mode in a weaker format. ⇒ The reader is gated OFF (`INDALA224_READER_TRUSTED 0`) and reports 0x43; it cannot return a credential | 4 captures x 3 rules | ✓ the 64-bit regression is untouched at 110/160 all correct | ✓ every wrong frame was caught by comparing against the tag's memory | L97 |
| C105 | ⚠ **The Proxmark's T5577 dump misread one block, and I built two claims on it.** Two dumps of the same tag in one session gave block 4 as `0x3CBEE4AF` and `0xBCBEE4AF` — one bit apart. The clone had requested `0x3CBEE4AF`, and the demodulated air signal matches that, so the second dump was wrong. ⇒ A memory dump is an independent reference but NOT an infallible one, and this project has been treating it as ground truth since C90. ⚠ The error was invisible until a 224-bit match failed at exactly bit 96 — three 32-bit blocks in — which is what pointed at the block boundary | 2 dumps + 4 captures | ✓ the air signal is the tiebreaker and agrees with the clone's request | ✓ the disagreement is between two runs of the Proxmark's own tool | L97 |
| C106 | ⛔ **Momentum's own structural test rejects the TRUE frame at our bit error rate.** `protocol_indala224_can_be_decoded` demands an exact 30-bit preamble at bit 0 AND at bit 224 (normal or inverted). Run over all four captures at every offset in both views: **0 candidates pass, including the correct one**. ⇒ At ~2% bit errors an exact match over 30 specific bits fails about 45% of the time, and over two such windows it almost never holds. Momentum decodes from edge timing on a cleaner path and can afford it; we cannot. ⭐ The lesson generalises: with this error rate a discriminator must be STATISTICAL over many bits, never exact over few | 4 captures x 32 offsets x 2 views | ✓ the true frame is known to be present, so this is a false-negative measurement, not an absence | ✓ the rule is Momentum's, transcribed from its source | L98 |
| C107 | ⭐⭐⭐ **INDALA 224-BIT READS — 6 of 6 on device, correct-or-nothing, and the fix was to stop guessing the modulation.** The obstacle in C104 was not tuning: **the direct view of a PSK2 tag is the running XOR of its data**, a deterministic transform, so it repeats at the frame period exactly as well as the data and is identical across captures. No test inside one capture can separate a frame from its own integral. ⇒ A reader must be TOLD the modulation, and every Indala224 specimen — the Proxmark's clone, Momentum's decoder — is PSK2, so this format decodes the differential view and only that one. On device: **6/6** `80000001b23523a6c2e31eba3cbee4afb3c6ad1fcf649393928c14e5`, matching the tag's memory; empty antenna **3/3 not found**; the IDTECK tag **3/3 at 0x43 with zero credentials**; `lf idteck read` still **3/3**. Host: 2 of 4 single captures decode and **both correct, zero wrong** | 15 device reads + 4 captures | ✓ two nulls — empty antenna and a loud wrong-protocol tag | ✓ the credential matches the T5577's memory, and the 64-bit set is unchanged at 110/160 | L98 |
| C108 | ⚠ **The BLE advertising guard makes NO measurable difference, and C47 is still untested.** Paired on one T5577 wearing H10301, one session, the only change being a recompile: guard OFF **12 of 12** reads, guard ON **12 of 12**. Both match the Proxmark's independent read, FC 118 CN 1603 parity ok. ⇒ The guard neither helps nor harms a well-coupled specimen — and it could not, since there is nothing to fix at 12/12. C47 predicts an effect only where reads are MARGINAL, and this specimen is not. ⚠ C45 and C46 were measured at 10:47 and 11:44, AFTER the 09:53 front-side discovery, so their failures are not the wrong-side artefact — I checked that before assuming it | 24 reads, 2 builds | ✓ the two arms are each other's control, same tag same session | ✓ the credential matches the Proxmark | L99 |
| C109 | ⭐⭐ **`lf pac read` FAILS 10 of 10 on a tag the Proxmark reads perfectly — a reproducible failure at last.** The same T5577, written PAC/Stanley `CD4F5552` and confirmed by `lf pac reader`, returns **"LF tag not found" on all 10 attempts** with the advertising guard enabled. ⇒ PAC is genuinely broken, the guard is not its fix, and unlike HID this one reproduces on demand — which is what §2 has lacked since C46. ⭐ A bug that reproduces is worth more than one that is merely believed | 10 reads | ✓ the Proxmark reads the same tag in the same session, so the tag and placement are not the problem | ✓ the credential and modulation come from the Proxmark's own writer and reader | L99 |
| C110 | ⭐⭐ **The PAC signal is CLEAN and the failure is in interpretation, not reception.** Three 14336-sample captures of the PAC T5577 (config `0x00080080`, DIRECT/NRZ RF/32, frame `FF204990 6D8511C5 93155B56 D5B2649F`): per-bit levels are sharply bimodal — clusters at **2803 and 7037**, separation 4234 — and the recovered bit stream repeats at **99.1% at lag 128**, exactly the frame length, against 53-65% at every other lag tried. ⇒ A real, stable, correctly-clocked 128-bit frame is reaching the ADC. ⛔ **But it is not the tag's blocks**: the best alignment over every start, rotation and polarity is **102 of 128 bits**, and it sits on a five-wide plateau rather than a sharp peak, with 12 consecutive mismatches in one run. A true alignment would be 128/128 and unique | 3 captures, 32 offsets, 128 rotations x 2 polarities | ✓ the lag sweep is its own control — 128 stands alone at 99.1% | ✓ the reference is the tag's memory dump and the Proxmark reads it correctly from the same placement | L100 |
| C111 | ⛔ **RETRACTED by C113 — the air bits ARE the block data; my demodulation was wrong, and the two 'independent' receivers shared one analysis.** (as written) **PAC's ON-AIR BITS ARE NOT ITS BLOCK DATA — two independent receivers agree with each other and disagree with the memory dump.** The same level-threshold demodulation run on the Proxmark's OWN samples (36295, its `lf read` + `data save`) gives **99.0% periodicity at lag 128** and **no exact match** to `FF204990 6D8511C5 93155B56 D5B2649F` — best alignment 119/128. Our Chameleon capture gave 99.1% and 102/128. ⇒ The Chameleon's front end is NOT at fault: a stable repeating 128-bit frame is received identically by both, and it is not the blocks. The Proxmark's printed `Raw:` is therefore a DECODED form, not the literal air bits. ⭐ That reframes §2: the reader's job is to decode PAC's on-air encoding, and `pac.c` already names it — 10-bit UART frames carrying STX, '2', '0', 8 ASCII card-ID bytes and an XOR checksum | 36295 PM3 samples + 3 Chameleon captures | ✓ the two receivers are each other's control and share no hardware or code | ✓ the block data comes from the tag's own memory | L101 |
| C112 | ⚠ **A `.pm3` sample file is TEXT, and reading it as int8 produced a confident wrong answer.** `data save -f` writes one decimal sample per line. Parsed as raw bytes it gave "n=126850, min=10, max=57" — which are the ASCII codes for newline and '9' — and a plausible-looking 59.7% periodicity with a 89/128 best alignment. ⇒ Caught only because the sample COUNT disagreed with what the Proxmark had just printed (36295). ⭐ A length that does not match the tool's own report is the cheapest possible check on a parser, and it is the one that worked | 1 file | ✓ the Proxmark printed the true count in the same command | n/a | L101 |
| C113 | ⛔⛔ **THE TAG'S BLOCKS ARE A PERFECT PAC FRAME — so the air data is fine and MY DEMODULATION IS THE BUG.** Run `pac.c`'s own rules over `FF204990 6D8511C5 93155B56 D5B2649F` in software: the first 19 bits are **0x7F902, exactly `PAC_PREAMBLE`**; the twelve 10-bit UART frames starting at bit 8 all carry a valid start bit, odd parity and stop bit; they spell **STX '2' '0' C D 4 F 5 5 5 2** and the XOR of the eight card-ID bytes is **0x72**, which is exactly the twelfth byte. Nothing about that is chance. ⇒ C111 inverted the conclusion. ⛔ **The flaw was that the two receivers were independent but the ANALYSIS was not** — one level-threshold script processed both, so they agreed because they shared a bug, not because the air differed from the memory. M1's lesson from a different direction | 1 frame, checked against the protocol's own decoder | ✓ the checksum is a self-check the data would fail if misread | ✓ pac.c and the T5577 memory were written by different projects | L102 |
| C114 | ⚠ **≤ 18 mm is solid; the upper end is UNCERTAIN and should be re-taken.** The Chameleon's HID read shows a cliff rather than a slope — 16/16 out to 18 mm — but the gap at which it reached 0/16 was named 20 mm and then 21 mm in messages that arrived out of order, and the rig was physically rearranged in the same window (the T5577 moved off the Proxmark onto the table). ⇒ Treat only the ≤ 18 mm points as measured. Air-gap sweep between the T5577 and the Chameleon, paper spacers, 16 reads each: **6 mm 16/16 · 12 mm 16/16 · 18 mm 16/16 · 24 mm 0/16**. ⇒ There is no gradual degradation to sit in; the marginal band is somewhere in 18-24 mm and may be only a millimetre or two wide. ⚠ This is why C47 has never been testable: the reader is either comfortably above threshold or entirely below it, and a guard that helps marginal reads has almost no window to help in. ⭐ Incidentally 18 mm is far beyond what the user's Flipper work reached — but that work was T5577 DEEP READS, which need a bidirectional downlink, against this broadcast read which only needs to hear the tag | 64 reads across 4 gaps | ✓ the 24 mm arm is the null and it is total | ✓ the Proxmark reads the same tag throughout, unaffected on its side of the stack | L103 |
| C115 | ⭐ **PAC's timing is confirmed correct — the frame period is EXACTLY 4096 samples — so the remaining bug is amplitude recovery alone.** Autocorrelation of the smoothed envelope peaks at lag **4096** in both captures (r=0.66, 0.65), which is 128 bits x 32 samples/bit: the RF/32 bit rate and the 128-bit frame length are both right. ⇒ Bit-boundary locking and clock recovery are NOT the problem, which removes two of the three candidates. ⛔ What remains is the bit decision: thresholding raw samples toggles on carrier ripple — 821 of 1301 runs were under 8 samples, median 4 — and neither smoothing (windows 4-32) nor edge-interval demodulation has recovered a valid frame yet | 3 captures, 6 smoothing windows x 5 thresholds x 2 polarities | ✓ autocorrelation needs no threshold, so it is independent of the thing under suspicion | ✓ 4096 is predicted by the protocol, not fitted | L103 |
| C116 | ⭐⭐ **PAC's frame IS in the capture and correctly aligned — a matched filter built from the tag's known bits scores |r| = 0.70.** Correlating the expected waveform (the tag's 128 bits, 32 samples each, ±1) against the smoothed envelope peaks at one definite sample offset per capture, and those offsets differ by exactly **4096** between captures — one frame period. ⇒ The signal, the timing and the alignment are all right. ⛔ But r = 0.70 implies roughly **15% of bit decisions are wrong**, which is what the 102-of-128 alignment was measuring. The bug is the amplitude decision and nothing else | 3 captures | ✓ the template comes from the tag's memory, not from the capture | ✓ the 4096-sample spacing between captures is predicted by the protocol | L104 |
| C117 | ⚠ **The sample-phase hypothesis for PAC is UNTESTED, not disproved — the air gap confounded it.** Captured at phases 0, 8, 16, 24, 32, 44, 56 and 64 hoping to see the kind of phase dependence Indala shows. All eight scored |r| = 0.09-0.12 with per-bit cluster separation ~230, against **4234** for the same tag flat on the pad. ⇒ The 21 mm spacer costs roughly **18x** of the PAC signal, so every phase is buried and the comparison measures the gap. ⛔ Testing it needs the tag flat again | 8 captures | ✓ the flat-on-pad captures are the comparison and they are 18x louder | ✗ **none for the phase question itself** — it was not measured | L104 |
| C118 | ⭐⭐⭐ **THE LF PATH IS TRANSITION-SENSITIVE, WHICH IS WHY LEVEL DECODING CANNOT WORK FOR NRZ — and it explains every PAC error.** At the matched-filter alignment on a flat-on-pad capture, per-bit levels of KNOWN-1 bits depend on RUN LENGTH: isolated 1s read **10679**, runs of two **6791**, runs of three **5959**, runs of four or more **5422**. Known-0 bits sit at 2114 with a quarter of the spread. ⇒ The response is to CHANGES, not levels: an isolated bit is a sharp excursion while a long run decays toward the baseline, which is an AC-coupled front end doing what AC coupling does. ⭐ **Every error is in the long runs** — isolated 1s score 0 wrong out of 20 — and even a perfect threshold leaves 22 of 128 bits wrong, d-prime 2.31. ⚠ Harmless for PSK, which is constant-amplitude; fatal for NRZ. ⇒ `pac.c`'s edge-interval architecture is RIGHT and my level-threshold host decoder was the wrong model | 3 captures, 128 bits each | ✓ known-0 bits are the control and show none of the effect | ✓ the run-length grouping comes from the tag's memory, not from the capture | L106 |
| C119 | ⚠ **A first-order integrator does NOT undo it — 22 errors become 13-19, not 0.** Undoing the high-pass by accumulating the signal with time constants 64-2048 improves the matched filter only from 0.715 to 0.729 and leaves 13-19 of 128 bits wrong across three captures. ⇒ The AC-coupling model is directionally right but the real response is not a simple first-order high-pass, so compensating for it in software is not the cheap fix it looked like. ⭐ Which raises the better question: `pac.c` already decodes TRANSITIONS rather than levels, so the firmware's architecture is not what is wrong, and the next work is to instrument that decoder rather than keep modelling the analogue path | 3 captures x 6 time constants | ✓ the uncompensated decode is the baseline | ✗ no independent check — this is a negative result about one model | L106 |
| C120 | ⭐⭐⭐ **THE SHIPPING PAC DECODER FAILS ON THE HOST TOO — an offline reproduction of the device bug.** `pactest/` builds the real `pac.c` against Nordic stubs (its modulator declares PWM types the decode path never touches) and feeds it the committed captures: **0 of 3 decode**, matching `lf pac read`'s 0 of 10 on hardware. ⇒ The bug is in the decoder and reproduces with no bench at all, so it can be iterated on at compile speed | 3 captures | ✓ the device's own 0/10 is the comparison | ✓ the .c under test is the one that ships, not a copy | L107 |
| C121 | ⭐⭐⭐ **PAC's AMPLITUDE MODEL DOES NOT MATCH THIS HARDWARE, and the numbers say so three ways.** `pac.c` documents its expectation: NRZ low 500-2000, NRZ high 4000-6000, spikes 10k-14k which it clips away as "LC ringing at NRZ transitions". Mirroring its calibration on the captures: `raw_min` 364-556 so `spike_cap` pins at its **8000** floor; `clip_min` comes out **0**, not the 500-2000 the model assumes, which stretches the range and misplaces both thresholds to 2000/6000; then **24-25% of samples are clipped** and **34% land in the dead zone** where no decision is made. ⛔ And by C118 the clipped excursions are not ringing at all — they are the isolated bits, the ones read with **zero errors** at level 10679. The decoder is discarding its most reliable data | 3 captures, ~13600 samples each | ✓ C118's run-length measurement identifies what is being clipped | ✓ the calibration is transcribed from pac.c, not invented | L107 |
| C122 | ⛔⛔ **NO MEMORYLESS LEVEL DECODER CAN READ THIS SIGNAL — that is a ceiling, not a tuning problem.** At the matched-filter alignment, with the bit boundaries known exactly and the threshold placed at the ideal midpoint of the two class means, **22 of 128 bits are still wrong** (d-prime 2.31). A sliding local threshold gives 19; no sub-window of the bit period beats 18; a first-order integrator gives 13-19. ⇒ Every decoder that decides each bit from its own amplitude is capped around 85% per bit, which cannot produce a frame that passes parity on twelve bytes plus a checksum. ⭐ A working decoder must use MEMORY — the run-length dependence of C118 is information, not noise | 3 captures x 128 bits, 4 decoder families | ✓ the ideal threshold is the best case by construction, so this bounds all of them | ✓ the bit labels come from the tag's memory | L108 |
| C123 | ⚠ **Transition-detection against a running baseline also fails — 0 of 3 captures.** Excursions from a 64/128/256-sample baseline, thresholded at 1.0-3.0 sigma, edges taken at each excursion's peak, intervals quantised to 32-sample bits, both polarities: no capture yields a valid PAC frame. ⇒ The architecture is right in principle (C118 says the signal is transition-sensitive) but naive edge detection is not enough, which is consistent with `pac.c` — whose interval logic is sound — failing for the same reason | 3 captures x 3 windows x 5 thresholds x 2 polarities | ✓ the frame is known present at r=0.70 | ✗ no independent check; this is a negative result about one family | L108 |
| C124 | ⭐⭐⭐ **WHY ONLY PAC BREAKS: AC coupling is harmless to every OTHER modulation this device reads.** Manchester/biphase (em410x, Viking, Jablotron) guarantees a transition every bit, so a run never lasts long enough to decay. PSK (Indala, IDTECK) is constant-amplitude with the data in the subcarrier phase, so there is no level to lose. **NRZ is the only one that permits long constant runs**, and it is the only reader that fails. ⭐ PAC's UART framing bounds runs at 8 bits — Momentum's decoder clamps `pulses >= 9` as impossible — but 8 bits is 256 carrier cycles, about **2.05 ms**, which is ample time to decay (C118 measures the decay across runs of 1 to 4+) | the 6 working LF readers vs the 1 failing | ✓ the working readers are the control and they span both other modulation families | ✓ the run-length bound comes from Momentum's own decoder | L109 |
| C125 | ⭐⭐ **Momentum decodes PAC from EDGE TIMINGS, never from levels — `decoder_feed(protocol, bool level, uint32_t duration)`.** It converts each duration to a bit count (`pulses = round(duration / CYCLE_LENGTH)`) and pushes that many bits of that level; there is no threshold, no calibration and no amplitude anywhere in it. Its front end is a comparator. ⇒ The Chameleon already HAS such a path — `register_rio_callback`, used by em410x, Viking and Jablotron, all of which work — while PAC alone uses the SAADC. ⚠ `lf_pac_data.c` justifies that choice with "the comparator may not produce clean digital edges for NRZ signals", which is an ASSERTION with no measurement behind it and is the cheapest thing left to test | 1 reference implementation read | ✓ three working Chameleon readers already use the comparator path | ✓ Momentum shares no code with us | L109 |
| C126 | ⛔ **RETRACTED by C128 — a slot-type change does NOT kill emulation.** The round trip Indala → EM410X → Indala now reads **3/3** with instrumentation confirming the state is intact throughout. The earlier 0/4 was EM410X on a pad that cannot read it. ⛔⛔ **§3'S MECHANISM IS REFUTED, AND A WORSE BUG IS REPRODUCIBLE IN ITS PLACE: CHANGING A SLOT'S LF TAG TYPE KILLS EMULATION UNTIL A POWER CYCLE.** Full paired run on rig A after a fresh power cycle: Indala emulation reads **4/4**; switch the slot to EM410X and force `pwm_init` via a mode cycle; switch back to Indala with no mode cycle → **0/4**; then force `pwm_init` again → **still 0/4**. ⇒ The stale-base-clock hypothesis predicts the last step restores it, because `lf_sense_enable()` re-runs `pwm_init()`. It does not, so that is NOT the mechanism. ⛔ What IS established: the device transmits nothing afterwards while its receive path still reads the Flipper **3/3**, and neither a mode cycle nor a **DFU reflash** (C96) restores it — only removing power. ⭐ Whatever is stuck survives a software reset, which rules out anything `pwm_init` touches | 16 emulation reads + 3 reverse reads | ✓ the 4/4 before and the 3/3 reverse read are both controls | ✓ reproduced twice on different days from different starting types | L110 |
| C127 | ✅ **RESOLVED by C129/C130 — EM410x emulation is FINE (5/5). The 0/4 was two real firmware bugs, not the protocol or the pad.** ⚠ **EM410x emulation is not readable by the Flipper on this pad — 0 of 4 — and that is unexplained.** Taken with the slot correctly set to EM410X, id `DEADBEEF88`, and `pwm_init` freshly forced. Indala emulation on the same pad in the same session reads 4/4. ⇒ Either EM410x emulation is broken, or the Flipper's ASK read needs a geometry this pad does not give. ⛔ It blocked the ASK arm of the §3 test twice; the test was restructured to use PSK as the readable side instead | 4 reads | ✓ the PSK arm is the control and it works | ✗ not diagnosed — no second ASK reader was tried | L110 |
| C128 | ⭐⭐⭐ **§3'S MECHANISM IS REAL AND NOW CAUGHT IN THE ACT — but its consequence is still untested, and two claims built on the wrong explanation are retracted.** New instrumentation (`hw emudebug`) reads the LF emulation state in one call. Across `hw slot type` Indala → EM410X → Indala: **`tag type` tracks correctly (300 → 100 → 300) while `PWM base clock` stays at 1MHz and `pwm_init runs` stays at 1.** ⇒ The clock is NOT re-initialised on a type change, exactly as §3 said. ⚠ But the round trip reads **3/3**, because returning to Indala restores the match — so the mechanism is proven and the user-visible failure is not. Demonstrating it needs a protocol whose clock differs AND which this pad can read; EM410x fails the second condition (C127). ⛔ C96 and C126 are retracted: the reflash RESTORES emulation and a slot-type change does not break it. Both were the same mistake — a device left emulating EM410X, which reads 0/4 here | 3 state dumps + 3 reads | ✓ the 4/4 immediately after a reflash refutes C96 directly | ✓ the state dump is independent of the read outcome | L111 |
| C129 | ⛔⛔ **BUG: A MODE CYCLE SILENTLY DISARMS EMULATION — `lf_sense_disable()` nulls `m_pwm_seq` and `lf_sense_enable()` never reloads it.** Instrumented directly: after loading Indala `have pwm seq = True`; after `hw mode -r` **False**; after `hw mode -e` **still False**. The device is in Tag Emulator mode, sense ENABLED, clock correct — and emitting nothing, because there is no waveform. Only reloading the slot restores it, which a reboot does. ⭐ This is what killed emulation every time: I used mode cycles constantly to force `pwm_init`, and each one disarmed the thing I was about to test. It is also the bug that most obstructs automated work — a driver switching between reading and emulating trips it every cycle | 3 state dumps | ✓ the state dump is independent of any read | ✓ reproduced across both tag types | L112 |
| C130 | ⭐⭐⭐ **§3 DEMONSTRATED AT LAST: the stale PWM base clock makes emulation unreadable — 0/4 against 5/5.** Same EM410X slot, same id `DEADBEEF88`, same pad, same session, sequence loaded in both arms. With the clock left at **1MHz** from the previous Indala type: **0 of 4**. After a reboot with the type persisted, clock correctly **125kHz**: **5 of 5**, decoded as `EM4100 DEADBEEF88`. ⇒ `hw slot type` changes `m_tag_type` without re-running `pwm_init`, so the waveform plays at the previous protocol's rate — 8x too fast here. ⚠ The two bugs interlock: you cannot reach "correct clock AND loaded sequence" for a newly-changed type without a reboot, because the only thing that re-runs `pwm_init` is a sense cycle, and that is exactly what nulls the sequence (C129) | 9 reads across 2 arms | ✓ the 5/5 arm is the positive control the ASK side never had | ✓ the base clock is read from the device, not inferred | L112 |
| C131 | ⚠ **`hw slot type` does not persist without `hw slot store`, and it has cost two experiments.** A reboot reloads the slot from flash, so an unstored type change silently reverts — once leaving three experiments running against EM410X when I believed the slot was Indala, and once reverting EM410X to Indala mid-test so an "EM410x" arm was really an Indala arm. ⇒ After `hw slot type`, either `hw slot store` or re-read the type before trusting any measurement | 2 incidents | ✓ `hw emudebug` reports the live type, which is how both were caught | n/a | L112 |
| C132 | ⭐⭐⭐ **ALL THREE FIXED AND VERIFIED — a slot's LF type can now be changed and emulated with no reboot, in either direction.** **C129:** `lf_sense_disable()` no longer nulls `m_pwm_seq` (the sequences are static and outlive the uninit, which `utils/psk1.h` already documented), so a mode cycle keeps the waveform — `have pwm seq` stays True and ASK reads **4/4** where it read 0/4. **C130:** `lf_tag_data_loadcb()` is now a wrapper that calls `pwm_reinit_if_clock_changed()` after every load, so the base clock follows the type; `pwm_init runs` increments and the clock moves 125kHz ↔ 1MHz on demand. **C131:** `hw slot type` now prints that the change is RAM-only until `hw slot store`. ⇒ Round trip with no reboot and no power cycle: Indala → EM410X **ASK 4/4** → Indala **PSK 4/4** | 19 reads across 4 configurations | ✓ each arm is the other's control — both protocols read in both directions | ✓ the clock and sequence state are read from the device, independent of the read outcome | L113 |
| C133 | ⭐⭐ **The emulation burst is now a TIME budget, and the frame count is derived per protocol from the sequence itself.** `LF_TAG_FRAMES_PER_BURST (32)` was the wrong unit: a frame is 16.4 ms for Indala 64-bit, 32.8 ms for PAC and EM410x, and **57.3 ms for Indala 224-bit** — so one constant meant 524 ms for one protocol and **1.83 s** for another, with the worst latency landing on the longest-frame protocol. Now `LF_TAG_BURST_TARGET_MS (500)` is converted by summing the sequence's own `counter_top` values, so a protocol added later is right without a table. Verified on device: Indala **31** frames, EM410x **16**, both matching the arithmetic, and both still emulate (PSK 3/3, ASK 3/3) | 6 reads + 2 state dumps | ✓ each protocol is the other's control | ✓ the computed counts match hand arithmetic from the carrier rate | L114 |
| C134 | ⛔⛔ **NO CARRIER-RATE SIGNAL REACHES THE MCU IN ANY MODE — carrier-edge field detection and carrier recovery are both impossible on this board.** All four LF pins are accounted for on sheet 2 of the Ultra schematic: **P0.31** is the SELECT input of U1 (RS2257XC6, an SPDT analog switch) that ties the coil's drive terminal to GND or 3V3 — it never touches antenna signal, so it cannot be re-purposed as a sense input; **P1.13** is the gate of Q3, which shunts the RECTIFIED node; **P0.02** and **P0.29** both sit behind **VD1**, a 1N4148 half-wave detector at the coil, whose hold is 235 kΩ against ≈7.3 nF → **τ ≈ 1.7 ms**. ⚠ Doubly blocked: the SAADC is clamped to 200 kHz and a 125 kHz carrier needs 250 kHz, so surviving ripple would be unresolvable anyway. ⇒ **§5 cannot lock to the reader's carrier**, and §4's burst cannot be removed the way the Proxmark and Flipper do it — both of those recover a clock this board discards before the MCU. ⛔ Retracts the closing sentence of L114, which proposed exactly that | the board, not a measurement — the claim is exhaustive over the pin list, not sampled | n/a | ✓ two independent sources predating the question: the manufacturer's schematic, and an upstream maintainer's "envelope-only" comment in `lf_tag_em.c` (commit `a421e99`, an ancestor of `upstream/main`). The firmware's own "~2 ms peak detector" delay agrees with τ computed from components it never mentions | L115 |
| C135 | ⭐⭐ **§4 MEASURED — the burst-length curve is FLAT above ~500 ms and falls below it, so 500 ms stays.** 24 `emutest.py` runs across **120× of burst length**, one bench, one frame, Proxmark facing Chameleon #2. **2 frames / 32.8 ms:** 7 of 8 decode, every one at exactly **66 ms** and never higher. **31 frames / 508 ms:** 1 of 5, to 197 ms. **32 frames / 524 ms** (the older build): 4 of 6, to 164–197 ms. **245 frames / 4014 ms:** 4 of 5, to 131–164 ms. ⇒ Two separate regimes. Above ~500 ms **more burst buys nothing** — and at 4014 ms a 290 ms capture is essentially never boundary-crossing, yet 262 ms and 290 ms decoded in **none** of the 5, so whatever stops them is not the boundary. Below ~500 ms the boundaries **do** cost: the 32.8 ms arm lost a factor of 2–3, while carrying **27% more signal** than every other arm (fc/2 22.7 against 17.9), which confounds it in the favourable direction and so understates the loss. ⛔ Replaces L114's reason for the constant — "the only length with evidence" — with a curve. ⚠ The 131–197 ms ceiling at long bursts is **unexplained**; it is not boundaries, and two free-running oscillators are the obvious suspect, which is §5's subject | 24 runs across 4 arms | ✓ the 32.8 ms arm is the paired manipulation, and its confound runs the favourable way | ⚠ **weak — no carrier-locked positive control in this geometry.** The 131–197 ms ceiling is not yet known to be the emulator's rather than the Proxmark demodulator's at this amplitude | L115 |
| C136 | ⚠ **`emutest.py` reports the longest decodable PREFIX, which is not the longest decodable WINDOW — every §4 ceiling above is a LOWER BOUND.** One capture, 2-frame burst, prefix ceiling 66 ms: sliding a fixed **131 ms** window across the same samples in 32.8 ms steps decodes at offset 65.5 ms — 1 of 5 offsets. ⇒ A bad patch early in a capture poisons every longer prefix, so "longest length that decodes" silently conflates *how long* with *where*. The honest statistic is the FRACTION of fixed-length windows that decode, swept over offset; `offsetsweep.py` measures that, and §4's arms should be re-taken with it before the curve is trusted to more than its shape | 5 windows on 1 capture | ✓ the prefix ladder on the same capture is the control, and the two disagree | ✓ same demodulator, same samples, same session — only the window origin changed | L115 |
| C137 | ⭐⭐⭐ **THE UNEXPLAINED CEILING IS CLOCK SLIP, AND IT IS NOW A NUMBER: 131 ppm, one subcarrier cycle every 122 ms.** `clockoffset.py` measures the emulator's frame period against the reader's own clock with no reference to whether anything decoded: mix by `(-1)^n`, **square** to remove the ±1 data, and find the residual tone — at fc/2 = fs/2 the baseband is real, so a frequency error survives as `A·d[n]·cos(2πΔnT+φ)` and squaring leaves a clean tone at 2Δ. Five runs: **131.1, 131.1, 131.2, 131.5, 132.0 ppm**, peak/median 59–72. ⇒ 16 µs of accumulated error — one whole subcarrier cycle — after **122 ms**. ⭐ The decode ceiling at long bursts, measured by the Proxmark's own demodulator on pass/fail alone, is **131–197 ms** (C135). Same scale, two routes that share no step. ⇒ The ceiling C135 could not explain is the emulator free-running against the reader, exactly as the §4/§5 preamble predicted in words — now with the coherent window sized. ⚠ It measures the **pair**, not our crystal: 131 ppm exceeds the nRF52 HFXO spec alone (±40 ppm), so the Proxmark's sampling clock carries some of it, and separating them needs a third clock. ⚠ Squaring also loses the **sign** | 5 runs | ✅ **null taken, and it is clean (C138)** — a carrier-locked T5577 on the same frame in the same field reads NO TONE, peak/median 2.4–3.1 against this claim's 59–72 | ✓ the estimator was validated on synthetic PSK1 at known offsets before it was pointed at the bench: ±1 ppm from 20 ppm upwards even at 2× noise, and at a TRUE zero it returns nonsense with peak/median ≈3 against ≥10 for a real tone, which is why the ratio gates the reading | L116 |
| C138 | ⭐⭐⭐ **THE PAIRED CONTROL LANDS — a carrier-locked tag decodes the WHOLE capture 5 of 5 where the emulator never passed 197 ms, and the estimator reads nothing on it.** Same frame `a0000000e6bd0e92`, same Proxmark, same sandwich geometry, same demodulator, same analysis. Only the **source** differs. **T5577, which divides the reader's own carrier:** `clockoffset.py` reports NO TONE on all 5 runs, peak/median **2.4, 2.7, 2.8, 2.6, 3.1** — precisely the band its synthetic TRUE-ZERO case produced (2.7–3.3) — and `emutest.py` decodes to **290 ms, the full buffer, 5 of 5**. **Chameleon emulator, free-running:** **131 ppm**, peak/median 59–72, decoding 0–197 ms with heavy variance. ⚠ And the tag did it on **less signal** — fc/2 **16.0 against 17.9** — so it decoded twice as far with a weaker source. ⇒ Amplitude is excluded; the Proxmark's demodulator is excluded, since it reaches 290 ms the moment the source is locked; and C135 had already excluded the burst boundary. What remains is the clock, and it was measured independently at both ends. ⇒ **Our emulation is inferior to a real tag in one specific, quantified way: any reader that demodulates coherently for longer than ~122 ms will fail on it and succeed on a tag** | 10 runs across 2 sources | ✓ **this IS the null for C137**, and the strongest kind — the same measurement on a source that cannot exhibit the effect | ✓ two instruments sharing no step: a spectral estimator on raw samples, and the Proxmark's own pass/fail demodulator. They agree on which source is which, from opposite directions | L117 |
| C139 | ⭐ **The one artefact that could have faked C137 was tried, and it fails.** The burst cycle at 31 frames is ~510 ms; its 4th harmonic is ~7.9 Hz against a measured tone of 8.20 Hz, and the FFT bin is 3.44 Hz — so arithmetic **cannot** separate them and the objection had to be tested. Synthetic PSK1 with **zero** clock offset and a 2 ms gap every burst, swept 507.9 / 250 / 120 / 32.8 ms (0 to 8 gaps in the record): peak/median **2.4, 2.4, 3.3, 5.5** — never reaching the gate of 6, so the burst structure alone never produces a reportable tone. The same signals at 131 ppm are recovered to ±0.5 ppm with peak/median 30–56, **and the tone stays at 8.17–8.21 Hz at every burst length** — it does not track the burst, which is the discriminator. ⚠ Worth keeping: at 32.8 ms the artefact reaches 5.5, close to the gate. The margin is large at the burst we ship (0 gaps in a 290 ms record) and would narrow at very short bursts | 8 synthetic cases | ✓ the zero-offset arm IS the null, and it is the adversarial one | ✓ the discriminator is independent of the gate: the tone's FREQUENCY is invariant to burst length, which no burst harmonic can be | L118 |
| C140 | ⛔⛔ **PAC FAILS BECAUSE THE FRONT END SATURATES ON A LOUD TAG — the levels are destroyed BEFORE the ADC, so no decoder change can recover them.** Across all four captures **33–37% of samples are pinned at the bottom rail** (below 0x39) and 2.6–4.1% at the top; `lf sniff`'s own diagnostic says it out loud — *"gain 1/6 is too high for the 5328 count DC pedestal"*. ⚠ 1/6 is already the **lowest** SAADC gain, and the clipping is in the op-amp chain *upstream* of the ADC, so no gain setting reaches it. The alternative tap is no help: **AIN0 reads `0xff–0xff`, mean `0xff`** — pinned at the top rail, which confirms the older "measured dead" note with a reason. ⇒ This reframes §2: the plan said *fix the host demodulation until it recovers the frame*, and the frame's LEVELS are not in the capture to recover. ⭐ It also explains C46 exactly — three byte-identical tags reading 0/6, 3/6 and 7/9 is what coupling-dependent saturation looks like | 4 captures | ✓ the Proxmark reads this same tag perfectly in the same geometry, so the information is on the air | ✓ the capture tool's saturation warning is computed independently of any decoder, and AIN0 is a second physical tap | L119 |
| C141 | ⭐⭐ **TIMING SURVIVES THE SATURATION EVEN THOUGH LEVELS DO NOT — the frame period is exactly 4096.000 samples.** Autocorrelation of the level signal peaks at **4096.0** on all three committed captures (correlation 0.55), i.e. **32.000 samples per bit**, confirming the clock is right and the RF/32 assumption sound. ⚠ Correlation 0.55 rather than ~0.9 is the level damage showing up: the frame recurs on schedule while its bit VALUES differ between repetitions. ⇒ This is the mechanism behind L109's recommendation to decode PAC from edge timings via the comparator, which until now rested on an analogy with Momentum. Clipping preserves transition times and destroys amplitudes, and NRZ needs only the former. ⛔ It also kills the bit-rate theory I raised on the way: the interval histogram clusters at 33 / 65.5 / 98 samples, which looks like 32.7 samples per bit, but that is the dead zone biasing rising against falling crossings — the true period is 32.000 | 3 captures | ✓ autocorrelation is independent of any threshold or decoder | ✓ the 4096.0 figure is exact and predicted a priori from 128 bits x RF/32 | L119 |
| C142 | ⭐⭐ **NO LEVEL-BASED RECOVERY GETS BELOW 6 ERRORS OF 128, AND PAC HAS NO ERROR CORRECTION — so none of them can ever decode.** With the tag rewritten to a known frame (`FF2049906D8511C593155B56D5B2649F`) the question becomes a bit error rate instead of a verdict (M33). Exhaustive over threshold x bit phase x polarity, three captures: **mean-of-32 gives 8, 15, 18 errors; majority vote gives 6, 8, 6** — the vote wins because one 16380-count ringing spike moves a 32-sample mean by 512 counts and a rank statistic ignores it. ⛔ The random baseline for the metric is **47**, so 6 is real signal — the frame is genuinely there — but the preamble, twelve UART parities and an XOR checksum all have to land, so **6 errors is as useless as 60**. ⚠ An exhaustive brute force over every threshold and phase with the full validity check finds **no valid frame in any capture**, which is C109's "best 102/128, a plateau not a peak" reached by an independent route and with a stronger search | 3 captures x 224 configurations | ✓ random baseline 47; the tool self-tests that truth scores 0 and inverted truth scores 0 | ✓ ground truth comes from the Proxmark's own clone command, not from any decode of ours | L119 |
| C143 | ⚠ **`lf sniff` does NOT suspend BLE advertising, but every LF READER does — the diagnostic instrument and the thing it diagnoses run different acquisition paths.** `cmd_processor_lf_sniff` calls `raw_read_to_buffer` with no guard, while `lf_pac_data.c`, `lf_hidprox_data.c`, `lf_ioprox_data.c` and `lf_reader_generic.c` all call `lf_adv_suspend`/`lf_adv_resume`. ⛔ **It is NOT the cause of the PAC failure** — the drops in these captures are 1.5–1.9 bits long (47–60 samples) against the ~1.6 ms / 200 samples an advertising burst costs, and they recur far too often. But it means every capture-based conclusion about a reader is drawn from a path the reader does not use, which is a bad property for the instrument this project leans on hardest | 4 captures + the call sites | ✓ gap length measured against the known advertising-burst cost, which excludes it | ✓ the call sites are read from source, independent of any measurement | L119 |
| C144 | ⭐⭐⭐ **PAC FIXED — `lf pac read` goes 0 of 10 to 10 of 10, and the bug was the READER'S OWN FIELD, not the decoder.** The amplifier saturates on a well-coupled tag (C140), so the cure is to drive the field more weakly — the only way to reduce the signal into the amplifier without moving the tag. Scored against the known frame as bit errors of 128: **drive 2 → 6 errors, drive 4 (stock) → 6, drive 6 → 0, drive 7 → 1**, with the rail fraction tracking it (34.0%, 36.4%, 26.0%, 0.0%). ⭐ A genuine optimum: enough field to modulate strongly, little enough not to clip — drive 7 stops clipping entirely and is then too weak. ⇒ `pac_read()` now **sweeps** `{4, 6, 2, 7}` with a fresh calibration per step, because which step wins is a property of the COUPLING, not the protocol — stock first so a tag that already reads is unaffected. On device, same tag and bench as C109's 0/10: **10 of 10**, exact frame `FF2049906D8511C593155B56D5B2649F`. ⚠ The change moves the shared LF PWM from 500kHz/top 4 to 1MHz/top 8 for the finer duty steps, which every LF reader runs on | 10 device reads + 4 scored captures | ✓ C109's 0/10 on this same tag is the before-arm | ✓ **regression on both hardware paths after the shared PWM change: Indala 6/6 (SAADC + phase) and EM410x 6/6 (GPIOTE + TIMER2)** | L120 |
| C145 | ⛔ **THE COMPARATOR / EDGE-TIMING ROUTE IS WORSE THAN LEVELS HERE — L109's recommendation is refuted, and I had already written it into the plan as "the main line of §2".** Simulating the GPIO input buffer on the captures gives **77–92% sub-bit glitches at every hysteresis setting** and no frame; the ringing spikes are full-amplitude, so a Schmitt trigger does not touch them. Low-passing first fixes the glitches completely — a boxcar of k≥9 gives **0.0%** and a stable **235 intervals** across all four captures — and the frame still does not decode: scored, the clean intervals give **26 errors of 128** against the level-plus-majority-vote path's **6**. ⚠ The intervals carry a systematic duty bias — 1-bit runs measure 30.0 samples, 2-bit 67.2, 3-bit 99.3 — because rising and falling edges cross the hysteresis band at different points, so rounding mis-assigns runs and 445 bits are recovered where 448 were sent. ⇒ Momentum's approach does not transfer to this front end. The fix was upstream of the whole argument (C144) | 4 captures × 6 hysteresis settings × 12 filters | ✓ the level path on the same captures is the control, and it wins | ✓ the interval histogram is independent of any decode — the bias is visible directly in the run lengths | L120 |
| C146 | ⛔⛔ **HID PROX DOES NOT HAVE THE SATURATION DISEASE, AND ITS 0 OF 12 WAS INTERMITTENCY — I called it a new specimen, then called it my own regression, and both were wrong.** A/B/A on the same tag and bench: post-change build **0/12**, pre-change build **12/12**, post-change build again **12/12**. ⇒ The firmware is not the variable. ⛔ The mechanism claim was wrong too: HID captures show **0.0% of samples at either rail**, against PAC's 33–37% at the bottom, and no capture of HID ever triggered the tool's saturation counter. What I read as "the same disease" was the sniff tool's **gaps** line, which counts samples below a threshold computed *relative to the capture* — not a rail measure, and it reads 3396–3723 on a signal with nothing railed at all. ⇒ A 0/12 on HID is inside behaviour C45 already recorded: 15–20% lock failures plus an unexplained 0/15 that cleared on its own. This datapoint extends that to 0/12 and says the intermittency is not the field, not the PWM config and not coupling | 36 device reads across 3 builds + 4 captures | ✓ **the A/B/A return leg is the control, and skipping it is what produced both wrong conclusions** | ✓ the rail fraction is computed from 14-bit samples and agrees with the tool's independent saturation counter, which never fires on HID | L121 |
| C147 | ⛔⛔ **RETRACTED THE SAME DAY — coupling never changed; the DRIVE CONTROL went inert.** The claim was that the bench's coupling rose 68% and saturated every step of the sweep. The discriminating measurement is drive 4, and I never took it: **drive 4 is unchanged** (rms 5592 then, 5435 now; on the HID tag 3100 then, 3105 now) while **drive 7 stopped attenuating** (1105 → 3106 on the same tag contents). A coupling rise moves both arms; only the weak arm moved. ⇒ Not the bench, not the tag, not the firmware version — the drive setting stops taking effect at some point during a session. ⚠ I sent the user to check their bench on the strength of this. See C148 | superseded | — | — | L122 |
| C148 | ⛔ **THE DRIVE CONTROL WORKS AND THEN STOPS WORKING WITHIN A SESSION, AND I CANNOT YET SAY WHY.** It worked reproducibly: two independent firmware builds (top_value 4 and 8), two tag contents, a clear dose-response — drive 3/4 and drive 6/8 are the same duty and gave the same rail fraction (25.9%, 26.0%), and drive 7 dropped the signal 2.8x on HID (rms 3100 → 1105). It is now inert: **drive 1, 4 and 7 all give rms 5489, 5435, 5544** where 1 and 7 should be 0.38x of 4. Held constant and verified: firmware version, tag contents, block 0, stock-drive amplitude, and the absence of any external field (Chameleon in tag mode reports no field on 3 checks). A reader/tag mode cycle does not restore it. ⭐ **A REBOOT RESTORES IT, EXACTLY** — after a DFU reflash, drive 4 reads rms 3099 and drive 7 reads **1106**, against the working 3100 / 1105. ⛔ **The trigger is not reproducible.** From a fresh boot it survives a reader/tag mode cycle, an AIN0 sniff and a HID read — all still 1104-1106. ⇒ An intermittent instrument defect with a known workaround (reboot) and no identified cause. ⭐ **UPTIME is an untested candidate and fits the shape better than anything else on the list:** the fault appeared late in a long session, a reboot clears it, and the 112-operation soak ran for only 6 minutes on a freshly-flashed device — so the soak may have been too SHORT rather than the wrong mix. ⇒ The cheap version is to leave `drivesoak.py --rounds` large and let it run for hours on an otherwise idle bench. ⚠ The chunk leak fixed alongside it is real but is NOT demonstrated to be the mechanism: the A/B/A that would have blamed it came out 6/6, 6/6, 6/6. ⭐ With drive live again, **`lf pac read` is 10 of 10** and C144 stands unchanged | 22 captures across 2 sessions and 2 tag contents | ✓ drive 4 is the control and it is unchanged throughout — which is what refutes C147 | ✓ the amplitude is measured from raw samples, independent of any decode | L122 |
| C149 | ⭐⭐ **A TRAP IS SET FOR C148, and it splits the question in two rather than adding another behavioural test.** `hw lfdebug` reads the LF radio's drive value and PWM0's live registers — base clock, COUNTERTOP, DECODER load mode, ENABLE, SEQ[0].CNT, and whether SEQ[0].PTR still points at our own sequence array. ⭐ The load-bearing fields are snapshotted **at the moment playback starts**, because a readback afterwards is useless: the capture path restores the stock drive on the way out, so querying later always reports 4 no matter what the capture ran at — the instrument showed that about itself on its first run. ⇒ When the inertness recurs, one call decides: **drive wrong at start** ⇒ the RAM the PWM reads by DMA was clobbered before the capture; **drive right and the capture still inert** ⇒ the peripheral ignored a correct value, and `ptr ours at start` says whether it was even reading our array. Validated against a working control: drive 4 → "drive 4, ptr ours True", rms 5533; drive 7 → "drive 7, ptr ours True", rms 3267 | 2 captures + 4 state dumps | ✓ the working drive control is the positive control — the trap must agree with it, and does | ✓ the registers are read from the peripheral, independent of any capture or decode | L123 |
| C150 | ⚠ **LATENT HAZARD, not yet shown to be the cause: `lf_tag_em.c` and `lf_125khz_radio.c` both drive `NRFX_PWM_INSTANCE(0)` — the SAME peripheral — with two independent beliefs about who owns it.** The reader tracks `m_reader_inited`; the tag path tracks nothing explicit and calls `nrfx_pwm_uninit()` on that instance from `lf_sense_disable()` and again from `pwm_reinit_if_clock_changed()`. ⇒ A tag-path uninit while the reader believes it is initialised would leave `m_reader_inited` true with the peripheral unconfigured, and `lf_125khz_radio_init()` skips `pwm_init()` on that flag. ⛔ **Not demonstrated:** a reader/tag mode cycle does NOT reproduce C148, and the two configs differ enough (WAVEFORM vs INDIVIDUAL decoding) that a straight swap should break the carrier outright rather than merely flatten the drive. ⇒ Recorded because it is the only shared-resource collision in the LF path and the trap will settle it in one call | static read of both modules + 1 negative mode-cycle test | ⚠ the mode-cycle test is the null and it passes, which weakens this | ✓ the ownership collision is read from source, independent of any measurement | L123 |
| C151 | ⚠ **C148 IS NOT REPRODUCIBLE BY THE OPERATIONS THAT PRODUCED IT — 112 attempts, clean.** `drivesoak.py` ran the session's own mix against the fixed firmware: drive sweeps 1-7, AIN0 / phase / gain / settle / free-run sniffs, mode cycles, slot changes and type flips, all four reader paths, and Proxmark rewrites cycling PAC → HID → Indala → EM410x, checking the drive control after **every single operation**. 112 operations across 8 rounds and 4 tag contents: **maximum deviation from baseline 0.020**, against a detection margin of 0.20 and a real fault that moves it 0.41. ⇒ Either the trigger is not in this mix, or the chunk-leak fix removed it. ⛔ **The fault has never been observed with the trap armed** — `hw lfdebug` did not exist when it happened — so nothing here says WHICH of C149's three branches it was. ⚠ The leak is now the leading unexcluded suspect and my earlier A/B/A did **not** exonerate it: that test measured HID read success, which is insensitive to drive (6/6 at every setting), rather than drive effectiveness. It answered a question nobody had asked | 112 operations × 2 captures each | ✓ per-tag baselines make the tag rewrites their own control — 4 protocols, each re-measured | ✓ the detector's own false positive on run 1 was caught by the trap's register dump, which is independent of the ratio |
| C152 | ⭐⭐⭐ **AN ODD-PARITY PSK2 FRAME REPEATS AT TWICE THE FRAME PERIOD, AND A ONE-FRAME BUFFER EMULATES A CONFIDENT IMPOSTOR.** Our PWM replays one buffer verbatim; a real tag's PSK modulator is never reset at the frame boundary, so when the frame's bits XOR to 1 it emits the whole next frame INVERTED. A single-frame buffer instead jumps back to the starting phase — a discontinuity once per frame that is not in the data. ⛔ **The cost is not one bit.** The wrap error moves where the preamble search lands, so the frame re-frames around it: Momentum decoded the broken buffer **6 of 6 as a stable, wrong 224-bit credential**, and a *different* wrong one for each encoding tried — `80000000D91A91D3…` and `80000003646A474D…` against the true `80000001B23523A6…`. Both were our exact frame rotated by one bit (223 of 224), which is what sent the first fix in the wrong direction. ⭐ Appending the phase-inverted copy fixes it: **6 of 6 exact**. ⚠ PSK1 needs none of this — its telescoping form is periodic at the frame period whatever the parity, which is why Indala26 and IDTECK were always right | 18 Flipper reads across 3 encodings | ✓ ASK control 0/6 on every arm; Indala26 A/B/A 5/6 → 6/6 | ✓ the winning encoding was chosen by a model FITTED to the two wrong readings, then confirmed on hardware | L125 |
| C153 | ⭐⭐ **C100 IS EXPLAINED AND CLOSED: the Proxmark's 224-bit read-back is NOISY, not wrong.** `lf indala clone -r 80000001…928c14e5` then three consecutive `lf indala reader`: **len 224 one nibble out**, **len 225 badly wrong**, **len 224 EXACT**. ⇒ A real Indala224 tag does read back exactly — about 1 time in 3 — and C100 caught a bad read and recorded it as an unexplained mismatch, which then cost §1d its independent reference for a whole session. ⭐ The lesson is M35's: a single read of an intermittent instrument is not a measurement. ⚠ It also makes the Proxmark a POOR verifier for this format; the T5577 memory dump remains the reference | 1 write + 3 reads | ✓ the 64-bit tag reads back exactly in the same rig | ✓ the exact read matches the clone's own request, independent of any reader of ours | L125 |
| C154 | ⭐⭐ **ONE PWM ENTRY PER BIT: the PSK1 emulation buffer drops 8192 bytes → 3584 while its longest frame grows 64 bits → 224.** All 16 entries within an RF/32 bit are identical by construction, so `SEQ[n].REFRESH` (the sequence's `repeats`) holds one entry for 16 PWM periods instead of storing 16 copies. Storing them cost 28 KB at 224 bits, on a part whose Indala reader already holds 28 KB of capture buffer. Verified by the arms that proved the old representation: Indala26 emulation **6/6** with the right credential, and `frames per burst` **31** — the exact number C133 measured before the change. ⚠ `recompute_frames_per_burst()` had to learn about `repeats`; it reads the value off the sequence, so a protocol storing one entry per symbol still computes correctly | 12 Flipper reads + 3 state dumps | ✓ ASK control 0/6; the burst count is arithmetic, checked by hand | ✓ 31 reproduces C133's independently-measured value | L125 |
| C155 | ⛔ **THE T5577 NO LONGER COUPLES TO EITHER CHAMELEON, AND EVERY INSTRUMENT CHECK IS CLEAN.** `lf em 410x read` returns not found on both units; a `lf sniff` rms is **415 at drive 4 against 5541** measured on this bench hours earlier — the empty-antenna floor. ⭐ Three independent checks exclude our hardware and our firmware: `hw lfdebug` shows drive 4, PWM0 enabled, COUNTERTOP 8, DECODER INDIVIDUAL, pointer ours; drive 4 and drive 7 give **different** levels, so this is not C148; and Chameleon #1 reads the Flipper's emulation **3 of 3** in the same session, so the LF receive path is healthy. The Proxmark reads and writes the same tag perfectly throughout. ⇒ It is the geometry between tag and Chameleon, and it blocks rig B's whole Proxmark-writes-Chameleon-reads loop | 4 reads + 2 sniffs + 1 state dump | ✓ the Flipper-to-Chameleon read is the positive control for the receive path | ✓ the Proxmark's own reads and writes to the tag are the positive control for the tag | L125 |
| C156 | ⭐⭐ **THE EMULATION ENCODER NOW HAS A HOST TEST, AND IT IS SHOWN TO CATCH C152.** `ctest/roundtrip.c` compiles the shipping encoder (`utils/psk1.c`, through a 20-line `nrf_pwm.h` shim) and the shipping decoder (`lf_indala_psk.c`) into one binary, renders the PWM sequence to samples the way the peripheral would — honouring `repeats`, and **replaying the buffer rather than extending it**, which is the property C152 turned on — and decodes it. Five arms pass exactly: Indala26 PSK1, IDTECK PSK1, Keri PSK1, Indala224 PSK2 odd parity (448 entries), Indala224 PSK2 even parity (224 entries). ⚠ For Keri that round trip is the ONLY verification its emulate arm has — the bench lost coupling before it could be read off the air (C159). ⭐ **Verified by reintroducing the bug**: with the doubling disabled, the odd-parity arm alone fails on both the entry count and the decode, and the other three stay green. ⚠ The even-parity arm is the control that stops "always double" passing. ⭐ Host-compiling the encoder beside the decoder also surfaced a macro collision — both headers defined `LF_PSK1_MAX_FRAME_BITS` — which nothing on the device would have reported | 4 arms x 2 builds | ✓ the bug-reintroduced run is the negative control; ✓ the even-parity arm is the specificity control | ✓ encoder and decoder share no code and were written months apart | L126 |
| C157 | ⭐⭐⭐ **KERI IS THE SAME AIR LAYER AS INDALA26 AND IDTECK, MEASURED — and the cross-protocol null earned its keep in ten minutes.** Four `lf sniff` captures of a Momentum-emulated Keri credential decode through our Indala demodulator **unchanged**, 4 of 4, to the exact frame predicted from Momentum's source: `e000000080003039` — a 33-bit preamble (`111`, 29 zeros, a 1) then the 32-bit internal id. The T5577 side agrees: `lf keri clone` writes config `603E1040`, PSK1 / PSKCF_RF_2, and `lf t55xx detect` reports PSK1 RF/32. ⇒ Keri is a `lf_psk1_format_t` entry, nothing more. ⛔ **But with no veto, 1 of 5 committed INDALA captures returned a confident Keri credential** — `e0000000e69f0f0a` off the tag whose frame is `a0000000e6bd0e92`, the Indala payload under a forged Keri preamble. C90's failure mode in a third pairing: a 64-bit PSK1 frame at RF/32 carries real data at every offset, so any protocol on this layer is a preamble generator for the rest. With `reject_preamble = INDALA`: Keri **4/4**, Indala **0 of 160**, empty **0 of 160**, IDTECK **0 of 16**, Indala224 **0 of 4** | 4 captures + 340 null captures | ✓ four independent nulls, one of which failed first and drove the fix | ✓ the expected frame was derived from Momentum's source before any capture was taken, and the T5577 block form was checked against a real clone's own dump | L127 |
| C158 | ⭐ **A KERI TAG'S T5577 BLOCKS ARE A ROTATION OF ITS AIR FRAME, NOT A COPY.** The Proxmark writes `(internal_id << 3) | 7` — for id `80003039` that is blocks `00000004` / `000181CF`, which looks nothing like the air frame `E0000000 80003039` our decoder recovers. They are the same 64-bit cycle read from different starting points: a T5577 clocks blocks 1-2 round continuously, so the three leading preamble 1s sit at the END of the block form (that is the `\| 7`) and the reader's window begins three bits earlier. ⇒ A writer must ROTATE, not transcribe; transcribing would produce a tag nothing reads. ⚠ Found by comparing a real clone's block dump against the decoded air signal, not by reading the datasheet | 1 clone dump + 4 captures | ✓ the air frame is the control — it was recovered independently of the block values | ✓ the block arithmetic is the Proxmark's own, read from cmdlfkeri.c | L127 |
| C159 | ⛔⛔ **BOTH RIGS LOST COUPLING IN THE SAME SESSION, AND THE SIGNATURE IS NOT "SOMETHING MOVED".** Rig A read the Flipper's Indala26 emulation **3 of 3** at 06:20 and returns 0x43 on the same arm hours later. The fc/2 subcarrier amplitude — the direct measure — has fallen from **24.3 to 9.6**, below `emutest.py`'s 10.0 presence threshold, and **the Flipper emulating makes no difference to it: 9.8 idle against 9.6 emulating.** Meanwhile broadband rms ROSE, 937 → 1550. ⭐ A raised noise floor with a collapsed signal is what INTERFERENCE looks like; a moved tag lowers both together. ⚠ Rig B went the same way earlier (C155), also with a clean `hw lfdebug`. ⛔ Every instrument check is clean on both: sense DISABLE, emulating False, playbacks 0, HFXO balanced, PWM0 enabled with COUNTERTOP 8 and the pointer ours, drive 4 and 7 still separating. ⇒ **Two rigs failing the same way within hours, with a raised floor, points at something shared — bench, supply or an emitter — not at two independent placements.** A person is needed to look | 6 captures + 4 state dumps, 2 rigs | ✓ the idle-vs-emulating pair is the control, and it is FLAT — that is what rules out the emulation source | ✓ the fc/2 amplitude is computed on the host from raw samples, independent of any decoder | L127 |
| C160 | ⭐⭐⭐ **EMULATE WHAT THE TAG PUTS ON THE WIRE, NOT THE READER'S FRAME VIEW — they differ by a rotation and the rotation is NOT free.** Keri's air stream and its reader-facing frame are the same 64-bit cycle three bits apart: a T5577 holds `(id << 3) \| 7`, our decoder's window starts at the `111` that ends it. Emulating the FRAME view gave Momentum a stable, confident, **wrong** credential — `FD9FD9FB` against the true `80003039`, **6 reads of 6** — and no rotation or inversion of our frame maps to that value (20 of 32 bits differ). Emulating the **block form** reads **5 of 5 correct**, `Keri 80003039 / Internal ID 12345`. ⭐ An Indala26 control on the same encoder, same rig, read correctly in both directions (5/5, 5/5), so this is Keri-specific and not the encoder. ⚠ The cyclic sequences are identical, so the cause can only be the **burst boundary**: playback always starts at buffer index 0 and pauses after 31 frames, and Momentum's Keri decoder wants its preamble at bit 0 AND bit 64 of its own window. **That mechanism is inferred, not isolated** — what is measured is which rotation works | 22 Flipper reads across 3 configurations | ✓ Indala26 control correct on the same encoder, run either side | ✓ the working value matches the Proxmark's own clone output, `Internal ID: 12345` | L129 |
| C161 | ⭐⭐⭐ **KERI READS AND WRITES ON DEVICE — and the reader needed a LONGER CAPTURE than Indala26, which is a measured number and not a guess.** At `INDALA_PSK_CAPTURE_SAMPLES` (4096) `lf keri read` returned 0x43 **six times running** on a real Proxmark-written tag while the host decoded the same captures 3 of 4. Sweeping the length over those captures: **4096 → 0 of 4, 5120 → 3 of 4, flat at 3 of 4 through 14336.** With 8192 (double the threshold, 65 ms against 33 ms) the device reads **6 of 6**, `e000000080003039`. ⭐ The write closes the same day: our `lf keri write --id 80003039` onto a wiped tag, read back by the Proxmark as `KERI - Internal ID: 12345`, **3 of 3**, config `603E1040`. ⚠ The likely reason for the length is that Keri's preamble ends at the block boundary rather than starting there, so the usable window closes earlier — inferred, not demonstrated | 6 device reads + 4 captures x 8 lengths + 3 pm3 reads | ✓ the veto was disabled and re-tested to exclude it as the cause — still 0 of 4 | ✓ the credential matches the Proxmark's own clone and its own reader | L129 |
| C162 | ⚠ **`lf t55xx dump` MISREPORTS THE FIRST BIT OF EVERY BLOCK ON A PSK2 TAG — one dump, and worth re-testing before it is believed.** The Indala224 tag our writer produced dumps as seven blocks each differing from what was written in **bit 31 and nothing else** — identical XOR `80000000` across all seven. Two independent readers recover the credential exactly (`lf indala reader` and ours), so the tag's memory must hold what was sent and the dump is the instrument at fault. ⭐ The control is in the same session: the PSK1 tag written by the same code path dumps **byte-exact**. ⛔⛔ **SUPERSEDED BY C168 — RE-TESTED AT n = 70 AND THE 'IDENTICAL ACROSS ALL SEVEN' PART IS WRONG.** The bit-31 effect is real and PSK2-specific, but it is STOCHASTIC per block read, not systematic: a random subset of blocks each dump, 1 dump in 10 completely clean. What follows was written from a single draw of that process. ⚠ Original text kept for the retraction: n = 1 dump of 1 tag, not re-run, and no other downlink mode tried — it is recorded so it is not mistaken for a write bug, not as an established Proxmark defect. ⚠ Different in character from C105, which was a single RANDOM misread; this is fixed and systematic, so repeating the dump would not wash it out. ⇒ **Do not verify a PSK2 write against the dump** — that advice, which this project gave, is backwards | 1 dump, 7 blocks, + 1 PSK1 control dump | ✓ the PSK1 dump from the same rig and writer is exact | ✓ two independent readers of the same tag agree against the dump | L129 |
| C163 | ⛔⛔ **THE COUPLING FAILS AGAIN WITHIN THE HOUR, AND THIS EPISODE HAS A DIFFERENT SIGNATURE FROM C159's — so "interference" cannot be the whole story.** Thirty minutes after reading the same tag 6 of 6, rig B reports `LF tag not found` and the Chameleon's write does not land (the tag is still `603E1040`, unchanged). ⭐ **A drive sweep settles what is and is not working**: rms scales cleanly with drive — **298 / 417 / 596 at drives 1 / 4 / 7** — so the field is emitted and the receive path measures it, while **fc/2 stays at 0.1-0.4 at every drive**. The tag is simply not answering into it, and the Proxmark reads that same tag perfectly throughout. ⚠ **This is NOT C159's signature.** There, fc/2 fell to 9.6 while broadband rms ROSE to 1550 — signal down, noise up. Here both are at the floor (fc/2 0.2, rms 418 at stock drive), which is what an empty antenna looks like. ⇒ Either two different faults, or one fault whose signature depends on state. ⛔ **C159's inference of interference is therefore weakened and should not be quoted as settled** — what is solid in both episodes is that our field is healthy and the tag is not answering it. Nobody touched the bench between the working reads and this | 3 sniffs x 3 drives + 2 device reads + 1 write + 2 pm3 reads | ✓ the Proxmark reading the same tag throughout is the positive control for the tag | ✓ the drive-versus-rms slope is the positive control for our own transmit and receive paths | L130 |
| C164 | ⭐⭐⭐ **NEXWATCH READS THROUGH THE UNCHANGED DEMODULATOR — 4 of 4 exact, and every structural prediction in the design held.** A Proxmark-cloned NexWatch reports `lf t55xx detect` **PSK1, RF/32, block 0 `00081060`** — Indala's `00081040` with the block count raised 2 → 3 — and its three data blocks are `56000000 / 00436455 / 121E6000`. Four captures at four sample phases decode to **`5600000000436455121e6000`**, byte for byte the Proxmark's own `Raw`, at capture lengths from 3456 samples upward. The de-scramble, parity and checksum reproduce the Proxmark's reading exactly: card number **12345678**, mode 1, parity `0xE` computed = `0xE` read, checksum `0x60` matching magic `0x88` Nexkey and neither of the other two vendors. ⭐ So the fourth protocol through `lf_psk1_format_t` cost one preamble table, one acceptance callback and a de-scramble, with **nothing in the demodulator changed** — which is the claim §10 was built on. ⚠ The `accept` hook is new and belongs INSIDE the candidate loop, not after it: a computed check applied to the winner can only reject, where one applied during the search lets a failing candidate be passed over so a correct one at another offset still wins | 4 captures × 4 sample phases, all exact | ✓ **508 null captures, 0 false positives** — Indala26 320, empty field 320, IDTECK 16, Indala224 4, Keri 8; and the reverse direction is clean too, a NexWatch capture at energy 9024-10508 forging no Indala26, IDTECK, Indala224 or Keri frame | ✓ the credential, mode, magic and raw frame all match the Proxmark's own independent reader, and `make check` still agrees with the numpy decoder word for word on all 320 captures | L131 |
| C165 | ⭐⭐ **NEXWATCH NEEDS A SHORTER CAPTURE THAN KERI DESPITE A 50% LONGER FRAME — which is backwards, and is the prediction C161's inferred mechanism makes.** Measured by truncating the same four captures: **3328 → 0 of 4, 3456 → 4 of 4, flat to 14336.** That is 108 bits for a 96-bit frame. Keri, at 64 bits, needed 5120 (C161) where Indala26 needs 4096. ⛔ C161 measured its threshold and could only INFER why: Keri's T5577 holds `(id << 3) | 7`, so its frame starts three bits BEFORE a block boundary and the usable window closes early, while Indala26's block 1 IS its preamble. NexWatch's blocks are `56000000 / ...` — the frame starts AT the boundary — so the mechanism predicts Indala26-like behaviour despite the longer frame. It got it. ⭐ **The prediction was made from the block dump before the sweep was run**, which is what separates this from a post-hoc fit. ⚠ It does not make the mechanism causal — a capture-length sweep still cannot separate alignment from any other property that tracks it — but C161's inference now has a second protocol behind it rather than one | 4 captures × 9 truncation lengths (3072-14336) | ✓ the shipping value is 6144, exactly two frames and 1.8x the measured threshold, so the reader does not sit on the cliff | ✓ the block alignment is read off the Proxmark's own clone output, not from our writer | L131 |
| C166 | ⭐⭐⭐ **NEXWATCH IS COMPLETE ON READ AND WRITE, VERIFIED IN BOTH DIRECTIONS AGAINST AN INDEPENDENT READER.** `lf nexwatch read` on device: **6 of 6**, card 12345678 / mode 1 / Nexkey, identical to the Proxmark's own reading of the same tag. `lf nexwatch write --cn 87654321 -m 2 --magic quadrakey` onto a **wiped** tag, read back by the Proxmark **3 of 3** as card 87654321 / mode 2 / Quadrakey, `lf t55xx detect` = PSK1 / RF-32 / `00081060`. ⭐ **The write arm changes all three fields at once** — card number, mode and vendor magic — against the credential previously on the tag, so no component of the verification could be a stale read rather than our data (M33). ⛔ **The writer transcribes rather than rotates, and that is measured**: a Proxmark clone holds `56000000 / 00436455 / 121E6000`, so the frame begins AT a block boundary, where Keri's `(id << 3) | 7` begins three bits before one (C158). Emitting the wrong one of those two forms is what gave Keri a stable wrong credential 6 of 6 (C160), so which case a protocol is in is read off a clone's block dump and never assumed | 6 device reads + 3 pm3 reads + 1 detect + 1 wipe | ✓ the tag was WIPED between the Proxmark's credential and ours, and all three reported fields differ from the old ones | ✓ the Proxmark is the independent reader for our write, and our reader is the independent check on its write — each direction verified by the other tool | L132 |
| C167 | ⭐⭐⭐ **NEXWATCH EMULATES 10 OF 10 WITH A CLEAN NULL, CLOSING THE PSK1 FAMILY.** The Flipper's own NexWatch decoder reads our emulation as card **11223344 / mode 3 / Honeywell**, **6 of 6**; with the Chameleon switched to reader mode it reads **0 of 4**; back in emulation mode, **4 of 4**. ⭐ **A/B/A, not A/B** — this bench has twice lost coupling mid-session (C159, C163), so a pass followed by a null proves nothing without the return leg (M35). ⭐ **The credential was chosen to be unforgeable by accident**: 11223344 differs from the factory default (12345678) AND from what is on the T5577 (87654321), in card number, mode and vendor magic all three, so no stale state could produce this reading. ⛔ **Emitted with NO rotation, and that was established before the flash rather than after a failure**: the frame is byte-identical to a Proxmark clone's T5577 blocks (C166), where Keri's block form is a 3-bit rotation of its air frame and emitting the wrong one cost a stable wrong credential 6 of 6 (C160). ⭐ `ctest/roundtrip.c` gained TWO NexWatch arms rather than one — the reference credential and our own, differing in mode, parity and checksum — so an encoder corrupting any of those three fields fails at least one arm | 10 emulation reads + 4 null reads + 7 host round-trip arms | ✓ **0 of 4 with the emulation stopped**, and 4 of 4 again on the return leg — the null is genuinely empty, not a coupling drift | ✓ the reader is Momentum's own NexWatch decoder on a Flipper, which shares no code with ours; and the host round trip verified the encoder before it reached the radio | L133 |
| C168 | ⭐⭐⭐ **THE PSK2 DUMP ARTEFACT IS REAL, IS EXACTLY BIT 31, AND IS STOCHASTIC — WHICH REFUTES HALF OF C162.** Ten `lf t55xx dump` runs against an Indala224 (PSK2) tag whose contents we wrote and know: **23 of 70 data-block reads (33%) came back with bit 31 wrong, and ZERO of the 2170 other bit positions read were wrong.** Not one error anywhere but the first bit of a block. ⭐ **It is bidirectional** — 19 reads flipped 1 → 0 and 4 flipped 0 → 1 — so it is not "the first bit reads as zero". ⛔ **And it is NOT systematic, which is where C162 was wrong**: that claim described seven blocks all differing by an identical `80000000`, i.e. one draw from this process mistaken for the process. Here a random subset differs each time and **1 dump of 10 was completely clean**. ⭐ **The PSK1 control is spotless**: 5 dumps × 8 blocks = 40 block reads of our NexWatch write, byte-exact and byte-identical every time. ⭐ **The tag holds what was written** — the Proxmark's own `lf indala reader` recovers the full 224-bit credential 2 of 2 and ours 1 of 1, so the memory is right and the dump is the instrument at fault. ⚠ **Mechanism inferred, not demonstrated**: in PSK2 a bit is a phase CHANGE, so the first bit of a block read has no predecessor inside that read and its value depends on where the demodulator seeds its phase — a per-read coin flip on that seed would produce exactly this signature. ⚠ Block 0 read correctly 10 of 10, but that is NOT evidence of immunity: the dump may print it from the config `detect` already cached rather than re-reading it. ⇒ **The advice stands and gets sharper: do not verify a PSK2 write against the dump — and a single CLEAN dump is not evidence either, since 1 in 10 comes back perfect** | 10 dumps × 7 data blocks = 70 PSK2 block reads, plus 5 × 8 = 40 PSK1 control reads | ✓ the PSK1 control on the same tag, same session, same geometry: 40 of 40 block reads exact | ✓ two independent readers — the Proxmark's `lf indala reader` and ours — both recover the whole credential exactly, against a dump that does not | L134 |
| C169 | ⭐⭐⭐ **THE SAADC SATURATION REACHES THE ASK FAMILY TOO, AND FOR ASK IT IS NOT COSMETIC — MEASURED BEFORE ANY ASK FIRMWARE WAS WRITTEN.** Captures of a Proxmark-cloned Gallagher tag rail at BOTH ends at every drive tested but one: **bottom rail 26.4 / 33.2 / 36.3 / 26.1% at drives 1 / 2 / 4 / 6, and 0.0% at drive 7** (top rail 3.2-5.0% against 0.0%). ⭐ **Why this matters more here than for Indala**: PSK1 rides a constant-amplitude subcarrier and carries its data in PHASE, so clipping costs it little — which is why Indala reads 110/160 through the same front end. **ASK carries the data IN the amplitude**, so this is the same disease that took PAC to 0 of 10 (C140), in the family that has not been built yet. ⭐ **The damage shows up in the decoding instrument, not only in the sample histogram**: run counts over four sample phases are **717 / 745 / 755 / 833 when clipped and 646 / 646 / 646 / 648 at drive 7** — noise-driven against deterministic, on an unchanged tag. ⛔⛔ **CORRECTED WITHIN THE HOUR BY C171, AND THE DESIGN IMPLICATION IS WITHDRAWN.** This claim originally read "every ASK reader added here must sweep drive as `pac_read()` does". That does NOT follow, and the paired measurement refutes it: the SAME bit-centre decoder recovers the credential **4 of 4 exact from the CLIPPED captures** as well as from the clean ones. Manchester is a TRANSITION code — the bit is which way the level moves at mid-bit — so clipping destroys the magnitude and preserves the sign, which is the same structural reason C124 gives for Manchester surviving AC-coupling decay. PAC was vulnerable because NRZ needs absolute levels across long runs; Gallagher is not. ⛔⛔ **AND THE WITHDRAWAL WAS ITSELF TOO BROAD — C182 REINSTATES THE DRIVE SWEEP ON MEASUREMENT.** Gallagher tolerates the clipping; **Noralsy does not** — it decodes at drive 7 and at NO other setting including stock, and the device read returned nothing 6 times until the sweep was added. ⇒ Both of my earlier statements generalised from ONE protocol — PAC to the family, then Gallagher to the family — and the family disagrees with both. **What survives: the saturation is real and measured; clipping does not stop Manchester in principle; and it demonstrably stops it for SOME protocols in practice, so the ASK readers sweep drive and let the tag decide** | 4 sample phases × 5 drive settings, plus 4 × 8 low-pass widths | ✓ drive 7 is the paired control: same tag, same geometry, same session, 0.0% at both rails | ✓ the rail fractions are computed from raw ADC counts and are independent of any decoder; the run-count stability is a second, independent symptom | L135 |
| C170 | ⚠ **GALLAGHER'S RUN LENGTHS SHOW THREE CLUSTERS WHERE MANCHESTER PERMITS TWO — OPEN, AND NOT EXPLAINED AWAY.** On unclipped drive-7 captures the run-length histogram peaks near **16, 26 and 32 samples**. Manchester at RF/32 permits only a half-bit (16) and a whole bit (32); the ~26 cluster fits neither. ⛔ **The frame does not decode**, at any of eight low-pass widths, either pairing phase or either polarity, so the preamble `0x7FEA` is never found — a blank column, and it is recorded as one. ⭐ What IS established: the bit period is right (median run 16-20 samples against a 16-sample half-bit) and ~646 runs per 14336 samples matches ~4.7 frames of 96 bits at RF/32, so the CLOCK is confirmed even though the bits are not. ⚠ Three candidate explanations, none tested: the sequence-terminator bit is SET in this tag's config and the T5577's terminator is a deliberate Manchester violation; the threshold slicing may carry the duty bias C145 measured directly (1-bit runs at 30.0 samples against a true 32); or the encoding may not be plain Manchester at all. ⭐⭐ **RESOLVED AS A BLOCKER BY C171, THOUGH NOT AS A MYSTERY.** The predicted next step — the level-plus-bit-centre path that beat edge timings 6 errors to 26 on PAC (C145) — decoded the frame **4 of 4 exact on the first attempt**, so the encoding IS Manchester at RF/32 and the clock was right all along. ⚠ **The three-cluster histogram is still unexplained**, and saying so matters: the bit-centre path does not consult run lengths at all, so it sidesteps the anomaly rather than accounting for it. The duty-bias hypothesis (C145, C141) remains the most likely of the three and is untested here. ⇒ Unexplained but no longer blocking — do not quote this as "solved" | 4 captures × 8 low-pass widths × 2 pairing phases × 2 polarities | ✓ the same captures at the same settings produce a stable 646 runs, so the failure is not capture noise | ✓ the Proxmark reads this tag perfectly throughout — region 1, facility 4321, card 6789, CRC ok — so the tag is good and the gap is ours | L135 |
| C171 | ⭐⭐⭐ **GALLAGHER'S AIR LAYER IS MEASURED AND THE ASK/BIPHASE FAMILY IS OPEN — 4 of 4 exact, 17 nulls clean.** A Proxmark-cloned Gallagher decodes on the host to **`7feaa31e76d86c6d868cc249`**, byte for byte the Proxmark's own raw, from four captures at four sample phases. So the family claim §10 was built on holds a second time: **ASK/Manchester at RF/32, 96-bit frame, 16-bit preamble `0x7FEA`**, blocks `7FEAA31E / 76D86C6D / 868CC249` and therefore block-aligned — its writer will transcribe as NexWatch's does, not rotate as Keri's does (C158). ⭐⭐ **THE LEVEL PATH BEAT THE EDGE PATH AGAIN, AND THIS TIME THE EDGE PATH SCORED ZERO.** Sampling the level at bit centres decoded every capture; the run-length/edge decoder found NOTHING at any of 8 low-pass widths × 2 pairing phases × 2 polarities. That is C145's PAC result reproduced in a different modulation family and a starker margin — there it was 6 errors against 26, here it is 4 of 4 against 0 of 4. ⛔ **And the bit-centre decode needed NO low-pass at all** (lp=1 on the clean captures), which is the opposite of what the edge route needed to suppress its glitches. ⇒ For this front end, sample the level where the duty bias is not; do not measure edges | 4 captures × 4 sample phases, both drive settings — 8 decodes, 8 exact | ✓ **17 cross-protocol nulls, 0 false positives** — Indala26 4, empty field 4, IDTECK 3, Indala224 2, Keri 2, NexWatch 2 | ✓ the target is the Proxmark's own clone and its own reader's raw output, which shares no code with this decoder | L136 |
| C172 | ⭐⭐ **THE SHIPPING ASK DECODER READS GALLAGHER 4 OF 4 — AND BOTH CONSTANTS I DID NOT MEASURE WERE WRONG.** `lf_ask_manchester.c` recovers `7feaa31e76d86c6d868cc249` from all four committed drive-7 captures, host-compiled through `ctest/cdemod`, with **17 cross-protocol nulls clean** (Indala26 4, empty 4, IDTECK 3, Indala224 2, Keri 2, NexWatch 2) and the CRC gate enforced. ⛔ **Wrong constant 1 — the CRC.** Written from memory as poly 0x1D / init 0xFF, which returns 0xEE against the 0x49 the tag actually carries. The reference uses **poly 0x07, init 0x2C**, and takes the eight payload bytes at **stride 9** rather than by filtering out every 9th bit. A wrong CRC is a gate that fails safe and reads NOTHING, which on a bench looks identical to an unsupported protocol — it was caught only because it was checked against a real frame before the file was compiled. ⛔ **Wrong constant 2 — the capture length.** Guessed at 6144 by analogy with NexWatch's two frames; measured, **6144 decodes 0 of 4 and the threshold is 10240** (3072 .. 6144 → 0/4; 7168 .. 9216 → 3/4; 10240 .. 14336 → 4/4). A 96-bit frame is 3072 samples, so the requirement is ~3.3 frames. ⚠ The shipped value is 14336 — **114ms a read against Indala26's 33ms**, which is this protocol's honest cost. ⭐ **It reuses the existing capture engine unchanged**: `lf_psk1_read` is modulation-agnostic despite its name, so the ASK family inherits the phase rotation, the BLE guard (C47) and the two-agreeing-stacks rule that stops ~20% wrong credentials | 4 captures × 8 truncation lengths, 8 decodes exact | ✓ 17 cross-protocol nulls, 0 false positives, with the CRC gate active | ✓ the target is the Proxmark's own clone and its reader's raw output; and `askdemod.py`, which shares no code with the firmware, reached the same 12 bytes first | L137 |
| C173 | ⭐⭐⭐ **GALLAGHER READS AND WRITES ON DEVICE, OPENING THE ASK/BIPHASE FAMILY ON HARDWARE.** `lf gallagher read`: **6 of 6**, region 1 / facility 4321 / card 6789 / issue 2, every field identical to the Proxmark's own reading of the same tag. `lf gallagher write` onto a **wiped** tag with a credential differing in **all four fields** (region 3 / facility 1111 / card 2222 / issue 5): the Proxmark reads it back **3 of 3**, `lf t55xx detect` = ASK / RF-32 / `00088060`. ⭐ **Nothing was rebuilt to get there.** The ASK decoder is handed to the existing capture engine, which is modulation-agnostic despite being called `lf_psk1_read`, so the new family inherits the sample-phase rotation, the BLE-advertising suspension (C47) and the two-agreeing-stacks acceptance rule that stops ~20% wrong credentials. ⚠ **One place the sharing had to stop**, and the code says why: `scan_gallagher` does not use `lf_psk1_failure_status`, because the ASK decoder reports `energy` as a Manchester-violation percentage where that mapping expects a bit-integrator amplitude against `INDALA_PSK_ENERGY_PRESENT`. Same field, different quantity, different scale — reusing it would have produced a confident and meaningless "signal present" hint. ⚠ **Emulation is NOT part of this claim.** The shared PSK1 modulator emits a phase-modulated fc/2 subcarrier; ASK needs the field amplitude keyed, so the emitter is its own work and the grid says so | 6 device reads + 3 pm3 reads + 1 detect | ✓ the tag was WIPED between the Proxmark's credential and ours, and all four reported fields differ from the old ones | ✓ each direction is verified by the other tool — the Proxmark reads our write, our reader reads its write — and the host descramble is asserted at import against the clone's own four fields rather than against its inverse | L138 |
| C174 | ⭐⭐⭐ **GALLAGHER EMULATES 10 OF 10 WITH A CHANGED RETURN LEG — THE ASK/BIPHASE FAMILY IS COMPLETE ON READ, WRITE AND EMULATE.** The Flipper's own decoder reads our emulation as region 1 / facility 4321 / card 6789 / issue 2 **6 of 6**; in reader mode, **0 of 4**; and on the return leg, with the emulated credential CHANGED to region 3 / facility 1111 / card 2222 / issue 5, **4 of 4 following the change**. ⭐ **Changing the credential on the return leg is a stronger control than repeating it** — a repeat is consistent with a cached or ambient read, while tracking a change is not — and it costs nothing extra (M35). ⭐ **The emitter reuses em410x's mechanism whole.** At the 125kHz base clock one PWM tick is one carrier cycle, so an entry with `counter_top = 32` spans one RF/32 bit and a half duty gives a half-bit low then high, which IS Manchester; the data bit selects polarity through the top bit of channel_0. em410x is the same code with `counter_top` 64 and a 64-bit frame — the two differ by two numbers. ⛔ **And that is why the PSK1 modulator could not carry it**: PSK emits a phase-modulated fc/2 subcarrier at a 1MHz base clock with constant amplitude, ASK keys the amplitude itself, and `IS_PSK1_TYPE` selects between those two base clocks — Gallagher must stay out of that macro or be clocked 8x too fast. ⚠ **No sequence terminator is emitted**, though a real Gallagher T5577 sets the ST bit (C171). Both reference decoders gate on the 16-bit preamble recurring 96 bits later, which a plain looping frame satisfies. Deliberate, and the first thing to suspect if a reader is found that needs it | 10 emulation reads + 4 null reads, across 2 different credentials | ✓ **0 of 4 with the emulation stopped**, then 4 of 4 again on the return leg — and the return leg carried a DIFFERENT credential, which the reader tracked | ✓ the reader is Momentum's own Gallagher decoder on a Flipper, sharing no code with ours, and it reports all four descrambled fields rather than a raw frame | L139 |
| C175 | ⭐⭐ **SECURAKEY DECODES 4 OF 4 AND THE ASK DECODER IS NOW BIT-RATE PARAMETERISED — because the family does NOT share one.** Securakey is **ASK at RF/40**, config `000C8060`, 96-bit frame, 19-bit preamble `0111111111001011010`; Gallagher is RF/32 and em410x RF/64. `LF_ASK_BIT_SAMPLES` was a hard-coded 32 and is now `lf_ask_format_t.bit_samples`; with that single change the decoder read a second protocol at a different bit rate first time, **4 of 4 exact against the Proxmark's raw**, with Gallagher still 4 of 4. ⛔ **The failure mode this avoids is silent**: a decoder sampling a 40-sample bit at 32 recovers nothing and reports no reason. ⭐ **The null that mattered is the same-family one.** Gallagher's preamble is `0111111111101010` and Securakey's `0111111111001011010` — **they share their first ten bits**, which is exactly the constant-run shape that let an IDTECK tag forge Indala (C90) and an Indala tag forge Keri (C157). Neither decoder fires on the other's tag in either direction, measured on 8 captures, rather than being argued from their differing bit rates. ⚠ **Securakey's gate is those 19 bits and nothing more** — both references agree no checksum is known (the Proxmark's reader prints "How the checksum is calculated is unknown"), so `accept` is NULL and the code records that as a gap, not a design. ⚠ Capture threshold measured at 9216 (2.4 frames) against Gallagher's 10240 (3.3 frames); the shipped 14336 is the buffer maximum, so this format has no headroom left | 4 captures × 7 truncation lengths, 8 decodes exact | ✓ **21 cross-protocol nulls, 0 false positives** — Indala26 4, empty 4, IDTECK 3, Indala224 2, Keri 2, NexWatch 2, **and Gallagher 4 both ways** | ✓ the target is the Proxmark's own clone and its reader's raw output; Gallagher's arm is the regression control and stayed exact through the parameterisation | L140 |
| C176 | ⭐⭐ **SECURAKEY READS AND WRITES ON DEVICE — 6 of 6 and 3 of 3.** `lf securakey read` returns `7fcb400001adea5344300000` **6 of 6** against the Proxmark-cloned tag. Our write of a DIFFERENT credential onto a **wiped** tag reads back on the Proxmark **3 of 3** as FC 0x2A / card 60057 — against FC 0x35 / card 64169 before — with `lf t55xx detect` confirming ASK / **RF/40** / `000C8060`. ⭐ The writer transcribes, and that was CHECKED rather than inherited from Gallagher: a clone of `7FCB400001ADEA5344300000` holds `7FCB4000 / 01ADEA53 / 44300000`, so block 1 is the frame head. ⚠ The credential reported is the raw frame only. Securakey's field layout has three variants and no checksum either reference can compute, so no host-side extraction is asserted — adding a plausible-looking one with nothing to check it against is how `idteck.c` shipped a format nobody had verified | 6 device reads + 3 pm3 reads + 1 detect + 1 wipe | ✓ the tag was WIPED between the two credentials, and the Proxmark reports different FC and card after | ✓ each direction verified by the other tool, and RF/40 confirmed independently by `lf t55xx detect` | L141 |
| C177 | ⛔⛔ **RETRACTED WITHIN THE HOUR BY C178 — THE EMULATION WAS WORKING AND THE INSTRUMENT COULD NOT SAY SO.** Momentum's name for this protocol is **"Radio Key"**, with a space, and `flipper.py`'s success matcher was `^([A-Za-z][A-Za-z0-9]*) <hex>$` — one word. A perfectly good read could never match it. With the pattern widened the same emulation reads **6 of 6**. ⚠ **The control below was SOUND and my inference from it was not**: Gallagher 4/4 on the same rig correctly excluded the bench, and I concluded "protocol-specific defect" when the available conclusion was "protocol-specific defect in the emulator OR in the instrument" — and I never questioned the instrument. ⚠ The polarity arm is void for the same reason: it would have read 0 of 4 whatever was emitted. ⇒ Original text follows. ~~SECURAKEY'S EMULATION DOES NOT DECODE — 0 of 6.~~ The Flipper reads our Securakey emulation 0 of 6. ⭐ **The control was taken in the same minute on the same rig: Gallagher, 4 of 4.** So the pad, the Flipper, the PWM path, the slot machinery and the ASK emitter as a mechanism are all working; only this protocol fails. ⭐ **Two candidates eliminated by measurement:** (1) the INVERTED frame also reads 0 of 4, so it is not that Momentum's Securakey decoder searches only one polarity; (2) both protocols declare `LFRFIDFeatureASK` in Momentum's registry, so `rfid read normal` was genuinely sweeping Securakey and the null is a real failure rather than an unasked question. ⚠ **Leading hypothesis, UNTESTED: the sequence terminator.** A real Securakey T5577 sets the ST bit, and Momentum's `protocol_securakey_can_be_decoded` checks its 19-bit preamble ONCE, where Gallagher's checks the preamble at bit 0 AND again at bit 96. A plain looping frame satisfies Gallagher's two-preamble test and may give Securakey no frame boundary at all. ⇒ That is precisely the caveat recorded in the Gallagher emitter — "if a reader is ever found that needs the terminator, this is the first thing to suspect" — arriving one protocol later. ⇒ Next step is to emit an ST-like gap, not to tune the bit rate | 10 emulation reads + 4 same-rig control reads + 1 registry check | ✓ **Gallagher 4 of 4 on the same rig, same minute, same emitter mechanism** — the bench is excluded | ✓ the polarity arm is an independent null that removes one hypothesis; the feature-flag check confirms the reader was asked the right question | L141 |
| C178 | ⭐⭐⭐ **SECURAKEY EMULATES 10 OF 10 — AND THE "FAILURE" AN HOUR AGO WAS MY MEASURING INSTRUMENT, NOT THE EMULATOR.** With `flipper.py`'s success pattern widened to admit a protocol name containing a space, the unchanged emulation reads **6 of 6** as facility code 53 / card 64169 — exactly what the Proxmark reads off the original tag — then **0 of 4** with the Chameleon in reader mode, then **4 of 4** on a return leg carrying a DIFFERENT credential (facility 42 / card 60057) which the reader tracked. ⛔ **The bug: Momentum calls this protocol "Radio Key", and the matcher was `[A-Za-z][A-Za-z0-9]*` — one word.** A successful read could never match it, so a working emulation reported zero. ⭐⭐ **This is M28 biting from the side it was written to guard.** That rule says match the success PATH, not the protocol name; the pattern DID match the path, and still encoded an assumption about names that no reference supports. ⇒ The lesson is narrower and sharper than M28 as written: **an instrument that can only express some outcomes reports the others as absence.** ⚠ And the isolating control was sound while the inference from it was not — Gallagher 4/4 on the same rig excluded the bench, which leaves "the emulator OR the instrument", and I read it as the emulator alone | 10 emulation reads + 4 null reads, 2 credentials | ✓ **0 of 4 with the emulation stopped**, then 4 of 4 again, and the return leg carried a different credential the reader followed | ✓ the decoded facility code and card number match what the Proxmark reads from the real T5577 carrying the same frame | L142 |
| C179 | ⚠ **AN EMULATION VERIFIED ONLY BY THE FLIPPER IS ONE READER'S OPINION — THE T5577 IS THE AUTHORITY ON WHAT A TAG ACTUALLY PUTS ON THE WIRE.** Every emulate arm in this project's grid — Indala224, Keri, NexWatch, Gallagher, Securakey — rests on a Flipper reading rig A. That is a genuine independent decoder, and it is NOT the same evidence as a real tag: a T5577 written with the same credential and read by the Proxmark is actual hardware behaviour, including whatever the chip does at frame boundaries that we may not reproduce (the sequence terminator is the known example — C171, C174). ⭐ **The write arms already carry that stronger evidence** — our credential onto a real T5577, read back by the Proxmark — so for read and write the bench is authoritative. ⛔ **For emulate it is not, and cannot be without a placement change**: the Proxmark and Chameleon #2 face each other only with the T5577 between them, so reading our emulation on the Proxmark needs the tag lifted out. ⇒ Recorded as a standing limitation on every emulate claim here, and added to Needs hands. ⚠ A cheaper partial substitute needs no hands: compare our emitted waveform against a committed capture of the REAL tag carrying the same credential, on the host. Not yet done | the 5 emulate arms in the grid | ✓ the write arms are the contrast: those ARE verified against a real T5577 | ✓ raised by the operator, and it identifies a class of difference the Flipper cannot detect | L142 |
| C180 | ⭐⭐ **NEITHER ASK T5577 EMITS A FRAME-BOUNDARY GAP — 12 measured intervals, 3072 samples against a nominal 3072 — SO OUR PLAIN LOOPING EMISSION MATCHES IT THERE.** Frame preamble starts were located at every bit phase and both polarities on four committed captures of the real tag: spacings **3072, 3072, 3072 / 3072, 3072, 3071 / 3072, 3071, 3073 / 3071, 3072, 3072**, i.e. zero excess to within ±1 sample, which is 1/32 of a bit and is preamble-localisation noise rather than a gap. ⛔ **This matters because the config sets the SEQUENCE TERMINATOR bit** (C171) and our emitter does not emit one — the caveat written into `gallagher.c` when it was built. The terminator produces no measurable displacement on the wire, so a plain 96-bit loop is not distinguishable from the tag at the frame boundary. ⭐ **It also explains WHY the emulation reads**: Momentum gates Gallagher on the preamble recurring exactly 96 bits later, and a real tag does exactly that. ⚠ **Scope, stated narrowly: this measures the frame BOUNDARY, not waveform identity.** It does not show our amplitudes, rise times or duty match a T5577's, and it is not a substitute for the Proxmark reading our emulation, which still needs the tag lifted out (C179). ⭐⭐ **SECURAKEY NOW AGREES, AND IT TOOK FIXING THE TOOL RATHER THAN THE TAG: 8 intervals across 4 captures, all exactly 3840 samples = 96 bits × RF/40, zero excess.** Its frame start of 4124 reproduces the shipping decoder's `ph 4 pos 103` (4 + 103×40) exactly. ⛔ **The first hypothesis for the disagreement was WRONG and the measurement said so**: widening the low-pass sweep to match the decoder's changed nothing. The real cause was the SLICING REFERENCE — the tool used a 512-sample trailing moving average, which lags by ~256 samples, where the firmware uses 256-sample BLOCK MEANS, which do not. Gallagher tolerated the difference and Securakey did not. ⇒ **An analysis tool that reasons ABOUT a decoder must share the decoder's front end, not merely resemble it; a different DC estimator is a different instrument** — and the symptom it produced was a confident zero | **20 frame-to-frame periods** — Gallagher 4 captures × 3 and Securakey 4 × 2 — each searched at every bit phase, both polarities and both low-pass widths ⭐⭐ **AND C188 FINDS THE EXCEPTION THAT PROVES THE METHOD WORKS: InstaFob's frame period EXCEEDS its nominal by 98-99 samples.** The same tool, the same measurement, on the one protocol in this family whose reader explicitly times a terminator — so the 20 zero-excess intervals above are a real negative result rather than a tool that cannot see a gap | ✓ the ±1-sample spread IS the null — it bounds any real gap below 1/32 of a bit on Gallagher and below 1/40 on Securakey, whose 8 intervals are all exact | ✓ the frame starts are located by preamble search, independent of the decoder that reads the credential; and both nominals — 3072 at RF/32, 3840 at RF/40 — are predicted a priori from the frame length and bit rate | L143, L144 |
| C181 | ⭐⭐ **NORALSY READS ON DEVICE 6 OF 6 — the third ASK protocol, and the cheapest read in the family.** ASK at RF/32, config `00088068`, 96-bit frame, `BB0214FF / 01124022 / 33670000` so block 1 is the frame head (transcribe, checked not inherited). Host decode 4 of 4 exact against the Proxmark's raw with **23 cross-protocol nulls clean**, including Gallagher both ways — the same bit rate and the same frame length, which is the real forging risk. ⭐ **Its gate is the strongest in the family**: a 12-bit preamble plus TWO computed 4-bit nibble checksums, both verified against the bench credential before the code compiled (calc1 6 = chk1 6, calc2 7 = chk2 7). Momentum checks only 12 preamble bits and says why — the next 20 look constant but are not confirmed — and that restraint is copied rather than "improved". ⭐ **Capture threshold 3584 samples against Gallagher's 10240 at the SAME bit rate**, because its frame decodes at bit 12 of the stream where Gallagher's sits at 104; shipped at 6144, which is 49ms against 114ms. ⚠ `lf t55xx detect` CANNOT identify this tag — the Proxmark writes it and then reports "Could not detect modulation automatically" — so unlike every other config here there is no second opinion from `detect`, and the clone's block dump is the only authority | 4 captures × 8 truncation lengths + 23 nulls + 6 device reads | ✓ 23 cross-protocol nulls, 0 false positives, both directions against Gallagher | ✓ the Proxmark's own reader is the target for the frame, and both checksums were reproduced on the host before any firmware was written | L145 |
| C182 | ⛔⛔ **THE ASK READERS MUST SWEEP FIELD STRENGTH AFTER ALL — NORALSY DECODES AT DRIVE 7 AND AT NO OTHER SETTING.** Measured with the shipping decoder, one capture per setting: **stock, 1, 2, 4 and 6 all decode 0; drive 7 decodes.** The device read returned `LF tag not found` **6 times running** while the host read the same tag 4 of 4 — from captures that had been taken at `--drive 7`, so the reader was never given the field strength the evidence was collected at. With a drive sweep in `lf_ask_read` the device reads **6 of 6**. ⛔ **This reinstates the rule C169 raised and C171 withdrew, and BOTH of those generalised from one protocol.** C169 inferred from PAC that every ASK reader must sweep; C171 measured Gallagher decoding fine from clipped captures and withdrew it. Gallagher tolerates clipping, Noralsy does not, and neither protocol licenses a statement about the family. ⇒ The defensible rule is procedural rather than physical: **sweep, and let the tag decide.** ⭐ Steps are `{4, 7, 6, 2}` with stock first, so Gallagher and Securakey — which already read at stock — pay nothing; the caller's timeout is DIVIDED between steps rather than multiplied, as `pac_read` does; and the stock drive is restored afterwards, because a reader that leaves the field weakened breaks whatever runs next and is invisible in the thing being measured (C148) | 6 drive settings × 1 capture + 6 device reads before + 6 after | ✓ the before/after device reads are the paired control: 0 of 6 without the sweep, 6 of 6 with it, same tag, same build otherwise | ✓ the per-drive decode table is produced by the SHIPPING decoder on fresh captures, independent of the device read path | L145 |
| C183 | ⚠ **AN IMPORT-TIME ASSERTION THAT COVERS THE GATE AND NOT THE OUTPUT CERTIFIES NOTHING ABOUT WHAT THE USER READS.** Noralsy's host field extraction shipped printing **card 1315392 / year 255** for a tag the Proxmark reads as **112233 / 2024** — the fields are BCD and the extraction was not. ⛔ **The assertion beside it passed**, because it checked only the checksum flag `[2]` and not the card and year returned alongside. The decoder's own gate was fine, the frame was byte-exact, and the two numbers a person actually reads were both wrong. ⇒ **Assert what you report, not only what you gate on.** The assertion now compares the whole tuple against the reference clone, which is what NexWatch's and Gallagher's do and what this one should have done from the start | 1 defect, caught on the first device read | ✓ the raw frame was byte-exact throughout, which is what localises the fault to the extraction rather than the decode | ✓ the Proxmark's own reader is the reference for both fields | L145 |
| C184 | ⭐⭐⭐ **NORALSY IS COMPLETE — read 6/6, write 3/3, emulate 10/10 — AND THE DRIVE SWEEP IS REGRESSION-CLEAN ON THE PROTOCOLS THAT DID NOT NEED IT.** Write: onto a **wiped** tag, card 445566 / year 2019 against the 112233 / 2024 before it, read back by the Proxmark **3 of 3**. Emulate: the Flipper reads card 112233 / year 2024 **6 of 6** with the frame byte-exact, then **0 of 4** with the Chameleon in reader mode, then **4 of 4** on a return leg carrying card 445566 / year 2019 which it tracked. ⭐⭐ **The regression is the part that matters here**, because C182's drive sweep changed the read path of all three ASK protocols and not just the one that needed it: after the change **Gallagher reads 4 of 4 and Securakey 4 of 4**, both re-written to the tag for the purpose. ⇒ The sweep costs the already-working protocols nothing, which is what putting the stock drive first was meant to achieve and is now measured rather than argued. ⚠ Noralsy's emulation, like every other emulate arm here, is verified by the Flipper alone — C179's limitation applies unchanged | 6 device reads + 3 pm3 reads + 10 emulation reads + 4 null reads + 8 regression reads | ✓ the null is 0 of 4 and the return leg carried a DIFFERENT credential the reader followed; the write was onto a wiped tag with both fields changed | ✓ Gallagher and Securakey re-verified on hardware after the shared-path change, each against its own Proxmark-written tag | L146 |
| C185 | ⚠ **INSTAFOB IS STRUCTURALLY DIFFERENT FROM THE REST OF ITS FAMILY, AND ONE ARM OF IT CANNOT BE VERIFIED ON THIS BENCH AT ALL.** Established before any code was written: (1) **the Proxmark has NO InstaFob command** — `lf help` lists none — so unlike Gallagher, Securakey and Noralsy there is no independent writer OR reader on rig B; (2) its frame is **225 bits** (`(8*4*7)+1`) against the others' 96, which exceeds `LF_ASK_MAX_FRAME_BITS` (128) and the per-protocol PWM entry budget (96); (3) at RF/32 that frame is **7200 samples**, so our 14336-sample maximum capture holds **1.99 frames**; (4) its decoder carries `INSTAFOB_READ_SEQTERM_TIME_BASE` — **the first protocol in this family whose reader explicitly times a sequence terminator**, which is exactly the case C171/C174/C180 flagged as the thing our plain looping emitter would not satisfy. ⭐ **The 1.99 frames is NOT the blocker it first looks like**: a whole frame lands inside the buffer for 7136 of 7200 possible start offsets, **99.1%**, so capture length is a cost rather than a wall — an arithmetic correction I made to my own first reading. ⛔ **The WRITE arm is the one that cannot be done**: we can write config `0x00107060` to the T5577, but nothing on this bench except our own reader can read it back — the Proxmark has no support and the Flipper is on the other rig — and a write verified only by the writer's own reader is precisely the self-certification this project forbids. ⇒ Needs hands. ⭐ **The READ arm IS available with no hands**: the Flipper's own InstaFob emulation is audible to our reader at **fs/2 39314 against a 15287 paired null, 2.6x**, captured and committed under `caps/instafob-flipper/` | 2 captures + 1 paired null + 2 reference-source reads | ✓ the null was taken on the same rig seconds later with the emulation stopped, and is 2.6x down | ✓ the absence of pm3 support is read off its own command list, and the frame size and terminator constant off Momentum's source | L147 |
| C186 | ⭐⭐⭐ **INSTAFOB'S READ ARM WORKS, AND ITS "PREAMBLE" IS THE T5577 CONFIGURATION WORD ITSELF.** The frame is 1 + 7x32 = **225 bits** — the tag clocks out its ENTIRE page 0, block 0 included — and both references identify it by finding `0x00107060`, the chip's own config word, inside that stream. ⭐ **Verified against a controlled plaintext, twice**: the Flipper emulating `0011223344556677` decodes to `00107060 44556677 000...01`, and `AABBCCDDEEFF0102` decodes to `00107060 eeff0102 000...01` — the low 32 bits of the payload, tracked across a change (M33). A paired null taken seconds later with the emulation stopped yields nothing. ⛔ **Two shared buffers had to grow first, and the overflow was caught by arithmetic rather than by a crash**: 225 bits exceeded `id[]` (28 bytes, needs 29) and `word_bits[]` (224, needs 225) by exactly one bit's worth. Both live in `indala_psk_result_t`, which the PSK and ASK families share, so an off-by-one there would have corrupted a neighbouring member and surfaced as a wrong credential somewhere unrelated. ⚠ **The returned frame is a 7-bit ROTATION of Momentum's numbering** — we search for the config word and return 225 bits from there, where Momentum puts it at bit 7. The Keri situation exactly (C158), and recorded so nothing compares the two raws naively. ⚠ **Nothing in the frame checks the payload**: the 32 gated bits are strong, but a bit error inside the card data is undetectable, as with Securakey and unlike Gallagher and Noralsy | 3 captures (2 payloads + 1 null) + 24 cross-protocol null captures | ✓ **24 nulls, 0 false positives** — Indala26 4, empty 4, IDTECK 3, Indala224 2, Keri 2, NexWatch 2, Gallagher 2, Securakey 2, Noralsy 2, its own null 1; and the three other ASK decoders fire on neither InstaFob capture | ✓ the payload was chosen by us and CHANGED between captures, and the decode followed it; `make check` still agrees with the numpy decoder on all 320 captures | L148 |
| C187 | ⭐⭐⭐ **INSTAFOB READS ON DEVICE — 5 of 5, null 0 of 4, and 4 of 4 tracking a changed payload.** `lf instafob read` on Chameleon #1 against the Flipper emulating `0011223344556677` returns config word `00107060` and card data `44556677` **5 of 5**; with the emulation explicitly stopped, **0 of 4**; with the payload changed to `AABBCCDDEEFF0102`, **4 of 4** reporting `eeff0102`. ⛔ **The first null I took was INVALID and I nearly recorded it** — it read `44556677` four times with the emulation supposedly stopped, because a background `flipper.py` started in an earlier shell was still running: each command runs in a fresh shell, so `wait` found no job to wait for. That is M29, which is **rule 6 of this project's own autopilot contract**, and knowing the rule did not prevent it. ⇒ Stop the emulation EXPLICITLY with an ETX to the Flipper's port and verify the null is empty before believing any return leg. ⭐ **No InstaFob writer ships, deliberately.** The config word is known and the writer would be four lines, but nothing on this bench could verify it — the Proxmark has no InstaFob support and the Flipper is on the other rig — so it would ship on self-certification, which is the `idteck.c` failure exactly (C185) | 5 + 4 + 4 device reads across 2 payloads and 1 null | ✓ **the null is 0 of 4 after an explicit stop**, and the return leg carried a DIFFERENT payload that the reader followed | ✓ the source is the Flipper's own InstaFob encoder, which shares no code with ours, and the payload was chosen by us | L149 |
| C188 | ⭐⭐ **INSTAFOB EMITS A FRAME-BOUNDARY GAP AND THE OTHER THREE ASK PROTOCOLS DO NOT — 98-99 samples of excess, measured.** Frame period against nominal, `framedrift.py` on two committed captures of the Flipper's own InstaFob emission: **7299 and 7298 samples against a 225-bit nominal of 7200 — excess 99 and 98.** Gallagher and Securakey measured **zero excess across 20 intervals** with the same tool (C180). ⭐ **This is the prediction C185 made from Momentum's source, confirmed from the signal**: InstaFob is the only protocol here whose decoder carries a terminator timing constant (`INSTAFOB_READ_SEQTERM_TIME_BASE`, 400us = 50 carrier cycles), and it is the only one whose frame does not repeat at its nominal length. ⭐ **It also validates C180's negative result**: a tool that reports zero excess on three protocols and ~98 on a fourth is measuring the tags rather than itself. ⛔ **What this does NOT give is an emitter.** The gap does not decompose into clean Manchester runs — runs of 24, 24, 26, 40, 28, 20 samples where a half-bit is 16 and a whole bit 32 — which is what an encoding violation should look like, and Momentum decodes it with a SIX-state machine, not a single interval. One capture at this slicing quality cannot reconstruct that sequence. ⇒ InstaFob's emulate arm is materially harder than the other three, whose emitters were two numbers each, and it is not attempted on a shape I can only measure approximately | 2 captures, 1 interval each, against 20 intervals of control | ✓ Gallagher and Securakey through the same tool show 0 excess — the negative control for the measurement itself | ✓ the gap was PREDICTED from Momentum's terminator constant before it was measured in the signal | L150 |
| C189 | ⛔⛔ **CORRECTED BY C190 WITHIN THE HOUR — THE DIVIDING LINE IS NOT ASK VERSUS PSK.** I published this as a modulation-family split on n=2 PSK protocols. Extending it to all four found **Indala224 (PSK2, differential) reading 14 of 16** — as well as the ASK protocols — while the three PSK1 protocols total 3 of 21. The real line is **absolute-phase versus everything else**, which is a better mechanism and a stronger claim; see C190. ⇒ The measurements below stand; the generalisation drawn from them did not. ⭐⭐⭐ **[original] THE PROXMARK READS OUR ASK EMULATION 16 OF 16 AND OUR PSK1 EMULATION 1 OF 13 — THE FIRST DIRECT EVIDENCE THAT CARRIER LOCKING MATTERS AGAINST A THIRD-PARTY READER.** With the T5577 lifted out of the sandwich the Proxmark faces Chameleon #2 directly, so our own emulation can be read by a second independent reader for the first time. Measured: **Gallagher 10/10, Securakey 3/3, Noralsy 3/3 — every ASK protocol perfect; NexWatch 1/10 and Keri 0/3 — both PSK1 protocols essentially unreadable.** ⛔ **The Flipper reads those SAME PSK emulations 6 of 6 and 10 of 10 all night** (C167, C174, C184), so this is not a broken emulation — it is a reader-dependent one, and one reader was hiding it. That is exactly the limitation C179 named. ⭐ **The mechanism is C74's, and the split is its signature.** Our PWM is FREE-RUNNING; a real T5577 and the Flipper both clock their emulation from the reader's own carrier. ASK carries its data in AMPLITUDE and is indifferent to clock drift; PSK1 carries it in subcarrier PHASE, which a reader correlating against its own carrier cannot recover once the two clocks walk apart. ⚠ **Intermittent rather than dead** — NexWatch read 1 of 10 here and 1 of 2 on a third mode cycle earlier, which is what a slow relative drift predicts: it works while the phases happen to align. ⇒ **This makes §5's carrier-locking decision concrete rather than theoretical.** It was recorded as "likely closed, document and move on"; it now has a measured cost — PSK emulation is 1/13 against a Proxmark and 16/16 against a Flipper | 13 PSK reads + 16 ASK reads across 5 protocols | ✓ **the three ASK protocols are the control**: same device, same slot machinery, same PWM peripheral, same session — 16 of 16 | ✓ the Flipper's readings of the same emulations are the second control, and they disagree with the Proxmark's in exactly the modulation-dependent way the free-running clock predicts | L152 |
| C190 | ⭐⭐⭐ **OUR FREE-RUNNING EMULATION FAILS FOR ABSOLUTE-PHASE ENCODING AND ONLY FOR THAT — measured across all seven emulated protocols against a Proxmark.** With the T5577 lifted out, our own emulation read by a second independent reader:
| C191 | ⭐⭐ **THE ASK FAMILY NOW HAS ROUND-TRIP COVERAGE — AND THE HARNESS ITSELF HAD A FALSE-PASS BUG.** Every PSK protocol has had a `ctest/roundtrip.c` arm since C152 shipped three wrong encodings to the radio in one session; the four ASK protocols had none, and went to hardware with nothing between them and it. Three arms added — Gallagher, Securakey, Noralsy — compiling the **shipping emitters** (`gallagher.c`, `securakey.c`, `noralsy.c`) against the **shipping decoder**. All ten arms exact. ⭐ **The ASK renderer is not the PSK one and could not be**: a PSK entry is one bit held for `repeats`+1 periods of an fc/2 subcarrier whose POLARITY is the data; an ASK entry is one bit of `counter_top` CARRIER CYCLES whose two halves carry the Manchester transition. ⭐⭐ **Sensitivity proven, not assumed**: with Securakey's `CARRIER_CYCLES_PER_BIT` broken 40 → 32 — exactly the hard-coded-bit-rate bug C175 found in the decoder — its arm reports NO DECODE and the suite fails. ⛔ **And that experiment exposed a FALSE-PASS in the harness**: after restoring the source, `make` saw tied mtimes, skipped the rebuild and re-ran the OLD binary. It printed the stale FAILURE, but the same mechanism in the other direction prints a stale PASS for code that no longer exists — in a correctness harness that is worse than a false failure. `make check` now rebuilds from scratch | 10 round-trip arms + 1 deliberate break + 320-capture regression | ✓ the deliberate break IS the null — the arm fails when the emitter is wrong and passes when it is right | ✓ the emitters and the decoder share no code, and `make check` still agrees with the numpy decoder on all 320 captures | L154 |
| C192 | ⭐⭐ **§9's BLOCKER #1 IS CLEARED: the shared capture engine no longer claims to be PSK.** `lf_psk1_read` was the engine BOTH families use — it rotates the sample phase, suspends BLE advertising (C47) and requires two independent captures to agree, none of which depends on how the bits are carried — and `lf_ask_manchester.c` called it, which reads as a bug to anyone not already told otherwise. ⭐ **The rule applied: shared things lose `psk1`, genuinely-PSK things keep it.** Renamed: `lf_sampled_read`, `lf_sampled_read_t`, `lf_sampled_decode_fn`, `lf_decode_result_t`, `LF_SAMPLED_MAX_CAPTURE_SAMPLES`, `LF_DECODE_MAX_FRAME_BITS/BYTES` — about 120 references. Kept: `lf_psk1_format_t`, `lf_psk1_decode_fmt`, `LF_PSK1_FORMAT_*`, `lf_psk1_modulator`, `LF_PSK1_EMU_*`. ⛔ **The first attempt left the tree HALF RENAMED**, twice: BSD `sed` silently ignores `\\b` so the substitution did nothing, and the Python retry died on a file that is not UTF-8 after rewriting some files but not others. ⇒ The rename that worked operates on BYTES, which cannot fail to decode. ⚠ A half-applied rename is a worse state than an unstarted one, and neither failure announced itself — only counting the residual references did | ≈120 references across 49 files | ✓ counted both ways: residual shared names 0, PSK-specific names unchanged at 21/10/25/6/5 | ✓ firmware builds; all 10 round-trip arms exact; 320-capture cross-check word for word; **and the renamed READER path reads 4 of 4 on hardware with a clean null** | L155 |
| C193 | ⭐⭐⭐ **FSK RESOLVES CLEANLY ON OUR SAMPLED PATH — 99% of sub-periods land exactly on RF/8 or RF/10 — SO §10's REASON FOR DEFERRING THE FSK FAMILY NO LONGER APPLIES.** A Flipper AWID emulation captured by our reader at drive 7: pairing adjacent slicer runs into full sub-periods gives **1650 of 1655 pairs exactly 8 or 10 samples (99%)** — 8 appearing 1222 times and 10 appearing 428 — against a paired null with no structure at all (a broad 1..13+ spread). At one sample per carrier cycle those ARE RF/8 and RF/10, the FSK2a tones AWID, Paradox and Pyramid all share (`MIN_TIME`/`MAX_TIME` 64/80us in all three of Momentum's sources). ⭐⭐ **The runs are asymmetric and it does not matter**: 8 splits as 3+5 and 10 as 4+6, which is the duty bias C145 measured directly — but FSK needs the PERIOD, and the bias cancels in the sum. That is why FSK is easier here than ASK was, not harder. ⛔ **§10 deferred this family because it "reuses the HID Prox/ioProx SAADC machinery" and HID's 15-20% intermittency (C45) is unexplained and lives there. That premise is now wrong**: the ASK family proved a new decoder on the SHARED engine needs none of the legacy per-protocol readers, and `lf_hidprox_data.c` rolls its own `saadc_cb` — the family C47 identified as lacking the BLE-advertising guard, which is the leading explanation for the intermittency. ⇒ An FSK decoder on `lf_sampled_read` INHERITS that guard rather than the defect, and may be the fix for C45 rather than a victim of it. ⚠ **No frame is decoded yet** — this is the physical layer confirmed, not a working reader, and nothing here claims otherwise | 1655 sub-period pairs from 1 capture + 1 paired null | ✓ the null capture has no 8/10 structure whatever — a broad spread with no cluster above 3% | ✓ the tone periods were predicted a priori from three independent Momentum sources before the capture was analysed | L156 |
| C194 | ⭐⭐ **AN FSK2a DEMODULATOR ON THE SHARED SAMPLED PATH RECOVERS AWID FRAMES — the family §10 put last is the cheapest one left.** `fskdemod.py` slices, pairs adjacent runs into sub-periods, classifies 8 against 10 samples and applies Momentum's own pulse-count rule (a '0' is SIX periods of RF/8, a '1' is FIVE of RF/10 — both exactly 50 carrier cycles). From a Flipper AWID emulation it returns frames passing **all three of the reference's checks**: the 8-bit preamble `00000001`, the same preamble again 96 bits later, and ODD parity over every nibble of bits 8..95. ⭐ **Two payloads give two distinct frames** — `010203040506070809` → `01114121171211481d12d181` and `AABBCCDDEEFF001122` → `01b4b7be28de7de7ee111218` — so the decode tracks the plaintext, and the **paired null returns ZERO BITS**, not merely no frame: with the emulation stopped the demodulator finds no valid tone periods at all. ⭐⭐ **THE FIELD MAPPING IS NOW SOLVED, AND MY FAILURE TO FIND IT WAS A BUG IN THE SEARCH, NOT A PROPERTY OF THE FORMAT.** `protocol_awid_encode` takes THREE bits of payload at a time, shifts left one and fills the vacated bit with odd parity — so each nibble at `8 + 4i` is 3 data bits plus a parity LSB, 22 nibbles giving **66 payload bits**. Inverting that recovers **`010203040506070800` and `aabbccddeeff001100` exactly**, against the emulated `010203040506070809` and `aabbccddeeff001122`: the final 6 bits are simply not carried on the wire. ⛔ **My earlier search failed because I prepended the 8 preamble bits to the payload**, shifting every subsequent bit — 512 strip alignments found nothing because the answer was never an alignment. ⇒ **AWID's read arm is host-complete**: frame, gate and credential, exact on two independent plaintexts | 2 payloads + 1 paired null + 1 alignment search over 512 combinations | ✓ **the null returns 0 bits** — the strongest form available, since it fails at the tone level rather than the frame level | ✓ the gate is the reference's own three checks, not one of my choosing, and the two frames differ across a plaintext change | L157 |
| C195 | ⭐⭐⭐ **THE FSK2a DECODER IS IN FIRMWARE AND REPRODUCES THE HOST RESULT EXACTLY — AWID's read arm is built, with 22 cross-protocol nulls clean.** `lf_fsk2a.c` pairs adjacent slicer runs into sub-periods, classifies 8 against 10 samples and applies the reference's pulse-count rule; host-compiled through `ctest/cdemod` it returns `010203040506070800` and `aabbccddeeff001100` for the two emulated payloads — byte-identical to what `fskdemod.py` found, from code that shares nothing with it. ⭐ **Its gate is all three of AWID's own checks**: the 8-bit preamble, the preamble again 96 bits later, and odd parity over every nibble of bits 8..95. ⭐⭐ **The slicer is now SHARED rather than duplicated.** `lf_slicer.c` holds the block-mean DC and the level test that the ASK and FSK families both need. That is a direct response to the `framedrift.py` bug: a tool that sliced against a trailing average where the firmware used block means disagreed with it about which captures contained a frame at all. Two decoders claiming to see the same signal must share a front end for the same reason. ⚠ The extraction touched every ASK decode path, so it was regression-tested: **10 of 10 round-trip arms exact and 320 captures word for word**, unchanged | 2 payloads through the shipping decoder + 22 null captures + 320-capture regression | ✓ **22 cross-protocol nulls, 0 false positives**, both directions, including the three ASK protocols and InstaFob | ✓ the shipping C decoder and the numpy/python host decoder agree byte for byte on both payloads while sharing no code | L159 |
| C196 | ⭐⭐⭐ **AWID READS ON DEVICE — 5 of 5, null 0 of 3, 4 of 4 on a changed payload — SO THE FSK FAMILY IS OPEN AND §10'S ORDERING WAS BACKWARDS.** `lf awid read` on Chameleon #1 against the Flipper emulating `010203040506070809` returns payload `010203040506070800` **5 of 5**; with the emulation explicitly stopped, **0 of 3**; with the payload changed to `AABBCCDDEEFF001122`, **4 of 4** returning `aabbccddeeff001100`. The trailing zeros are the format — AWID carries 66 of the 72 bits — and the CLI says so rather than leaving it to be discovered. ⭐⭐ **§10 ranked FSK LAST of four families on the premise that it must reuse the HID/ioProx per-protocol SAADC reader**, where HID's unexplained 15-20% intermittency lives (C45, C47). It does not: this is a decoder handed to the shared capture engine, which carries the BLE-advertising guard those per-protocol readers lack. ⇒ **Measured end to end, FSK was the CHEAPEST family of the three attempted** — one decoder, one evening, no new capture path — against ASK's two false starts (the comparator route, then the drive sweep). ⚠ **Read only.** The writer is DEFERRED, not refused: the Proxmark has `lf awid clone`, so it is verifiable the moment the T5577 goes back in the sandwich — unlike InstaFob's, which nothing here can ever check (C185) | 5 + 3 + 4 device reads across 2 payloads and 1 null | ✓ **the null is 0 of 3 after an explicit stop**, and the return leg carried a DIFFERENT payload the reader tracked | ✓ the source is the Flipper's own AWID encoder, sharing no code with ours, and the shipping decoder agreed byte for byte with an independent python demodulator beforehand (C195) | L160 |
| C197 | ⭐⭐ **PARADOX AND PYRAMID DECODE ON THE FIRST ATTEMPT — one `lf_fsk2a_format_t` entry each, exactly as predicted.** Both share AWID's tones and pulse counts, so neither needed a line of new demodulator. From Flipper emulations: Paradox returns `0f55565559555a5565556655` and Pyramid `000101010101010101010161201910bc`, both through the shipping decoder, with **26 cross-protocol nulls clean** and the 320-capture regression unchanged. ⭐ **Their gates are structural rather than a preamble**, which is what makes short preambles survivable: Paradox has 8 fixed bits plus **44 independent alternating-pair checks** over bits 8..95; Pyramid has a 24-bit preamble, the frame repeating at 128, AND a CRC-8 (poly 0x31, input and output reflected) over 13 bytes. ⭐⭐ **Pyramid's CRC parameters were transcribed and then CHECKED against a real capture before shipping** — the gate passing on a genuine signal is what proves the transcription, because a wrong CRC rejects everything and reads as "the protocol does not work here" (C172, Gallagher). ⚠ **Host-verified only**; the device arms are not wired and nothing here claims hardware | 2 captures + 26 null captures + 320-capture regression | ✓ **26 cross-protocol nulls, 0 false positives**, both directions, including AWID against the new captures and both new formats against AWID's | ✓ Pyramid's CRC and Paradox's 44 pair-checks both PASSED on real signals, which is what distinguishes a correct transcription from one that merely compiles | L161 |
| C198 | ⭐⭐⭐ **PARADOX AND PYRAMID READ ON DEVICE — three FSK protocols now, and the second and third cost one descriptor each.** `lf paradox read` returns `0f55565559555a5565556655` **4 of 4** with a **0 of 2** null; `lf pyramid read` returns `000101010101010101010161201910bc` **4 of 4**, and with the payload changed to `AABBCCDD`, `000101010101010101010157bc67755e` **3 of 3** — the 24-bit preamble and the fixed body identical, the tail and CRC changing, which is exactly what a payload change should look like. ⭐ **Both match the host decode byte for byte**, and both gates held on device: Paradox's 44 alternating-pair checks and Pyramid's CRC-8. ⭐⭐ **This is the claim §10 made for grouping by modulation, and the FSK family is the only one that has fully honoured it**: after AWID, neither protocol needed a line of demodulator — just a preamble, a frame length and an `accept`. The ASK family needed a new renderer, a drive sweep and a bit-rate parameter before its second protocol worked. ⚠ **Read only.** All three FSK writers are DEFERRED until the T5577 returns to the sandwich; the Proxmark can verify each then (`lf awid clone`, `lf paradox clone`, `lf pyramid clone`) | 4 + 2 + 4 + 3 device reads across 3 payloads and 1 null | ✓ the null is 0 of 2 after an explicit stop, and Pyramid's return leg carried a DIFFERENT payload the reader tracked | ✓ both frames match what the host decoder produced from independent captures, and the sources are the Flipper's own encoders | L162 |
| C199 | ⭐⭐ **FDX-A DECODES — the FSK family is complete on read, and its "failure" was my test payload, not the format.** FDX-A is the only protocol here with TWO encoding layers: FSK2a on the wire carrying MANCHESTER inside it, so bits 16..95 are 40 bit-pairs and its 80 carried bits hold 40 real ones. Its gate is the heaviest on the bench — a 16-bit preamble `0x551D`, the frame repeating at 96, **40 Manchester pair checks**, and odd parity on each of the five decoded bytes. ⛔ **The first attempt decoded perfectly and was rejected, correctly.** The Manchester layer was clean (0 illegal pairs of 40) and the bytes came back as exactly the emulated `01 02 03 04 05` — but two of them have EVEN parity, because **bit 7 of each FDX-A byte IS the odd-parity bit**, so an arbitrary 5-byte string is not a valid credential. Re-emulating `0102830485`, which satisfies the rule, decodes and passes: payload recovered exactly. ⭐ **The diagnosis came from printing the intermediate state, not from tuning**: the preamble was found at bits 37/133/229 with spacing exactly 96, which localised the rejection to `accept` before anything was changed. ⭐ **AND IT READS ON DEVICE: 4 of 4, null 0 of 2**, payload `0102830485` recovered exactly — so all four FSK protocols now read on hardware. ⚠ **26 cross-protocol nulls clean in both directions**, including the other three FSK protocols against FDX-A's capture and FDX-A against theirs | 2 payloads + 26 null captures + 320-capture regression | ✓ 26 nulls, 0 false positives, both directions | ✓ the invalid-parity payload is itself the null for the parity gate — it proves the check rejects a frame that is otherwise perfect | L163 |
| C200 | ⛔⛔ **A MISSING COMMAND ON `PATH` MASQUERADED AS A DEAD DEVICE FOR FOUR HOURS — the third instrument failure of this campaign and the most expensive.** Every check of the form `nrfutil device list | grep -c nordicDfu` returned 0, which I read as "no device entered DFU". `nrfutil` is not on this shell's `PATH` at all: the command failed and `grep -c` dutifully counted zero lines. With the real path — `.tools/bin/nrfutil`, which the flash script resolves explicitly and I did not — the same trigger shows **exactly one nordicDfu device**, and `nrfutil device program --traits nordicDfu` flashes the intended unit unambiguously. ⭐ **The targeted flash procedure works and is now proven**: trigger DFU on the chosen `/dev/cu.` port, confirm exactly ONE device in DFU (the other's normal port still present is the cross-check), then call nrfutil directly rather than the script, whose own the DFU trigger in `resource/tools/` picks whichever port the OS lists first. ⚠ **What WAS real: Chameleon #1 genuinely was wedged** — `hw mode` returned `CMD 1035 exec timeout`, which is an independent observation and needed the power cycle. The false part was the inference chain built on top of it. ⇒ **Three times now an instrument that could not express an outcome has reported its absence** — `flipper.py`'s one-word name matcher (C178), `framedrift.py`'s mismatched slicer (C180), and now a command that is not installed on the search path. ⭐ The common defence is the same each time: check the instrument against a KNOWN-GOOD case before trusting a negative result from it | 1 root cause, 4 hours lost, 1 successful targeted flash | ✓ the successful flash is the positive control — same trigger, same device, real path, one DFU device and a working result | ✓ `hw mode`'s timeout was independent of nrfutil and is what correctly identified the wedged device | L164 |
| C201 | ⭐⭐⭐ **THREE FSK READ ARMS UPGRADED FROM AN EMULATION TO A REAL TAG — 13 reads, every frame byte-identical to the Proxmark's own.** With the T5577 back in the sandwich, the Proxmark wrote each credential and our reader read the resulting tag: **AWID 5 of 5** (`011d81711dd1181111111111`, FC 12 / card 3456), **Paradox 4 of 4** (`0f55555695596a6a9999a59a`, FC 96 / card 40426), **Paradox and Pyramid each on a wiped tag**, **Pyramid 4 of 4** (`00010101010101010101016eb35e5da4`, FC 123 / card 11223). ⭐ **This is the evidence distinction the operator drew (C179), applied to the read side**: those three were previously verified only against a Flipper EMULATION, which is one decoder's rendering of the protocol; a T5577 written by the Proxmark is actual tag behaviour. Both agree exactly, which also retires any worry that the Flipper's emulation differed from a real tag for these formats. ⛔ **FDX-A CANNOT be upgraded: the Proxmark has no FDX-A clone command** — `lf fdx` is FDX-B, a different protocol — so like InstaFob it stays verified against a Flipper emulation alone, and the grid must not imply otherwise | 13 device reads across 3 protocols and 3 freshly written tags | ✓ each tag was written on a WIPED T5577 and confirmed by the Proxmark's own reader before ours was asked | ✓ the Proxmark is an independent writer AND reader; our frames match its raw output byte for byte in all three cases | L165 |
| C202 | ⭐⭐ **THE T5577 BLOCK FORM EQUALS THE AIR FRAME FOR ALL THREE FSK2a PROTOCOLS — asked per protocol, because the same question has the opposite answer for Keri.** Each of AWID, Paradox and Pyramid was cloned by a Proxmark and its blocks dumped: AWID `00107060 / 011D8171 1DD11811 11111111`, Paradox `00107060 / 0F555556 95596A6A 9999A59A`, Pyramid `00107080 / 00010101 01010101 0101016E B35E5DA4`. Every block form is **byte-identical** to the air frame our own decoder reads off that same tag, so the writer is a straight big-endian transcription. ⛔ **Keri's is not**: its block form is `(id << 3) | 7`, three bits out of phase with its air frame, and emitting the wrong one of the two put a stable, confidently-decoded, WRONG credential on the air 6 times out of 6 (C160). ⇒ One shared `fsk2a_t55xx_blocks()` serves all three, and a fourth FSK2a protocol does not get to use it until its own dump has been read. ⚠ The config words are the second measured constant: AWID and Paradox share `00107060` exactly, Pyramid differs only in the block-count field (4 blocks for its 128-bit frame) | 3 protocols, 1 clone-and-dump each | ⚠ **no null applies — this is a transcription identity, not a detection.** The CONTROL is Keri, the protocol where the same measurement comes back different, which is what makes the question non-vacuous | ✓ two independent sources per config word (`lf t55xx detect` on the real tag, and the Proxmark client's own header) and two per block form (the Proxmark's dump and our decoder's raw off the same tag) | L166 |
| C203 | ⭐⭐⭐ **THREE FSK WRITE ARMS VERIFIED THE STRONG WAY — we write the T5577, the PROXMARK reads it, 13 of 13, and the block dumps are identical to the Proxmark's own clone of the same credential.** Per protocol: the Proxmark cloned a reference credential and its blocks were dumped; the tag was **wiped** and the null confirmed; our writer wrote the frame from Chameleon #2; the Proxmark read it back. **AWID 5/5** (FC 34 / card 5678, `011db218271bd81111111111`), **Paradox 4/4** (FC 7 / card 12345, `0f55555556a5a555a965569a`), **Pyramid 4/4** (FC 45 / card 6789, `000101010101010101010164d0d51576`). ⭐ **Every credential is DIFFERENT from C201's**, so a stale tag cannot produce a pass — and the wipe means a silently-failed write reads as nothing rather than as the old value. ⭐ The dumps match the reference clone **block for block including block 0**, so this is identity with the reference, not merely a frame some reader happens to accept. ⭐ Bonus control: three FRESH credentials re-confirm C202's block-form-equals-air-frame result, taking it from n=3 to n=6. ⛔ **No FDX-A write arm, and that is a refusal rather than a deferral** — `lf fdx` is FDX-B, so nothing here can read an FDX-A tag we wrote and the arm would certify itself (C185) | 13 Proxmark reads of our writes, across 3 protocols and 3 wiped tags | ✓ each tag WIPED first and the protocol's own reader confirmed to find nothing before our write | ✓ the Proxmark is an independent reader AND the source of the reference blocks; our written tag is byte-identical to its own clone | L166 |
| C204 | ⭐⭐⭐ **GPROXII DECODES ON THE HOST, 96 of 96 BITS EXACT — and it needed a FOURTH decode path, because the level path this bench's whole ASK family runs on is physically unable to read RF/64.** ⛔ **Measured, not asserted: the LF front end is AC-coupled with a time constant of about 27 samples** — a step to 14112 decays to 4664 in 30 samples. At RF/32 a half-bit is 16 samples so the level survives to be sliced, which is why Gallagher, Securakey and Noralsy read. At RF/64 a half-bit is **32 samples, longer than the time constant**, so the level is gone before the half-bit ends — and sampling half-bit centres finds NO frame at any phase, low-pass width or inversion. ⭐ **The fix is to ask whether the envelope STEPPED, never what level it sits at.** Biphase puts a transition at every bit boundary by construction, so the boundaries are a self-calibrating amplitude reference and the only question per bit is whether the middle stepped too: `bit = 1` iff a mid-bit transition. One decision per bit, against a threshold this capture supplies itself. ⭐ **Graded by bit errors on a known credential, not pass/fail (M33)**: drive 7 **96/96**, drive 6 95/96, drive 4 84/96, drive 2 83/96 — which is C182's drive dependence appearing again, on a fourth protocol and a different decoder. ⚠ Drive 2 is the STOCK field and it clips: p5/p95 amplitude 4/16364 against drive 7's 2304/13536 | 4 captures, one credential, scored by bit error | ✓ the level path is the null and it is BLANK — no frame at any of 3 low-pass widths × 32 phases × 2 pairings × 2 inversions | ✓ the credential and its 96-bit frame come from the Proxmark's own `lf gproxii clone`; the frame period was independently measured by autocorrelation at **exactly 6144 samples** = 96 bits × RF/64 | L167 |
| C205 | ⭐⭐⭐ **THE CROSS-PROTOCOL NULL CAUGHT A READER THAT WOULD HAVE CLAIMED A SECURAKEY TAG WAS A GPROXII — STABLY, 3 CAPTURES OUT OF 3, SO TWO-AGREEING-STACKS WOULD HAVE CONFIRMED IT RATHER THAN REJECTED IT.** The firmware biphase decoder reads the real GProxII tag **4 of 4 exact** (`fac2a38c2b081af0210b12c2`). But gated only on what the Proxmark AND Momentum each check — a 6-bit preamble plus 18 always-zero spacers, 24 bits of 96 — it also produced frames on **Securakey 4/4, Noralsy 4/4, Gallagher 4/8, InstaFob 2/3, NexWatch 1/4**. ⭐ **The fix is the format's own length field**: strip the spacers, take the XOR key from the first byte, un-XOR the second, and its top six bits are the length — 26 on the bench tag, with key 141, which are exactly the `--fmt 26 --xor 141` we cloned, so the descramble is confirmed against values WE chose rather than by self-consistency. Both references compute this and both know only 26 and 36; neither REJECTS on it, because a single-protocol demod has nothing to be confused with and a multi-protocol reader does. ⭐ **With the length gate: Securakey 0/4, Noralsy 0/4, Gallagher 0/8, InstaFob 0/3, Keri 0/8, Indala224 0/4, IDTECK 0/16, AWID 0/6, PAC 0/4, empty 0/5, and 0 of 320 front captures.** ⚠ **NexWatch 1 of 4 SURVIVES and is reported rather than rounded away** — but it is sporadic where Securakey's was stable, which is the shape two-agreeing-stacks does catch. ⚠ The cost, said plainly: a genuine GProxII whose format length is neither 26 nor 36 would be rejected here | 386 captures across 11 protocols, before and after | ✓ the null IS the measurement here — 11 false positives before the length gate, 1 after | ✓ the true frame, its xor key and its format length all come from the Proxmark's own `lf gproxii clone` arguments | L168 |
| C206 | ⛔⛔ **THE SAME DECODER READS THE SAME TAG EXACTLY ON THE HOST AND WRONG BY ONE BIT ON THE DEVICE — open, and the arm is NOT graded.** `cdemod` decodes five `lf sniff --drive 7` captures taken FROM THIS DEVICE 5 of 5 byte-exact; the device's own reader returns `...12c6` where the tag holds `...12c2`, a persistent single-bit error at frame bit 93, on 7 reads of 8. ⚠ That bit falls outside format 26's facility and card fields, so the credential still prints correctly — which is why this is recorded as a FAILURE rather than a 7-of-8 success. Before the two fixes below, one read in 8 put the error INSIDE the card field and printed Card 1321 for a tag holding 1337. ⭐ **Two causes found and fixed, neither sufficient.** (1) `lf_ask_read`'s drive sweep starts at stock drive 4 and stops at the FIRST success — and drive 4 succeeds here, with errors, where the host measured drive 7 at 96/96 and drive 4 in the low 80s (C204); GProxII is now pinned to drive 7, not swept. (2) `raw_read_samples` returns SUCCESS on a capture that dropped ring samples — it checks only that the buffer filled, and a SPLICED buffer fills completely; the shared engine now discards those. ✅ Wrong-card reads went to 0 of 8; ⚠ that is n=8 against a ~12% rate and is NOT evidence the failure is gone. ⛔ **The bit-93 error survives both.** Ruled out by measurement, not argument: SAADC sample phase (8 values, all exact), settle time (6 values, all exact), sample width (the reader keeps the full 14 bits), and a stale host binary (rebuilt from scratch, still exact). ⇒ Something in the reader path still differs from the sniff path and it is not yet identified | 8 device reads + 19 host captures across 3 swept parameters | ✓ the host decode of captures from this same device IS the control, and it is exact | ✓ the tag's true frame is the Proxmark's own clone, `fac2a38c2b081af0210b12c2` | L169 |
| C207 | ⭐⭐ **THE GPROXII WRITE ARM IS VERIFIED WHILE ITS READ ARM IS NOT — our write, the PROXMARK's read, 4 of 4, block dump identical to the Proxmark's own clone.** Wrote `f84602a46119d4a114211046` (FC 45 / card 6789 / xor 200) from Chameleon #2 onto a WIPED tag whose null was confirmed; the Proxmark reads it back 4 of 4 and its blocks are `00150060 / F84602A4 6119D4A1 14211046`, byte-identical to what `lf gproxii clone` wrote for the same credential. ⭐ A different credential from the read tests, so a stale tag cannot pass. ⛔ **And our own reader reads that same tag 0 of 6** — the failure is now total where on the previous credential it was one bit, so it is credential-dependent rather than marginal. ✅ The host decodes `lf sniff` captures of this same tag 3 of 3 EXACT, taken by this same device minutes later: the decoder is right, the reader path is not (C206). ⭐ **Regression check on the spliced-capture discard: AWID still reads 5 of 5 exact**, so that change did not break the arms that were working | 4 Proxmark reads, 6 device reads, 3 host captures, 5 AWID regression reads | ✓ tag WIPED first and `lf gproxii reader` confirmed to find nothing before our write | ✓ the Proxmark is an independent reader AND the source of the reference blocks | L170 |
| C208 | ⭐⭐⭐ **C206'S MYSTERY IS THE PHASE ROTATION ORDER, NOT THE CAPTURE PATH — GProxII decodes on device at SAADC phase 112, which is SEVENTH of eight in the rotation, so the 3-second budget never reaches it.** With the budget raised to 8s and the device left in reader mode, it returns `f84602a46119d4a114211046` EXACTLY, at phase 112, after 4 to 8 tries. ⛔ The default read spends its whole budget in phases 20, 12, 28, 36, 44 and 16 — eight captures each — and either times out (0 of 6) or accepts a marginal decode from an early phase (the persistent bit-93 error). ⭐ **That explains every symptom in C206 at once**, including why the host was always exact: `lf sniff` captures at ONE phase and the host decoder has unlimited time, so it never had to reach the seventh entry. ⚠ **The rotation's own comment predicted this**: "THIS MAP IS ONE TAG, ONE UNIT, TWO COUPLING GEOMETRIES... it could move". It was measured on an Indala tag and 112 is in it only as SPREAD insurance, chosen for being far from the rest rather than for rank. ⚠ A second difference is measured and NOT explained: the reader path's edge energy is ~5050 where the same decoder on sniff captures reports 2300-2900, so the two paths really do see about twice the amplitude — it is not fatal, since the read succeeds, but it is not understood | 5 device reads at an 8s budget | ✓ two of the five still failed and reported their energy, which is what distinguished a bad capture from an unreached phase | ✓ the frame matches the Proxmark's own clone of the credential WE chose | L171 |
| C209 | ⛔⛔ **THE READER PATH MEASURES 1.8x THE SIGNAL THE SNIFF PATH DOES ON THE SAME TAG, SAME DEVICE, MINUTES APART — and that, not timing or phase order, is what stops the GProxII read.** Reader path: mean bit-boundary step **~5070 ADC counts on every failing read**, stable to a tenth of a percent. Sniff path, same tag, same minute: **2819**, and the host decodes it exactly. ⚠ A constant that large on every boundary is not tag envelope. ⛔ **Three configurations measured, best is 3 of 10**: phases {112,120,104,96,20,0} × 8 tries × 3s gave 3/10 exact and 0 wrong; {104,112,96,120,88} × 4 tries gave 0/12; {104,112,120,96,88,20} × 8 tries × **6s** gave 0/12. ⭐ **The 6s run is the one that matters: doubling the budget to reach five sample phases recovered nothing, which rules TIME out** — and C208's explanation with it. ✅ Ruled out by measurement, not argument: SAADC phase (16 values over 0..120, ALL decode on sniff), settle (6 values), gain (5 values), sample width, spliced captures, a stale host binary, and the bench itself (checked immediately after the 0-of-12: the Proxmark reads the tag and our own sniff decodes it). ✅ Both paths share `capture_begin`, `init_saadc_hw` and the same phase and gain setters, so the configuration is identical by inspection. ⇒ **Open. The read arm ships as research and is NOT graded; the write arm is unaffected and is verified (C207)** | 34 device reads across 3 configurations, 27 host captures across 3 parameter sweeps | ✓ the 6s arm IS the null for the timing hypothesis, and it came back blank | ✓ the Proxmark reads the tag throughout, and our own sniff capture decodes exactly on the host at every point in the sequence | L172 |
| C210 | ⭐⭐⭐ **THE READER'S OWN CAPTURES ARE FINE AT DRIVE 7 — SO THE READER IS NOT RUNNING AT DRIVE 7.** A new command runs the READER's capture path and hands the samples back undecoded, which is the one measurement `lf sniff` cannot make because sniff is the other path. Its captures, decoded on the host: **drive 7 phase 0 → exact, edge 2178; drive 7 phase 112 → exact, edge 2944; drive 4 phase 0 → NO FRAME, edge 7514.** ⭐ That kills the 1.8x mystery of C209 at a stroke: the reader path is not different from the sniff path at all, it is the same path at a different FIELD STRENGTH. An edge of 5000-7500 is the signature of drive 4, and the failing `lf gproxii read` reports ~5070. ⛔ **So `gproxii_read`'s `lf_125khz_radio_drive_set(7)` is not reaching most of its captures.** The probe sets the drive once and takes ONE capture and is clean; the real read sets it once and takes up to 40, each with its own `capture_begin`/`capture_end` cycle — and the two-agreeing-captures rule needs two good ones, which would explain a 3-in-10 success rate rather than none. ⚠ **Hypothesis, not yet confirmed**: the next test is setting the drive before EVERY capture rather than once per read | 3 probe captures at 2 drives and 2 phases | ✓ drive 4 IS the null and it reproduces the failure exactly, edge and all | ✓ the host decoder is the same binary that decodes sniff captures, and the frame matches the Proxmark's clone | L173 |
| C211 | ⭐⭐⭐ **THE SECOND BACK-TO-BACK CAPTURE IS DEGRADED, AND EVERY ONE AFTER IT — reproduced deterministically, and it is the whole of C206 through C210.** The reader-capture probe gained a `repeats` option: take N captures back to back, return the LAST. Same drive, same phase, same tag, one command: **repeats=1 → exact, edge 2266. repeats=2 → WRONG frame, edge 5033. repeats=4 and 8 → same wrong frame, edge ~5030. repeats=16 → badly wrong, edge 5027.** ⭐ 5030 is exactly the ~5047 every failing `lf gproxii read` reports, so the two are the same phenomenon. ⛔ **This retires the drive hypothesis (C210) and the phase-order one (C208) together**: the drive is re-asserted before every capture in this test and the phase is fixed at 112, and the degradation happens anyway. It is not the field strength and it is not which phase — it is BEING THE SECOND CAPTURE. ⚠ The two-agreeing-captures rule makes it fatal rather than merely lossy: capture 1 is clean and captures 2..N agree with each other on something WRONG, so the rule confirms the wrong answer or finds no pair at all. ⭐ It also explains why every other reader here is unaffected — they decode at RF/32 or on phase, where a degraded capture still slices; this decoder reads amplitude STEPS, which is what changes. ⇒ Next: whether a longer settle before each capture recovers it. `capture_begin` waits 2ms for the antenna AND the tag, and 2ms after a capture is not 2ms after a cold start | 5 probe runs at 1, 2, 4, 8 and 16 repeats | ✓ repeats=1 IS the control and it is exact every time it has been run — 14 of 14 across two sessions | ✓ the wrong frames still pass the 24-bit gate and the format-length check, which is why the device reported them confidently | L174 |
| C212 | ⭐⭐ **THE DEGRADED CAPTURE IS A CLIPPED ONE — and three fixes aimed at the wrong thing were tried and refuted.** Comparing the samples directly: a clean capture runs min 472 / max 14176, a capture taken second in a back-to-back run runs **min 0 / max 16380** — both rails, on a 14-bit ADC whose full scale is 16383. The percentiles are otherwise identical (p5 2332 vs 2328, median 3648 vs 3644, p95 13908 vs 13916), so it is not a level shift or a gain change: it is the PEAKS being cut off. ⭐ That is the mechanism, and it is specific to this decoder: it thresholds on step MAGNITUDE, which is exactly what clipping destroys — where the ASK family survives clipping because Manchester keeps the SIGN (C169, C171). ⛔ **Refuted, each by measurement:** (1) settle time — swept 0, 5, 10, 20, 50 and 100ms before every capture, all still degraded, so it is not the antenna or the tag settling; (2) a MEDIAN threshold instead of the mean — robust to clipped outliers in principle, and it broke the CLEAN captures too (2 of 4 bench captures wrong); (3) a LOCAL threshold from the two bracketing boundaries — 2 of 7 exact against the global mean's 7 of 7. ⚠ `capture_end` does call `stop_lf_125khz_radio()`, so the field IS dropped between captures; whatever stays charged, stays charged across that | 6 settle values, 2 threshold variants over 11 captures | ✓ the clean capture is the control in every comparison and it decodes exactly | ✓ the sample statistics come from the reader's OWN buffer, not a sniff, via the C210 probe | L175 |
| C213 | ⭐⭐⭐ **FIXED, AND THE GPROXII READ ARM IS VERIFIED: 12 of 12 EXACT, 0 WRONG, on a credential the Proxmark wrote and we chose.** The fix is a **field-off gap between captures**, and it is the one thing that worked after settle, threshold shape, drive, phase order, budget and try count had each been tried and refuted. Measured with the reader-capture probe, four captures back to back returning the last: **gap 0 and 5ms → wrong frame; 20 → wrong; 25, 30, 40 → exact but 35 came back one bit out; 50, 150, 250 → exact every time.** The threshold is around 25ms and soft near it, so the shipped value is **50ms, 2×**. ⭐ On device: `lf gproxii read` goes from **3 of 10** to **12 of 12 exact, 0 wrong**, at phase 112 in 2-3 tries. ⚠ **What is charging is NOT established** — `capture_end` already stops the field, so it is something holding charge across that: the tag's own storage, or the LF amplifier's AC coupling. What IS established is the symptom chain: too soon after another capture → CLIPS at both rails (C212) → step magnitudes saturate → this decoder, which thresholds on magnitude, fails. ⚠ **It costs latency and only this protocol pays it**: 114ms capture + 50ms gap means a 3s budget buys ~14 captures rather than 20. Every other reader passes 0 and is untouched | 12 device reads + 10 probe runs across 9 gap values | ✓ **cross-protocol nulls re-run on the shipping decoder**: Securakey 0/4, Noralsy 0/4, Gallagher 0/8, InstaFob 0/3, Keri 0/8, Indala224 0/4, IDTECK 0/16, AWID 0/6, PAC 0/4, empty 0/5, front 0/320 — and NexWatch 1/4, unchanged and still reported | ✓ the Proxmark wrote the credential and reads it back; our frame matches its raw byte for byte | L176 |
| C214 | ⭐⭐ **FDX-B DECODES ON THE HOST 4 of 4, AND ITS CROSS-PROTOCOL NULLS ARE COMPLETELY BLANK — 0 of 386, better than GProxII's.** The biphase decoder carried the second protocol as a format entry: 128 bits at RF/32, inverted, header `00000000001`, frame `00339a080402079f8040797788040201` (country 999 / national 1337). ⛔ **The 24-bit structural gate alone is WORTHLESS here and failed on the first capture tried**: a header of ten zeros and a one followed by 117 ONES satisfies every control bit trivially, and a drive-7 capture returned exactly that — `003fffffffffffffffffffffffffffff`. ⭐ **The real gate is a CRC-16 whose parameters were SOLVED against the bench frame, not copied**: the Proxmark reaches it through `crc16_fast(d, n, 0x0000, false, true)`, which reads as unreflected input and does NOT reproduce the tag's CRC; a search over poly / init / reflection / xor returns exactly one match, KERMIT (0x1021, init 0, reflected both ways), payload `39050000c0f90080` -> 0x1ED3, the value the tag carries. ⚠ **The CRC covers the first 8 bytes ONLY and the last 40 bits are unprotected** — already seen to matter: one capture in four returned `...88042a01` for a tag holding `...88040201`, gate satisfied. The Proxmark's own source remarks on it. ⚠ **Drive 4, not 7** — where GProxII needs 7 and decodes at nothing else; field strength is per protocol, a third time (C182, C204). ⛔ Capture length measured: 4608-8448 → 2 of 4, **8704-12288 → 4 of 4, and the FULL 14336 → 3 of 4** — the first constant here where longer is WORSE, so 10240 sits mid-plateau | 4 tag captures, 4 drives, 13 truncation lengths, 386 null captures | ✓ **nulls blank across 12 protocol sets, 5 empty and all 320 front captures** — no exceptions, unlike GProxII's NexWatch 1/4 | ✓ the frame, its payload and its CRC all come from the Proxmark's own clone of a credential we chose | L177 |
| C215 | ⭐⭐⭐ **FDX-B COMPLETE ON HARDWARE, BOTH DIRECTIONS — read 6 of 6 on device, write 4 of 4 by the Proxmark — and it needed NONE of GProxII's special handling.** Read: `lf fdxb read` returns `00339a080402079f8040797788040201`, country 999 / national 1337 / animal, 6 times of 6. Write: our writer put `0031bd39740201f8804039b518040201` (country 124 / national 7654321) on a WIPED tag whose null was confirmed, and the Proxmark reads it back 4 of 4 with **CRC-16 ok** and block 0 `00098080`, identical to its own clone. A different credential from the read tests, so a stale tag cannot pass. ⭐⭐ **The second protocol of a family is where you find out whether it IS a family, and this one is — but not by inheritance.** Measured rather than assumed: FDX-B reads at the STOCK drive where GProxII needs 7 and decodes at nothing else, and four back-to-back captures with NO gap decode exactly where GProxII's second capture is already wrong (C211). ⇒ it uses the plain shared reader and GProxII's overrides stay GProxII's. ⚠ Its config is DIPHASE where GProxII is BIPHASE — one bit apart, and the wrong one writes a tag nothing here could read | 6 device reads, 4 Proxmark reads, 2 credentials | ✓ tag WIPED and `lf fdxb reader` confirmed blank before our write | ✓ the Proxmark is an independent reader AND the source of the reference blocks; the host field decoder returns the country, national and animal values WE chose | L178 |
| C216 | ⭐⭐ **THE FIRST FSK2a EMITTER ROUND-TRIPS EXACTLY ON THE HOST — emitter to air to decoder, both halves the shipping firmware.** AWID's 96-bit frame `011db218271bd81111111111` emits as 543 PWM entries and our own decoder reads it back byte-exact. ⭐ **FSK2a emulation is one PWM entry per TONE PERIOD, not per bit**, which is the whole difference from the ASK emitters: a 0 is six entries of `counter_top` 8 (RF/8) and a 1 is five of 10 (RF/10). ⚠ **Those are 48 and 50 carrier cycles — NOT the same length** — so anything on this family that assumes a constant bit period is wrong by up to 4%. ⚠ It is also the first emitter here whose ENTRY COUNT depends on the data (543 for this frame, 480-576 possible), so the sequence length is computed per call where every ASK emitter's is fixed at compile time. ⭐ The tone-to-bit mapping came from the shipping DECODER's own `pulses_short`/`pulses_long`, not a datasheet — emitting what our reader counts is exactly what the round trip tests. ✅ `render_ask()` renders it unchanged, and that is not a shortcut: it emits `counter_top` samples per entry, first half high, which is one Manchester bit for an ASK entry and one tone cycle for an FSK one. The peripheral really does treat them the same | 1 emitter, 1 frame, host only — NOT yet on hardware | ✓ **sensitivity proven by a deliberate break**: swapping the two pulse counts gives 513 entries and NO DECODE | ✓ the decoder half is the same binary that reads real AWID tags 5 of 5 (C201) | L179 |
| C217 | ⛔⛔ **THE AWID EMULATION IS SILENT TO THE FLIPPER — 0 of 6 — AND THE POSITIVE CONTROL PASSES 6 of 6 MINUTES LATER ON THE SAME RIG.** Chameleon #1 emulating AWID `011db218271bd81111111111`: the Flipper reads nothing, on both its ASK and PSK arms. Switch the SAME slot to Gallagher `7feaa31e76d86c6d868cc249` and the Flipper reads it **6 of 6** with the right credential. ⇒ the rig, the coupling and the emulation path are all working; the FSK emission specifically is not being read. ⭐ **This is why F05's no-positive-control rule exists**: the first result on its own was a null that could equally have meant a dead rig, and `flipper.py` said so in as many words before the control was run. ⚠ **And it does NOT refute C216**: the emitter round-trips through our own shipping decoder exactly, so what the PWM is asked to emit decodes. What is not established is whether the PERIPHERAL emits it — this is the first emitter here to use `counter_top` 8 and 10 where every other uses 32, 40 or 64, and the first whose sequence length is computed at runtime. ⛔ **Nothing on this bench can capture rig A's emission**: both Chameleons are on separate rigs and the Flipper has no raw-capture path here, so the next step needs either the two Chameleons facing each other or a scope | 6 Flipper reads + 6 control reads, same rig, same session | ✓ **the control IS the null's null** — Gallagher 6/6 on the same slot minutes later | ✓ the emitter's own round trip against the shipping decoder is exact, and a deliberate break fails it (C216) | L180 |
| C218 | ⚠⚠ **THE AWID EMITTER DOES PRODUCE BOTH TONES — which CORRECTS C217's lead — but the long one is not clean, and this measurement has NO CONTROL so it is reported as a lead, not a finding.** 10,784 pulse periods from the Flipper's raw capture of our emission: **~62-65 carries 7,716 of them (mode 63) and ~73-79 carries 1,678 (spread, centred ~76)**. The ratio 76/63 = 1.21 against the 10/8 = 1.25 the protocol wants, so both tones ARE there and roughly where they belong. ⛔ **C217's single-tone reading is retracted**: it was 24 pairs eyeballed out of 72,518 and the second tone simply was not in them. ⭐ What the histogram DOES show is an asymmetry: the short tone is TIGHT (63 ± 1, 74% of its mass on one value) and the long one is BROAD (73-79 with no dominant value where 79-80 is expected). A tone that measures 2-8% short and scattered is a plausible reason for a decoder to reject it. ⛔⛔ **AND THE CONTROL COULD NOT BE TAKEN**: the Gallagher run that should have calibrated the instrument returned ZERO periods twice, on a protocol the same rig reads 6 of 6. So the instrument is uncalibrated and these numbers cannot be promoted past a lead. ⚠ **`rfid raw_analyze` over the CLI is NOT a usable instrument here and should not be tried again** — it has now left the Flipper's CLI unresponsive twice, once badly enough to drop the port | 10,784 periods, 1 protocol, NO control | ⛔ **the control is BLANK and that is the finding's own limit** — Gallagher 0 periods twice where it reads 6/6 | ⚠ none available: the Flipper is the only reader that can see rig A's emission | L182 |
| C219 | ⭐⭐⭐ **THE FLIPPER'S OWN THRESHOLD SAYS WHY IT REJECTS OUR AWID: 909 ANOMALOUS PERIODS LAND JUST ABOVE ITS SHORT/LONG BOUNDARY, AND THEY OUTNUMBER THE GENUINE LONG TONES 909 TO 613.** Read out of Momentum's source rather than guessed: `protocol_awid.c` builds `fsk_demod_alloc(64-20, 6, 80+20, 5)`, so a period counts only inside **[44, 100)** and is called LONG at or above **mid = 72**. ⭐ Calibrating the Flipper's time unit from our own emission — 8 carrier cycles (64us) measures 63 units — puts its true long tone at **~78.7 units**, and the histogram's **77-79 cluster (613 periods)** is exactly that. ⛔ **But 73-75 carries 909 periods and nothing should be there.** They sit above mid=72, so every one is counted as a LONG pulse; with `hi_pulses = 5` a handful of spurious longs is enough to corrupt the bit the group decodes to. ⚠ **Mechanism is a hypothesis and is labelled one**: a hybrid period at each tone-group boundary, as would happen if the PWM applies a new `counter_top` one period late. The count fits — ~96 group boundaries per frame over ~17 captured frames is the right order — but nothing here has confirmed the peripheral behaves that way. ✅ What IS established: AWID is registered in Momentum with `LFRFIDFeatureASK`, so `rfid read ask` does try it, and the protocol's own acceptance window is what our emission falls foul of | 10,784 periods, 1 protocol, no control (C218) | ⚠ inherits C218's missing control — the WINDOW arithmetic is from source and is solid, the period counts are not calibrated | ✓ the thresholds are Momentum's own constants, not our reading of the waveform | L183 |
| C220 | ⭐⭐⭐ **MOMENTUM'S OWN DEMODULATOR DECODES OUR EMITTER'S IDEAL OUTPUT EXACTLY — so what the emitter ASKS FOR is not the problem, and the fault is what the peripheral EMITS.** Two explanations survived C217 and no instrument on this bench separated them: (a) the PWM does not emit what the emitter asks for, or (b) what it asks for is not what the Flipper accepts. ⭐ **(b) is decidable without hardware**: render the emitter's PWM entries as the level/duration pairs they describe, with NO hardware artefacts, and run Momentum's `fsk_demod_feed` over them. `momdemod.py` transcribes that function and AWID's own `fsk_demod_alloc(64-20, 6, 80+20, 5)`, and our ideal emission yields **383 bits containing `011db218271bd81111111111` exactly**. ⇒ **(b) is dead by elimination and (a) stands.** ⭐ It also corroborates C219 from the other side: if the design is right and the emission is rejected, the 909 anomalous periods at 73-75 are hardware artefacts rather than a design error — which is what C219's one-period-lag hypothesis predicted. ⚠ **A transcription, not a resemblance**, and deliberately: a demodulator that merely resembled the reference would prove nothing about it, which is what `framedrift.py` cost a day to learn (C180) | 4 frames rendered ideally, 383 bits | ✓ the demodulator is Momentum's algorithm and its constants, transcribed from `fsk_demod.c` and `protocol_awid.c` | ✓ the emitter half is the SHIPPING firmware's own PWM entries, the same ones ctest/roundtrip.c renders | L184 |
| C221 | ⭐⭐ **THE GPROXII BIPHASE EMITTER ROUND-TRIPS EXACTLY — and TWO deliberate breaks FAILED TO FAIL IT, each for a reason worth having.** 192 PWM entries, two per bit, decoded byte-exact by the shipping biphase decoder. ⛔ **Break 1, inverting which symbol carries the mid-bit transition: still exact.** That is not a broken test — the decoder sweeps inversion by design, because real biphase tags differ in polarity and `lf t55xx detect` reports it per tag. A global convention flip is genuinely indistinguishable. ⛔ **Break 2, removing the bit-boundary transition ENTIRELY: still exact.** This one is a statement about OUR DECODER and it is worth recording: `lf_ask_biphase.c` asks one question per bit — did the middle step — and uses the boundaries only as a threshold reference. Strip them and the mid-bit steps still fall exactly on the 1-bits, so the answer is unchanged. ⚠ **⇒ a signal that is not biphase at all decodes identically**, and what stops that mattering is the format gate (preamble + spacers + format length, C205), not the decoder. ⭐ The emitter uses `counter_top` 32 — the same magnitude the working ASK emitters use, against AWID's 8 and 10. If it reads on the Flipper where AWID does not, that localises the AWID defect to small counter_tops; if it does not read, the fault is broader than FSK (C217, C220) | 1 emitter, 1 frame, 2 breaks, host only | ⚠ **the sensitivity control is WEAKER than AWID's and that is said rather than glossed** — AWID's arm fails on a pulse-count swap, this one survived both breaks tried | ✓ the decoder half reads real GProxII tags 12 of 12 on device (C213) | L185 |
| C222 | ⛔⛔ **THE COUNTER_TOP HYPOTHESIS IS REFUTED: GProxII uses 32, the same as the working Gallagher, and the Flipper reads it 0 of 6 too — with the control at 6 of 6.** The experiment C221 was built for, run on the same slot in one session: **Gallagher 6/6, AWID 0/6, GProxII 0/6.** ⇒ whatever silences our two new emitters, it is not the magnitude of `counter_top`. ⭐ **One structural difference survives: every emitter that WORKS stores exactly ONE PWM entry per bit, and both that fail store several.** em410x, Gallagher, Securakey, Noralsy, Viking, Jablotron and PAC are all one-entry-per-bit at the 125kHz clock; AWID stores five or six and GProxII stores two. ⚠ That is a correlation across seven working and two failing protocols, not a mechanism — and the obvious candidates do NOT fit: GProxII's entry RATE is identical to Gallagher's (32 ticks each), and sequence LENGTH is not it either, since Indala224 plays 448 entries and reads 11 of 11 (C152). ⚠ A second difference applies to GProxII alone: its entries hold a LEVEL (duty 0 or duty == counter_top) where every working emitter emits a 50% square. ⛔ **Two failures, two possible causes, and this bench cannot see the emission** — the instrument that would separate them is still the one in Needs hands | 18 Flipper reads across 3 protocols, one session, one slot | ✓ **the control is the point of the experiment and it passed twice** — Gallagher 6/6 before and after | ✓ both emitters round-trip exactly through our own decoders, and Momentum's own demodulator accepts AWID's ideal output (C220) | L186 |
| C223 | ⛔⛔ **ENTRIES-PER-BIT IS REFUTED TOO — GProxII rebuilt as ONE entry per bit is still 0 of 6, control 6 of 6 — and ONE structural difference now survives: every working emitter emits a 50% SQUARE IN EVERY ENTRY.** The two-way test C222 set up, run: the emitter was re-expressed as one PWM entry per bit (96 instead of 192) with nothing else changed, and it reads exactly the same nothing. ⭐ **The table after four measurements:** Gallagher 1 entry/bit, top 32, every entry a square → **6/6**; AWID 5-6 entries/bit, top 8 and 10, squares → 0/6; GProxII v1 2 entries/bit, top 32, HELD levels → 0/6; GProxII v2 1 entry/bit, top 64, half squares and half HELD → 0/6. ⇒ entries-per-bit dead, counter_top magnitude dead (em410x uses 64 and works). ⭐ **What is left for GProxII is the held level** — duty 0 or duty == counter_top, no switching inside the entry at all — and it has a mechanism: the emulation modulates by switching a load, and the reader front end this branch MEASURED as AC-coupled with a ~27-sample time constant (C204) cannot see a level that never changes. ⛔ **AWID stays unexplained**: every one of its entries IS a 50% square, at periods of 64 and 80us, comfortably inside that passband. ⇒ two failures, and now only one of them has a candidate cause | 24 Flipper reads across 4 emitter builds, 3 protocols | ✓ **the control passed on every one of the four runs** — Gallagher 6/6 each time, same slot, same session | ✓ every emitter round-trips exactly through our own decoders, and Momentum's own demodulator accepts AWID's ideal output (C220) | L187 |
| C224 | ⛔ **AC COUPLING IS RULED OUT FOR AWID — its emission still decodes through Momentum's own demodulator at every high-pass time constant tried, down to 20 samples.** The mechanism proposed for GProxII's silence (C223) was that a held level cannot modulate a load-switching emulator seen through an AC-coupled front end. AWID holds nothing — all its entries are 50% squares — so the same mechanism should spare it, and the sweep confirms that: rendered to samples, high-passed at tau of 2000, 1000, 500, 200, 100, 50 and 20 samples, then re-thresholded to edges, **the frame is still found at every one**. ⚠ **AND THE MODEL CANNOT TEST THE GPROXII HALF OF THE CLAIM, which is said here rather than quietly enjoyed**: the high-pass decays toward zero and the edge detector thresholds AT zero, so a held level decays asymptotically and never crosses — it survives as a clean long run at any tau. A real comparator has hysteresis and a baseline and a decayed level DOES vanish into it. ⇒ the model says something about emissions made of squares and NOTHING about emissions containing held levels, and the GProxII row it prints is not evidence | 7 time constants × 3 emitters, compute only | ✓ tau=0 is the control row and it decodes, matching C220 | ⚠ the time constant is NOT measured for the Flipper's front end — C204's ~27 samples was the Chameleon's receiver, a different circuit — so this is a sensitivity analysis and its useful output is that AWID survives the whole range | L188 |
| C225 | ⛔ **THE PROXMARK CANNOT STAND IN FOR THE FLIPPER WHILE THE TAG IS IN THE SANDWICH — with Chameleon #2 emulating, it reads NEITHER the emulation NOR the tag.** An attempt to get a second, independent reader onto our emissions without hands: flash #2, emulate **Gallagher** (the protocol the Flipper reads 6/6), and ask the Proxmark. **0 of 4.** And `lf fdxb reader` in the same state also returns nothing, on a tag it reads perfectly the moment #2 goes back to reader mode. ⇒ the two sources modulate the same field and take each other out, exactly as the bench notes predicted when the lift was first requested (C179). ⭐ **Recorded so the next session does not spend a flash cycle rediscovering it**: with the T5577 in place, rig B has no emulation-verification path at all, in either direction. ⚠ It also rules out the boring explanations for the silent emitters on the way: the emulated frame reads BACK correctly through `econfig` for AWID as well as Gallagher, so the buffer plumbing, the tag-type registration and `tag_emulation_load_by_buffer` are all working — the data reaches the emulator | 4 Proxmark reads of the emulation + 2 of the tag, before and after | ✓ **the after-reading IS the control**: the tag reads 124-000007654321 the moment #2 stops emulating, so nothing was broken | ✓ Gallagher was chosen deliberately as the arm the Flipper DOES read, so a null here is about the geometry and not the emitter | L189 |
| C226 | ⭐⭐ **CAPTURED A REAL, WORKING FSK2a EMISSION ON THIS BENCH — the Flipper emulating AWID, sniffed by Chameleon #1 and decoded byte-exact by our own reader — and it does NOT use a 50% duty.** The instrument was there all along and I had written the rig off: rig A runs BOTH ways, so with the Flipper as the SOURCE, Chameleon #1's `lf sniff` is a reference-grade capture of a working emission. ⭐ **Its structure, 1,668 periods: the HIGH run is 4 samples on 1,207 of them regardless of tone, and only the LOW run varies — 4 for the RF/8 tone and 6 for RF/10.** Periods come out 8 and 10 as expected, but the mark is FIXED and the gap carries the data. Our emitter used 5 high / 5 low for the long tone, which sums to the same period and which our own decoder reads perfectly, because it only ever looks at the sum. ⛔ **Matching the measured duty did NOT fix it: still 0 of 6, control 4 of 4.** So duty is not the cause either — the fourth hypothesis to fall. ⭐ The change is KEPT regardless: a real tag shorts its coil for a fixed time, 50% duty leaves the field loaded 25% longer than any real AWID tag at the long tone, and guessing differently from the only working emission on this bench needs a reason we do not have. ✅ **The capture is committed** under `caps/awid-flipper-emit/` as the reference any future emitter work compares against | 1,668 periods from one reference emission; 6 Flipper reads of ours, control 4/4 | ✓ our decoder reads the reference emission byte-exact, so the capture is a valid recording of a working AWID | ✓ the control ran after the test on the same slot | L190 |
| C227 | ⛔⛔ **RETRACTED BY C228 — THE INPUT WAS WRONG.** ~~**THE BIPHASE PATH FAILS IN BOTH DIRECTIONS BETWEEN THESE TWO DEVICES — our decoder reads real GProxII T5577 tags 12 of 12 and reads the FLIPPER's GProxII emulation 0 of 4.** Captured with the Flipper as source and Chameleon #1 sniffing, the same rig and method that produced a byte-exact AWID reference minutes earlier (C226). Four drives tried, 2 / 4 / 6 / 7: nothing at any of them. ⭐ **That reframes the problem, and the table is the finding:** ASK/Manchester works BOTH ways (Gallagher: our emitter read by the Flipper 6/6, real tags read by us); FSK2a works ONE way (the Flipper's AWID emission decodes byte-exact for us, ours is silent to it); **biphase works NEITHER way** even though both ends read real T5577 tags of that protocol. ⚠ So this is not simply "our emitter is wrong" — whatever differs, it differs symmetrically, and a real tag satisfies both devices where neither device satisfies the other. ✅ The emission IS present and structured: sliced as levels it runs 29-35 samples (half-bit 32) with longer runs at 66 and 96, so the Flipper is emulating and the capture is real. ⚠ Runs of 96 are THREE half-bits, which biphase forbids — but that is measured through a level slicer, and C204 established that levels decay at this bit rate, so it is not evidence about the emission | 4 captures across 4 drives, 1 protocol | ✓ **the AWID capture from the identical setup decodes byte-exact**, so the source, the rig and the capture path are all sound | ✓ our decoder reads real GProxII tags 12 of 12 with 0 wrong (C213) | L191~~ ⇒ see C228 | L191, retracted in L192 |
| C228 | ⛔⛔ **C227 IS RETRACTED AND THE ERROR WAS MINE: I FED THE FLIPPER THE WRONG BYTES.** Momentum's GProxII ENCODER reads `protocol->data` — the full 96-bit frame, preamble and spacers included — where its DECODER writes `protocol->decoded_data`, the 90 bits after the preamble. I passed the decoded form, so the Flipper faithfully emitted a frame with no valid preamble, and our reader found nothing because there was nothing to find. ✅ **Given the frame it actually wants, our decoder reads the Flipper's GProxII emulation 3 of 4 EXACT** (`f84602a46119d4a114211046`), captured through the reader path at drive 7. ⭐ **The corrected table is cleaner and points one way:** ASK both directions; FSK2a Flipper→us ✓, us→Flipper ✗; biphase Flipper→us ✓ (3/4), us→Flipper ✗. ⇒ **every failure is in the SAME direction — our emission into the Flipper — and every Flipper emission decodes for us.** That is a sharper statement than C227's symmetry and it puts the fault on our emitter side, where four hypotheses have already been eliminated. ⚠ **How this slipped through**: the decoded and encoded forms are both 12 bytes, so nothing rejected the input and the null looked like a finding. A length check would not have caught it; only reading the encoder would have, which is what should have happened first | 4 reader-path captures, 1 protocol | ✓ the wrong-input capture is the null and it contains no preamble at ANY phase, threshold or inversion — 60 hits on a real tag, 0 on that one | ✓ the frame matches what the Flipper was told to emulate | L192 |
| C229 | ⭐⭐⭐ **BOTH SILENT EMITTERS ARE NOW SHOWN CORRECT BY REFERENCE TO MOMENTUM'S OWN CODE — not by assertion — so the fault is the peripheral's realisation and this bench cannot observe it.** For AWID that was C220: Momentum's own `fsk_demod_feed`, transcribed, decodes our emitter's ideal output exactly. For GProxII it is the encoder itself: `protocol_gproxii_encoder_yield` emits **two SHORT pulses of 32 for a 1 and one LONG pulse of 64 for a 0**, toggling the level on every yield — which is precisely the waveform both our versions describe. v1 held a level across each half-bit (32+32 for a 1, one 64 run for a 0); v2 expresses the same thing as one entry with duty 32 or a held level. **Structurally identical to the reference, in both versions.** ⭐ **So the emitter question is closed as far as source can close it**: design verified against the reference implementation for one protocol by its DECODER and for the other by its ENCODER, and both are silent on the air. ⛔ Five hypotheses are now dead — counter_top magnitude, entries per bit, AC coupling, duty shape, and the design itself. ⚠ What remains is unobservable here: the only reader that can hear rig A's emission is the Flipper, and it is the thing under test. ⇒ **the tag lift is no longer a nice-to-have, it is the only way this advances** | source reading + every measurement from C216 onward | ✓ the Flipper's own emissions of BOTH protocols decode for us — AWID byte-exact, GProxII 3 of 4 — so the capture path and our decoders are not in question | ✓ Gallagher, the control, reads 6/6 in the same direction our two fail | L193 |
| C230 | ✅ **THE BIPHASE FAMILY'S NULL IS NOW CLEAN IN BOTH DIRECTIONS — the half I had not run comes back completely blank.** Rule 11 asks for both, and until now only one had been done: the GProxII and FDX-B decoders against everyone else's captures (C205, C214). This is the converse — **eleven other decoders against the biphase captures**: Keri, NexWatch, Gallagher, Securakey, Noralsy, InstaFob, AWID, Paradox, Pyramid, FDX-A and Indala224, plus plain Indala and IDTECK. **Not one produces a frame on either the GProxII or the FDX-B tag captures.** ⭐ Worth having rather than assumed: the ASK/Manchester decoders in particular slice the same amplitude these tags modulate, and Securakey's entire gate is 19 preamble bits with no payload check (C175) — it is exactly the decoder most likely to claim a frame it should not | 11 decoders × 12 captures | ✓ the null IS the measurement here and it is blank with no exceptions | ✓ every one of those decoders reads its own protocol on device, so none of them is blank because it is broken | L194 |
| C231 | ⭐⭐ **EIGHT T5577 WRITERS NOW HAVE HOST COVERAGE, UP FROM THREE — AND KERI'S ROTATION IS PINNED.** C202 flagged that six writers had no host test at all; five of those had their reference block dumps already written down in the tree and unused, so the arms cost typing rather than bench time. Added: **Keri, Gallagher, NexWatch, GProxII and FDX-B**, each against the blocks a real Proxmark clone was recorded leaving on a tag. ⭐ **Keri is the one that matters.** Its block form is `(id << 3) | 7` — three bits out of phase with the air frame it is handed — and sending the frame view where the block form belongs gave a stable, confident, WRONG credential 6 times out of 6 (C160), with nothing on the host able to see it. Now there is: removing the rotation makes the arm print `00000000 80003039` against the wanted `00000004 000181CF` and fail. ✅ **Sensitivity proven by that deliberate break**, not assumed from a green tick — which matters after the GProxII emitter arm survived two breaks (C221). ⚠ Securakey and Noralsy still have none: their reference dumps were never recorded, and inventing expected blocks from the writer's own code would test nothing | 8 writer arms, 1 deliberate break | ✓ the break IS the control and it fails with a printed diff | ✓ every expected block set comes from a Proxmark clone recorded in the tree, not from our own writer | L195 |
| C232 | ✅ **EVERY T5577 WRITER IN THE TREE NOW HAS HOST COVERAGE — ten arms, and the last two needed a measurement rather than a search.** C231 had to leave Securakey and Noralsy out because nobody had ever written their block forms down, and inventing the expected blocks from our own writer's code would have tested nothing. Two Proxmark clones later: **Securakey `000C8060 / 7FCB4000 01ADEA53 44300000`** and **Noralsy `00088068 / BB0214FF 01100022 33070000`**, both now recorded in `t55xx.h` beside the config words that previously cited a dump nobody could see. ⭐ **Both are straight transcriptions — block form equals air frame — which is what makes the arms cheap and is exactly the thing that had to be checked rather than assumed**, because Keri's is not and that cost a confident wrong credential 6 of 6 (C160). ✅ Ten writers, ten arms: AWID, Paradox, Pyramid, Keri, Gallagher, NexWatch, GProxII, FDX-B, Securakey, Noralsy | 2 clone-and-dump measurements, 10 arms | ✓ the Keri arm's deliberate break (C231) is the sensitivity control for the whole family of arms | ✓ every expected block set comes from a Proxmark clone, never from our own writer | L196 |
| C233 | ⭐⭐ **THE THREE ASK WRITE ARMS ARE UPGRADED TO THE EVIDENCE STANDARD THE NEWER ONES SET — wiped tag, confirmed null, 4 of 4 each.** Gallagher, Securakey and Noralsy were graded "3/3 via Proxmark" months of work ago, before the tag was routinely WIPED first and before the credential was routinely CHANGED — the discipline C203 introduced for the FSK writers and C207/C215 for the biphase ones. Re-run to that bar: **Gallagher 4/4** (region 2 / facility 1234 / card 5678 / issue 3, a different credential from the old region 1 / 4321 / 6789 / 2), **Noralsy 4/4** (card 445566 / year 2015, changed from 112233 / 2000), **Securakey 4/4** (`7FCB400001ADEA5344300000`). Each on a tag wiped immediately before, with the protocol's own Proxmark reader confirmed to find NOTHING first. ⚠ **Securakey's credential is NOT changed and cannot be**: `lf securakey clone` takes only a raw frame and nothing here encodes one, so its evidence rests on the wipe alone. Said rather than glossed — the wipe does the work a changed credential would, but it is one leg instead of two | 12 Proxmark reads across 3 protocols, 3 wipes | ✓ **the wipe-and-null is the control for each**, run immediately before every write | ✓ the Proxmark is an independent reader and the source of every reference credential | L197 |
| C234 | ⭐⭐⭐ **KERI'S ROTATION IS NOW VERIFIED END TO END ON HARDWARE, AND NEXWATCH WITH IT — every write arm on this branch is at the same evidence standard.** Keri matters most: its block form is `(id << 3) | 7` and getting it wrong gave a stable, confident, WRONG credential 6 times out of 6 (C160). Our writer, given internal id **8000D431** (card 54321, changed from the old 12345), on a tag wiped immediately before with `lf keri reader` confirmed blank, produces **`603E1040 / 00000004 0006A18F` — byte-identical to the Proxmark's own clone of the same credential**, and the Proxmark reads it back **4 of 4**. ⭐ The rotation is therefore pinned twice over: on the host by `ctest` with a deliberate break (C231), and on hardware against a reference clone here. NexWatch likewise: card **87654321** (changed from 12345678), blocks `00081060 / 56000000 0012776A 2F1AF000` identical to its clone, 4 of 4. ✅ **All ten write arms now carry: wiped tag, confirmed null, independent reader, and — for nine of ten — a credential different from the previous test.** Securakey is the one exception and only because nothing here can encode a second frame for it (C233) | 8 Proxmark reads across 2 protocols, 2 wipes | ✓ the wipe-and-null ran immediately before each write | ✓ the block dumps are compared against the Proxmark's own clone of the SAME credential, not against our writer's own output | L198 |
| C235 | ⭐⭐⭐ **THE LAST FIVE READ ARMS UPGRADED FROM AN EMULATION TO A REAL TAG — 25 reads, every frame byte-identical to the Proxmark's own raw, and four of the five on a credential I chose.** C179 is the operator's distinction and C201 applied it to the three FSK arms; these five were still graded against a Flipper EMULATION, which is one decoder's rendering of a protocol rather than tag behaviour. Re-run against T5577s the Proxmark wrote: **NexWatch 5/5** (card 87654321), **Keri 5/5** (internal id 80007A69, card 31337), **Gallagher 5/5** (region 3 / facility 999 / card 4242 / issue 1), **Noralsy 5/5** (card 778899 / year 2022), **Securakey 5/5** (`7fcb400001adea5344300000`). ⭐ **Every credential is new to this bench**, so none of these is a reader returning a value it has seen before — except Securakey, where nothing here can encode a second frame and which is flagged for that in the grid. ✅ **With C201 this completes the upgrade: every read arm on the branch that CAN be tested against a real tag now has been.** The two that cannot — FDX-A and InstaFob — stay emulation-verified and the grid says so | 25 device reads across 5 protocols, 5 freshly written tags | ✓ each tag was written and confirmed by the Proxmark's own reader before ours was asked | ✓ the Proxmark is an independent writer AND reader; our frames match its raw byte for byte in all five cases | L199 |
| C236 | ✅ **THE COMMITTED CAPTURE CORPUS NOW HAS REAL-TAG REFERENCES FOR THE FSK FAMILY, NOT JUST EMULATIONS.** Rule 18 was already satisfied — every FSK reader had a capture under `caps/awid-flipper/` — but all of them came from a FLIPPER EMULATION, which is exactly the evidence C179 distinguishes from tag behaviour and which C201 and C235 spent two ticks upgrading for the ARMS. The corpus had not followed. ✅ Added `caps/fsk-realtag/`: three captures each of **AWID** (FC 77 / card 16180, `011d84747e8d811111111111`), **Paradox** (FC 21 / card 31415, `0f5555556666a999a6a9aa6a`) and **Pyramid** (FC 88 / card 27182, `00010101010101010101016b8651babc`), all taken through the READER path with `rdrcap.py` and all decoding byte-identical to the Proxmark's own raw. ⭐ **Every credential is new to this bench**, so the corpus is not three more recordings of a frame the decoders have already memorised. ⛔ **FDX-A gets none and cannot**: the Proxmark has no FDX-A clone, so its only possible capture is of an emulation — the same limit that keeps its write arm unshipped (C185) | 9 captures, 3 protocols, 3 freshly written tags | ✓ **cross-protocol nulls over the new corpus are blank** — GProxII, FDX-B, Gallagher, Securakey, Noralsy, Keri and NexWatch all find nothing in any of the 9 | ✓ each tag was written and confirmed by the Proxmark's own reader first | L200 |
| C237 | ⭐⭐ **THE MEASURED DUTY IS NOW PINNED, AND THE BREAK PROVES THE ROUND TRIP COULD NEVER HAVE CAUGHT IT.** C226 measured a real AWID emission and found the mark FIXED at 4 samples with only the gap varying; our emitter was emitting 5 and 5 for the long tone, the same PERIOD, and was corrected. ⛔ **Reverting that correction passes the round-trip arm with "decode exact" and fails the new one** — which is the whole point: our decoder sums high and low into a period and cannot see the split, so no encode-decode test in this file could ever have noticed. `counter_top / 2` is the obvious thing to write and it is exactly what was there before. ✅ The arm asserts every entry has `counter_top` 8 or 10 and a duty of 4, and reports **378 short + 165 long entries** on the bench frame. ⭐ **The failure line reports what it FOUND, not what it wanted** — "mark reaches 5, want 4" rather than restating the intention — because a red line that describes the test rather than the defect is one a reader skims | 1 arm, 1 deliberate break | ✓ **the break is the control and it separates the two arms cleanly**: round trip ✓, duty pin ⛔ | ✓ the expected shape comes from a real Flipper emission decoded byte-exact by our own reader (C226), not from our emitter | L201 |
| C238 | ⚠⚠ **NO SHAPE PIN IS POSSIBLE FOR GPROXII, AND THE REASON IS THE MEASUREMENT ITSELF — recorded as a negative rather than dressed up.** C237's AWID pin worked because a real emission's mark and gap could be measured separately. The same attempt for GProxII: a Flipper GProxII emission (captured with the CORRECT frame this time, 3 of 3 exact) and a real GProxII T5577 both slice to **HIGH runs of ~29 and LOW runs of ~36, ~67 and ~99** — a short fixed mark with a variable gap, which looks nothing like the symmetric held level our emitter describes. ⛔ **But that is not evidence our emitter is wrong**, because a HELD level through this front end produces exactly that pattern: the AC coupling measured at a ~27-sample time constant (C204) decays a 64-sample held level into a short high run followed by a long low one. The two hypotheses predict the same slicer output and the measurement cannot separate them. ⇒ **a pin built on it would encode an artefact of the receiver as a requirement on the transmitter.** ⭐ What the captures DO establish: a real tag and the Flipper's emulation are indistinguishable at this level, which is worth having and is why they are committed | 2 emissions compared, 3 reference captures | ✓ the reference captures decode 3 of 3 exact, so the recording is sound | ⚠ the negative is the finding — there is no control that would make this discriminating, and inventing one would be the mistake | L202 |
| C239 | ⭐⭐ **OUR AWID EMITTER NOW MATCHES THE FLIPPER'S OWN EMISSION DUTY EXACTLY — AND THE FLIPPER STILL WILL NOT READ IT, WHICH KILLS DUTY AS A CAUSE HARDER THAN C226 DID.** Measured on the committed real-tag captures, sliced the same way: a **real AWID T5577** runs HIGH 3 (1144 runs) or 4 (349) and LOW 5 (1270) or 6 (352) — so short tone ≈3+5 and long ≈4+6, roughly 37% and 40% duty. The **Flipper's own AWID emission** (C226) runs HIGH fixed at 4 and LOW 4 or 6 — 50% and 40%. **They differ**, and our emitter matches the FLIPPER: 4/4 and 4/6. ⇒ we now emit what the Flipper itself emits, and it reads us 0 of 6 while reading a real tag and its own emulation. ⚠ **The C141 CAVEAT APPLIES AND IS NOT WAVED AWAY**: slicer run lengths carry a systematic duty bias — 1-bit runs measuring 30.0 where the true period is 32.000 — so 3/5 may be a true 4/4 seen through that bias. But both emissions were measured through the SAME slicer, so the DIFFERENCE between them survives the caveat even if the absolute values do not. ⭐ Also measured, and reassuring: **Noralsy HIGH 16 / LOW 16 and Securakey HIGH 19 / LOW 21 are symmetric**, which is exactly what those emitters produce and they read 6/6 | 4 real-tag capture sets, one slicer | ✓ Noralsy and Securakey are the positive controls — symmetric tags, symmetric emitters, arms that work | ⚠ no control separates absolute duty from slicer bias, and the claim is limited to the difference | L203 |
| C240 | ✅ **THE EMULATION ENGINE IS ELIMINATED — it treats all three tag types identically and reports the two silent ones as transmitting.** `hw emudebug` read while each is emulating, one slot, one session: **AWID (307)** sense ENABLE, base clock **125kHz**, have pwm seq **True**, playbacks started **210**, frames per burst **14**; **GProxII (308)** the same with 11 frames; **Gallagher (304)**, the control that reads 6/6, the same with 21. ⭐ **The burst arithmetic is right for all three**: 14 × 37.6ms, 11 × 49ms and 21 × 24.6ms are all ≈520ms, so `recompute_frames_per_burst` is handling the variable-length FSK sequence and the biphase one correctly. ✅ The base clock is 125kHz for each, not the PSK1 path's 1MHz — the one thing `IS_PSK1_TYPE` could have got wrong. ⇒ **the sequence is loaded, the clock is right, the burst length is right and the peripheral is being told to play 210 times — and the Flipper hears one of the three.** Seventh hypothesis down, after counter_top magnitude, entries per bit, AC coupling, duty shape, the design itself and the buffer plumbing | 3 tag types, one slot, one session | ✓ **Gallagher is the control and its numbers are the same shape as the two that fail** — if the engine were the difference it would show here | ✓ the instrumentation reads the engine's own counters, not a behavioural inference | L204 |
| C241 | ✅ **ALL FIVE WORKING EMULATE ARMS ARE REGRESSION-CLEAN ON THE CURRENT BUILD, each with its own built-in control.** Worth running rather than assumed: since those arms were graded the shared sweep function has been RENAMED, `lf_sampled_read_phases` has changed signature four times (phase list, try count, drive, inter-capture gap), two tag types have been added, a whole biphase decoder has landed and Chameleon #1 has been reflashed a dozen times. **Keri PSK 4/4** (ASK arm 0/4), **NexWatch PSK 4/4** (ASK 0/4), **Securakey ASK 4/4** (PSK 0/4), **Noralsy ASK 4/4** (PSK 0/4), and **Gallagher ASK 6/6** (PSK 0/6), the latter run repeatedly this session as the control for the silent emitters. ⭐ **The opposite-modulation arm IS the control in each case** — it says the reader was looking and found a tag of THAT type, not any tag at all (C83) — and it came back empty every time. ⚠ These remain Flipper-verified only; C179's standing limitation is unchanged and the upgrade needs the tag lift | 22 Flipper reads across 5 protocols | ✓ every arm carries its own opposite-modulation null | ✓ the frames are the same credentials the grid records, so a pass here is comparable to the original grade | L205 |
| C242 | ⭐⭐⭐ **A HELD-LEVEL PWM ENTRY EMITS NOTHING — PROVED ON A WORKING, SHIPPED EMITTER BY CHANGING TWO ENTRIES, AND RESTORED.** The isolating control first: **em410x reads 4 of 4** at `counter_top` **64** with every entry a 50% square — so counter_top 64 is fine and it is not the size that matters. Then the A/B/A: making entries 20 and 21 of em410x's 64 emit a HELD level instead (`channel_0` 0 or 64 rather than 32) takes it to **0 of 4**, and restoring them brings it back to **4 of 4**. ⭐⭐ **That explains GProxII completely.** Its 0-bits ARE held levels — duty 0 or duty == counter_top for a whole bit period — so roughly half its frame emits silence and there is nothing for any reader to find. Both versions of the emitter had this, v1 as two held half-bits and v2 as one held bit, which is why re-shaping it changed nothing (C221, C222, C223). ⛔ **And it means BIPHASE CANNOT BE EMULATED THIS WAY AT ALL**: a biphase 0 is by definition a level held across the whole bit, and this peripheral does not transmit one. ⚠ **It does NOT explain AWID**, whose entries are all 50% squares — that failure is still open, and this finding must not be stretched over it | 1 shipped emitter, 2 entries changed, 12 Flipper reads | ✓ **A/B/A, not A/B** (M35): 4/4 working, 0/4 broken, 4/4 restored | ✓ em410x at counter_top 64 with all squares is the isolating control that separates size from shape | L206 |
| C243 | ⛔ **THE TONE VALUE IS NOT AWID'S PROBLEM EITHER — 8 and 12 is as silent as 8 and 10.** The last cheap probe available: the Flipper's AWID demodulator accepts any period in **[44, 100)** and calls it long at or above **72**, so 8 and 12 carrier cycles (64 and 96us) straddle that threshold just as 8 and 10 do, and the pulse counts (6 short per 0, 5 long per 1) are unchanged — a valid AWID emission by that demodulator's own rules, differing from ours only in the long tone's `counter_top`. **0 of 4.** ⇒ if a specific counter_top value were being mishandled, changing it would have shown. ⭐ **Ninth hypothesis down**: counter_top magnitude, entries per bit, AC coupling, duty shape, the design itself, the buffer plumbing, the emulation engine, held levels, and now the tone value. ✅ Restored, and the restore is checked two ways: `make check` green from a clean rebuild, and Gallagher 4 of 4 on the same slot afterwards | 4 Flipper reads at the probe value | ✓ **Gallagher 4/4 after the restore is the control** — the rig, the flash and the emulation path all still work | ✓ the probe value was chosen from Momentum's own window constants, not guessed | L207 |
| C244 | ✅ **C242 SURVIVES A CHALLENGE TO ITS OWN METHOD — held levels are silent whichever idiom expresses them — and a real bug in our GProxII emitter came out of asking.** ⚠ **The challenge**: `jablotron.c` and `pac.c` both hold a level and both use compare = **counter_top + 1**, each citing the nRF52840 PS — "compare >= counter_top -> pin held HIGH" — and warning that exactly counter_top leaves a **1-tick glitch** because the counter does reach it. Our GProxII emitter used `counter_top`, and so did C242's em410x probe. ⇒ what that probe demonstrated was that the GLITCHY form is silent, which is a weaker claim than the one it was written up as. ✅ **Re-run with compare = counter_top + 1: em410x still reads 0 of 4 with two held entries, and 4 of 4 restored.** So the finding holds and the mechanism claim is now earned rather than assumed. ✅ **GProxII's emitter is fixed regardless** — it was carrying a documented glitch on every held-high bit, which is a defect whether or not it was THE defect — and it still reads 0 of 6, as C242 predicts | 2 probes × 2 idioms, 14 Flipper reads | ✓ **A/B/A again**: 4/4 working, 0/4 with the correct-idiom held entries, 4/4 restored | ✓ the correct idiom came from two shipped emitters in this tree that use it and work | L209 |
| C245 | ⭐⭐⭐ **AWID'S SILENCE IS NOT OUR BUG — THE FIRMWARE'S OWN SHIPPED FSK2a EMITTER IS SILENT TOO.** `hidprox.c` has been in this firmware all along and its modulator is structurally IDENTICAL to our AWID one: `counter_top` **8 and 10**, duty **top/2**, several entries per bit, and `m_hidprox_pwm_seq.length = k * 4` computed per call. Emulating **HID Prox H10301, FC 123 / card 4567**, the Flipper reads **0 of 6** — and Gallagher on the same slot immediately after reads **4 of 4**. ⇒ **FSK2a emulation does not work on this device, for the protocol that ships as well as for the one we added.** ⭐ That is why nine hypotheses about OUR emitter all died: there was nothing wrong with it. ⚠ It also means the duty "correction" of C226 was chasing a difference that does not matter — the shipped emitter uses top/2, which is exactly what ours did before. ⚠ **Nobody had tested this**: every emulate arm ever graded on this branch is PSK1 or ASK/Manchester, so the FSK2a emulation path had never faced a reader. ⚠ **Limit of the claim, stated**: this bench cannot present a REAL HID tag to the Flipper — the T5577 is in rig B — so the Flipper's HID read path is not independently confirmed here. What is confirmed is that Momentum registers both HID protocols with `LFRFIDFeatureASK`, the same feature as AWID, so `rfid read ask` does try them | 6 + 4 Flipper reads, one slot, one session | ✓ **Gallagher 4/4 on the same slot is the control** and it passed immediately after | ✓ the shipped emitter is the reference implementation — it is not ours and we did not write it | L210 |
| C246 | ⭐⭐⭐ **ALL THREE FSK2a EMITTERS ON THIS DEVICE ARE SILENT — TWO OF THEM SHIPPED LONG BEFORE THIS BRANCH — WHILE EVERY ASK AND PSK1 EMITTER WORKS.** ioProx is the second shipped one and its modulator uses the same `LF_FSK2a_PWM_HI/LO_FREQ_TOP_VALUE` constants as HID Prox: emulating **IOProxXSF v2 / FC 83 / card 4567**, the Flipper reads **0 of 6**. With C245 that makes **HID Prox 0/6, ioProx 0/6, AWID 0/6**, against **Gallagher 4/4** run as the control immediately after each. ⭐ **The pattern is by MODULATION, not by authorship**: PSK1 (Keri, NexWatch) and ASK/Manchester (Gallagher, Securakey, Noralsy, em410x) all emulate; FSK2a does not, for anyone. ⛔ **So the grid's ✓ for HID Prox and ioProx EMULATE was inherited from upstream's feature list and has never been true on this bench** — corrected. ⚠ The same limit as C245 applies: no real HID or ioProx tag can be put in front of the Flipper from here, so its read path for those protocols is not independently confirmed — but three different FSK2a emitters failing while six non-FSK2a ones succeed is not a reader coincidence | 16 Flipper reads across 3 FSK2a protocols + 3 controls | ✓ **Gallagher 4/4 ran after every one of the three** | ✓ two of the three emitters are upstream code this project did not write | L211 |
| C247 | ⛔ **VARYING `counter_top` PER ENTRY IS NOT THE CAUSE — em410x with an alternating 63/65 still reads 4 of 4.** The hypothesis was worth testing because it is the one property that separates the FSK2a emitters from every working one: ASK and PSK1 sequences hold `counter_top` CONSTANT and vary only the polarity or the duty, while FSK2a alternates it between 8 and 10 every few entries. ✅ Probe: em410x's 64 entries given tops of 63 and 65 alternately, with the duty following — a ±1 tick jitter, 1.5% of a 512us bit and far inside any Manchester tolerance. **4 of 4 before, 4 of 4 with the jitter, 4 of 4 restored.** ⇒ the peripheral changes COUNTERTOP between periods without trouble. ⭐ **Tenth hypothesis down.** ⚠ **What that leaves cannot be separated on this bench, and saying so is the honest end of this line**: every working emitter uses a `counter_top` of 31 or more and every failing one uses 8 or 10 — but for a Manchester emitter a small top and a fast square wave are the SAME change, so "the magnitude" and "the resulting waveform" cannot be told apart by any probe available here | 3 probe builds, 12 Flipper reads | ✓ **A/B/A**: 4/4, 4/4 jittered, 4/4 restored | ✓ the jitter was sized from Momentum's own Manchester tolerance, not guessed | L212 |
| C248 | ⭐⭐ **C45'S HID INTERMITTENCY DOES NOT REPRODUCE ON THE CURRENT BUILD — the legacy decoder is 48 of 48 exact — and the shared FSK2a engine matches it arm for arm on the SAME TAG.** A controlled-plaintext A/B/A on the rig-B T5577, one session, one coupling: **A** `lf hid prox read` on H10301 FC 123 / CN 4567 → **32/32 exact, 0 wrong, 0 null**; **B** the shared-engine `lf awid read` on AWID-26 FC 200 / CN 30000 (`011d8d41e48d181111111111`, the Proxmark's own clone bytes) → **32/32 exact**; **A'** the HID credential rewritten and re-read → **16/16 exact**. C45 measured 22/24 (≈8%) on the white coin at rig A. ⇒ **The shared engine does not INHERIT the defect**, which is what U8's deferral was afraid of. ⛔ **But it is not shown to have FIXED it either, and the reason is worth stating rather than burying: the baseline defect is not present in this configuration, so there is nothing here for the new engine to have fixed.** Two candidate explanations survive and this measurement cannot separate them — (a) the **C47 advertising guard**, which `cf745fb` added to `lf_hidprox_data.c` and which is the ONLY change that decoder has had on this branch, and (b) the **package/coupling factor of C46**, where three tags with byte-identical memory read 0/6, 3/6 and 7/9. Separating them needs the white coin on a pad the Chameleon can reach, or a deliberate break of the guard | 80 device reads scored on the credential, not on pass/fail (M33), + 24 null reads | ✓ **A/B/A** (M35) — the closing HID arm rules out bench drift across the run | ✓ four blank columns, all silent: AWID reader on the HID tag **0/8**, HID reader on the AWID tag **0/8**, and both readers on the wiped tag **0/4** each | L214 |
| C249 | ⚠ **CHAINING `-c` ARGUMENTS IN ONE `pm3` INVOCATION CAN REPORT THE STATE BEFORE THE WRITE.** `./pm3 -c "lf t55xx wipe" -c "lf search"` printed the FULL pre-wipe HID credential — parity ok, raw and all — from a tag that had just been zeroed; the identical `lf search` in its own invocation a moment later returned `No known 125/134 kHz tags found!`. The wipe had landed. ⛔ Taken at face value that output is a clean, confident, **false** verification of a write that did not happen, in exactly the place this campaign puts its wipe-and-confirm-null step. ⇒ **Run the verifying read as its own `pm3` invocation, always** | 2 invocations, same tag, ~20 s apart | ✓ the separate-invocation read is the control and it disagrees | ✓ the subsequent AWID clone verified normally, so the tag and the link were both healthy throughout | L214 |
| C250 | ⭐⭐⭐ **C45 IS EXPLAINED, AND THE DEFECT IS WORSE THAN "INTERMITTENT": AN UNGUARDED HID READ RETURNS WRONG CREDENTIALS AS SUCCESSES.** The paired test `LF_ADV_GUARD_ENABLED` was put there for, run as A/B/A on one tag (H10301 FC 123 / CN 4567) in one session, each arm flashed from its own commit so `hw version`'s hash proves which build produced which number: **guard ON `768cdab`/`ee59b45` — 96 reads, 96 exact, 0 wrong, 0 null**; **guard OFF `d37b450` — 80 reads, 71 exact, 5 WRONG, 4 null** (11.2% defective, Fisher exact **p = 6.4e-4**). ⛔ **Five of the nine failures are not failures.** Of the two whose values were recorded, one returned FC 123 / **CN 4375** — bits 6 and 7 of the card number cleared, two adjacent bits inside ONE parity group, which H10301's parity cannot see — and one returned **FC 251** / CN 4567, a single bit set. That is the signature C47 predicted: a ~1.6 ms field collapse mangles a few bit-times and leaves the rest of the frame intact. ⛔ **I first wrote here that our read path checks no parity — that is FALSE and is retracted the same session it was published.** `unpack_h10301()` in `wiegand.c` checks both parity bits and the reader reaches it through `unpack()`; the grep that said otherwise only looked in `hidprox.c` and `lf_hidprox_data.c`, where the check does not live. ⇒ **The corruptions got through a parity check that was working, and HOW is not yet separated** (C251): either two flips inside one parity group, which is invisible by construction, or acceptance under a DIFFERENT 26-bit format — `lf hid prox read` passes `format_hint = 0`, and `unpack()` then tries every 26-bit format and returns the first one that fits. ⇒ C45's headline was wrong about the location: the fault is in the CAPTURE, perturbed by a BLE burst, not in the decode | 176 device reads across 3 flashed builds, credential-scored (M33) | ✓ **A/B/A** (M35) — 48/48 before, 48/48 after, and the restore arm is a different commit from the opening one | ✓ the guard-OFF build was verified on the device by git hash, not assumed from a flash that reported success (L162's lesson) | L215 |
| C251 | ⭐⭐⭐ **A CORRUPTED HID FRAME IS RELABELLED AS INDALA AND PRINTED AS A CONFIDENT CREDENTIAL — and that path, not parity blindness, is most of the damage.** Probe build `235dfa3`, guard OFF, one tag, two arms of 48: **`format_hint = 0`** (what `lf hid prox read` sends when you do not name a format) → 40 exact, **7 wrong**, 1 null; **`-f H10301`** → 46 exact, **1 wrong**, 1 null. ⛔ **Six of the seven came back as `Indala 26-bit`**, FC 1953-1977 / CN 471, clustered on **FC 1969 / CN 471** — which is precisely what the Proxmark reports as the ind26 reading of this tag's UNCORRUPTED frame. ⇒ The mechanism is now measured end to end: a burst flips a few bits, H10301's parity REJECTS the frame correctly, `unpack()` walks on to the next 26-bit format, ind26's weaker checks accept it, and the operator is shown an Indala credential for an HID tag with nothing marking it as a fallback. ⭐ **Pinning the format cuts wrong credentials 7/48 → 1/48.** ⭐ The four that DID come back as H10301 are all two flips inside ONE parity group, where the check is blind by construction: CN 4567→4375 (CN bits 6,7) and 4567→4823 (bits 8,9) in the odd group over CN[0:11]; FC 123→83 (FC bits 3,5) in the even group over CN[12:15]+FC; and FC 123→251, a lone FC bit 7, which fits only if the even-parity bit flipped with it — recorded as the inference it is. ⛔ **This is upstream's behaviour, not this branch's**: both the walking `unpack()` and the CLI's `format = 0` default are on `main` | 144 reads across 2 arms + a 48-read closing control, credential AND format scored | ✓ **A/B/A** (M35) — the guard restored (`a049bc6`) reads 48/48 clean on the same tag | ✓ the pinned arm is the discriminator: it disables the relabelling path and only the parity-blind corruptions survive it | L216 |
| C252 | ⭐⭐⭐ **EVERY FRAME GATE WE SHIP, MEASURED AGAINST A WRONG FRAME — and the spread is 100% to 12%.** C251 is about a decoder accepting a frame it should have refused, and nothing in this harness asked that question: every arm tested a CORRECT frame surviving a round trip. New `sweep()` in `ctest/roundtrip.c` flips one frame bit, re-emits through the shipping emitter and re-decodes through the shipping decoder, for all 96/64/224 bits of ten protocols. Rejected / accepted-wrong: **AWID 96/0 (100%)**, Gallagher 88/8, NexWatch 80/16, Noralsy 80/16, Indala26 33/31, Keri 33/31, IDTECK 32/32, **GProxII 36/60 (37%)**, **Securakey 19/77 (19%)**, **Indala224 28/196 (12%)**. ⭐ The extremes are explained by the formats, not by luck: AWID carries 22 INDEPENDENT odd-parity nibbles over bits 8..95, so any single flip breaks exactly one of them; Indala26 and IDTECK have a preamble and nothing else. ⛔⛔ **One number is ours to fix rather than to report: `LF_ASK_FORMAT_SECURAKEY.accept` is `NULL`**, where both its ASK siblings have a structural hook — and the reference implementation checks 9-bit `ParityAlways0` spacers across the frame that we do not. ⚠ **AND IT UNDERSTATES ANY FORMAT GATED ON REPETITION — see C254**, which also records why the obvious fix fails. ⚠ **What this does NOT measure**: the device readers require two agreeing stacks before reporting, which catches an error that does not repeat; these arms feed one perfect rendering of a corrupted frame, so they characterise the FRAME GATE alone | 1,024 corrupted frames through emitter and decoder, 10 protocols | ✓ **sensitivity proven by deliberate break** — `LF_ASK_FORMAT_GALLAGHER.accept` set to NULL moved it 88/8 → 16/80 and failed the harness, while its round-trip arm stayed ✓ exact, which is precisely the blindness this closes | ✓ counts are PINNED, so a gate that quietly weakens fails `make check` rather than printing a smaller number | L217 |
| C253 | ⭐⭐⭐ **SECURAKEY'S MISSING GATE IS CLOSED, AND IT BITES ON REAL AIR — 8/8 on a valid frame, 0/4 on one whose spacer is wrong.** C252 put Securakey second-weakest of everything we ship because `LF_ASK_FORMAT_SECURAKEY.accept` was `NULL`. The source note explaining that was wrong in its reasoning — it argued from "no checksum", and the reference's gate is not a checksum: `protocol_securakey_can_be_decoded` rejects any frame whose 9-bit groups do not open with a ZERO SPACER, ten of them at bits 10, 19, 28 … 91. `securakey_accept()` now enforces exactly those and nothing more. **On hardware, A/B/A on one tag:** the bench frame `7fcb400001adea5344300000` reads **8/8 exact**; the same frame with bit 46 — a spacer — set to 1, written and read back by the Proxmark so the tag provably holds it, reads **0 of 4, all silent**, while `lf sniff` shows the field fully modulated (peak-to-peak 16380, 6067 real field drops), so the tag is answering and it is the GATE refusing; restoring the valid frame returns **4/4 exact**. ⭐ Cost on the sweep: rejected 19→28, wrong 77→68. ⚠ **The frame also carries Wiegand parity at bits 35 and 63 that NEITHER reference checks on decode.** Enforcing it would make us stricter than both references on the strength of one bench credential, which is how a format starts refusing real tags — written down, left unenforced | 12 device reads across 3 tag states + 4 committed captures + 53 null captures + 96 swept frames | ✓ **A/B/A** (M35), and the broken arm is a DELIBERATE BREAK of one named bit, not a vague degradation | ✓ the tag was wiped first and the null confirmed on both the Proxmark and our reader (0/4) before anything was written | L218 |
| C254 | ⛔ **C252'S TABLE CANNOT REACH `require_repeat`, AND THE OBVIOUS WAY TO FIX THAT DOES NOT WORK — recorded so nobody spends the hour again.** Both renderers loop the sequence with `i % entries`, so the sweep emits a corrupted frame IDENTICALLY in every repetition and a format whose gate is "the same frame twice" sees two agreeing copies of the corruption. Indala224's 12% is partly that artefact. ✅ **The fix attempted**: render one corrupted pass, then clean passes — what a burst actually does. **It failed on its own control.** Indala26, which has no `require_repeat` and should therefore not move, went 33/0/31 → **0 rejected, 39 silent, 25 wrong**: once the buffer contains a VALID frame the decoder simply finds it, so "rejected" becomes nearly unreachable and the split between silent and wrong is decided by which copy comes FIRST. ⛔ And Indala224 did not move at all (28/0/196, identical) for a second reason: flipping one bit of an even-parity frame makes it odd, the differential emitter then DOUBLES its buffer, and "one pass" is already two corrupted copies. ⇒ **What the instrument would need**: a buffer capped at exactly two passes, corrupted first, with the corruption chosen to leave the frame's parity — and so the emitter's doubling — unchanged. ⚠ Until then the table's low numbers are a FLOOR for repeat-gated formats, not an estimate | 2 sweeps × 288 corrupted frames, one of them the control that killed it | ✓ the control is what settled it — a test arm whose control moves as far as the test measures the splice, not the gate | ✓ the parity-doubling explanation is read off the emitter's own `want_entries` rule, which the round-trip arm already asserts | L219 |
| C255 | ⭐⭐⭐ **GPROXII GAINS THE REFERENCE'S LAST GATE AND IT BITES ON REAL AIR — but one corruption in four still walked through it, and that is the half worth reading.** `protocol_gproxii_can_be_decoded` does not stop at the format length the way ours did: it pulls the credential out of the descrambled stream and checks the two Wiegand parity bits either side of it. Added, and **sweep rejected 36→64, wrong 60→32**. ⭐ The transcription is confirmed against values we chose — the bench frame descrambles to key 200, length 26, **FC 45 / card 6789**, exactly `lf gproxii clone`'s arguments (C207). **Hardware A/B/A on one tag, build `3f46af2`:** valid frame **8/8 exact**; block 2 rewritten `6119D4A1`→`611BD4A1`, which leaves the preamble, all 18 spacers and the length 26 intact and breaks ONLY the Wiegand parity (FC 45→47), reads **0 exact, 3 silent**; restored, **8/8 exact**. ⛔⛔ **The fourth read of the broken arm returned `f84602a4411a14a114211046` — FC 51, card 6789 — and that frame passes EVERY check both references make, parity included.** It is not the frame on the tag; it is a mis-slice that landed self-consistent. ⇒ **C205's shape in a second place**: a false frame stable enough that two-agreeing-stacks confirms rather than rejects it. ⚠ **"One in four" is n=4 and reads as a rate it is not — C266 measures it at 3 in 52 and explains the mechanism.** ⚠ All three frames carry card 6789 and differ only in FC, so the corruption is landing in one field rather than scattering | 20 device reads across 3 tag states + 8 committed captures + 41 null captures + 96 swept frames | ✓ **A/B/A** (M35), and the break is one named bit chosen so that ONLY the new gate can reject it — the length gate still passes it | ✓ the 8 committed captures decode byte-identically before and after, gp_d6's known one-bit error included, so the gate costs the reader nothing | L220 |
| C256 | ⭐⭐ **EVERY FORMAT'S GATE AUDITED AGAINST ITS REFERENCE — three match, three do not, and the three that match are the more useful half.** C252 gave the numbers; this says which are defensible. **Match, nothing missing:** *IDTECK* (reference checks the preamble and nothing else), *InstaFob* (reference checks the 32-bit block-1 constant and nothing else — so its `.accept = NULL` is correct, NOT the gap Securakey's was), *Indala224* (reference wants the preamble twice; our `require_repeat` is the same demand). **Gaps found:** *Securakey* — closed (C253); *GProxII* — closed (C255); ⛔ *Indala26* — the reference also requires **bits 60 and 61 to be zero** and the frame to REPEAT at offset 64, and we check neither; ⛔ *Keri* — the reference requires the frame at 0 and at 64 **and that both copies carry the same 32-bit id**, which its own comment calls the only integrity check the format has, and we require neither. ⚠ **The two open gaps are not equivalent to the two closed ones**: bits 60/61 is a pure frame-content check and safe to add, but everything else on the list is a REPEAT requirement, and whether our capture window even holds two frames is per-protocol and measured, never assumed (C161). ⇒ Add the 2-bit check; measure before requiring any repeat | 6 reference implementations read against 6 of ours | ✓ each verdict is the reference's `can_be_decoded` read in full, not its header or its name | ✓ the two matches are blank columns and are reported as findings, not as absence of work | L220 |
| C257 | ⭐⭐⭐ **INDALA26 GAINS THE REFERENCE'S TWO ZERO BITS — 4 false frames gone, 0 true ones — AND THE TWO REFERENCES DISAGREE ABOUT IT, which the hardware arm demonstrates rather than argues.** `protocol_indala26_can_be_decoded` requires bits 60 and 61 to be zero; we checked nothing but the preamble. ⭐ **Measured on the committed 320-capture corpus BEFORE the change, not after**: 68 frames → **64**, `== truth` **51 → 51**, wrong **17 → 13**. The four rejected — `a0000000e6550e1a`, `a0000000eebd0e9a`, `a0000000e4bd1e96`, `a0000000e5bc4e16` — are all wrong. Sweep 33/31 → 35/29. **Hardware A/B/A, build `9bd111d`:** `a0000000e6bd0e92` reads **8/8 exact**; `a0000000e6bd0e96`, the same frame with bit 61 set, reads **0 of 4, all silent** — ⛔ **while the Proxmark reads that same tag perfectly, `Indala (len 64) Raw: a0000000e6bd0e96, Fmt 26 FC: 52 Card: 63612`**; restored, **6/6 exact**. ⇒ **Momentum rejects those frames and the Proxmark does not, and we have followed Momentum.** The justification is C205's, already this branch's precedent: a single-protocol demod has nothing to be confused with and a multi-protocol reader does. ⚠ The cost is exactly what the B arm shows — a genuine 64-bit Indala of some other format with either bit set is now refused here and readable elsewhere, and every real Indala frame on this bench comes from ONE tag | 18 device reads across 3 tag states + 320 committed captures, before and after | ✓ **A/B/A** (M35), and the middle arm's tag is confirmed good by an INDEPENDENT reader that accepts it | ✓ the corpus measurement was taken BEFORE the gate was written, so it could not be fitted to the result | L221 |
| C258 | ⭐⭐ **KERI TAKES THE REFERENCE'S FRAME-REPEAT CHECK AND INDALA26 CANNOT — the difference is C161's capture window, not the protocol.** `protocol_keri_can_be_decoded` demands the preamble at 0 AND at 64 AND the same 32-bit id in both copies, and its own comment calls that the only integrity check the format has. Probed with the FIRMWARE's rule on real captures: **Keri 3 of 4 with it and 3 of 4 without**, same files, same credential; **Indala26 51 true frames → 43**, so the same flag costs it 8 real reads in 51. ⇒ Keri's window is 8192 samples and Indala26's is 4096 (C161); 4096 holds barely one frame plus a fragment. **Hardware: `lf keri read` 8 of 8 exact** on a freshly cloned `-t i --cn 12345`, raw `e000000080003039`, and the **Proxmark reads the same tag 3 of 3 with a byte-identical raw**. ⚠ **THE BENEFIT IS ARGUED, NOT MEASURED**: across **705 null captures Keri produced ZERO false frames both with it on and with it off**, because C157's Indala veto had already taken that to zero. What it should catch is the C255 shape — a mis-slice that lands self-consistent will not reproduce at the next frame offset — and that is untested. ⚠ The cost rests on 4 captures plus one device arm. ⭐ C254 confirmed in passing: the sweep's Keri row does not move at all (33/31 both ways), because the renderer loops the corrupted frame and the repeat check sees two agreeing copies of it | 8 device reads + 3 Proxmark reads + 8 real captures probed both ways + 705 null captures both ways | ✓ the control is INDALA26 under the identical flag, and it moves — so the Keri result is the window, not the flag doing nothing | ✓ an independent reader reads the same tag to the same raw frame | L222 |
| C259 | ⭐⭐ **OUR KERI READER PRINTED THE RAW FIELD UNDER THE LABEL `Internal ID` — FIXED — and half of what I first published here was my own comparison error, corrected the same session.** ✅ **REAL AND FIXED**: `cmdlfkeri.c` reports `ID &= 0x7FFFFFFF` because bit 31 is the 33rd bit of Keri's preamble, not payload. We printed the whole field, so a tag cloned `--cn 12345` read back as `Internal ID: 80003039` against the Proxmark's `12345`. Both print sites now show **`Internal ID: 12345   (raw field 80003039)`** — the reference's number AND the value `lf keri write --id` demands, which refuses a clear top bit for the same reason. Verified on the tag: `lf keri read` and `lf keri econfig` both print 12345, and the econfig block form is `00000004000181CF`, the vector `ctest` already pins. ⛔ **RETRACTED**: I also reported our MS card number disagreeing with the Proxmark's, 12544 against 3223872, and called it unexplained. It is explained and it is not a disagreement — **I compared our settled read against the Proxmark's reading taken seconds after its own write**, which was the marginal frame `E00000008000377E`. That frame descrambles to exactly 3223872; the settled frame descrambles to 12544 on BOTH tools. The tables and the loop are byte-identical between `keri_descramble` and `CmdKeriMSScramble`. ⇒ **Never compare a settled reading against one taken immediately after a write** — and I had already noticed that read reporting id 14206 instead of 12345 before using its other field anyway | 2 print sites verified on a cloned tag + 3 Proxmark reads + the descramble computed from both sources | ✓ the fixed value is confirmed against the `--cn 12345` we chose, not against either reader | ✓ the retraction is arithmetic, not judgement: 0x8000377E descrambles to 3223872 and 0x80003039 to 12544 | L222, L223 |
| C260 | ⭐⭐⭐ **THE DISPLAY LAYER AUDITED ACROSS TEN PROTOCOLS AGAINST THE PROXMARK, ON THE SAME TAG EACH TIME — one defect, one gap, eight clean.** Every read arm in this campaign scores the RAW FRAME, so what the CLI *prints* had never been checked in any protocol. Each row is a Proxmark clone of a credential we chose, read by both tools within the same minute: **HID Prox** FC 123 / CN 4567 ✔; **ioProx** version 1 / facility 101 / id 1337 against pm3's `XSF(01)65:01337` ✔; **Indala26** Fmt 26 / FC 52 / Card 63612 ✔; **NexWatch** card 12345678 / mode 0 / nexkey ✔; **Gallagher** region 1 / facility 4321 / card 6789 / issue 2 ✔; **Noralsy** card 112233 / year 2022 ✔; **PAC** CN `CD4F5552` ✔; **GProxII** len 26 / FC 45 / card 6789 / xor 200 ✔; **Keri** — ⛔ the one DEFECT, fixed in C259. ⚠ **Securakey is the one GAP**: we print the raw frame and nothing else, where the Proxmark prints `len: 26 FC: 0x35 Card: 64169` and `Wiegand: 006BF553 parity ( ok )`. ⭐ **And that parity line is the middle path C253 did not consider**: the reference REPORTS the Wiegand parity without gating on it, which gives the operator the signal with none of the risk of refusing a real tag. ⇒ Nine of ten displays are right; the tenth does not exist | 10 protocols × (1 wipe + 1 clone + 1 read each on both tools) | ✓ every credential is one WE chose and passed to the clone, so both readers are checked against a third thing rather than against each other | ✓ each comparison is same tag, same minute — the failure mode C259 was retracted for | L224 |
| C261 | ⭐⭐ **SECURAKEY'S DISPLAY EXISTS NOW, MATCHES THE PROXMARK FIELD FOR FIELD, AND TAKES THE MIDDLE PATH C253 MISSED — the Wiegand parity is REPORTED, not gated.** C260's one gap: we printed the raw frame alone where the reference prints a credential. `_securakey_fields()` transcribes `protocol_securakey_decode`'s 26-bit branch — FC is bit 36 plus bits 38-44, card is bit 45 plus 47-54 plus 56-62, split that way because the 9-bit zero spacers `securakey_accept` gates on (C253) cut across the fields. **Checked against the Proxmark BEFORE shipping and verified on the tag after:** ours `Length: 26  FC: 0x35 (53)  Card: 64169` / `Wiegand: 006BF553  parity ok`, the Proxmark `len: 26 FC: 0x35 Card: 64169` / `Wiegand: 006BF553 parity ( ok )` — the wiegand word byte-identical. ⭐ **Deliberate break, A/B/A on the tag**: frame bit 38 flipped, which leaves every spacer and the length field intact so the GATE still passes it — read **4 of 4**, fields `FC: 0x75 (117) Card: 64169`, and **`PARITY FAILS` flagged**; restored, 4 of 4 correct. ⭐ On that broken frame the Proxmark prints the fields and NO wiegand line at all, so we give more signal than the reference, not less. ⇒ C253 chose between enforcing those two parity bits and ignoring them and picked ignore, for the good reason that being stricter than both references refuses real tags. **Reporting was the third option and it costs nothing** | 12 device reads across 3 tag states + 2 Proxmark reads | ✓ **A/B/A** (M35), and the break is chosen so the gate CANNOT be what changes the outcome — only the display can | ✓ the transcription was checked against the reference's printed output, including the 32-bit wiegand word, before a line of it shipped | L225 |
| C262 | ⚠ **THE UPSTREAMING ASSESSMENT HAD GONE STALE IN FOUR PLACES, AND THE COMMAND-ID COUNT WAS WRONG IN BOTH DIRECTIONS.** §9 is the document a reviewer is handed, and it was written before the FSK and biphase families existed. Recounted by diffing `data_cmd.h` against `main`: **48 new ids**, not the 46 the row claimed — **3033-3062** (30) and **5014-5031** (18) — and the ranges it gave missed Indala's own emu-id pair at 5014/5015 and GProxII's at 5030/5031. ⭐ **The number that matters is 41**: drop the three instrumentation ids (3037, 3038, 3060) and the four emu-id pairs belonging to the two emitters §9d marks DO NOT SHIP. ⛔ **Three further staleness bugs, each of which would have misled a reviewer**: the protocol grid still called HID Prox read "intermittent, 15-20% (C45)" three claims after C250 closed it; §9a's evidence grid silently OMITTED six protocols — AWID, Paradox, Pyramid, FDX-A, GProxII, FDX-B — a third of the set; and the blocker "two formats have no payload check" justified itself with *"both references are the same"*, which was never checked and is false for Securakey (C253). ⇒ A grid that omits a third of its rows is worse than no grid | 4 corrections, 1 count re-derived from a diff | ✓ the id count is a `comm` between two checked-out headers, not a memory or a range quoted forward | ✓ each stale row is contradicted by a claim already in this ledger, so the staleness is demonstrable rather than asserted | L226 |
| C263 | ⭐⭐ **THE INSTRUMENTATION SPLIT IS SUBTRACTION, NOT SURGERY — checked, not assumed, and now a checklist with line numbers (§9h).** §9b has said "strip all three or land them separately" since it was written, which is a decision without a plan. Every site located by `grep`: **3037 `LF_EMU_DEBUG`** and **3038 `LF_RADIO_DEBUG`** are 5-line `static` handlers reached only through their own dispatch rows, with one CLI command each; **3060 `LF_READER_CAPTURE`** is a 39-line handler plus `lf_reader_capture_probe()`, which has **exactly one caller**; and the **GProxII failure-energy payload** turns out to be the cheapest of the four — `scan_gproxii()` ALREADY EXISTS at `lf_reader_main.c:192` as the plain wrapper that calls `scan_gproxii_energy()` and discards the energy, so the strip is two lines. ⇒ **Nothing shippable depends on any of them.** ⭐ `rdrcap.py` needs nothing done to it: it hardcodes `CMD = 3060`, never imports the enum, and lives under `research/`, which is in no PR. ⛔ **Deliberately NOT executed here** — removing them would break the tooling this branch exists to use, and the cut belongs in the PR preparation. ⚠ What is lost is named rather than binned: the probe cracked C211 after six refuted hypotheses, and the failure energy is what separates a bad capture from a bad decode on a silent read (C206) | 4 items × every call site grepped across firmware and host | ✓ the "nothing depends on them" claim is a caller count, not an impression — one caller for the probe, zero for the two debug handlers beyond their dispatch rows | ✓ the GProxII sibling was found in the tree rather than proposed | L227 |
| C264 | ⛔ **SECOND INSTRUMENT FOR `require_repeat`, BUILT TO C254'S OWN DESIGN, AND IT DOES NOT REACH THE FLAG EITHER — proved by toggling the flag rather than by reasoning about it.** C254 wrote down what the arm needed: a buffer capped at EXACTLY two passes, the corrupted one first, on a format whose emitted length does not depend on the data (so Keri and Indala26, PSK1 DIRECT at 64 bits — NOT Indala224, whose differential emitter doubles its buffer on odd parity, which is how the first attempt fooled itself). Built with a sanity check (two CLEAN passes must decode, or the numbers are just "the buffer is too short") and a control. ⛔ **`require_repeat` toggled ON for Indala26 — the format under test, edit confirmed on disk, `rm -f roundtrip` then a full rebuild — and the numbers did not move by one: 0 rejected / 41 read-the-clean-copy / 23 wrong, both ways.** Keri's row likewise did not move when its flag was turned OFF. ⇒ **The arm is backed out.** ⚠ The flag is NOT inert — C258 measured it changing real captures (Indala26 51 true frames → 43, Keri's chosen bit position 69 → 133). It is the SYNTHETIC two-pass buffer that fails to reach it, and why is not established. ⭐ **One observation survives its own instrument and is labelled as what it is**: given one corrupted copy and one clean copy in the same buffer, the decoder returns the CORRUPTED one 23 times in 64 — a confident wrong credential with a good frame sitting right behind it. That is about the candidate-selection rule, not about `require_repeat`, and it is host-synthetic and unverified on hardware | 4 builds: baseline, Keri flag off, Indala26 flag on, and a forced clean rebuild to rule out a stale binary | ✓ the disproof is a flag toggle on the format under test, which is stronger than any argument about why it should have worked | ✓ C258's own wording already said the benefit was ARGUED, not measured — it still is | L228 |
| C265 | ⭐⭐ **C260's AUDIT WAS NOT COMPLETE — three more protocols print derived fields and one of them was wrong.** C260 checked ten and called the display layer done; `lf fdxb`, `lf idteck` and `lf jablotron` also derive fields and were never compared. Checked the same way, a Proxmark clone of a credential we chose read by both tools: **FDX-B** country 999 / national 1234567890 / animal True ✔ — identical to `lf fdxb reader` and to the `-c 999 -n 1234567890 -a` cloned; **Jablotron** card 12345678 ✔ (we print decimal where the Proxmark prints `bc614e`, the same number — and that command is upstream's, not ours). ⛔ **IDTECK was the defect: we printed `Checksum byte: 55` and stopped**, where the reference prints the same byte AND whether it holds. An operator comparing the two tools got a number from us and a judgement from it. ✅ Fixed and verified on the tag in BOTH directions — `4944544b55667788` now reads `Checksum: 0x55 (fail — calculated 0x65)` against the Proxmark's `chksum 0x55 ( fail )`, and `4944544b65667788` reads `0x65 (ok)` against its `( ok )`. ⚠ **Reported, not gated, and with the reference's own caveat carried across**: `cmdlfidteck.c` marks this "checksum check (TBD)" and the worked example in that comment does not agree with the code beside it. There is no real IDTECK tag on this bench, so an "ok" here has never been seen on a genuine credential — only on one we constructed. ⇒ Matching the reference byte for byte is the most that can honestly be claimed | 3 protocols × (wipe + clone + read on both tools), IDTECK twice for the two branches | ✓ each credential is one WE chose and passed to the clone | ✓ the fix is verified on its FAILING branch as well as its passing one, which is the half a display fix usually skips | L229 |
| C266 | ⭐⭐⭐ **THE FALSE FRAME IS REAL, IT IS 3 IN 52 RATHER THAN 1 IN 4, AND ITS MECHANISM IS NOW EXACT: A MIS-SLICED BIT RE-BALANCES THE PARITY THE TAG'S OWN FRAME BREAKS.** C255 saw one self-consistent wrong frame in four reads of a deliberately parity-broken GProxII tag; n=4 cannot carry a rate and it read as 25%. Re-run on the same tag, same build, two blocks of 24: **24/24 silent, then 22/24 silent with 2 false frames** — so **3 in 52 across everything, ≈6%**. ⛔ **Every false frame carries the RIGHT CARD NUMBER AND A WRONG FACILITY CODE**: card 6789 in all three, FC 39, 46 and 51 against the tag's 47 and the true 45. ⭐ **Two of the three are ONE BIT from the frame on the tag** — the tag's frame fails Wiegand parity by construction, and a single mis-sliced bit inside the same parity group restores it, so the frame passes every check both references make. C255's was four bits out, so the mechanism is not only single-bit. ⇒ **The gate works** — it refuses the tag's own frame 49 times in 52 — **and a wrong FC with a right CN is exactly the near-miss that matters in access control**. ⚠ This needed a tag deliberately broken to produce on demand; nothing says a naturally marginal tag does the same at the same rate | 52 reads on the corrupted tag + 12 control reads before + 8 after | ✓ **A/B/A** (M35) — valid frame 12/12 exact before, 8/8 after, so the bench did not drift across the hour | ✓ each false frame was re-run through the full gate offline: spacers intact, length 26, Wiegand parity OK — they pass on their own merits, not through a hole in the reader | L230 |
| C267 | ⭐⭐⭐ **A VALID TAG AT MARGINAL FIELD PRODUCES WRONG CREDENTIALS TOO — 4 in 14 frames — AND THE TWO-AGREEING-STACKS RULE IS WHAT STOPS THEM, NOT THE FRAME GATES.** C266's false frames needed a deliberately parity-broken tag, which leaves open whether ordinary marginality does the same. It does, and worse. The tag holds the CORRECT GProxII credential throughout; `rdrcap.py` captures through the reader's own path at the reader's own six phases, decoded by the shipping decoder: **drive 7 — 6 captures, 6 frames, 6 exact, 0 false**; **drive 6 — 18 captures, 12 frames, 4 of them not the true raw**; **drive 4 — 18 captures, 0 frames at all**; **drive 2 — 18 captures, 2 frames, neither the true raw**. ⛔ **Scored on the CREDENTIAL rather than the raw frame — which is what a user acts on — 4 of the 14 frames are WRONG**: FC 44/card 7044, FC 45/card 6861, FC 40/card 6789 and FC 12/card 21124, against the true FC 45 / card 6789. ⚠ **The other two "false" raws decode to the CORRECT credential** — they differ only in bits outside the credential field, so classifying by raw hex OVER-COUNTS and saying so matters. ⭐⭐ **All four wrong credentials are DISTINCT, so the reader's two-agreeing-stacks rule would have rejected every one.** That is a measured argument for the agreement rule and against leaning on the frame gates: C252's table cannot see this at all. ⚠ **And the contrast with C266 is the sharp part** — on the BROKEN tag, false frames survived that same rule 3 times in 52 device reads, so they repeated. ⇒ Marginal field: caught by agreement. Broken frame: not always | 60 captures through the reader's own path at 4 drives × 6 phases, decoded by the shipping decoder; 8 committed under `caps/gproxii-marginal/` | ✓ drive 7 is the control and it is 6/6 exact in the same minutes | ✓ every false frame re-run through the full gate offline — spacers, length and Wiegand parity all pass, so they are not a hole in the reader | L231 |
| C268 | ⭐⭐⭐ **NOT ONE MARGINAL-FIELD ERROR REPEATED IN 92 CAPTURES — so "two agreeing stacks would have caught them" stops being an inference and becomes a measurement.** C267 argued the agreement rule saves this reader because its four wrong credentials were distinct; four is not enough to rest a defence on. Re-captured 8 times at each of the four drive-6 phases that had produced an odd frame — 32 more captures, same valid tag, same path. **19 frames, and every non-true frame appeared EXACTLY ONCE**: the true frame came back 5, 4, 0 and 4 times at the four phases while six odd frames came back once each. ⇒ Across both runs: **92 captures, 33 frames, 12 odd raws, and all 12 unique.** A marginal capture's error does not reproduce, so a rule demanding two agreeing captures rejects it — measured, not argued. ⛔ **9 of the 12 are WRONG CREDENTIALS** (FC 44/7044, 45/6861, 40/6789, 12/21124, 108/6789, 45/4749, 13/15012, 45/8069, 44/2693 against a true FC 45 / card 6789), so what the agreement rule is catching is not cosmetic. ⭐ **And the pattern differs from C266's**: the broken tag's false frames ALL kept card 6789 because they sat one bit from a frame whose card field was intact, while marginal-field errors scatter across both fields. Two different failure modes, and only one of them repeats | 92 captures at 4 drives × 6 phases plus 4 phases × 8 repeats, all through the reader's own path | ✓ the true frame repeating 13 times across the same captures is the positive control — the pipeline CAN produce a repeat, so "none repeated" is not an artefact of never seeing one | ✓ each odd frame re-run through the full gate offline and each passes on its own merits | L232 |
| C269 | ⭐⭐⭐ **WHY C266'S FALSE FRAMES SURVIVE AGREEMENT AND C268'S DO NOT: the broken tag's error population is SMALL AND CONCENTRATED, so two captures can land on the same wrong frame.** Captured the parity-broken tag through the reader's own path at its own drive and six phases, 8 repeats each — 48 captures, the same method C268 used on the valid tag, so the two are finally comparable. ⭐ **The on-tag frame was returned 0 times in 48**: the Wiegand gate refuses it on every single capture, which is the gate working exactly as C255 built it. **13 frames, 12 distinct, and 12 of 12 are WRONG CREDENTIALS** — where the valid tag at marginal field gave 9 wrong of 12 (C268). ⛔ **11 of the 12 keep card 6789 and differ only in the facility code** (43, 63, 111, 175, 15 …), because the tag's frame already fails parity by one bit and only a small nudge re-balances it — so the nudges land in the same few places. ⭐⭐ **One frame recurred — `f84602a4611ad4a114211046`, FC 43 / card 6789 — at two DIFFERENT capture phases.** ⛔ **I read that as the shape that passes two-agreeing-stacks, and it is NOT — corrected by C293.** The rule requires two CONSECUTIVE decodes at the SAME phase and resets when the phase changes, so cross-phase recurrence never satisfies it. What does defeat it is deterministic distortion at one configuration, which our own `gproxii_read` comment has documented since C207. **Marginal field scatters its errors and agreement catches them all (C268: 12 odd frames, 0 recurrences); a broken frame concentrates them and agreement sometimes cannot.** ⚠ Both captures of the recurring frame decode at the decoder's internal phase 4 despite being taken at SAADC phases 104 and 120 — the alignment, not the capture phase, is what repeats | 48 captures + an 8-read A/B/A control on the restored tag; 3 committed under `caps/gproxii-broken/` | ✓ the restored tag reads 8/8 exact afterwards, so the bench did not drift | ✓ the two committed captures reproduce the recurring frame from two different phases, checkable without the bench | L233 |
| C270 | ⭐⭐⭐ **THE FALSE-CREDENTIAL PROBLEM IS A PHYSICAL-LAYER PROPERTY OF RF/64, NOT A WEAK GATE — and the protocol with the STRONGEST gate is the only one that has it.** Swept field strength on all three ASK-family protocols through the reader's own capture path, same tag position, same session: **Gallagher (RF/32, half-bit 16 samples)** — drives 7/6/4/2 all exact, **0 false in 40 captures**, then a cliff to 0 frames at drive 1; **Securakey (RF/40, 20 samples)** — identical shape, **0 false in 40 captures**, cliff at drive 1; **GProxII (RF/64, 32 samples)** — degrades GRADUALLY into wrong answers, 12 odd raws and 9 wrong credentials in 92 captures (C267, C268). ⭐ **The front end's AC coupling has a ~27-sample time constant (C204).** RF/32 and RF/40 put their half-bit well inside it, so the level is either cleanly sliceable or gone — perfect, then nothing. RF/64 puts it OUTSIDE, which is why GProxII has a degraded regime at all and why that regime produces frames rather than silence. ⛔⛔ **This inverts the natural reading of C252's table.** GProxII's gate is the STRONGEST of the three — preamble, 18 spacers, format length and Wiegand parity — and Securakey's is the weakest, spacers only; yet GProxII produces wrong credentials and Securakey produces none. **Gate strength and field robustness are independent, and C252 measures only the first.** ⚠ Zero false in 40 captures is good evidence, not a zero rate; Gallagher and Securakey were swept at 4 phases × 2 repeats against GProxII's 92. ⚠ FDX-B is RF/32 and should sit in the safe class — predicted, not measured | 120 captures across 3 protocols × 5 drives, all through `lf_reader_capture_probe` | ✓ each protocol's own known-good credential, cloned by the Proxmark, with drive 7 as the in-sweep control every time | ✓ the cliff at drive 1 reproduces on both ASK protocols and on two separate passes, so "no marginal regime" is a measurement rather than an absence of looking | L234 |
| C271 | ⚠ **`rdrcap.py --drive 0` SILENTLY MEANS DRIVE 4.** `cmd_processor_lf_reader_capture` reads it as `(data[2] != 0) ? data[2] : 4`, so a zero is the default rather than a floor — and `lf_125khz_radio_drive_set()`'s own clamp (`<1` → 1) never sees it. It cost twenty minutes: drive 0 read a tag 8 of 8 where drive 1 read 0 of 8, which looks exactly like C148's inert-drive defect striking mid-session and was chased as such. ⇒ The tool's `--drive` help should say so, and any drive sweep should start at 1 rather than 0 | 3 sweeps at drive 0 and drive 1, reproducing both ways | ✓ settled by reading the handler, not by inferring from behaviour — the behaviour is what made it look like a hardware fault | ✓ C148 is EXCLUDED rather than assumed away: drive 1 gave 0-of-4 twice more after the drive-0 result, so the control was live throughout | L234 |
| C272 | ⭐⭐⭐ **FDX-B SETTLES C270'S HYPOTHESIS THE HARD WAY: SAME BIPHASE DECODER AS GPROXII, RF/32 INSTEAD OF RF/64, AND ZERO WRONG CREDENTIALS.** GProxII and FDX-B share `lf_ask_biphase.c` whole — same decoder, same acceptance machinery, same capture engine — and differ in bit rate and format. Swept the same way: **56 captures, 24 frames, and not one wrong credential.** ⇒ The false-credential behaviour cannot be the biphase decoder, because the decoder is identical; it is RF/64's half-bit exceeding the front end's ~27-sample AC-coupling constant, exactly as C270 proposed. ⭐ **One raw frame did differ, and it is the case C267 warned about**: `…58044201` against `…58040201`, a SINGLE bit at position 113 — past bit 64, in the tail the CRC-16 does not cover — and country 999 / national 1234567890 / animal all decode identically. A wrong raw is not a wrong credential, and scoring by hex would have logged a false frame that does not exist. ⚠ The unprotected tail is the reader's own printed warning (`the CRC covers the first 8 bytes only`), and this is the first time it has been seen to matter | 56 captures across 5 drives × 4 phases; 2 committed under `caps/fdxb-drive/` | ✓ the discriminator is the SHARED DECODER — Gallagher and Securakey use a different one, so C270 alone could not separate decoder from bit rate and this does | ✓ the differing bit was located to position 113 and the credential recomputed from both raws, not assumed equal | L235 |
| C273 | ⛔ **FDX-B READS AT DRIVE 4 AND AT NO OTHER FIELD STRENGTH — 16 of 16 at drive 4, 0 of 16 at drive 7, 0 of 16 at drive 6, 0 of 8 at drives 2 and 1.** Reproduced across two independent passes. ⭐ It works today only because the shared ASK reader sweeps `{4, 7, 6, 2}` **with 4 first**, which is why C214 read it 6/6 without anyone noticing how narrow the window is. ⛔ **That ordering is load-bearing and nothing says so in the code**: reorder the sweep, shorten it, or pin a drive the way `gproxii_read()` pins 7, and FDX-B stops reading with no other symptom. ⚠ C182 established the opposite failure for Noralsy — it decodes at drive 7 and at no other — so the sweep is carrying two protocols that need DIFFERENT single settings, and neither is documented at its call site | 56 captures over 5 drives, drive 7 and 6 re-run in a second pass to rule out a fluke | ✓ drive 4 giving 16/16 in the same sweeps is the positive control — the tag and the path are fine, it is the field setting that decides | ✓ two passes hours apart in the same session, same tag position | L235 |
| C274 | ⛔ **A COMMENT IN SHIPPING CODE ASSERTED A SAFETY PROPERTY THE SWEEP DOES NOT HAVE — corrected, and nothing is broken today only because of a 3× margin nobody had noticed was load-bearing.** `LF_ASK_DRIVE_STEPS` said *"step order matters for latency, not correctness"*. C182 has Noralsy reading at drive 7 and no other; C273 has FDX-B reading at drive 4 and no other. For those two the sweep is not an optimisation, it is the only reason they read. ⛔ **And the table TRUNCATES**: `steps = timeout_ms / 250` takes a PREFIX, so a 250 ms budget reaches only drive 4 and 500 ms only {4,7} — Noralsy needs the second entry and would go unreadable with no other symptom. ⭐ **Traced to the actual numbers rather than left as a worry**: every caller passes `INDALA_READ_TIMEOUT_MS = 3000`, which asks for 12 steps and clamps to 4, so all four are always tried and **there is no live bug**. ⇒ The margin is the whole protection, and *shortening the timeout to make reads snappier* is the innocuous-looking change that breaks Noralsy. The note now says so at the table | 5 timeout values worked through against the step arithmetic; the firmware rebuilt clean | ✓ the claim is checked against the constant every caller actually passes, not against the host's 10 s CLI timeout, which is a different number and does not govern this | ✓ comment-only change — no behaviour to verify, and saying that is better than inventing a test for it | L236 |
| C275 | ⚠ **A SAMPLE PHASE CAN BE USEFUL AT FULL SIGNAL AND EMIT NOTHING BUT GARBAGE AT MARGINAL SIGNAL — so a phase map measured at one signal level cannot show which phases are dangerous.** Aggregated the GProxII captures by CAPTURE phase, the way the Indala 60-92 dead band was mapped. **At drive 7, the reader's own setting, all six phases return the true frame.** At drives 6 and 2, 14 captures each: phase 0 → 7 true / 2 odd, phase 112 → 6/2, phase 20 → 6/4, and **phase 120 → 0 TRUE, 3 odd, 11 silent — it never once produced the correct frame**. On the parity-broken tag at drive 7, phase 120 produced **6 of the 13 odd frames** from one of six phases. ⇒ Phase 120 is not a dead band at the operating point; it is **the first phase to fail as the field weakens, and the only one that yields exclusively wrong frames once it does**. ⛔ **This is why "drop it" is NOT the conclusion**: at drive 7 it is a contributing phase, and the reader runs at drive 7. ⇒ What it does establish is that `PHASE_ROTATION`'s own warning — *"one tag, one unit, two coupling geometries … it could move"* — understates the problem: the ranking is a function of SIGNAL LEVEL too, and every phase map in this tree was measured at one level | 56 valid-tag captures across 4 phases at 2 marginal drives + 48 broken-tag captures across 6 phases + 6 drive-7 controls | ✓ the drive-7 row is the control and it is TRUE at all six phases, so "phase 120 is bad" is false as stated and the finding had to be narrowed | ⚠ **the drive-7 control is one capture per phase** — enough to refute "dead band", not enough to rank phases at the operating point | L237 |
| C276 | ⭐⭐ **THE UNPINNED HID READ NOW SAYS IT IS A GUESS — and `unpack()` turns out to have EXACTLY ONE CALLER, which narrows the upstream conversation considerably.** C251 measured a corrupted H10301 frame coming back as a confident `Indala 26-bit` 6 times in 7, because `unpack()` returns the FIRST layout that fits and we print it as the answer — where the Proxmark prints EVERY match under a *"False Positives ARE possible"* banner. `lf hid prox read` now prints, when and only when no format was pinned: *"⚠ no format pinned — this is the FIRST layout that fits, not the only one"*, names the exact `-f` flag to require it, and quotes C251's 7-in-48 against 1-in-48. Verified on a real H10301 clone: unpinned warns, `-f H10301` is silent. ⭐⭐ **And §9f was too broad about the risk of changing the walk.** It said narrowing `unpack()` touches *"every reader that guesses a format"*. A whole-tree grep says there is **one caller: `hidprox.c:134`.** Nothing else in the firmware calls it — ioProx has a fixed XSF layout and Indala prints its own 26-bit interpretation without the table. ⇒ The upstream question is not "do not disturb the shared walk", it is a change confined to one reader — worth knowing before the conversation starts | 2 device reads on a cloned H10301, one per branch + a whole-tree caller grep | ✓ both branches exercised, not just the one that shows the new text | ✓ the caller count is a grep over `application/src`, excluding the `unpack_*` per-format helpers and the definition itself | L238 |
| C277 | ⭐⭐⭐ **A VALID H10301 FRAME IS ACCEPTED BY TWO FORMATS, AND A CORRUPTED ONE IS NEVER STILL H10301 — measured exhaustively against the shipping `wiegand.c`, not sampled.** New `ctest/ambig.c` compiles the real `wiegand.c` (Nordic's macros shimmed verbatim, `parity.h` included as-is) and asks `unpack()` one format at a time. **The bench credential FC 123 / CN 4567 is accepted by H10301 (FC 123, card 4567) AND by IND26 (FC 1969, card 471)** — the same two the Proxmark printed under its *"False Positives ARE possible"* banner at the start of this session. ⇒ **`unpack()`'s first-match ordering decides the answer on EVERY read, not only on corrupted ones**, and `lf hid prox read` reports H10301 solely because it precedes IND26 in the table. ⛔ **And of all 26 single-bit corruptions: 13 are rejected by every format, 13 are first-matched by a DIFFERENT format, and ZERO still match H10301.** That is why C251 saw 6-in-7 relabelled — relabelling is not an accident, it is what corruption does here. ⇒ **Narrowing the walk is therefore NOT cheap** (C276 wondered): reorder or truncate the table and working readers change what they report. ⭐ Counts are PINNED in `make check`, so a table edit says so | 1 valid frame + 26 corruptions × 42 formats, exhaustive | ✓ the shipping `wiegand.c` is compiled, not transcribed — the answer is the firmware's own | ✓ the two-match result independently reproduces the Proxmark's own output on the same credential | L239 |
| C278 | ⭐⭐⭐ **`lf hid clone -w ind26 --fc 1969 --cn 471` AND `-w H10301 --fc 123 --cn 4567` PRODUCE THE SAME TAG — the ambiguity is not a corruption artefact, it is the format table.** Wrote a genuine Indala-26 credential onto an HID Prox tag with the Proxmark; our reader returns **H10301 FC 123 / CN 4567**, the credential C248 has been using all session. Identical 26 bits, two valid readings, and which one you are told depends on which tool you ask. ⭐ **Swept in `ctest/ambig.c`: H10301 accepts ALL 8,658 validly packed ind26 frames tested.** ⇒ At 26 bits a non-HID format can NEVER win the walk, because H10301 precedes it and accepts everything it accepts — so a genuine Indala-layout tag reports as H10301 and the "no HID layout fits" path is reachable **only through corruption**, which is exactly what C251 saw. ⛔ **This caught a wrong message before it shipped, twice.** The first draft asserted a non-HID match means corruption; writing a real ind26 tag showed that a non-HID credential is a real thing, so I hedged it to "either" — and the sweep then showed the hedge is wrong AT 26 BITS, where it really is only corruption. The message is now length-aware: corruption for IND26, "the reader cannot tell" for longer formats where no HID layout need accept a valid foreign credential | 4 ind26 credentials written to a real tag + an 8,658-frame overlap sweep against the shipping `wiegand.c` | ✓ the same-tag result is two independent tools disagreeing about one physical frame, which is stronger than either tool's self-report | ⚠ **the corruption branch's text is NOT freshly verified on hardware** — reaching it needs the guard-off build, and C251's six observations are what say it fires | L240 |
| C279 | ⭐⭐ **C278'S RELABELLING MESSAGE HAS NOW BEEN SEEN TO RENDER — on the branch it was written for, which no test could reach without deliberately corrupting a capture.** C278 shipped user-facing text on a path C251 proved FIRES but that nobody had walked with the text in place; a wrong f-string there would have stayed wrong indefinitely. Flashed the guard-off probe (`512a7be`), read the H10301 tag 40 times, and **read 29 walked it**: `HIDProx/Indala 26-bit` followed by *"⛔ no HID layout fits this frame … At 26 bits that means the capture was corrupted … The numbers below are fiction — re-read"*, then `FC: 1973  CN: 471`. ⭐ **And the credential is C266's signature in a second protocol**: the uncorrupted frame's ind26 reading is FC 1969 / card 471, so the corruption moved the facility code by 4 and left the card number untouched — right card, wrong facility, exactly as the broken GProxII tag behaved. ⚠ **1 of 40, where C251 measured 7 wrong in 48 with 6 relabelled** — a different session and coupling, and not reconciled; the rate is reported, not claimed to match. ⭐ The restore also brings #2 to HEAD, closing the "one commit behind" note | 40 guard-off reads + 12 guard-on closing reads across 2 flashed builds | ✓ **A/B/A** (M35) — 12/12 correct on the restored build, so the bench did not drift | ✓ the branch was reached by the same instrument C250/C251 used, not by a mock or a unit test standing in for it | L241 |
| C280 | ⛔ **THE MESSAGE C278/C279 SHIPPED WOULD HAVE CRIED WOLF ON EVERY 27-, 28-, 29- AND 30-BIT READ — because there is NO HID FORMAT AT THOSE LENGTHS AT ALL.** Extended `ctest/ambig.c` to ask, per bit length, whether some HID format accepts every valid foreign frame there. **26, 32 and 37 bits: yes, totally** (121/121, 363/363, 121/121) — a foreign winner is necessarily corruption. **34 bits: partly** (307 of 484) — both explanations live, the reader cannot tell. ⛔ **27, 28, 29 and 30 bits: there is no HID format to accept anything.** `ind27`, `indasc27`, `tecom27`, `w2804`, `ind29` and `atsw30` are the ONLY layouts at those lengths, so a foreign result is simply what the reader found — and the `⛔ no HID layout fits this frame` text would have fired on every one of them, every time. ⇒ The message now branches three ways on measured ground: corruption at 26/32/37, "the reader cannot tell" at 34, and a neutral note where no HID format exists. ⚠ Lengths above 37 are NOT measured — the credential grid produces no valid frames there — and fall through to the cautious wording. ⭐ The per-length VERDICTS are pinned in `make check` (not the counts, which depend on the grid), because the CLI now branches on them | 8 bit lengths × every foreign format × a credential grid, against the shipping `wiegand.c` | ✓ caught by extending the measurement rather than by a user hitting it — the 27-30 case has no tag on this bench and would never have appeared | ✓ vendor labels taken from the table's own comments, so `MDI37 // PointGuard MDI` is foreign by the table's word and not by mine | L242 |
| C281 | ⭐ **THE AMBIGUITY SWEEP IS COMPLETE, NOT PARTIAL — there are no formats above 37 bits — and the unimplemented enum names are unreachable from the CLI, so the defect I went looking for does not exist.** C280 said lengths above 37 were "not measured". The real reason nothing appeared there: **`formats[]` has 31 entries and stops at 37 bits.** The firmware's `card_format_t` declares **42** names — AVIG56, BC40, BQT38, C1K48S, CASI40, DEFCON32, H800002, IR56, ISCS, P10001, PW39 have no table row — so `unpack()` can never return them and `pack()` returns 0 for them. ⇒ C280's per-length table covers every format the code can actually produce, and the CLI's cautious fall-through wording applies to nothing. ⛔ **The predicted defect was refused by the CLI**: `lf hid prox write -f C1K48S` errors with *invalid choice*, listing exactly 31 — because the HOST's `HIDFormat` enum has 31 members and mirrors the table, where the firmware's has 42. A silent zero-write is therefore not reachable, and the tag was untouched (both readers still H10301 FC 123 / CN 4567 afterwards). ⚠ What remains is dead surface a reviewer will trip over — 42 declared against 31 implemented, in a header that is upstream's ⚠ **And citing those names broke `checkdocs.sh`, which is its own small finding**: `refs()` matched `\bC[0-9]+` with no trailing boundary, so `C1K48S` read as a citation of claim C1 and was reported missing. The trailing `\b` is now there and its sensitivity re-proved with a deliberate `C999` | 1 rejected write + 3 device reads + 1 Proxmark read + an enum/table diff | ✓ the negative result is confirmed by the CLI's own error listing all 31 accepted names | ✓ the tag was verified unchanged by BOTH readers after the attempt | L243 |
| C282 | ⛔ **THE MESSAGE I ADDED IN C280 DROPPED THE ISSUE LEVEL AND OEM FIELDS — caught by re-reading what I shipped, not by any test, because no tag of those lengths exists on this bench.** The `no HID format at this length` branch printed its note and then `return`ed, re-printing FC and CN itself and skipping IL and OEM entirely; it also emitted a blank line when FC was 0. Restructured into a clean three-way branch that falls through to the shared field printing. ⭐ **And the classification is now verified structurally, which is the part hardware cannot reach**: the three sets are **disjoint**, and exactly four formats fall through to the "cannot tell" wording — `N10002`, `OPTUS34`, `SMP34`, `BQT34` — which are precisely the four non-HID 34-bit formats, the one length C280 measured as PARTIAL coverage (307 of 484). Every format the table implements lands in exactly one branch, and the branch each lands in matches its measured length. ⚠ **Three of the four branches remain unexercised on hardware** and will stay so: 27-30 and 34-bit tags do not exist here, and the corruption branch needs the guard-off build (C279 walked that one) | 2 device reads + a disjointness-and-coverage check against the real `HIDFormat` enum | ✓ the fall-through set matching C280's partial-coverage length exactly is an independent check on the classification — I did not build the sets from that | ✓ the regression was found by reading the shipped code, and is recorded rather than quietly fixed | L244 |
| C283 | ⭐⭐ **ALL FOUR BRANCHES OF THE HID MESSAGE ARE NOW EXERCISED ON HARDWARE — C282 said three of them never could be, and that was wrong.** The thing I had missed: the Proxmark WRITES those formats. `lf hid clone -w <fmt>` puts any layout in the shared table onto an HID Prox tag, so the branches are reachable with no corruption and no hands. **`-w ind27 --fc 100 --cn 200`** → *"note: no HID format exists at this frame length — Indala 27-bit is simply the layout that fits, and that is normal here"*, FC 100 / CN 200, **no ⛔ and no alarm** — which is C280's crying-wolf case shown not to cry. **`-w Optus34 --fc 1 --cn 1`**, a credential `ambig` named as taken by no HID format → *"⛔ no HID layout fits … at this length the reader cannot tell"*, FC 1 / CN 1. The hedge is correct there: this tag really does carry a foreign layout, which is case (a), and the reader says so rather than guessing. ⭐ With C279's corruption branch and the pinned/unpinned HID branches, that is 4 of 4. ⚠ Three N10002 credentials read back as **H10306** first — at 34 bits an HID format takes 307 of 484, so finding the branch needed `ambig` to name a frame in the other 177 rather than guessing one | 5 credentials written by the Proxmark and read back, + the full harness green | ✓ each branch's text checked against the credential that was cloned, not merely observed to appear | ✓ bench restored to H10301 FC 123 / CN 4567, 4 of 4 | L245 |
| C284 | ⭐⭐⭐ **HALF THE HID FORMAT TABLE CANNOT SURVIVE AN UNPINNED READ — 15 of the 29 formats written to a REAL tag come back as a DIFFERENT format with a DIFFERENT credential.** C277 measured the ambiguity for one format on the host; C283 showed the Proxmark can write any of them, so this asks the hardware for all of them. Every format the CLI accepts, cloned by `lf hid clone -w <fmt> --fc 1 --cn 1` and read back unpinned: **14 round-trip exactly** (H10301, IND27, IND29, ATSW30, ADT31, HCP32, D10202, H10306, OPTUS34, C1K35S, C15001, S12906, H10302, P10004) and **15 are relabelled** — IND26→H10301 (cn 4097), KASTLE→HCP32 (cn 8389632), KANTECH→HCP32 (cn 1024), WIE32→HCP32 (cn 512), HPP32→HCP32 (cn 4096), SMP34→Optus34 (cn 8), BQT34→H10306 (fc 256), N10002→H10306, SIE36→Simplex36 (cn 65537), H10320→H10302 (cn 21), H10304→H10302 (cn 524289), MDI37→H10302 (cn 536870913), HGEN37→P10004 (fc 3072), INDASC27→ind27 (cn 24), TECOM27→ind27 (fc 1152). ⛔ **The credential is wrong in every relabelled case, not just the name** — a Kastle tag holding fc 1 / cn 1 reads as Check Point card 8389632. ⭐ **And pinning recovers it exactly**: `-f KASTLE` → Kastle fc 1 cn 1, `-f IND26` → Indala 26-bit fc 1 cn 1, `-f SMP34` → Cardkey Smartpass fc 1 cn 1, 3 of 3. So the tags are perfect and the WALK is what loses the information. ⇒ C276's *"no format pinned"* warning is not a corner case; it covers half the table. ⚠ W2804 and ACTPHID are missing because the Proxmark refused every credential shape tried — a writer limitation, not a reader finding | 29 formats × (1 Proxmark clone + 1 unpinned read) + 3 pinned controls | ✓ the pinned control is what separates "the walk loses it" from "the tag is wrong" — without it this would only show disagreement | ✓ each credential is fc 1 / cn 1, chosen so the same value is legal in nearly every format and a relabel is obvious on sight | L246 |
| C285 | ⭐⭐⭐ **C284'S RELABELLING IS STRUCTURAL: 12 of the 31 `unpack_*` functions ACCEPT ANYTHING of the right bit length, so a first-match walk cannot be made correct.** Audited every unpacker for a rejection path: **19 have one** (parity, spacers or range) and **12 have none at all** — `ind27`, `indasc27`, `tecom27` (one shared `unpack_nonlinear` helper), `ind29`, `adt31`, `hcp32`, `hpp32`, `kantech`, `wie32`, `optus`, `smartpass`, `p10004`. ⇒ **That list IS C284's relabel map**: HCP32 swallows KASTLE, KANTECH, WIE32 and HPP32 at 32 bits; Optus34 swallows SMP34; ind27 swallows INDASC27 and TECOM27; P10004 swallows HGEN37. The walk lands on the first format that checks NOTHING. ⭐ **And the guarded ones did their job** — our reader returned Optus34 rather than H10306 for the SMP34 tag precisely because `unpack_h10306` verified parity and refused, which the Proxmark independently marks `parity ( fail )` on that frame. ⛔ **These cannot be hardened.** A format with no parity or checksum has nothing to check, and the reference agrees: `lf hid reader` prints a parity verdict beside H10306, N10002 and BQT34 and NONE beside HCP32 or Optus34. ⇒ **The walk is not narrowable into correctness (C277 asked); the only correct behaviours are the Proxmark's — print every candidate with its parity — or pinning with `-f`.** That is the conclusion to take upstream | 31 unpackers audited + 3 tags read by both tools, 5 candidates each (⛔ counts corrected from "13 of 32" by C300, which MEASURED them; the list and the conclusion are unchanged) | ✓ the check-less list predicts C284's relabel map exactly, which neither was derived from the other | ✓ the Proxmark's own parity column is independent confirmation that the check-less formats have nothing to check rather than checks we skipped | L247 |
| C286 | ⚠ **§9f HAD BECOME A RECORD OF HOW I GOT THERE, AND ITS TAIL ACTIVELY MISLED — rewritten.** The section a reviewer is handed had grown by accretion across C251→C285, with each new understanding appended below the last. Three statements in its tail were contradicted by claims above them: *"must not fix it here: the format walk is load-bearing for every reader that guesses a format"* (one caller, C276), *"what to report, not to patch"* (a mitigation shipped, C276), and — the damaging one — *"this is reachable only when something else is already perturbing the capture"*, which **C284 disproved on perfectly good tags**. A reviewer reading to the end would have concluded the defect only matters under corruption. ⇒ Rewritten to state what is true now, strongest measurement first: half the table fails an unpinned read on VALID tags, 13 check-less unpackers are why, narrowing cannot fix it, and corruption is a separate and smaller part of the story rather than the whole of it. Same length, and `LOG.md` keeps the history. ⭐ **The general lesson is the one NEXT.md's own convention already states and I was not following**: a planning document that only ever gets appended to becomes a stratigraphy, and the oldest layer is the one a reader reaches last | 1 section, 3 contradicted statements identified against the ledger | ✓ each contradiction is named with the claim that refutes it, so this is a demonstrable correction rather than a matter of taste | ✓ nothing was dropped — the corruption measurements are still there, ranked below the stronger result instead of standing in for it | L248 |
| C287 | ⭐⭐⭐ **`lf hid prox read` NOW NAMES THE OTHER LAYOUTS THAT FIT — the behaviour C285 identified as one of only two correct ones, built and verified against the Proxmark.** The firmware owns `formats[]`, so it does the enumerating: new `wiegand_other_matches()` returns how many other layouts of the same bit length accept the frame and names up to two, reported in **payload bytes 13-15, which were already present and already zero** — so an older host reading `[:13]` is unaffected and a newer host on older firmware reads "no other match", which is the honest degradation. ⛔ A host-side copy of 31 unpackers was rejected as exactly the "share the front end, do not resemble it" mistake; the host only maps ids to names. **Verified against the reference on three tags**: Kastle → Check Point + **4 others** against pm3's 5 candidates; SMP34 → Optus34 + **1** against pm3's 5, of which 3 are annotated `parity ( fail )` and our unpackers correctly refuse; H10304 → H10302 + **3** against pm3's 5, one of which fails parity. ⇒ **Our counts equal the Proxmark's parity-PASSING candidates exactly, 3 for 3** — we report the genuine alternatives and omit the ones the reference itself marks as failing. ⭐ On the bench H10301 tag it reports exactly 1 other, `Indala 26-bit`, which is C277's two-format result appearing at the CLI. ⚠ Only two are named for want of payload space; the COUNT is exact and says `(+N more)` | 4 tags read on the new build + 3 compared candidate-by-candidate with `lf hid reader` | ✓ the count is checked against an independent tool's enumeration on every tag, not merely observed to be non-zero | ✓ the harness stayed green across the change | L249 |
| C288 | ⭐⭐ **EVERY READ ARM RE-VERIFIED ON THE CURRENT BUILD AFTER A DAY OF GATE CHANGES — 12 protocols, 48 reads, all exact.** Four acceptance gates were added or changed today (Securakey's spacers C253, GProxII's Wiegand parity C255, Indala26's zero bits C257, Keri's frame repeat C258) and one payload was extended (HID's other-layout enumeration C287). Each was verified when it landed; none had been checked against the others. On `5a5fdf7c`, each protocol wiped, cloned by the Proxmark and read 4 times: **HID Prox, Indala26, Keri, NexWatch, Gallagher, Securakey, Noralsy, GProxII, AWID, Paradox, Pyramid, FDX-B — 4 of 4 every one.** ⭐ **And the rate did not move where it plausibly could have**: C287 added 31 `unpack()` calls per successful HID read, so HID was re-measured credential-scored at **24 of 24 exact, 0 wrong, 0 null** — matching C248's 48/48 on the same tag before any of today's changes. ⇒ The four stricter gates cost nothing on real tags, which is the claim each of them needed and none of them could make alone. ⚠ This is a REGRESSION pass, not new evidence for any single gate — it re-runs known-answer arms and would not detect a gate that is too LOOSE | 12 protocols × 4 reads + 24 credential-scored HID reads | ✓ every credential is one the Proxmark wrote from arguments we chose, so a stale tag cannot pass | ✓ the one change with a plausible cost was measured against its own pre-change number rather than assumed harmless | L250 |
| C289 | ⭐ **THE EMULATE ARMS DID NOT NEED RE-RUNNING, AND THAT IS A DIFF RATHER THAN AN ASSUMPTION — which is what let Chameleon #1 be left alone.** C288 re-verified the read side after a day of gate changes; the obvious companion was to re-run C241's five working emulate arms, which would have meant flashing #1 — the unit not touched all session, holding a NexWatch slot, and carrying the documented `enter_dfu` targeting hazard. ⇒ Checked instead whether today's firmware could reach them. **Since `9a6285d` (the build C241 verified) seven firmware files changed: four under `reader/lf/` and three HID-only — `hidprox.c`, `wiegand.c`, `wiegand.h`.** Every file the five working arms run through — `keri.c`, `nexwatch.c`, `gallagher.c`, `securakey.c`, `noralsy.c`, `utils/psk1.c` and `lf_tag_em.c` — has **0 commits** in that range. ⇒ Provably unaffected, so the flash was not taken and the bench is left as the operator will find it. ⚠ The HID emulator DOES share `hidprox.c`, but its emulate arm is 0 of 6 already and has been since C246, so there is nothing there to regress. ⭐ **The general point is about when a regression run is evidence and when it is ritual**: re-running arms whose code cannot have changed proves only that the bench still works | 7 changed files against the 7 the working arms depend on | ✓ `git log --oneline 9a6285d..HEAD -- <file>` per file, not a reading of the diff summary | ⚠ this reasons from the FILE level — it would miss a behaviour change reached through a shared header, and no header the emulate path uses is among the three that changed | L251 |
| C290 | ⭐⭐ **C287'S NEW API IS NOW PINNED BY A SECOND, INDEPENDENT WALK OVER THE SAME TABLE — 1,519 frames, 0 disagreements.** `wiegand_other_matches()` decides what `lf hid prox read` tells an operator about the alternatives, and it was verified only by four hardware reads on four tags. New arm in `ctest/ambig.c`: for every format at every length, pack a credential and require the API's count to equal **this file's own per-format enumeration minus the excluded one**. Two different walks over `formats[]`, which is the whole point — the arm does not call the function it is checking. ⭐ **Sensitivity proven by deliberate break**: removing the `except` skip inside `wiegand_other_matches()` moved it from **0 disagreements to 1,519 of 1,519** and failed `make check`; restored, clean. ⇒ A table edit that changes what an operator is told now fails the harness instead of changing the message quietly | 1,519 packed frames across every format and length, before and after a deliberate break | ✓ the cross-check is an independent enumeration, not a restatement of the API — a bug in the API cannot hide in the expectation | ✓ the break was rebuilt from scratch (`rm -f ambig`) after the false-pass bug this harness was bitten by twice | L252 |
| C291 | ⭐⭐⭐ **THE FLIPPER FAMILY DOUBLES ITS REPEAT COUNT FOR PSK1 AND ONLY FOR PSK1 — two independent projects reached the same conclusion about the same family, from opposite ends.** U14, operator-requested. Audited all six reference trees on disk. **Three distinct models:** the **Proxmark** validates STRUCTURALLY and not at all by repetition — one capture, and it prints EVERY matching format with its parity verdict, `-@` merely repeating and printing each result independently; the **Flipper family** validates ONLY by repetition — N consecutive reads with **byte-identical** decoded data, any change resetting the count — and reports one protocol with no alternatives; **we** do both, plus C287's alternatives. ⭐ **All four Flipper worker files differ from each other and the agreement rule is identical in every one.** ⭐⭐ **`validate_count` is 3 almost everywhere and 6 for exactly six protocols: `indala26`, `indala224`, `keri`, `nexwatch`, `idteck` — the ENTIRE PSK1 family — plus `hid_generic`.** ⇒ This branch found PSK1 fragile from the other end: C190 measured our PSK1 emulation read **3 of 21** by a Proxmark where ASK read 16/16, because only absolute-phase encoding pays for a free-running clock. The Flipper authors emulate nothing and hard-coded twice the repeats on the READ side for the same five. ⭐ And `hid_generic` — the only non-PSK at 6 — is the format-agnostic HID reader, which is C284/C285 restated: when the layout cannot be validated, buy confidence with repetition. ⚠ **Official has no `indala224`; all three forks add it AND give it 6**, so the rule was applied deliberately rather than inherited. ⚠ **Why 3 and 6 is NOT established** — no tree comments it and none carries a test for it, where our own `INDALA_AGREE_COUNT` has a 50,000-trial bootstrap behind it | 6 trees, 24 protocols, 4 worker loops read | ✓ every claim cites the file and line that implements it, not the project's documentation | ✓ the four worker loops were compared by md5 first, so "identical rule" is a reading of four different files rather than an assumption of shared code | L253 |
| C292 | ⭐⭐ **OUR AGREEMENT COUNT OF 2 IS ENOUGH AND THE FLIPPER'S 6 WOULD BUY NOTHING — measured on our own captures, which also dates a warning in our own source.** U14 asked why the references pick 3 and 6; no tree explains it (C291), so the answerable question is whether OUR 2 is right. Ran the current shipping decoder over both committed corpora: **front, the documented placement — 160 captures, 110 frames, 0 WRONG**; **phasebits — 160 captures, 64 frames, 51 true, 13 wrong, and all 13 are DISTINCT**. ⇒ Agreement at 2 removes every wrong frame present, and 3 or 6 would remove nothing further because nothing repeats. ⛔ **And the note above `INDALA_AGREE_COUNT` is now history**: it warns that *"3 of the 21 front-side wrong frames REPEAT WITHIN A PHASE, two of them on 5 captures out of 5"*, which was measured before the straddle gate (C48) and Indala's zero-bit gate (C257). **Those 21 are now 0 on the same 160 captures.** ⇒ The paragraph is labelled as history rather than deleted — the 60-92 dead band is still out of `PHASE_ROTATION`, and widening that rotation would make the warning live again. ⚠ **This is 320 captures of ONE tag on ONE unit**; it says our 2 is sufficient here, not that 6 is wrong for a Flipper reading tags we have never seen | 320 committed captures through the shipping decoder, both corpora | ✓ the front corpus is the control and it has no wrong frames left to count, which is itself the result | ✓ "all 13 distinct" is a uniqueness check over the actual frames, not an inference from their being few | L254 |
| C293 | ⛔ **C269'S MECHANISM WAS WRONG, AND OUR OWN SOURCE HAD THE RIGHT ONE WRITTEN DOWN SINCE C207.** U14 asked me to place our rule against the references', which meant reading it rather than describing it from memory. **`lf_sampled_read_phases` requires two CONSECUTIVE decodes at the SAME sample phase to be byte-identical, and resets the comparison when the phase changes** — the source says why: *"a word decoded at one sample phase does not corroborate one at another"*. ⇒ **Cross-phase recurrence cannot pass the rule**, so C269's explanation of C266's three survivors is void. ⭐ The real mechanism is in `gproxii_read`'s own comment: *"at a fixed drive the distortion is the same in both captures, so they agree on the same wrong answer"* — deterministic distortion, not coincidence, which is why that reader pins drive 7 instead of sweeping. ⇒ Our rule is **as strict as the Flipper's and stricter in one respect**: theirs needs N consecutive identical reads, ours needs 2 consecutive identical reads *at the same sample phase* (C291). ⛔ **And a second stale warning found in the same file**, the same shape as C292's: `gproxii_read` still said *"this command ships as research, and the grid must say NOT VERIFIED rather than a read count"* — the open question it waits on was closed by C211/C213's 50ms inter-capture gap, after which the arm read 12 of 12 exact and 4 of 4 again today (C288). Dated, not deleted: everything above it still holds | the agreement loop and 2 protocol comments read against 3 claims | ✓ the correction comes from reading the implementation, which is what C269 should have done before inferring from an observation | ⚠ I closed this by saying **C266's 3 survivors are now UNEXPLAINED again**. ⭐ **C294 shows they are not**: the reader can only return a frame that matched its predecessor at the same phase, so those three recurred within a phase by construction | L255 |
| C294 | ⭐⭐ **C293 REOPENED C266'S SURVIVORS AS "UNEXPLAINED" AND THEY ARE NOT — the answer is ENTAILED by the rule, and it confirms C207 rather than leaving it likely.** `winner_res` is assigned in **exactly one place** in `lf_sampled_read_phases`, inside `if (have_prev && memcmp(prev_word, res.id, 8) == 0)`, and the function returns false when `ok` is unset. ⇒ **Every frame this reader has ever returned matched its immediate predecessor at the same sample phase.** So C266's 3 wrong credentials in 52 device reads are, by construction, wrong frames that recurred CONSECUTIVELY WITHIN A PHASE. No new measurement can add to that — it is what returning them means. ⇒ C207's account is not merely the likely one, it is the only one the code permits: deterministic distortion at a fixed configuration producing the same wrong answer twice in a row. ⛔ **And the reason my 48 broken-tag captures showed no within-phase recurrence is a methodological trap worth naming**: each was a separate `rdrcap.py` invocation with a 50ms gap and a re-initialised capture path, where the device's own loop takes its tries BACK TO BACK inside one read. Those are different timing regimes, and I compared them as though they were the same measurement. ⇒ A host capture loop does not stand in for the device's loop unless the timing matches | 1 assignment site and 1 return path read against 3 prior claims | ✓ entailment from the implementation, which is stronger than any number of captures and is what C269 and C293 both should have started from | ✓ checked that no early-accept path exists — with `tries` of 1, `have_prev` is never set and nothing is returned | L256 |
| C295 | ⭐⭐⭐ **THE WRITER NOW WARNS THAT THE TAG IT JUST MADE WILL NOT READ BACK AS ITSELF — 14 formats never do, and the reader's warning had no counterpart until now.** C284 measured the READ side on 29 real tags at one credential each; C287 made the reader name the alternatives. The write side said nothing, so `lf hid prox write -f IND26` produced a tag that reads back as H10301 with a different credential and the operator was never told — which C278 had already observed on hardware without my connecting it. ⭐ **Measured properly first, over a credential grid against the shipping `wiegand.c` rather than the single bench credential**: **14 formats NEVER read back as themselves** — IND26→H10301, INDASC27 and TECOM27→IND27, HPP32/KASTLE/KANTECH/WIE32→HCP32, N10002/SMP34/BQT34→H10306, H10304 and MDI37→H10302, HGEN37→P10004, **ACTPHID→C15001** — and **3 do so only sometimes** (OPTUS34 130 of 132, SIE36 65, P10004 33), which get a softer note. ⭐ **ACTPHID fills a gap C284 flagged**: the Proxmark refused to write it, so hardware could not reach it and the grid could. **Verified end to end on a real tag**: writing `-f IND26 --fc 1969 --cn 471` printed *"this tag will NOT read back as Indala 26-bit … reports it as HID H10301 26-bit"*, and the unpinned read then returned exactly **H10301 FC 123 / CN 4567** while `-f IND26` returned **1969 / 471** intact. ⇒ The prediction named the format AND was right. ⭐ Both lists are PINNED in `make check` | a credential grid per format + 3 device reads across one written tag | ✓ the warning's specific prediction was tested, not just its appearance — it names which format will be reported and that was checked | ✓ the pinned counts mean a table reorder fails the harness rather than silently making the warning wrong | L258 |
| C296 | ⭐⭐ **FIVE METHOD ENTRIES ADDED (M36-M40) — the session's transferable lessons were scattered across claims, where the next person would not find them.** `METHOD.md` is the file that holds method rather than results, and it stopped at M35 while today produced five lessons that generalise past this protocol and past this project. **M36 — entailment beats captures**: C269 inferred a mechanism from an observation, C293 read the code and voided it, C294 found one line had settled it all along; three claims to reach what reading one function gave immediately. **M37 — a host capture loop is not the device's loop unless the timing matches**: 48 separate invocations with a 50ms gap were compared against a back-to-back in-read loop as though they were one measurement. **M38 — an append-only document puts its oldest layer where the reader finishes**, and a superseded warning should be DATED rather than deleted because its reason is often still load-bearing. **M39 — re-running arms whose code cannot have changed proves the bench works, not the build.** **M40 — verify the FAILING branch of a display fix**: three messages shipped this session had only their happy path seen, and the one that was later reached turned out to drop two fields. ⇒ Each is written from a specific failure in this session with the claim that caught it, not as general advice | 5 entries, each traced to the claims that produced it | ✓ every entry names the concrete mistake rather than stating a principle — the project's existing M-entries are written that way and are useful because of it | ⚠ **method is the one thing here with no experimental test**; these are argued from single incidents and should be revised when one fails | L259 |
| C297 | ⛔⛔ **THE PROXMARK'S T5577 WRITE PATH HAS STOPPED WORKING IN THIS GEOMETRY AND REPORTS SUCCESS ANYWAY — the Chameleon's still works, so the bench is not blocked.** Restoring the bench credential after C297's write-warning tests, `lf hid clone -w H10301` printed `Done!` and the tag did not change; **three further attempts, each verified with `lf hid reader`, all failed** while the tag still read as the previous Optus34 credential on both tools. `lf t55xx write -b 0` likewise printed *"Writing page 0 block: 00"* and did not land, and **`lf t55xx detect` fails outright** — *"Could not detect modulation automatically"*. ⭐ **The tag is fine and so is the field**: `lf sniff` shows peak-to-peak 13908 with 3778 real field drops, all four devices enumerate, and both readers decode the tag's air perfectly. ⇒ **It is the DOWNLINK**: pm3 can hear the tag and cannot talk to it. That also retro-explains two things from earlier today that I logged and did not connect — `lf t55xx dump` returning all-ones and `lf t55xx read -b 2` returning an empty table, both of which need the same downlink. ⭐⭐ **The bench recovered with OUR writer**: `lf hid prox write -f H10301 --fc 123 --cn 4567` landed first time, read 4 of 4 by us and confirmed by the Proxmark as `parity ( ok )`. ⛔ **And the methodological point is that I trusted `Done!` three times**: pm3's clone reports success without reading back, exactly the shape M33 warns about. ⇒ Verify a write with an independent read, always — the tool's own word is not evidence | 4 failed pm3 writes + 1 successful Chameleon write, each checked by an independent reader | ✓ the field measurement separates "tag gone" from "cannot write to tag", which is the fork that matters | ✓ our writer succeeding on the same tag in the same minute is the control that makes this pm3's problem rather than the bench's | L260 |
| C298 | ⭐⭐ **THE OBVIOUS EXPLANATION FOR C297 IS WRONG — it is not a password — and testing it documented something nobody had written down: OUR WRITERS SET ONE.** C297 blamed pm3's downlink and left the cause open. The first hypothesis anyone would reach for is a T5577 password, and it had a compelling timeline: `write_t55xx()` calls **`try_reset_t55xx_passwd()` and then writes USING that password, default `51 24 36 48`** (`lf_reader_main.c:346`), and pm3 wrote fine all day until C295's tests, **which were the first use of our own writer today**. ⛔ **Tested directly and refuted**: `lf t55xx write -b 2 -d AAAAAAAA --pwd 51243648` printed its write line and **the air did not change** — still H10301 FC 123 / CN 4567, where writing block 2 would certainly have destroyed that credential. `lf t55xx detect` also fails identically with and without the password. ⇒ pm3 cannot write **with or without** it, so C297's downlink account stands and this candidate is eliminated by measurement rather than argument. ⭐ **The side finding is worth more than the refutation**: every write this project performs sets the card's password to `51243648` unless told otherwise, and that was undocumented. Anyone bringing a third tool to these tags needs to know. ⚠ Whether the PWD bit in block 0 is actually set — password *stored* is not password *enforced* — is NOT established, because reading block 0 needs the downlink that is broken | 1 password-authenticated write + 2 detects + 2 air reads | ✓ the test is destructive by design: block 2 would have wrecked the credential, so "air unchanged" cannot be a false negative | ✓ the tag was verified intact afterwards and needed no restore | L261 |
| C299 | ⭐⭐ **FIVE CAUSES ELIMINATED FOR C297, AND THE REMAINING ONE IS CHEAP TO TEST BY HAND — unplug the second Chameleon.** pm3 cannot write the T5577 while everything about both ends measures healthy. Eliminated, each by direct test: **a password** (a write authenticated with our writer's own `51243648` left the air unchanged — C298); **the downlink mode** (all four of pm3's `--r0`/`--r1`/`--r2`/`--r3` swept on a destructive block-2 write, **none landed**); **pm3's antenna** (`hw tune` reports **21.05 V at 125 kHz**, 15.93 V at 134.83 and *"LF antenna ok"*); **the tag** (both readers decode its air perfectly); and **the field** (`lf sniff` peak-to-peak 13908, 3778 real field drops). ⇒ **What remains is a hypothesis, not a measurement, and is labelled so**: writing a T5577 requires the reader to modulate the field with gaps the tag must detect, and a second antenna coupled to the same tag a centimetre away can smear those gaps while doing nothing to the tag's UPLINK — which is exactly the asymmetry observed, pm3 hearing perfectly and unable to talk. It also explains why OUR writer works: the Chameleon is the near antenna. ⛔ **The test is one action and needs no disassembly: power down Chameleon #2 and retry `lf hid clone`.** If it lands, the sandwich is the cause and nothing is wrong with either tool | 4 downlink modes + 1 password write + 1 antenna measurement + 2 air reads | ✓ every elimination is a direct test rather than an argument, and the block-2 writes are destructive by design so "no change" cannot be a false negative | ⚠ the remaining account is UNTESTED — it fits every observation and that is not the same as evidence | L262 |
| C300 | ⭐⭐⭐ **THE 12 CHECK-LESS FORMATS CANNOT BE HARDENED — MEASURED THROUGH THE SHIPPING `unpack()`, NOT READ OFF THE SOURCE.** C285 diagnosed the relabelling by auditing the unpackers, and that diagnosis has an obvious repair: implement each format's parity and the first-match walk stops mislabelling. ⇒ **The repair is only available if the checks exist**, and reading source is how this project has been wrong before (C269), so this asks the code. Each format gets 4096 pseudo-random frames of its own bit length — the length **discovered** by sweeping 26..56, never hardcoded — and the acceptance rate is counted: *k* check bits admits ~2^-k, none admits everything. **Result: 12 formats accept 100.0% of random frames** — IND27, INDASC27, TECOM27, IND29, ADT31, HCP32, HPP32, KANTECH, WIE32, OPTUS34, SMP34, P10004 — **19 reject some, 11 are the unimplemented enum entries**. ⛔ **Also corrects C285's arithmetic: "13 of 32" is 12 of 31.** `unpack_nonlinear` is a shared helper called by `unpack_indasc27` and `unpack_tecom27`, not a table row — C285 named it as a helper and counted it as an unpacker in the same sentence. The table has 31 rows. ⭐ **The list, the relabel map and the conclusion are unchanged.** ⇒ So C287's enumerate-every-match reader and C295's write-side warning are not workarounds for a fix that was skipped: **they are the only correct behaviour available**, and that is now measured rather than argued | 42 formats × 31 lengths × 4096 frames = 5,332,992 `unpack()` calls | ⭐ **the null is printed and pinned, not assumed**: H10301 carries two parity bits and accepts **26%** (expected ~25%), so the sweep demonstrably reaches the checks — a null near 100% would have made every 100% row worthless | ✓✓ the 12 reproduce C285's source-read list **exactly, name for name**, by behaviour rather than by reading; and the Proxmark's own unpackers for all 12 contain exactly one `return false` — the LENGTH check, which our `formats[]` already performs — with `Unpack_P10004` carrying the identical `// unknown parity scheme` comment | L263 |
| C301 | ⭐⭐⭐ **EVERY READER GATE MEASURED IN BITS — and 4 of 11 formats have no gate at all.** The four gates added this branch were each verified by breaking ONE named bit on a real tag, which proves a gate is not a no-op and says nothing about its strength: a gate rejecting one broken bit while admitting 99% of everything else passes that test looking healthy. New `ctest/gates.c` feeds each format's `accept` hook, reached through the **exported shipping descriptor**, 200,000 uniformly random frames of its own length. Totals (preamble + gate): **NEXWATCH 44.0, INDALA64 35.0, KERI 33.0, IDTECK 32.0, INSTAFOB 32.0, SECURAKEY 29.0, FDXB ≥28.6, GALLAGHER 24.0, GPROXII ≥23.6, INDALA224 30.0, NORALSY 19.9**. ⛔ **A PREDICTION OF MINE WAS WRONG AND THE CODE WAS RIGHT**: I predicted GProxII's gate at 2 bits, counting only the parity C255 added and ignoring eighteen spacer bits and the format-length gate already in `gproxii_accept`; it measures ~25. ⭐⭐ **The decomposition is the null that matters** — a 25-bit gate and a hook that returns false unconditionally are indistinguishable from outside, and the second would break every read while looking like excellent protection. Satisfying only the structural bits makes GProxII climb to **7.05** bits residual (predicted ~7: length gate + parity) and FDX-B to **17.61** (predicted 16: its CRC16), so both hooks are demonstrably reachable. ⚠ **The gap this exposes: INSTAFOB and IDTECK have neither a gate nor `require_repeat`** — 32 preamble bits and nothing else. INDALA224 and KERI also have no gate but do require the frame to repeat | 11 formats × 200,000 frames = 2.2M `accept` calls, plus 400,000 structured | ⭐ predictions written into the source BEFORE the run and pinned: SECURAKEY 10 → **10.03**, INDALA64 2 → **1.99**, GALLAGHER 8 → **8.01**, NORALSY 8 → **7.94**, NEXWATCH 4 → **3.99** | ✓ each measured figure matches the constraint count read independently from the source — ten zero spacers, two zero bits, eighteen spacers plus a length field — so the sweep and the code agree on structure, not just on a number | L264 |
| C302 | ⭐⭐⭐ **`unpack()` FIXED IN PLACE — the walk now prefers a format that can VALIDATE, and reports its own ambiguity.** Operator direction 2026-09-15: integrate the correction, do not ship a second function beside it; how it upstreams is a separate question from what the correct fix is. ⭐⭐ **The table already knew.** Every row carries `fields.has_parity`, and C300's behavioural sweep settles that the flag is accurate: the 12 rows with `has_parity == 0` are **exactly** the 12 that accept 100% of random frames. It was correct, populated, and never consulted by the walk — so this is not new data, it is the function reading the column beside the one it was using. **Two passes: validating formats first**, check-less rows only if nothing validates, and `verified` records which happened. `wiegand_card_t` gains `matches` (every format of this length that accepted, 1 = unambiguous), `verified` and `others[2]`; **`wiegand_other_matches()` is DELETED** — the count now comes from the same walk that picked the winner, so the two cannot drift apart. ⇒ **Measured effect: formats that never read back as themselves 14 → 12.** ⭐ **KASTLE — C284's headline failure, a Kastle tag holding fc 1 / cn 1 reporting as Check Point card 8389632 — now reads back as itself ALWAYS**; HGEN37 went 0 → 120 of 132 and OPTUS34 0 → 130 of 132. ⛔ **Not a cure, and C300 says why**: where nothing validates there is nothing to prefer, so HPP32/KANTECH/WIE32 still fall to HCP32. IND26 still reports as H10301 because BOTH validate and table order decides. ⚠ **The harness was grading the wrong algorithm** — `ambig.c`'s read-back arm called its own `matches()`, which walks in TABLE order; pointed at the real unpinned `unpack()` it revealed both the 14 → 12 gain and that the CLI's write-side warning lists had gone stale (now 12 and 4) | 1,519 frames cross-checked; 132 credentials × 31 formats in the read-back sweep | ✓ `unpack()`'s own `matches` against `ambig.c`'s independent per-format enumeration: **1,519 frames, 0 disagree** | ✓✓ `has_parity` is a STATIC table column written upstream, and C300's random-frame sweep is a behavioural measurement; they agree on all 31 rows, which is why the flag can be trusted as the preference key. Firmware links clean (334,388 B text); `make check` green on all four arms | L265 |
| C303 | ⭐⭐⭐ **THE RESEARCH INSTRUMENTATION IS COMPILE-GATED, NOT STRIPPED — and the cost of keeping it is 1,088 bytes of flash.** §9b had said "strip all three" since it was written. Operator pushback 2026-09-15: stripping useful tooling is counter-intuitive, what is the industry-standard way? ⇒ **Conditional compilation, and the tree was already doing it** — `app_cmd.c` gates whole handlers AND their dispatch rows behind `#if defined(PROJECT_CHAMELEON_ULTRA)` for the Ultra/Lite split, and this branch had already added `#if !INDALA224_READER_TRUSTED`. One macro, `LF_RESEARCH_CMDS_ENABLED`, defined **0** in `data_cmd.h`, so a plain build IS the shipping build; `application/Makefile` sets it to 1 and **that one line is what an upstream PR deletes**, instead of a hunt through five files. ⭐ **Measured both ways rather than asserted: off = 333,300 text / 164,744 bss; on = 334,388 / 168,752** — 1,088 B flash and 4,008 B RAM — and the gated-ON image is the same size as the pre-gating one, so the gate is transparent when enabled. ⚠ **One of the four is not a separate command**: the GProxII failure-energy payload changes the WIRE SHAPE of a failure reply, so it is gated with `#else` calling `scan_gproxii()`, the plain sibling already in the tree — it must not ship on, or a host expecting an empty failure payload misreads four bytes of energy. ⇒ **Nothing is lost, which was the point**: the capture probe that cracked C211 and the energy payload C206 turned on both survive at zero cost to a shipping image | two full firmware builds per configuration + one `nm` symbol diff + one hardware capability query | ⭐ **the 4,008 B of RAM is attributed TO THE BYTE rather than quoted**: `out.0` (4,000) + `buf.1` (4) + `nsamp.2` (4), all static locals of the capture handler | ✓✓ that attribution **confirms an existing source comment** claiming the probe itself costs no RAM — `m_samples` (0x7000) is present identically in BOTH builds, so the 4 KB is the command's chunk staging buffer, a different thing. ✓ And the host needs no gating, verified on hardware: `GET_DEVICE_CAPABILITIES` makes #2 declare **195** command ids including 3037/3038/3060, and the host already handles a command a device does not implement | L266 |
| C304 | ⭐⭐⭐ **C302 AND C303 VERIFIED ON HARDWARE — AND THE VERIFICATION CAUGHT A MESSAGE MY OWN FIX HAD MADE FALSE.** Flashed #2 to HEAD (`v2.2.0-534-g3f40bc5`, was `513-g96c9d1c`) by the targeted procedure: exactly one `nordicDfu` device with #1 still in normal mode as the cross-check. **C302: wrote KASTLE fc 1 / cn 1 and read it back as `Kastle 32-bit, FC 1 CN 1` — 4 of 4.** On the previous build that same tag was C284's headline failure, reporting as HID Check Point card 8389632. The HID tag reads `H10301` and now names *"1 other layout also fits: Indala 26-bit"*. **C303: 195 command ids declared, 3037/3038/3060 all present**, so the Makefile flag preserves the tooling exactly. ⛔⛔ **THE BUG**: that Kastle read printed *"the capture was corrupted … the numbers below are fiction"* over numbers that were exactly right, four times. `FOREIGN_MEANS_CORRUPTION`'s premise — an HID format accepts every valid 32-bit frame, so a genuine foreign tag never lands here — held **only** because the old walk took the first row in TABLE order. C302's validating-first walk lets KASTLE (which has parity) beat HCP32 (which has none). ⚠ **The harness had the identical defect**: its per-length arm asked whether an HID format ACCEPTS, not who WINS. Pointed at the real walk, 32 bits flips from *0 NOT → non-HID can never win* to **121 NOT → a genuine foreign tag CAN win** — and its own worked example is `fmt 12, fc 1, cn 1`, the exact credential on the bench. ⇒ The three 32-bit formats now fall through to the existing honest *"the reader cannot tell"* branch; **26 and 37 bits are unchanged and keep the corruption message**, because there the verdict genuinely still holds | 4 reads of the Kastle tag + 4 of the restored HID tag + 1 `lf search` + 1 capability query | ✓ the bench was restored and re-verified: `H10301 FC 123 / CN 4567`, 4 of 4 on our reader | ✓✓ pm3 independently reports *"found 2 matching 26-bit formats"* for the HID frame, corroborating `matches = 2`; and the harness's per-length example named the same KASTLE credential the hardware failure did, from a different direction | L267 |
| C305 | ⛔⛔ **DISSOLVED BY C325 — the rule was real as a pattern and wrong as a cause. The writes that failed carried the WRONG PASSWORD (`51243648` vs the tag's `20206666`), which has nothing to do with config words.** (original text follows) ⚠⚠⚠ **A T5577 WRITE FROM THIS DEVICE LANDS IF AND ONLY IF THE CONFIG WORD IT WRITES ALREADY MATCHES THE TAG'S. MECHANISM OPEN.** **Lands, 5 of 5** — `lf hid prox write`, whose block 0 is `T5577_HIDPROX_CONFIG`, *the value already on the tag*: `KASTLE fc 1/cn 1`, then `H10301` at `123/4567`, `99/1234`, `55/777`, `123/4567`, every one confirmed by pm3. **Fails, 9 of 9 across four protocols** — GProxII ×3, AWID ×2, Keri, Indala ×2, each writing a DIFFERENT config word; after all nine pm3 still reads `H10301 FC 123 / CN 4567, parity ( ok )` **bit for bit**. ⛔ **Refuted by direct test, not by argument:** the tag, field and coupling (HID writes succeed *interleaved between* the failures); time-ordering (HID still writes afterwards); the password path (the same `51243648` pair, and `t55xx_write_data()` sends every block TWICE, with the password and without); a preceding read; **block count** — ⛔ my own wording here was WRONG and the correction makes the refutation STRONGER. I wrote *"Keri writes 3 blocks like HID"*; `HIDPROX_T55XX_BLOCK_COUNT` is **4**. Measured counts: **HID 4 → succeeds; GProxII 4 → fails; AWID 4 → fails; Keri 3 → fails; Indala 3 → fails.** Same count, opposite outcomes. ⇒ **HID and GProxII are identical in every respect this code exposes** — same function, same field handling, same password pair, same block COUNT and the same block ADDRESSES 0-3 — **and differ only in the 32-bit values written** (C307 tick); and **my own changes** (C302/C303 touch no write path). ⛔ **ONE EXPLANATION IS DEAD AND MY REFUTATION OF THE OTHER WAS TOO STRONG.** ⚠ *Block 0 locked, data blocks writable* — I called this refuted because the loop would still have written GProxII data into blocks 1-3. **That refutation is WITHDRAWN**: the FIRMWARE transmits those blocks regardless, but a T5577 that rejects a write to a locked block may ignore the rest of the field session, in which case nothing lands — exactly what is seen. ⇒ It is the LEADING candidate again, and it rests on two unverified sub-assumptions (that a rejected write aborts the session, and that a write of IDENTICAL content to a locked block is tolerated, which is what HID's write does). *The packer returning `blk_count == 0`* is genuinely dead — refuted because Keri and Indala both carry the `== 0` guard that returns `PAR_ERR`, and all three writers queried directly returned **`LF_TAG_OK` (0x40), never `PAR_ERR` (0x60)**, so the block counts are non-zero and the writes ARE transmitted. ⚠ **The raw single-block path is 0 of 11 and is NOT evidence here**: the tree already documents `lf_t55xx_write_block()` as roughly one-in-three, because it charges the tag from cold and sends once — so it was never a valid control and is excluded from the count above. ⭐⭐ **AND IT IS BLOCK 0's VALUE SPECIFICALLY, NOT THE FRAME — entailed by data already in hand (M36).** The five successful writes carried **five different credentials**, so blocks 1-3 held five different 32-bit payloads and every one landed. ⇒ **The data words are demonstrably not the gate**, and since HID and GProxII are otherwise identical down to the block addresses (L273), the only variable left is **block 0's value**. ⛔ That is also what makes the result self-contradictory under every simple mechanism: if block-0 writes were merely IGNORED, GProxII's blocks 1-3 would still have landed and pm3 would read garbage — it reads a perfect HID credential, so **nothing at all is written**. A differing block-0 value therefore appears to abort the whole session, while an identical one does not, and no T5577 behaviour known here does that. ⇒ **The only surviving difference is the VALUE of the config word relative to the tag's current one**, and no mechanism is known by which that could gate a whole write sequence. ⭐ It would unify with C297/C299: pm3's `lf hid clone` necessarily writes a config word, and pm3 cannot write this tag at all | 14 write attempts across 6 paths + 9 independent pm3 reads | ✓ every before/after state read by pm3, never by the writer's own read-back (M33); the successes changed the credential four separate times, so the path is provably live | ⛔ **the mechanism is a blank column and that IS the finding** — "iff the config matches" is a RULE FITTED TO 14 OBSERVATIONS, not a cause | L268, L269, L270 |
| C306 | ⭐⭐ **pm3's DOWNLINK IS BROKEN WHILE ITS LISTENING IS INTACT — a SECOND, INDEPENDENT SYMPTOM CLASS for C297/C299, and it is reads, not writes.** Everything that only LISTENS works: `lf search` decoded `H10301 FC 123 / CN 4567, parity ( ok )` on every one of ~9 attempts across the session. Everything that needs pm3 to TRANSMIT fails: `lf t55xx detect` returns *"Could not detect modulation automatically"*, and `lf t55xx read -b 0/1/2` returns **the same word for every block** — `7FFFFFFF` at default settings, `0EAAACAA` once the modulation is correctly set to `--FSK2A --rate 50`. ⇒ **Identical words across blocks cannot be real block data**: block 0 is a config word and block 1 is credential data, and the tag is simultaneously broadcasting a valid H10301, so those two blocks MUST differ. What pm3 is decoding is the tag's continuous broadcast, not a response to a read command — i.e. the read command never arrived. ⭐ **This is the asymmetry C299 predicted, now shown on the READ side too**: not a second instance of the write symptom but a different operation failing the same way, which is what distinguishes a transmit-path fault from a write-path bug. ⚠ **It does NOT explain C305** — our own device changes data blocks on this same tag whenever the config word is unchanged | 6 block reads at 2 client configs + ~9 `lf search` reads | ⭐ `lf search` is the null and it passes throughout: the tag, the field and pm3's RECEIVE path are all healthy, so nothing here is a dead bench | ✓ the internal check is the identical-across-blocks signature itself, which is self-refuting as data and needs no second instrument | L271 |
| C307 | ⭐ **FIVE WRITERS COULD REPORT SUCCESS HAVING WRITTEN NOTHING, AND THE RAW BLOCK WRITER HAD NO CLI COMMAND.** Audited all **16** `write_*_to_t55xx()` functions for the `blk_count == 0` guard: **11 have it, 5 do not** — `awid`, `paradox`, `pyramid`, `gproxii`, `fdxb`, which are *exactly* the family sharing `fsk2a_t55xx_blocks()`. A consistent omission from when they were added together, not a random one. ⇒ Without it a packer returning 0 writes **nothing** and still returns `STATUS_LF_TAG_OK`, because a T5577 never acknowledges — a silent no-op reported as success. All five now return `STATUS_PAR_ERR`, as every sibling does. ⛔⛔ **THIS IS NOT THE CAUSE OF C305 AND MUST NOT BE CITED AS A FIX FOR IT**: all three failing writers were queried directly and returned `LF_TAG_OK` (0x40), never `PAR_ERR` (0x60), so their block counts were already non-zero. C305 made me look; the hole was real and separate. ⭐ **Also added `lf t55xx write`** — the firmware handler and the host binding both existed with no CLI command, so probing a tag block by block needed inline Python. That is precisely what the C305 diagnosis at the bench requires. ⚠ Its help states the unreliability up front rather than burying it: the raw path writes from cold and sends once, is documented at roughly one in three, and measured **0 of 11 here** | 16 writers audited; firmware rebuilt (+32 B text); CLI exercised on hardware; #2 reflashed and the HID arm re-verified | ✓ the probe write left the tag unchanged, exactly as C305 predicts, so the command is exercised without disturbing the bench | ✓ the audit covered every writer in the file rather than the ones I suspected, which is what turned a hunch into a family-shaped omission | L272 |
| C308 | ⭐⭐ **THIS FIRMWARE CAN NOW TRANSMIT A COMMAND INTO A LIVE CAPTURE, AND THE T5577 READ DEMONSTRABLY REACHES THE TAG — BUT THE BLOCK IS NOT DECODED YET.** C305 narrowed a write failure to one unobservable variable, block 0's value; pm3 cannot read it either (C306) and this tree had no block read at all. ⛔ **Two calls cannot work**: a T5577 regular-read answers only while the field it was addressed on stays up, and `capture_begin()` raises the field itself, so sending the command and then capturing resets the tag in between. ⇒ Added `raw_read_samples_probe()`, which fires a callback with the field up and settled and the ring already collecting, plus `lf_t55xx_read_block_probe()` and command **3063** (behind `LF_RESEARCH_CMDS_ENABLED`), chunked exactly like 3060 so existing host tooling parses it. ✓✓ **THE MECHANISM WORKS AND THE CONTROL PROVES IT.** The plain capture demodulates to a perfect 96-bit periodic frame — **self-agreement 1.000 at period 96**, the HID credential the tag is broadcasting. After the read command that frame is **gone (0.512)**, and the downlink itself is visible in the capture as field gaps at samples ~304-880 which the control does not contain. ⇒ The command reaches the tag and changes what it transmits, which is what entering regular-read mode looks like. ⛔⛔ **AND THE BLOCK IS STILL NOT RECOVERED.** No window shows 32-bit periodicity: p32 = 0.49-0.55 across four start offsets, i.e. chance. Bit timing is right (51.2 samples/bit against the control's 53.5, both RF/50), so the demodulator is finding bits — they just do not repeat at 32. ⚠ **Why is NOT established**: regular-read mode may not use the configured modulation, the settle may be wrong, or the tag may be in an error state rather than read mode. ⇒ The capability is real and committed; the measurement it was built for is NOT yet made | 2 captures of 8192 samples + 4 demodulation windows | ⭐⭐ **the control is what makes this reportable**: the SAME demodulator on the SAME tag recovers a perfect period-96 frame, so a failure to find period 32 is a fact about the signal and not about the tool | ⛔ **the block value is a blank column and that IS the finding** — the tool works, the answer is not in hand, and C305 stays open | L275 |
| C309 | ⭐⭐ **THE TAG NEVER ENTERS REGULAR-READ MODE — it keeps broadcasting its credential throughout, with or without a password. C308's open question is closed, negatively.** Switched from demodulating to **raw autocorrelation**, which assumes no modulation at all and so cannot be fooled by picking the wrong one. A 32-bit block at RF/50 must repeat every **1600** samples; the 96-bit HID frame repeats every **4800**. Measured on the 14336-sample captures: unauthenticated read **lag1600 = 0.440, lag4800 = 0.528**; authenticated read (`--pwd 51243648`, the key our own writers use) **lag1600 = 0.418, lag4800 = 0.510**. ⇒ **The 96-bit frame is present in both and the 32-bit block is in neither.** The tag is still transmitting the HID credential it was transmitting before the command. ⛔ **So the earlier signal was misread**: C308 saw the demodulator's period-96 agreement collapse after the read command and reported the tag's output as changed. Autocorrelation says it did not — the collapse is the downlink's own field gaps wrecking a short demodulation window, not the tag switching modes. ⚠ **What is NOT established**: whether the command is malformed, or is transmitted correctly and ignored. ⭐ But it joins a pattern — data-block writes are acted on, while block-0 writes (C305) and now reads are not | 2 captures of 14336 samples, authenticated and not, plus the 8192 control | ⭐⭐ **the method is the null**: autocorrelation needs no assumption about modulation, and it finds the frame that IS there (4800) while finding nothing at 1600 — a negative that cannot be blamed on choosing the wrong demodulator | ⛔ **I also caught my own statistic lying**: "best period" scored 1.000 at periods near the segment length, where only one or two comparisons exist. Re-run requiring ≥40 comparisons, those vanish | L276 |
| C310 | ⭐⭐⭐ **THE READ COMMAND IS WELL-FORMED AND THE TAG IGNORES IT — C309's open question answered, from air we already had.** C309 could not say whether the command was malformed or correctly sent and ignored. Decoding OUR OWN DOWNLINK out of the capture settles it. The firmware encodes a bit as an ON period then a field gap — `gap_sep_one` 54 Tc, `gap_sep_zero` 24 Tc, `write_gap` 9 Tc, `start_gap` 30 Tc — so a block-0 direct read must be a start gap then six bits **1,0,0,0,0,0** (opcode `10`, lock `0`, address `000`). ⇒ Measured on air by relative-dip detection: a 35-sample start gap at 303, then gaps at 356/381/406/431/456/482 with start-to-start spacings **53, 25, 25, 25, 25, 26** — i.e. **ONE, ZERO, ZERO, ZERO, ZERO, ZERO**, exactly the frame intended, with the one/zero ratio near 2:1 and no ambiguity between them. ⛔ **So the transmitter is not at fault and the tag is not answering a question it never heard — it heard a correct question and did nothing.** ⭐ The 8-sample-spaced dip train from 647 is the tag's own RF/8 FSK2a continuing, which is C309's result seen a second way. ⚠ **Absolute ON times are NOT claimed**: the detector reports ~17 samples where 24 was programmed, but an LC that rings ~400 µs cannot resolve a 72 µs gap edge, so the dip is wider than the true field-off and eats into the ON period. **The RELATIVE pattern is what this measures, and it is unambiguous.** ⇒ Joins C305 into one shape: **this tag acts on data-block writes, and ignores block-0 writes and reads** | 1 capture, 18 dips resolved, 7 of them the command | ⭐ the frame decoded off the air matches the frame the source says it builds — two independent derivations of the same six bits | ⛔ the absolute timing column is deliberately blank: the instrument cannot resolve it | L277 |
| C311 | ⭐⭐ **THE BENCH A/B BATTERY, BUILT AND BASELINED BEFORE ANYTHING MOVES.** Two open claims are about to be tested by moving hardware — C299/C306 (pm3's downlink dead, sandwich geometry the leading account) and C305 (this bench cannot change the tag's protocol) — and M35 is explicit that against anything intermittent A/B is not an experiment, A/B/A is. That only works if both halves are measured identically, which no improvised session will manage. `benchab.sh <label>` runs one battery and appends a comparable block: device count, pm3 `lf search` (listening), pm3 `detect` + two block reads (**the C306 signature is detect failing while every block returns the SAME word**), our reader ×4, a DATA-block write verified by pm3, a BLOCK-0 write verified by pm3, and a restore to the bench credential. ⇒ **Baseline `before` captured with the sandwich still assembled**: pm3 listening fine (`H10301 parity ( ok )`), detect fails, blocks 0 and 1 **both return `0EAAACAA`**, reader 4 of 4, data-block write lands (`FC 77 / CN 2468`), block-0 write fails, restored to `FC 123 / CN 4567`. ⛔ **The run order is the finding, not the script**: `nochamp2` — power down Chameleon #2 and change NOTHING else — must run BEFORE the sandwich is opened, because that single action is the whole C299 test and opening the stack destroys it irrecoverably | 1 full battery, 6 sub-tests, every tag state read by pm3 | ⭐ the battery is self-verifying: it ends by asserting the restore, so a run that leaves the bench wrong says so | ✓ the baseline reproduces C305, C306 and the 4-of-4 reader independently, in one pass — three standing claims re-confirmed by a script written after them | L278 |
| C312 | ⭐⭐ **THE `unpack()` FIX IS SEPARABLE TODAY — 3 files, +102 −16, depending on none of this branch's LF work.** U7 refresh (§9i). Recounted against `main`: the branch is 1,698 files and 208,250 insertions, but the **reviewable code is 64 files, +11,327 −154**; 1,633 files and 196,922 insertions are research notes and captures. ⚠ §9c's "59 files, 10,242 lines" is stale by 5 files and ~1,100 lines and is marked superseded rather than left to be quoted. ⭐⭐ **The finding is separability, and it is checked rather than assumed**: `wiegand.h` gains **no new includes and no new externs**, and all three files (`wiegand.c`, `wiegand.h`, `hidprox.c`) already exist upstream — so PR 0 can be sent without the five-PR protocol sequence ever happening. It fixes relabelling on **15 of 29 writable formats** (C284), consults a table column C300 proved accurate by measurement, and is hardware-verified at 4 of 4 (C304). ⛔ **It is a behaviour change to shipping code and the assessment says the PR must OPEN with that**: a reader that answered `HCP32` will now answer `KASTLE`, and a maintainer must be given the chance to disagree. ⭐ Three smaller independently landable items recorded with their blast radius: the 5 `blk_count == 0` guards (behaviour changes only when the count IS 0, which no working path produces), the `t55xx.h` `data_len`→`lock_bit` rename (documentation only), and `lf t55xx write` (host only). ⇒ §9h no longer describes a deletion — C303 gated the instrumentation and C308's command 3063 inherits that gating | 4 `git diff --numstat` measurements against `main` + 1 dependency check | ✓ separability is MEASURED, not asserted — the header's include and extern lines were checked for additions rather than the files being eyeballed | ⚠ the count of what a REVIEWER sees is the honest number and it went UP; quoting the older, smaller figure would have flattered the branch | L279 |
| C313 | ⭐⭐⭐ **PR 0's SEPARABILITY PROVEN BY CONSTRUCTION — it applies to `main` and BUILDS THERE, for +248 bytes.** C312 argued separability from the header's includes; this project's standard is to measure it. Built a clean `main` worktree, **built it unpatched first as the control** (297,892 text) so a failure could not be misattributed, then applied the 163-line 3-file patch: **`git apply --check` passes, it compiles and links, 298,140 text — +248 bytes of flash and NO new BSS**, no errors and no implicit-declaration warnings. ✓✓ **And the `formats[]` table is byte-identical to main** — zero table-row changes on this branch — so the fix rests entirely on `has_parity` values that already exist upstream and cannot depend on a branch-only edit. ⚠ **One dependency found, named and BOUNDED rather than waved at**: `lf_hidprox_data.c` carries a 4-line change the patch excludes — the C47/C250 BLE advertising guard. It changes **how often a read succeeds, not which format wins**, so PR 0's correctness transfers intact; but a maintainer testing on stock firmware meets C45's old 15-20% intermittency. ⇒ That guard is PR 0b — ⛔ **and "4 lines" was WRONG: it is 5 files and +140 −3 (C314), because the call site depends on the guard which depends on BLE connection state** | 2 full firmware builds (control + patched) + 1 patch-apply check + 1 table diff | ⭐⭐ **the unpatched control build is the null**, and it is why "+248 bytes" is a measurement rather than a subtraction between two numbers from different trees | ✓ the table check is independent of the build: zero `formats[]` rows differ, so the correctness argument and the compile argument rest on different evidence | L280 |
| C314 | ⛔⛔ **"PR 0b IS 4 LINES" WAS WRONG BY A FACTOR OF THIRTY-FIVE — it is 5 files and +140 −3, and only compiling it found that out.** C313 measured the BLE advertising guard by the size of its diff in `lf_hidprox_data.c` — 4 lines — and called it a standalone PR. Building `main` + PR0 + those 4 lines fails: `lf_adv_guard_t`, `lf_adv_suspend()` and `lf_adv_resume()` do not exist upstream. Adding their implementation (`lf_reader_data.c` +23, `lf_reader_data.h` +31 −1) fails too: `g_is_ble_connected` is undeclared. That comes from `ble_main.h` (+53) and `ble_main.c` (+29 −2) — **BLE connection-state tracking, which this branch also added**. ⇒ **Each link was invisible until the previous one compiled.** With all five files the build succeeds. ⭐ **The corrected ladder, each step a real build**: `main` **297,892** → +PR0 **298,140** (+248 B) → +PR0b **298,260** (+120 B). ✓ **C313's PR 0 result stands unchanged** — 3 files, +102 −16, applies and builds alone; it was the *second* patch that was mismeasured. ⇒ PR 0b is still worth sending, but it is a 5-file change touching BLE, not a 4-line one-liner, and a plan that promised the latter would have been rejected on contact | 4 build attempts against `main`, 3 of them failing in sequence | ⭐ the failures ARE the measurement: each compile error named the next dependency, which no amount of reading the diff would have surfaced | ⛔ C313's "4 lines" is corrected in place; its PR 0 numbers are re-verified and unchanged | L281 |
| C315 | ⛔⛔ **PR 1 AS §9d SPECIFIES IT DOES NOT BUILD — the five-PR split is a reading, not a plan, and M43 caught it at the second attempt.** After C314 found PR 0b understated 35×, the same test was applied to the first PR of the main sequence. Applied §9d's exact PR 1 file list (10 files, **+1,726 −35**) to a clean `main` worktree and built it. **Three dependency classes missing, each named by a compile error:** (1) `lf_adc_set_acq_fast` undeclared — it lives in **`ble_main.c/.h`**, which §9d does not list for PR 1 at all (+82 −2); (2) `g_is_ble_connected` undeclared — the same BLE dependency C314 found, from the same files; (3) after adding those, the **only** remaining errors are in **`app_cmd.c`** — `raw_read_to_buffer` now takes more arguments and `LF_SNIFF_MAX_SAMPLES` was renamed, and `app_cmd.c` calls both. ⇒ **PR 1's true minimum is the 10 listed files + `ble_main.c/.h` + a hunk of `app_cmd.c`.** ⛔ **That last one is the work the plan has never done**: §9d says `app_cmd.c` "must be split by hunk" and then never splits it, and its diff is **+827 −8 spanning all five PRs**. The hunk PR 1 needs is small — five error sites around the `lf sniff` handler — but it has to be extracted from that. ⇒ **No PR in the main sequence can be sent until the `app_cmd.c` split exists**, which is a different and larger task than the file list implies | 3 builds against `main`, 2 failing, isolating one dependency class at a time | ⭐ the isolation is the method: adding `ble_main` alone reduced seven errors to five and left only `app_cmd.c`, so the remaining gap is bounded rather than guessed | ✓ PR 0 remains the exception and is unaffected — it builds alone (C313), which is exactly why it should go first | L282 |
| C316 | ⭐⭐⭐ **PR 1 BUILDS — its true minimum is 13 files, and the `app_cmd.c` hunk C315 called the blocking task is THREE LINES. But PR 1 costs 10% of the chip's RAM, which §9d never says.** Extracted the minimal `app_cmd.c` adaptation and built it: **10 listed files (+1,726 −35) + `ble_main.c/.h` (+82 −2) + `app_cmd.c` (+4 −3)** links clean. ⭐ **The hunk is three lines because PR 1 should adapt CALLERS to the new engine API, not import HEAD's chunked sniff handler** — that is a feature belonging to a later change, and confusing the two is what made §9d's "+827 −8, must be split by hunk" sound enormous. The PR-1 share of that 827 is 3 lines. ⛔⛔ **AND THE REAL REVIEWER PROBLEM IS RAM, WHICH THE PLAN NEVER MENTIONS.** Measured against `main`: flash **297,892 → 298,916 (+1,024 B)**, but BSS **106,872 → 131,564 (+24,692 B)**. `nm` attributes almost all of it to ONE buffer: `sniff_buf` is `LF_SNIFF_MAX_SAMPLES` = **4,000** on main and `LF_SNIFF_MAX_BYTES` = **28,672** in PR 1's own `lf_reader_generic.h` — **+24,672 bytes, ~10% of the nRF52840's 256 KB, for one static buffer**. ⇒ **That is the most likely reason PR 1 gets declined**, and it is a design question (why does a sniff need 28 KB resident?) rather than a packaging one | 5 builds against `main` + 1 `nm` attribution + 2 constant lookups | ⭐ the cost is attributed to a NAMED symbol rather than quoted as a total, so the reviewer objection can be answered instead of merely reported | ⛔ I nearly filed the 3-line hunk as "minimal" without noticing it pulled a 7× buffer growth with it — the flash figure alone would have hidden that | L283 |
| C317 | ⭐⭐⭐ **TWO 28 KB CAPTURE BUFFERS ARE RESIDENT AT ONCE — 57,344 BYTES, 22% OF THE CHIP'S RAM — AND THEY ARE NEVER LIVE TOGETHER.** C316 found PR 1's real obstacle is a +24,672-byte buffer and asked whether it is necessary. `nm` on the shipping HEAD build says the problem is twice as large as PR 1 alone shows: **`m_samples` 0x7000 (28,672 B)** in `lf_indala_data.c` and **`sniff_buf` 0x7000 (28,672 B)** in `app_cmd.c`, both static, both resident for the life of the image. ⇒ **They cannot be in use simultaneously**: `sniff_buf` is touched only by the LF sniff handler, `m_samples` only by the sampled readers and the capture-probe command, and the device dispatches **one command at a time**. ⚠ **This is an ANALYSIS of who touches what, not a measurement of concurrency** — labelled so deliberately. ⇒ **Sharing one buffer would return 28,672 bytes**, which is more than PR 1's entire RAM cost and would turn C316's likely-decline into a non-issue. ⛔ **Placement matters and the obvious version is wrong**: `m_samples` is `static` inside a PROTOCOL reader, so having the generic sniff command reach into it inverts the dependency — a generic LF feature would import a protocol file. **The buffer belongs in `lf_reader_generic.c`**, which already owns `capture_begin()` and `raw_read_to_buffer()`, with both callers taking it from there. ⚠ **The one behavioural consequence, stated up front**: a sniff and a reader-capture would then clobber each other — which is ALREADY the documented contract for command 3060 (*"any scan between chunks destroys it"*), so the hazard is extended, not invented | 1 `nm` on the shipping build + 2 usage traces across the tree | ⭐ measured on the image that actually ships, not on a plan — both symbols are in the same binary at the same time | ⛔ NOT YET IMPLEMENTED, and deliberately not started this tick: it moves a buffer between modules and needs `lf sniff`, every read arm and `make check` re-verified, which is a unit of its own rather than something to half-land | L284 |
| C318 | ⭐⭐⭐ **28,672 BYTES OF RAM RETURNED — the two capture buffers are now one, and all three paths verified on hardware.** C317 found `m_samples` and `sniff_buf` both resident at 0x7000 and never live together. Implemented as C317 specified: the buffer moved into `lf_reader_generic.c` (which already owns `capture_begin()`), **not** reached into from a protocol file, with a `_Static_assert` that stops compilation if any protocol ever needs a longer capture than the shared buffer holds. ⇒ **BSS 172,760 → 144,088, exactly −28,672**, and `nm` confirms **one** 0x7000 symbol (`m_capture_buf`) where there were two. Flash +128 B for the accessor and its notes. ✓✓ **All three callers verified on the flashed device**: the reader path 4 of 4 (`H10301 FC 123 / CN 4567`); `lf sniff` capturing its full 28,672 samples; and the capture probe returning a frame with **period-96 self-agreement 1.000** — a clean HID frame through the shared buffer. `make check` green on all four arms. ⇒ **This removes the objection C316 predicted would sink PR 1**: PR 1's RAM cost was +24,692 B, and this returns more than that | 1 build + 1 `nm` + 3 hardware paths + the 4-arm harness | ⭐ the saving is PREDICTED THEN MEASURED to the byte — C317 said 28,672 and the linker says 28,672 | ⚠ the shared contract is now wider: a scan between two chunk fetches destroys the earlier capture ACROSS sniff and reader-capture, not just within each. Documented at the buffer rather than left to be discovered | L285 |
| C319 | ⭐⭐ **#2's POWER STATE AND A PHYSICAL NUDGE BOTH CHANGE NOTHING — the pm3 downlink failure is ROBUST, not marginal.** Operator ran the A/B at the bench 2026-09-16. **Two independent power-off variants**: USB pulled at the HOST end, and USB pulled at the DEVICE end (magnetic connector). In both, pm3 gave the identical C306 signature — `detect` fails, blocks 0 and 1 **both `0EAAACAA`**. **Two closing A-runs** (`benchab.sh replugged` after each) reproduced the full baseline exactly: listening fine, reader 4/4, data-block write landing (`FC 77 / CN 2468`), block-0 write failing, credential restored. ⭐ **The second disconnect involved an acknowledged small movement of the sandwich, and the numbers did not shift at all** — same word, same failures. ⇒ **That is the finding: a knife-edge coupling effect should have moved under a nudge, and nothing moved.** ⛔ **It weakens the marginal-geometry reading of C299**, which had been the leading account. ⛔ **It also retires the BLE-burst mechanism I raised** as the reason to try this: host USB traffic stops entirely when unplugged and nothing changed. ⚠ **Not excluded**: passive coupling from a coil that never moved far, and BLE advertising itself — the Ultra is battery-backed (`percentage_batt_lvl`, `g_is_low_battery_shutdown`), so unplugging USB does not power it down or stop it advertising. ⚠⚠ **And `0EAAACAA` is now the same value across every block, every power state, every run, before and after a perturbation — that is the signature of a CONSTANT, not of a measurement** | 4 pm3 batteries across 2 power states + 2 full benchab A-runs | ⭐ the A/B/A closes properly: both return legs reproduce the baseline, so the null result is not drift | ⛔ I predicted unplugging would be uninformative *for the wrong reason* — I argued the coil couples passively whether powered or not, which is true but was not what made the test flat | L286 |
| C320 | ⭐⭐ **REMOVING CHAMELEON #2 CHANGED WHAT pm3's BLOCK READS RETURN.** ⛔ **The detect half of this claim is WITHDRAWN by C321 — it was not significant.** #2 lifted off; tag and Proxmark untouched. `lf search` still reads `H10301 FC 123 / CN 4567`, so the tag is still coupled. ⇒ **Run 1: `detect` SUCCEEDED**, reporting T55x7, *PSK2 or PSK3 (ambiguous)*, RF/8, offset 222, downlink mode *leading zero reference*, block0 one of `00002000`/`00003000`/`00003800`; both block reads returned an **empty table**. **Run 2, configuration unchanged: `detect` FAILED**, and both blocks returned **`80000000`**. ⭐⭐ **Three things moved at once, and all three matter.** (1) `0EAAACAA` — identical across every block, power state and run for the whole investigation — is **gone**, replaced by different values. It was never tag data; it was tied to the assembled geometry. (2) `detect` went from **never** succeeding to **1 of 2** — the failure is now INTERMITTENT, not absolute. (3) ⛔ **The reads still do not work**: `80000000` came back for BOTH blocks, so the identical-across-blocks signature survives and the value is still not data. ⚠⚠ **The detect result is almost certainly a FALSE POSITIVE and must not be read as a win**: it reports PSK2/PSK3 at RF/8, while the tag demonstrably transmits FSK2a RF/50 HID Prox — both readers decode `H10301` from it. A detect that returns the wrong modulation is characterising noise, not reading block 0. ⚠ **CONFOUND, stated plainly**: #2 was resting ON the tag, so lifting it also changed how the tag sits against the antenna. This is not a pure coil-removal and cannot be quoted as one | 2 pm3 batteries in one configuration | ⭐ `lf search` succeeding in both runs is the built-in control: the tag stayed coupled, so the changes are in the DOWNLINK, not in whether the tag is present | ⛔ n=2 on an effect now known to be intermittent — far too few, and the configuration must not be disturbed again until it is characterised | L287 |
| C321 | ⛔⛔ **MY OWN HEADLINE IN C320 WAS NOT SIGNIFICANT, AND THE OPERATOR'S SCEPTICISM ABOUT THE POST-IT APPLIES EQUALLY TO IT.** The operator declined to read 1/8 → 0/8 as a real shift. Correct — and the same test retires the claim I had made one message earlier. Detect successes, Fisher one-sided: **#2 removed 2 of 19 vs #2 present 0 of ≥8 → p = 0.487**; **no-weight 2 of 10 vs post-it 0 of 9 → p = 0.263**. ⇒ **Neither difference is established.** "Detect succeeded for the first time" is a true sentence about one run and NOT evidence that removing #2 changed the detect rate; C320's ⭐⭐⭐ rating and its detect half are withdrawn. ✓ **What SURVIVES is categorical and needs no significance test**: the block-read value was **`0EAAACAA`** — identical for every block, every power state, every run, across the whole investigation — and since #2 was lifted it is **`80000000`**, equally invariant across every run since. A constant changed when the configuration changed. That association does not rest on a low-rate binary. ⚠ **And it still is not data**: `80000000` comes back for BOTH blocks, the same identical-across-blocks signature that made `0EAAACAA` suspect. ⇒ The live question is unchanged and now sharper: **is `80000000` coming from the tag at all?** | Fisher exact on 2/19 vs 0/8 and 2/10 vs 0/9 | ⭐ the operator caught this on the post-it and the arithmetic simply extended it to my own claim — the discipline was theirs | ⛔ I rated a p≈0.5 result ⭐⭐⭐ within minutes of writing that `0EAAACAA`'s constancy was 'the signature of a constant, not a measurement'. The rate was the weak half all along | L288 |
| C322 | ⭐⭐⭐⭐ **OUR WRITER PASSWORD-LOCKS EVERY T5577 IT TOUCHES — CONFIRMED BY ISOLATION, AND IT IS AN UPSTREAM DEFECT PRESENT ON `main`.** Built `LF_T55XX_SET_PASSWORD=0`, which removes three things together: the `T5577_PWD` bit from all 8 config constants, the `try_reset_t55xx_passwd()` block-7 write, and the authenticated copy of every block write. Default build is **byte-identical (334,932 text)**, so the switch is inert at its shipping setting. ⇒ **One HID write to a freshly wiped spare, then the Proxmark:** `lf search` reads `H10301 FC 123 / CN 4567`; **`lf t55xx detect` SUCCEEDS and is CORRECT** — FSK2a, RF/50, `Block0 00107060`, **`Password set...... No`**; and **`lf t55xx dump` returns a full clean dump with every block distinct** — `00107060 / 1D555955 / 5569AA69 / 595A99A9`, blocks 4-7 zero. ⭐ **Block 7 — the password block — is `00000000`.** ⇒ Compare the locked tag: detect fails, every block reads `80000000` (a start bit then silence), four `wipe` cycles rejected, `recoverpw` exhausted. **Same writer, same command, same reader; the only difference is the password machinery.** ⭐ The config word is the receipt: `00107060` here versus `00107070` with `T5577_PWD` (0x10) set. ⛔⛔ **THIS IS NOT THIS BRANCH'S BUG — `main` carries the identical 8 configs and the same `51243648` default**, so every ChameleonUltra user writing a T5577 is password-enabling it, and at least one tag here ended unrecoverable because the password it now demands is not one we can produce. ⚠ **The exact end state is NOT yet established**: on a tag that does not yet have PWD set, the block-7 write carries 32 password bits the tag is not expecting, so the frame is misaligned and lands elsewhere — which would leave PWD enabled with a password nobody chose. That mechanism FITS but is not measured | 1 isolation build + 1 write + 3 pm3 operations, against a locked-tag control of ~20 failed operations | ⭐⭐ the control is the whole experiment: the same write on the same rig with only the password machinery removed produces a perfectly healthy tag | ⭐ the default build compiling byte-identical proves the switch changed nothing else | L289 |
| C323 | ⭐⭐⭐⭐ **REPRODUCED ON DEMAND: ONE HID WRITE FROM THE SHIPPING FIRMWARE LOCKS A HEALTHY T5577 OUT OF THE PROXMARK.** Clean start proved first — `lf t55xx wipe` then `detect` **succeeds** (ASK RF/32, `Block0 000880E0`, **`Password set: No`**) and `dump` shows every block, **block 7 = `00000000`**. Then **a single** `lf hid prox write -f H10301 --fc 123 --cn 4567` from Chameleon #2 running the shipping build. Immediately after: **`lf search` reads `H10301 FC 123 / CN 4567, parity ( ok )`** — the credential wrote correctly — but **`lf t55xx detect` FAILS**, and fails identically with **`-p 00000000`** and **`-p 51243648`**. ⇒ **The defect needs one write, not a session**, and C295's many-writes theory is unnecessary. ⭐ **Mechanism (A) is confirmed over (B)**: block 0 holds a VALID FSK2a/RF-50 config — the tag emits a perfect credential — so the config word was NOT corrupted by a misaligned frame, which (B) predicted would leave FSK1a garbage. ⛔⛔ **But my password account is INCOMPLETE and is not to be quoted as the mechanism**: neither `00000000` (what an untouched block 7 would hold) nor `51243648` (what our writer intends to set) unlocks detect. Something is enabled that neither key opens, and the end state remains unobserved. ⚠ Note the symmetry that makes password tests weak in BOTH directions: pm3's own `-p` frames carry 32 bits a passwordless tag is not expecting, so `detect -p X` failing cannot distinguish *wrong key* from *no key at all* | 1 clean-start dump + 1 write + 4 pm3 operations, against an identical clean control | ⭐⭐ the before-state is MEASURED, not assumed: block 7 was read as `00000000` minutes before the write, on the same tag | ⛔ the end state is a blank column — every candidate key has failed and nothing has yet observed what the writer actually leaves behind | L290 |
| C324 | ⛔⛔⛔ **WITHDRAWN IN FULL BY C325 — I INVENTED A CORRUPTION THAT DOES NOT EXIST.** I claimed our writer intends `51243648` and "lands `20206666`, deterministically", and read a "timing signature" into the byte pairing `20 20 66 66`. **There is no corruption.** `chameleon_cmd.py:13` declares `new_key = b'\x20\x20\x66\x66'` — `20206666` is the password the host ASKS FOR. `51243648` is an OLD key used for authentication, and I had the two backwards. ⇒ The only part that survives is the recovery key itself, which is now explained rather than mysterious. ⛔ The byte-pairing I read as a resampling artefact is simply what that literal looks like — I pattern-matched a hex constant into a mechanism | — | — | see C325 |
| C325 | ⭐⭐⭐⭐⭐ **THE ACTUAL CAUSE, AND IT DISSOLVES C305: THIS CODEBASE SETS TWO DIFFERENT T5577 PASSWORDS DEPENDING ON WHICH WRITE COMMAND YOU CALL.** `chameleon_cmd.py:13-14` declares module globals `new_key = 20206666` and `old_keys = [51243648, 19920427]`. **`hidprox_write_to_t55xx` and `em410x` use those GLOBALS** (lines 629/632/665) — so an HID write sets the password to **`20206666`**. **`indala`, `indala224`, `gproxii` and `awid` instead take a FUNCTION DEFAULT** `new_key = 51243648` with `old_keys = [51243648]`. ⇒ **Everything follows.** A tag locked by an HID write holds `20206666`; every later HID write gets back in because `try_reset_t55xx_passwd()` ends with `reset(20206666 → 20206666)`, authenticating with the tag's real key. Every GProxII/AWID/Indala write authenticates with `51243648`, is refused, and **nothing lands — not one block**. ⛔⛔ **C305 IS DISSOLVED.** Its rule — *"a write lands iff the config word already matches the tag's"* — fitted 14 observations and was never about config words. HID succeeded 5 of 5 because it carried the right password; the other four protocols failed 9 of 9 because they carried the wrong one. ⇒ **Also dead: the "block 0 locked" hypothesis, L274's entailment that block 0's VALUE was the only variable, and C310's framing of a well-formed-but-ignored command.** ⚠ **And the upstream defect is now precisely stated**: writing a T5577 silently enables password protection with a key that DIFFERS BY PROTOCOL and is documented nowhere, so a ChameleonUltra can lock a tag with one command and be unable to open it with another | 4 source lines + the 14 write outcomes of C305, every one of which the explanation predicts | ⭐⭐ the fit is total and was NOT fitted: the rule was derived from 14 hardware results, and this mechanism — found afterwards in the source — reproduces all 14 including which protocols succeed | ⛔ I reached this only after inventing a corruption (C324) to explain a constant I had never looked up | L292 |
| C326 | ⭐⭐⭐⭐ **FIXED AND VERIFIED ON HARDWARE: a T5577 write no longer password-protects the tag.** Password protection is now **opt-in and runtime-controlled**. `T5577_PWD` removed from all 8 config constants; `write_t55xx()` ORs it into block 0 **only when a non-zero key is supplied**, and makes one pass per authentication choice — keys the tag may already hold, then the new password if one was asked for, then an open write last so an unprotected tag finishes correctly. Host side: `new_key` is now **zero** (no password) and `old_keys` is `[20206666, 51243648, 19920427]`, with the four writers that carried their own `51243648` literal unified onto the same globals. ⭐ **`20206666`'s absence from `old_keys` is exactly why a GProxII write could not open an HID-locked tag**; with it present, previously-locked tags stay writable. ✓✓ **Verified**: a blank tag written with the fix reads back `FC 31 / CN 1337`, `detect` succeeding, **`Block0 00107060`** (PWD bit clear), **`Password set: No`**, and **block 7 `00000000`** — against the same write before the fix, which produced a tag no tool could open. ⚠ **The landing write was a SINGLE open write** (the repetition was added afterwards), so the reliability worry that prompted it was speculative: this tree's *"one in three"* figure is about `lf_t55xx_write_block()`, which cycles the field per block, **not** about a write inside a held-field session. The repetition is kept because it matches the prior behaviour and costs only airtime, and is labelled belt-and-braces rather than needed. ⭐ **Also started `FIXES.md`** — a register of six defects found while doing something else, each scoped as its own upstream PR and each present on `main` | 1 blank-tag write + 1 full dump, against the pre-fix control of two unrecoverable tags | ⭐ the before/after is the same command on the same rig: one locked the tag, the other left `Password set: No` | ⛔ I reported "a tag written with it" from an empty pad — the HID writer prints `write done.` with no read-back, which is F6 in the register | L293 |
| C327 | ⭐⭐ **F6 CLOSED: `lf hid prox write` no longer reports success it cannot know.** It printed the arguments it was handed and `write done.` **with no tag on the pad**, because a T5577 never acknowledges and the firmware returns `STATUS_LF_TAG_OK` regardless — every sibling writer (`gproxii`, `awid`, `keri`, `indala`) already re-read and printed a real verdict. ⛔ **It cost real time the same day**: a write to an empty pad was reported as a success twice and believed, inside the investigation that produced F1. ⇒ Now reads back and prints one of five verdicts. ⭐ **Pinned to the format just written** — an unpinned read would report a CORRECT write as wrong for the 12 formats with no rejection path (C284/C302), making the check less trustworthy than the thing it checks. ⚠ **And it reads BEFORE the write too**, which is the only thing separating *the write failed* from *no tag is coupled*. ✓✓ **Verified both directions on hardware**: empty pad → `CANNOT TELL — nothing readable before or after`; tag present → `VERIFIED — read back off the tag as HID H10301 26-bit` | 2 hardware runs, one per branch of the verdict | ⭐ the EMPTY-PAD case is the real test and it was run first — a verifier that only ever sees success is not a verifier | ✓ the structure is the sibling writers', already proven on four protocols, rather than something new | L294 |
| C328 | ⭐⭐⭐ **THE EMULATE ARMS WORK ON THE CURRENT BUILD — and the earlier pm3 null was pm3, not us.** Re-graded with the **Flipper** as reader (the tree's own note: *the only reader that can hear rig A is the Flipper*), #2 on the Flipper pad, T5577 removed. ⭐ **Positive control first**: the Flipper read the real tag as `H10301 FC 42 / Card 999`, matching pm3 exactly — so the instrument is healthy and survived its unexplained crash. ⭐ **Null next**: #2 in reader mode → **0 of 3 on both arms**, proving the tag was out of range. ⇒ **IDTECK (slot 2, PSK1): PSK 4/4 with ASK 0/4** — the bracketed result, and `flipper.py` flags the empty ASK arm as the control itself (C83). **EM410X (slot 3, ASK): ASK 4/4.** ⇒ **Two emulate arms verified on the fixed firmware.** ⭐ **Indala (slot 1) read 0/4, and the firmware explained it**: `lf indala econfig` reports emu id `7FEAA31E76D86C6D` with *"Preamble : 7FEAA31E (not the Indala preamble)"*. The slot holds a frame no reader should decode — **a data problem in the slot, not a code problem in the emitter**. ⚠ **One anomaly, recorded unexplained**: on EM410X **both** arms hit (PSK 4/4 as well as ASK 4/4), which `flipper.py` flags as the null failing. Not investigated; do not quote the EM410X number as bracketed until it is | 1 control + 1 null + 3 slots × 4 attempts | ⭐⭐ this is what the pm3 attempt lacked: a POSITIVE CONTROL. IDTECK hitting 4/4 here proves the earlier pm3 null was about the Proxmark's ability to hear a PWM emulation, not about the emulator | ✓ the null was run before any emulation, so a hit cannot be the tag | L295 |
| C329 | ⭐⭐⭐⭐ **THE FIX RESTORES THE WRITE PATH — A GPROXII WRITE LANDS, AGAINST A PRE-REGISTERED CRITERION.** Sandwich rebuilt (pm3 + T5577 + #2), `benchab.sh post-fix-sandwich` run. ⇒ **`VERIFIED — GProxII frame read back off the tag`** — the first block-0-changing write to succeed since C305, which scored GProxII/AWID/Keri/Indala at **0 of 9**. ⭐⭐ **The criterion was written BEFORE the fix existed**: `benchab.sh`'s own line reads *"still HID means block-0 writes STILL fail. A GProxII frame means C305 IS FIXED."* — a pre-registered test, passed. ⭐ **And C306's signature is gone too**: pm3's `detect` now succeeds (FSK2a, `Block0 00107060`) and the block reads return **DIFFERENT** words (`00107060`, `1D555955`) where every block previously returned one identical value. That identical-word signature was the whole basis of *"pm3's downlink is broken"*. ⇒ **Everything C299/C305/C306 described was the password**, and fixing it restored reads, writes and detect together. ⚠ **U15 is now 1 of 8**: GProxII verified, with AWID, Keri, Indala, NexWatch, Gallagher, Securakey and Noralsy still to re-grade | 1 full 6-test battery against the identical battery run pre-fix | ⭐⭐ the comparison is the same script, same rig, same tag — only the firmware differs, and the script's verdict lines were authored before the cause was known | ✓ reader still 4/4 and the bench restored to `H10301 FC 123 / CN 4567` | L296 |

  ASK/Manchester — Gallagher **10/10**, Securakey **3/3**, Noralsy **3/3** → **16 of 16**
  PSK2, differential — Indala224 **14 of 16**
  PSK1, absolute — Indala26 **2/8**, NexWatch **1/10**, Keri **0/3** → **3 of 21**

⛔ **C189 called this an ASK-versus-PSK split on n=2. It is not.** Indala224 is PSK and reads as well as the ASK protocols; what separates it is that it is DIFFERENTIAL. ⭐⭐ **The mechanism is the project's own, written in `psk1.h` long before this measurement**: "the differential needs no polarity search: XOR of consecutive bits is invariant under global inversion, which is the whole point of differential encoding." A free-running emitter's phase drift relative to the reader's carrier IS a slowly-varying global rotation — differencing removes it, amplitude never saw it, and absolute phase is exactly what it destroys (C74). ✓ **The Proxmark reads REAL PSK1 T5577 tags perfectly** — Indala26, Keri and NexWatch all night off the tag — so its demodulator is sound and the fault is our emitter's clock, not its reader. ⚠ **One confound, stated rather than buried: Indala224 is also the longest frame** (224 bits against 64-96), and this bench has no short PSK2 protocol to separate frame length from differential encoding. The mechanism argument is strong; the experiment does not isolate it | 61 Proxmark reads across 7 protocols and 3 encodings | ✓ the ASK protocols are the same-device same-session control at 16/16, and real PSK1 tags read by the same Proxmark are the control for its demodulator | ✓ the Flipper reads every one of these emulations 6/6 to 10/10, so the disagreement is between READERS and localises the fault to a clock the Flipper tolerates and the Proxmark does not | L153 |
| The `lf hid prox read` 0/15 episode | ⛔ **unexplained, and not reproducible.** It sat at 0/15 for ~15 minutes, survived 150 s of rest, then cleared on its own. C25 excludes the antenna, the field, coupling and thermal drift; the control arm excludes `lf indala read`. What remains is the hidprox decoder or something above it. `lfprobe.py` will catch it in the act if it recurs |
| `lf hid prox read` lock rate | ⭐⭐ **CLOSED — it was the BLE advertising burst, and `cf745fb` fixed it.** 96/96 exact with the guard, 71/80 without it on the same tag in the same session (C250). ⛔ What replaces it as an open question is worse: with the guard off, 5 of those 9 came back as WRONG CREDENTIALS, past a parity check that was working (C251) |
| Does `lf indala read` false-positive on a **non-Indala tag**? | HID Prox closed (C24). ⚠ EM410x, ioProx, Viking, PAC and Jablotron are all still untested, and ASK/OOK tags modulate the envelope differently from HID's FSK |
| Is the phase window tag- or unit-specific? | ⚠ worse than that — it is **not stable across sessions on the same tag and unit**. Phase 12 was 5/5 in the sweep and 4/10 correct a day later; phase 28 went the other way. ⇒ do not hard-code a phase, rotate. Still untested on a second tag or unit |
| Indala parity | reported, deliberately **not** a gate — see C19. It only covers format 26, and a badly wrong frame passed it |

## What runs on the device

| | |
|---|---|
| `rfid/reader/lf/lf_indala_psk.c` | the demodulator. Pure integer, no nRF dependency, so it host-compiles for `research/indala-psk-read/ctest/` |
| `rfid/reader/lf/lf_indala_data.c` | capture, phase rotation, the two-capture agreement rule |
| `rfid/reader/lf/lf_reader_generic.c` | `capture_begin`/`capture_end` extracted so `lf sniff` and the Indala read share one copy of the BLE suspend, the ring and the settle discard |
| `DATA_CMD_INDALA_SCAN` = 3033 | `lf indala read` |

Cost: 8 KB of `.bss` for the sample buffer, ~1 KB of stack, 92 KB of RAM still free.
The decode is ~32 x 4096 adds and runs inside the inter-capture gap.

⭐ **The filter is free.** Summing a [1,2,1]-filtered signal over a 32-sample window is
identically a weighted sum of the *unfiltered* signal over 34 samples with weights
`1,3,4,4,...,4,3,1`, so the notch folds into the bit integrator as

```
integrator = 4*sum(y[a..a+31]) + y[a-1] - y[a] - y[a+31] + y[a+32]
```

— four extra adds per bit rather than three per sample, and no second buffer.

## Hardware reference

⛔⛔ **EVERY PATH FROM THE LF ANTENNA TO THE MCU PASSES THROUGH A DETECTOR DIODE. NO PIN CARRIES
A CARRIER-RATE SIGNAL, IN ANY MODE.** Stated first because it forecloses a whole family of
designs — carrier-edge field detection, carrier locking, coherent demodulation — and because
this project proposed one of them anyway, against a reference section that already said
`VD1 detector` (C134, M32). Source: `Chameleon_nrf52_ultra_V1.0.pdf`, sheet 2.

```
                       U1 RS2257XC6, SPDT analog switch
P_LF_ANT_DRV -- A            B1 -- GND        select S <- P0.31  LF_ANT_DRIVER  [OUTPUT]
                             B2 -- VCC3.3V

P_LF_ANT_RAW --+-- C33 / C34 / C10 resonance --|>|-- LF_OA
                                               VD1  1N4148, half-wave
LF_OA --+-- R11 220 -- Q3 drain, gate <- P1.13  LF_MOD  [OUTPUT]   load modulation
        +-- R12 470k --------------------- LF_RSSI -> AIN0 / LPCOMP0   P0.02
        +-- R13 470k -- GND
        \-- C28 10n / R9 82 / C35 27n / C36 33n -> IC1A (R17 4k7 / C38 1n)
                                                -> IC1B -> LF_OA_OUT -> AIN5   P0.29
                                                   both powered from LF_AMP_PWR = P1.15
```

**All four LF pins, and what each one can and cannot see:**

| pin | dir | what it is |
|---|---|---|
| P0.31 `LF_ANT_DRIVER` | **out** | the SELECT input of the analog switch. The coil's drive terminal is tied to GND or 3V3 on the *far side* of U1 — this pin never touches antenna signal, so it cannot be re-purposed as a sense input |
| P1.13 `LF_MOD` | **out** | gate of Q3, which shunts `LF_OA` through 220 Ω — the RECTIFIED node, not the coil |
| P0.02 `LF_RSSI` | in | `LF_OA` through 470 k, clamped by VD2. Post-detector: an envelope |
| P0.29 `LF_OA_OUT` | in | the same envelope, twice filtered. Dead unless P1.15 is high, and tag mode clears it (`rfid_main.c`) |

- ⭐ **VD1 is the whole reason.** It rectifies at the coil, and LF_OA's own load sets the hold:
  R12 ∥ R13 = 235 kΩ against C28 in series with C35 ≈ 7.3 nF → **τ ≈ 1.7 ms**, which is the
  "~2 ms peak detector" `lf_tag_em.c` already waits out between bursts. The number was in the
  firmware before the schematic was read; they agree.
- ⚠ **Doubly blocked.** Even if carrier ripple survived the detector, the SAADC is clamped to
  200 kHz (`lf_125khz_radio_saadc_rate_set`) and a 125 kHz carrier needs 250 kHz.
- ⭐ **Modulation and sensing share ONE node.** Q3 pulls `LF_OA` down; LF_RSSI and the op-amp
  chain both read `LF_OA`. That is not an implementation accident to work around — it is why
  LPCOMP must be disabled while emulating.
- In reader mode the carrier is **generated, not recovered**: TIMER2 counts our own
  `PWMPERIODEND` events. Nothing in the design ever knew a reader's carrier phase.
- Filter poles: R9/C36 = **58.8 kHz**, R17/C38 = **33.9 kHz**. At fc/2 = 62.5 kHz they cost
  −3.3 dB and −6.4 dB.
- `READER_POWER` (P1.15) is the schematic's `LF_AMP_PWR`; it feeds the bias divider
  R8 4k7 / R10 3k → LF_VBIAS ≈ 1.2 V.
- SAADC: 14-bit, PPI-triggered from the carrier PWM `PWMPERIODEND`, `GAIN1_6` against the
  internal 0.6 V reference → 3.6 V full scale, `ACQTIME_5US`.
- AIN5 sits at ~1.2 V (33% FS). AIN0 sits at **3.51 V (97% FS)** — 1/6 is already the
  lowest gain the SAADC offers, so single-ended there is no setting with more headroom.

## The tag

`a0000000e6bd0e92` — Indala, 64-bit, Fmt 26, FC 52, Card 63612. PSK1, RF/32, subcarrier at
fc/2 = 62.5 kHz. T5577 block 0 = `00081040`.

⛔ **The campaign registry's `PSK1` cell writes `DEADBEEF/12345678`** — correct PSK1 air
shaping, but not an Indala frame. Use the `INDALA26` cell for anything demod-related;
misreading this invalidated a day (L31).

Confirm before trusting any measurement:

```bash
cd /Users/Shared/code/personal/rfid/proxmark3 && ./pm3 -c "lf indala reader"
```
